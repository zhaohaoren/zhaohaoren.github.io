<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>赵浩任的博客</title>
  <icon>http://lollipops.work/favicon.png</icon>
  <subtitle>劝君惜取少年时</subtitle>
  <link href="http://lollipops.work/atom.xml" rel="self"/>
  <link href="https://pubsubhubbub.appspot.com/" rel="hub"/>
  <link href="http://lollipops.work/"/>
  <updated>2021-01-16T09:05:20.014Z</updated>
  <id>http://lollipops.work/</id>
  
  <author>
    <name>赵浩任</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>【Java】Java程序运行和对象创建过程简述</title>
    <link href="http://lollipops.work/2020/05/10/Java-Java%E7%A8%8B%E5%BA%8F%E8%BF%90%E8%A1%8C%E5%92%8C%E5%AF%B9%E8%B1%A1%E5%88%9B%E5%BB%BA%E8%BF%87%E7%A8%8B%E7%AE%80%E8%BF%B0/"/>
    <id>http://lollipops.work/2020/05/10/Java-Java%E7%A8%8B%E5%BA%8F%E8%BF%90%E8%A1%8C%E5%92%8C%E5%AF%B9%E8%B1%A1%E5%88%9B%E5%BB%BA%E8%BF%87%E7%A8%8B%E7%AE%80%E8%BF%B0/</id>
    <published>2020-05-09T17:42:55.000Z</published>
    <updated>2021-01-16T09:05:20.014Z</updated>
    
    <content type="html"><![CDATA[<p>Java中一个对象创建分为两个步骤： <strong>加载类，创建对象。</strong><br>加载类是将所写的程序.java文件编译生成的.class文件加载到内存中，保证了对象创建的预置环境。类加载完毕后才可以创建该类的对象。<br><a id="more"></a></p><h1 id="第一步：加载类"><a href="#第一步：加载类" class="headerlink" title="第一步：加载类"></a>第一步：加载类</h1><ol><li>当开始运行一个类，虚拟机首先试图访问指定启动类的 .main() 方法，加载该类的 .class 文件。</li><li>如果该类有父类，那么继续加载其父类，以此类推，直到加载出所有与main入口类相关的类（它的父类，父类的父类等）。</li><li>接着，从其顶级父类开始，对其static域按照顺序进行初始化！直至初始化完所有类的static域。&gt;将static域放到静态存储区。</li><li>至此类的加载工作完毕了，下面就进入main函数，执行main函数。一般main函数中来创建类的对象，如果发现要创建的类没有被加载，则继续加载该类）</li></ol><h1 id="第二步：对象创建"><a href="#第二步：对象创建" class="headerlink" title="第二步：对象创建"></a>第二步：对象创建</h1><ol><li>为对象获取内存，然后将内存全部置为0，此时对象中的所有属性都是被赋予0的默认值（内存为0时候的默认值：null-对象引用，0-int，false-boolean…）&gt; 在堆中生成对象所需空间，全部初始化为0，具体成员属性值为堆上对应内存子块—正是因为这种机制，所以java可以保证所有的类对象的属性都会被初始化，但是局部不会被初始化。</li></ol><ul><li>这里需要注意的是：一个对象内部组合了另一个对象，那么在堆中其实存的也是一个引用，这个引用指向被组合对象的堆内存地址（另外再创建）。<br>当引用在内存的二进制数据都为0的时候，他的表现形式是null；当他指向的数据内容内存区数据都是0的时候，他的值为0，””，False等初始标准值。所谓初始化就是修改内存区的二进制数据，因为对象在创建时候第一步就将内存清0，所以保证了所有属性都能至少被初始化为标准初值！<br>但是局部变量不一样，你不初始化，只是申明，那么开辟的内存区在栈中值会是一个未知数据（一旦使用该引用的话，使用的可能是你没有初始化而瞎几把乱指的地址）所以Java会代码检验的时候发现你使用了未初始化的引用，直接给予不通过，直接杜绝了这种可能性的发生。<br>综上，Java中使用任何变量或引用，必须初始化，初始化就是将内存中旧的二进制数据要么清0，要么赋予你要赋的值！</li></ul><ol><li>从顶级父类开始，按照申明顺序将给顶级父类的非static的成员属性初始化（static的只初始化一次在类加载阶段）&gt; 用属性定义的值覆盖0值。</li><li>调用顶级父类的构造函数，如果有成员属性初始化则覆盖前一个申明时初始化值。&gt; 构造函数再次初始化，覆盖前面申明时初始化。</li><li>以此类推，将所有的父级（先初始化属性，在调用构造，一层一层的构造完毕） 构造完成</li><li>最后，初始化当前类的非static属性，再调用当前类的构造函数，完成所有初始化工作。</li></ol><h1 id="注意："><a href="#注意：" class="headerlink" title="注意："></a>注意：</h1><p>1、类中static部分是发生在类加载时期的，并且只初始化一次。因为类只加载一次，加载完后创建对象过程中不会再去初始化static部分的东西，所以之后根本不会再走那块初始化代码，又怎么初始化第二次呢？</p><p>2、static的优先级是高于main函数执行的，因为它是在类加载时期初始化。当static作用的东西都加载完了才执行main，当然main是第一个被使用的static方法，但是虚拟机只是找到这个方法的位置，并不会先去执行里面的内容。</p><p>2、对于类的普通成员属性初始化三个步骤：先全初始化为0，再用申明时候初始化值进行初始化，再调用构造函数进行初始化。</p><p>实例程序：(来源于Thinking in Java)</p><pre><code class="lang-java">//: reusing/Beetle.java// The full process of initialization.import static net.mindview.util.Print.*;class Insect &#123;  private int i = 9;  protected int j;  Insect() &#123;    print(&quot;i = &quot; + i + &quot;, j = &quot; + j);    j = 39;  &#125;  private static int x1 =    printInit(&quot;static Insect.x1 initialized&quot;);  static int printInit(String s) &#123;    print(s);    return 47;  &#125;&#125;public class Beetle extends Insect &#123;  private int k = printInit(&quot;Beetle.k initialized&quot;);  public Beetle() &#123;    print(&quot;k = &quot; + k);    print(&quot;j = &quot; + j);  &#125;  private static int x2 =    printInit(&quot;static Beetle.x2 initialized&quot;);  public static void main(String[] args) &#123;    print(&quot;Beetle constructor&quot;);    Beetle b = new Beetle();  &#125;&#125; print(&quot;Beetle constructor&quot;);    Beetle b = new Beetle();  &#125;&#125; /* Output:static Insect.x1 initialized  static Beetle.x2 initialized //1.类加载和初始化静态x1,x2：只会初始化一次！Beetle constructor //2.进入main函数i = 9, j = 0  //3.new Beetle()，先初始化父类&gt;申请父类内存&gt;初始化为0&gt;初始化i=9，j=0&gt;调用Insect()构造函数&gt;输出ij值&gt;初始化j=39Beetle.k initialized //4.初始化子类&gt;申请子类内存&gt;..&gt;初始化k，输出改行&gt;调用构造Beetle()&gt;输出下面的k，jk = 47j = 39*///:~</code></pre>]]></content>
    
    
    <summary type="html">&lt;p&gt;Java中一个对象创建分为两个步骤： &lt;strong&gt;加载类，创建对象。&lt;/strong&gt;&lt;br&gt;加载类是将所写的程序.java文件编译生成的.class文件加载到内存中，保证了对象创建的预置环境。类加载完毕后才可以创建该类的对象。&lt;br&gt;</summary>
    
    
    
    
  </entry>
  
  <entry>
    <title>【Java】Java中的finalize()方法</title>
    <link href="http://lollipops.work/2020/05/10/Java-finalize/"/>
    <id>http://lollipops.work/2020/05/10/Java-finalize/</id>
    <published>2020-05-09T17:26:45.000Z</published>
    <updated>2021-01-16T09:05:12.508Z</updated>
    
    <content type="html"><![CDATA[<p>Java中假定finalize的工作原理为：一旦垃圾回收器准备回收内存而释放对象所占内存的时候，会先调用该对象的finalize方法，然后在下一次再需要垃圾回收的时候才真正的回收对象！</p><a id="more"></a><blockquote><p>finalize方法的作用：<br>日常开发中我们不需要也不建议同时也没意义去主动调用finalize方法。由JVM管理的内存交给GC来管理即可。<br>finalize的目的主要用于一些特殊内存：比如使用JNI调用中申请的特殊内存等。<br>finalize用于在GC发生前事先调用去回收JNI调用中申请的特殊内存，下次GC发生时候保证GC后所有该对象的内存都释放了。</p></blockquote><p><strong>再看垃圾回收</strong></p><ul><li>Java的垃圾回收器只会释放由我们new出来的内存堆块，那些不是由new出来的“特殊内存”，垃圾回收器是不会管理的。</li><li>所谓的特殊内存指通过JNI用C/C++向系统申请的内存，这些内存如果不手动去清除就会一直占据在内存中。</li><li>而且，垃圾回收本身就有开销，所以虚拟机不会经常GC，只有当内存快要耗尽的时候JVM才会触发GC。</li></ul><p><strong>finalize()</strong></p><ul><li><strong>由上，Java中的对象并不一定会被全部垃圾回收，当你不想要该对象的时候，你需要手动去处理那些“特殊内存”，java中没有析构，所以提供了一个finalize()方法让我们来执行清理工作。</strong></li><li>当系统进行GC的时候会先调用finalize方法，然后再下次才会回收对象的内存。因为native中申请的内存，GC没有办法回收所以finalize被用来做垃圾回前的重要清理工作：释放特殊内存。</li><li><strong>所以finalize一般使用在使用了JNI的情景下，需要在finalize中调用native方法释放特殊内存，一般情况下不要使用finalize！</strong></li><li>如果GC不发生那么finalize也不会发生，finalize是Object的protect方法，不可以在类族外访问。</li></ul><p><strong>对比析构函数</strong></p><ul><li>首先，它不是析构函数。Java因为有GC机制，所以没有析构函数的概念！</li><li>finalize不是C++中的析构函数：析构函数肯定是会销毁该对象的，但是finalize因为gc机制不会经常发生，所以其被执行的是不确定的。（不知道是不是我没理解透，没觉得这两个有什么可比性，一个主动发生一个被动发生）</li></ul><p><strong>其他</strong></p><ul><li>调用System.gc()，可以强制让gc发生来触发finalize方法！但是GC也只是GC垃圾的对象（没有被引用的），非垃圾的对象的finalize不会被调用。程序终止和GC不是一个概念。</li><li>正常情况下你调用finalize(）都是直接super.finalize()使用父类的就行了。</li><li>finalize一般不用！被执行的不确定性太大。不要指望使用finalize来回收你的对象，它只会在系统进行GC的时候清理特殊内存，不受你的控制！</li></ul>]]></content>
    
    
    <summary type="html">&lt;p&gt;Java中假定finalize的工作原理为：一旦垃圾回收器准备回收内存而释放对象所占内存的时候，会先调用该对象的finalize方法，然后在下一次再需要垃圾回收的时候才真正的回收对象！&lt;/p&gt;</summary>
    
    
    
    
  </entry>
  
  <entry>
    <title>【Java】&lt;? extend E&gt; 和 &lt;? super E&gt; 深入理解</title>
    <link href="http://lollipops.work/2020/05/10/Java-generic/"/>
    <id>http://lollipops.work/2020/05/10/Java-generic/</id>
    <published>2020-05-09T17:12:35.000Z</published>
    <updated>2021-01-16T09:05:16.329Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>在读Java编程思想的时候，对泛型通配符的上下限定这块有点难以理解。其实平常我也没有用到过这个统配符，只是看着有点困惑便去网上搜了下，答案又各有说法，并且大都不是我想要的。泛型的优点就不多说了，作为一个编程语言的优秀特性，很多语言都支持泛型。在Java编码中泛型可以结合通配符来实现更多的扩展。</p></blockquote><a id="more"></a><p>Java使用 “?” 在泛型参数中做通配符，作用在于：<strong>当前你不知道指定该容器是什么类型，你可以是?占位，表明将来在使用的时候你再指明类型去使用。</strong></p><h1 id="主要疑惑点"><a href="#主要疑惑点" class="headerlink" title="主要疑惑点"></a>主要疑惑点</h1><p>Java使用通配符时候可以通过extends和super用来对将来指向容器的参数类型做限制： </p><ul><li>向下限定: ? extends E </li><li>向上限定：? super E</li></ul><p>我起初最疑惑的地方是编译器如何知道对应的通配类型可以存放那些数据的。</p><h1 id="对这两个最基础的理解-结论-："><a href="#对这两个最基础的理解-结论-：" class="headerlink" title="对这两个最基础的理解(结论)："></a>对这两个最基础的理解(结论)：</h1><ul><li>&lt; ? extends E &gt;<ul><li>add： 不允许加入 <strong>任何</strong> 元素！</li><li>get： 可以获取元素，但是必须使用 <strong>E</strong> 来接受元素！</li></ul></li><li>&lt; ? super E &gt;<ul><li>add: 允许添加 <strong>E和E的子类</strong> 元素！</li><li>get: 可以获取元素，但是类的信息丢失了，所以返回<strong>只能使用Object引用</strong>来接受！如果需要自己的类型需要强制类型转换！</li></ul></li></ul><h1 id="分析前需要知道"><a href="#分析前需要知道" class="headerlink" title="分析前需要知道"></a>分析前需要知道</h1><p> 一定谨记： 编译器使用list的时候，只知道list的泛型参数范围(?,E]，至于他后面指向了什么编译器不知道，list自己是什么类型也不确定。 因为运行时候，不会再list上标记后面指向的ArrayList的类型，而是用一个capture#1来标记（原理我暂时还不知道）。<br> 由上，你使用list的时候，编译器所能获取的信息只有一个，list的泛型参数范围：(?,E] 或者 [E,?) 所以如果允许get或者add，为了安全一定要进行类型判断，保证类型安全！这些判断的依据来源也只有该参数范围！<br>     那么问题来了： 我只知道(?,E] 或者 [E,?)的范围，不知道后面ArrayList具体是什么？所以我做出的决策只能是规定：<strong>只允许 E的父类 或者 E的子类 的对象添加！</strong><br> （因为list不确定自己的类型，只知道自己的范围，所以它对应的可add对象的范围一定是这个泛型参数范围内无论取哪种类型都能保证add是类型安全的）。但是最终我们是要add进入ArrayList的，所以这个范围一定要ArrayList无论指定任何类型，都是类型安全的！</p><p>实例代码：</p><pre><code class="lang-java">        ArrayList&lt;C&gt; al = new ArrayList&lt;C&gt;();        al.add(new C());        List&lt;? extends A&gt; list = al;//        list不允许加入任何元素//        list.add(new A());//        list.add(new B());//        list.add(new C());//        list可以获取元素，但是接受元素必须是A，是其他的会报错！        A a1 = list.get(0);//        C c1 = list.get(0); 不可用C接受        List&lt;? super C&gt; list1 = new ArrayList&lt;B&gt;();//        list不允许加入C的父类元素//        list1.add(new A());//        list1.add(new B());        list1.add(new C()); //可以添加C和C的子类//        list获取元素需要强制类型转换（或用Object接受），因为类信息使用list会被丢失//        C c2 = list1.get(0);        C c2 = (C)list1.get(0);</code></pre><h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><h2 id="lt-extends-E-gt"><a href="#lt-extends-E-gt" class="headerlink" title="&lt; ? extends E &gt;"></a>&lt; ? extends E &gt;</h2><ul><li>List &lt; ? extends A &gt; 表示 这个list里面存的是A的子类，具体是啥不知道，只知道范围！<ul><li>那可以设定分两个范围： A子类，A父类。<ul><li>对于add：<ul><li>A子类情况：如果你允许add的元素是A的子类，那么因为泛型参数限定? extends A，所以ArrayList可以指定的类型也是A的子类，那么无法保证add的对象一定ArrayList指定类型的子类对象，比如： ArrayList指定C, list却add了A()对象—这就是非法的！（因为list并不知道指定了C，它可以确定的范围就是可以add所有A的子类。）所以A子类该范围不行！</li><li>A父类情况：ArrayList指定类型肯定实在(?,A]范围内，所以这种情况肯定不行！</li></ul></li><li>对于get:<ul><li>list不知道你存入ArrayList是什么类型，但是我可以确定使用A的引用肯定可以接受ArrayList中的元素（无论ArrayList设定什么类型），因为A肯定是里面元素的父类！但是你使用其他类型接受就不行了，因为如果ArrayList的类型是你指定的那个类型的父类，是接受不了的。</li></ul></li></ul></li></ul></li></ul><h2 id="lt-super-E-gt"><a href="#lt-super-E-gt" class="headerlink" title="&lt; ? super E &gt;"></a>&lt; ? super E &gt;</h2><ul><li>List &lt; ? super C &gt; 表示list里面存的是C和其父类，具体是啥不确定，只知道范围。<ul><li>同样的分为：C子类，C父类。（其实应该发现了没必要分为两类，有一类是肯定不行的）。<ul><li>add：<ul><li>C子类：如果允许add，就允许list来add任何C的子类元素，因为ArrayList指定的范围是C和其父类，所以ArrayList可以准确接受该假设范围的所有对象。 所以，C子类这个范围可行，这样就有一个标准的类型判断依据，不像extends，根本无法确定判断标准，编译器就不会知道怎么做。</li><li>C父类：显然和上面A子类情况是一样的！因为list无法知道ArrayList指定类型 ，所以无法确定判断标准。</li></ul></li></ul></li><li>get:<ul><li>因为list只允许add C的子子类对象，你或许也在想：那我直接用C不就可以接受ArrayList数据吗？并不是的，ArrayList在赋给list之前可能里面已经有值了，这个值的类型如果是ArrayList指定类型，并且是C的父类，那么C就无法接受！ 而且list不知道ArrayList会指定哪个类型，只知道范围，所以无法确定是哪个父类，所以干脆用Object那肯定就能接受了，所以返回的其实只有Object能接受，否则就得强转。</li></ul></li></ul></li></ul><blockquote><p>其实最初我疑虑点就在为啥我不能限定他只添加ArrayList制定范围之后的那个呢?</p><p>——原因很简单，编辑器使用list的时候不知道ArrayList是啥！</p><p>后来又纳闷为啥super没毛病同样也不知道啊为啥可以add？</p><p>——因为即便是super的时候，也是不知道的，super能add完全是靠常规逻辑判断！</p></blockquote><p>文章很久前写的了，写的不好，现在我自己看也懵逼了 -。-|| 后期修正下吧!</p>]]></content>
    
    
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;在读Java编程思想的时候，对泛型通配符的上下限定这块有点难以理解。其实平常我也没有用到过这个统配符，只是看着有点困惑便去网上搜了下，答案又各有说法，并且大都不是我想要的。泛型的优点就不多说了，作为一个编程语言的优秀特性，很多语言都支持泛型。在Java编码中泛型可以结合通配符来实现更多的扩展。&lt;/p&gt;
&lt;/blockquote&gt;</summary>
    
    
    
    
  </entry>
  
  <entry>
    <title>【PaperReading】Bigtable（个人翻译）</title>
    <link href="http://lollipops.work/2020/05/10/Paper-Bigtable/"/>
    <id>http://lollipops.work/2020/05/10/Paper-Bigtable/</id>
    <published>2020-05-09T17:07:57.000Z</published>
    <updated>2020-08-14T09:51:03.446Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>Google三驾马车的最后一篇论文，最先发表于2006年的OSDI上。HBase的前身。</p><p>最后一篇翻译了，这3篇真是把我翻译吐了。不是为了凑个整我就放弃了，翻译一篇文章要比读一篇文章要费劲的多得多！！！ 感觉纯粹是浪费自我的时间，头大-_-||后面还是就写笔记+总结形式吧。</p></blockquote><a id="more"></a><p><strong>Bigtable: A Distributed Storage System for Structured Data</strong></p><p>Fay Chang, Jeffrey Dean, Sanjay Ghemawat, Wilson C. Hsieh, Deborah A. Wallach Mike Burrows, Tushar Chandra, Andrew Fikes, Robert E. Gruber</p><h1 id="摘要（Abstract）"><a href="#摘要（Abstract）" class="headerlink" title="摘要（Abstract）"></a>摘要（Abstract）</h1><p>Bigtable是一个用来管理结构化数据的分布式存储系统，他被设计可以应对大规模的数据量：分布在上千台普通服务器上的数PB级的数据。在Google很多项目的数据存储在Bigtable上，包括web索引数据，Google地球，Google金融。这些应用对Bigtable发起了很多不同寻常的需求，包括在数据大小方面（从URLs到网页的数据及卫星图像等数据）以及低延迟需要方面（从后端服务的批处理到实时的数据服务）。尽管有着这些各种各样的需求，Bigtable依然为这些Google的产品成功的提供了一个灵活的，高可用的解决方案。在这篇论文中我们将描述一些由Bigtable提供的数据模型，这些数据模型可以让客户端动态的控制它的数据的布局和形式，我们也会在本文中描述Bigtable的设计和实现。</p><h1 id="1-简介（1-Introduction）"><a href="#1-简介（1-Introduction）" class="headerlink" title="1 简介（1 Introduction）"></a>1 简介（1 Introduction）</h1><p>在过去的两年半我们已经设计实现并发布了一个分布式存储系统，用来管理Google的结构化数据，叫做Bigtable。Bigtable是一个被设计可以可靠的应付PB级数据规模和上千台机器的系统。Bigtable已经完成了几个目标：广泛的适用性，可扩展性，高性能，以及高可用。Bigtable已经被超过60个谷歌产品和项目所使用，包括Google Analytics, Google Finance, Orkut, Personalized Search, Writely, and Google Earth。这些产品使用Bigtable来应对各种各样的工作量需求，从面向吞吐量的批处理任务到对时间延迟敏感的终端用户服务。这些产品使用的Bigtable集群跨越了各种配置，从少量的服务器到数千台服务器，并存储了高达几百tb的数据。</p><p>在许多方面，Bigtable依然和一个数据库相似：它和很多数据库有着相同的实现策略。平行数据库（Parallel databases）和内存为主的数据库已经获得了可扩展性和高性能这些特性了，但是Bigtable提供了不同于这些系统的接口。Bigtable并不支持一个完整的关系数据模型；替代的，他提供给客户端一个简单的数据模型，该模型支持对数据布局和格式的动态控制，并允许客户端可以从底层存储的表示推理出其局部属性。我们使用行和列的名字来索引数据，这些名字可以是任意的字符串。Bigtable可以吧数据当做未处理的字符串进行处理，尽管客户端通常已经将各种形式的结构化和半结构化数据序列化为这些字符串了。客户端可以通过仔细地选择模式来控制其数据的位置。最后，Bigtable模式参数允许客户端动态控制是在内存中还是在磁盘中提供数据。</p><p>第2节将描述数据模型的更多细节，第2节会提供一个客户端API的概述。第4节会简要介绍Bigtables所依赖的Google基础架构的底层结构。第5节将描述Bigtable实现的基本原理。第6节描述了我们为了提高Bigtable的性能做的一些改进。第8节中我们会讲述在Google中如何使用Bigtable的几个例子。第9节中讨论一下我们在设计和支持Bigtable中吸取的一些教训。最后，第10节会介绍相关工作，11节会给出我们的结论。</p><h1 id="2-数据模型（Data-Model）"><a href="#2-数据模型（Data-Model）" class="headerlink" title="2 数据模型（Data Model）"></a>2 数据模型（Data Model）</h1><p>Bigtable是一个稀疏的，分布式的，持久化，拥有多和维度的有序map。这个map通过一个行key和列key以及一个时间戳来定位一个元素。map中的每个值都是一个未被解析的字节数组。</p><p>(row:string, column:string, time:int64) → string</p><p><img src=":paper-bigtable/image-20200420115336139.png" alt="image-20200420115336139"></p><p><em>图1：存储web网页例子的一个切片，行的名字是的转置后的URL。contents列族存放的是网页的内容，anchor列族存放的是引用了该网页连接的anchors文本信息。CNN的主页被Sports Illustrated和MY-look 主页同时引用，所以这行数据包含了anchor:cnnsi.com和anchor:my.look.ca。每个anchor单元格都有一个版本，contents列有三个版本，对应的时间戳t3,t5和t6。</em></p><p>我们在仔细检查大量的类似Bigtable的系统的潜在使用场景。一个明确驱使我们做出的几个设计决定的例子是：假设我们想要保持备份一个很大的数据集，这个数据集是可能被很多项目使用的网页和相关信息；我们先临时称这个表为Webtable，在Webtable中，我们可以使用URL作为行key，web网页个各种相关信息作为列名，我们将网页的内容存放在contents字段中：每一列都在他们数据被抓取的时候用标记上时间戳，如图1说明的那样。</p><h3 id="行（Rows）"><a href="#行（Rows）" class="headerlink" title="行（Rows）"></a>行（Rows）</h3><p>在table中row key可以是任意字符串（目前最大支持64kb的字符串，但是对我大部分用户通常都在10-100kb大小）。每一次对一个row key下的数据进行读写的操作都是原子性的（不管这行里面有多少不同的列）。这么设计的原因是我们对一个行并发更新场景下可以让客户端更加容易的理解系统表现行为。</p><p>Bigtable通过row key的字典序来组织数据。table会被动态的分片为多个一连串的行。每个这些一连串的行叫做tablet，这是数据分布和负载平衡的最小单位。结果，对于那些比较小的tablet的读操作是高效的并且一般只需要少部分的机器来进行通信。客户端可以利用这个属性来选择他们的row key，这样可以更好的定位他们需要访问的数据。比如，在Webtable中，通过反转URL可以将相同域名的网页分到一个组里面去。举个例子：我们存储maps.google.com/index.html的数据就使用key为com.google.maps/index.html。通过域名让这些需要存储的相临近网页可以让一些对主机和和域名的分析更加高效。</p><h3 id="列族（Column-Families）"><a href="#列族（Column-Families）" class="headerlink" title="列族（Column Families）"></a>列族（Column Families）</h3><p>列的key会被分组为一系列集合叫做列族，这将构成基本的访问控制单元。存储在一个列族的中所有数据同上都是一个类型的（我们压缩同时会压缩同一个列族中的数据）。一个列族必须在数据存储到该列族下的列之前被创建；在列族创建完成后，任意该列族下的列key都可以被使用。我们这么设计的意图主要是因为考虑到在一个table中不同的列族的数量会很小（最多数百个），并且列族在操作的时候很少被修改。相对的，一个表的列可能是没有一个具体数量边界的。【按照web的那个例子，一个table中网站的所有信息大概可以分几类，但是具体存那些数据就有很多可能了，列族其实相当于是传统数据库的列，而列可以当成该列族存的一个map，列是map的key】</p><p>一个列key的命名方式需要遵循下面的语法：列族名:修饰符。列族的名字必须是可打印的，而修饰符的名字可以是任意字符串。Webtable一个列族的例子就是language，该列族用来存放编写该网页所用到的语言。我们在该列族里面只有一个列，该列用于存储每个网页的语言ID。另一个有用的列族是anchor；如图1所示，该族里面每一个列key都代表一个单独的anchor。使用的修饰符就是引用网站的名字，而里面存放的内容就是该引用的连接。</p><p>访问控制以及磁盘和内存记录均在列族级别来执行。 在我们的Webtable示例中，这些控制使我们能够管理不同类型的应用程序：一些如添加新的基本数据，一些如只允许读取基本数据以及创建衍生的列族，某些仅允许查看现有数据（出于隐私原因可能都不能访问所有现有列族）。</p><h3 id="时间戳（Timestamps）"><a href="#时间戳（Timestamps）" class="headerlink" title="时间戳（Timestamps）"></a>时间戳（Timestamps）</h3><p>table中每个cell可以包含同一个数据的多个版本，这些版本通过timestamps时间戳来索引。Bigtable的时间戳是64位的整型。他们可以由Bigtable来分配，代表着微秒级别的“真实时间”，也可以被客户端程序明确指定。为了避免数据的碰撞，客户端必须自己能生成唯一的时间戳。一个cell中不同版本的数据是按照时间戳降序顺序，所以最新的版本可以被最先读取。</p><p>为了减轻数据版本的管理工作，我们支持两种针对列族的专门设置，该设置会让Bigtable自动垃圾回收这些cell。 客户端可以指定仅保留单元格的最后n个版本，或者仅保留新的足够的版本（例如，仅保留最近7天写入的值）。【2个设置一个按照版本维度，一个按照时间维度】</p><p>在我们的Webtable例子中，我们设置<code>content:</code>列中存储的爬取来的网页的时间戳为实际的抓取时间。上述的垃圾收集机制会让我们仅保留每个页面的最新三个版本。</p><h1 id="3-API"><a href="#3-API" class="headerlink" title="3 API"></a>3 API</h1><p>Bigtable API提供了创建和删除表和列族的功能。它还提供用于修改群集，表和列族元数据的功能，比如如访问控制权限。</p><pre><code class="lang-pseudocode">// Open the table Table *T = OpenOrDie(&quot;/bigtable/web/webtable&quot;);// Write a new anchor and delete an old anchor RowMutation r1(T, &quot;com.cnn.www&quot;); r1.Set(&quot;anchor:www.c-span.org&quot;, &quot;CNN&quot;); r1.Delete(&quot;anchor:www.abc.com&quot;); Operation op; Apply(&amp;op, &amp;r1);</code></pre><p><em>图2：Bigtable的一个写入</em></p><p>客户的应用程序可以写入或删除BigTable中的值，从单个行中查找某个值，或者对表中某个数据子集进行遍历。图2显示了一段C++代码，它使用了RowMutation抽象来执行一系列的更新（为了使示例简短，省略了无关的细节。）。对Apply的调用，会对Webtable的执行一个原子变更操作：它向www.cnn.com添加一个锚点（原文叫anchor跟容易理解）并删除另一个不同的锚点。 </p><pre><code class="lang-pseudocode">Scanner scanner(T);ScanStream * stream;stream = scanner.FetchColumnFamily(&quot;anchor&quot;);stream - &gt; SetReturnAllVersions();scanner.Lookup(&quot;com.cnn.www&quot;);for (; !stream - &gt; Done(); stream - &gt; Next()) &#123;    printf(&quot;%s %s %lld %s\n&quot;,     scanner.RowName(),     stream - &gt; ColumnName(),     stream - &gt; MicroTimestamp(),     stream - &gt; Value());&#125;</code></pre><p><em>图3：Reading from Bigtable.</em></p><p>图3显示了一段C++代码，它使用了一个Scanner这样的抽象来遍历一个行中的所有anchor。客户端可以遍历多个列族，并且提供了几种机制可以限制一次扫描中产生的行、列和时间戳。例如，我们可以限制上面的扫描让其只产生的anchor所在的列与正则表达式anchor:*.cnn.com匹配的，或者只产生那些时间戳距离当前时间10天以内的anchor。</p><p>BigTable支持几种其他的功能允许用户以更加复杂的方式来操作数据。首先，BigTable支持单行事务，可以允许对某个row Key下面的数据执行原子的“读-改-写”操作。BigTable目前还不支持通用的跨行row Key的事务，虽然它在客户端提供了跨rowKey的批量写入数据的接口。其次，BigTable允许单元格被用来作为整数计数器。最后，BigTable支持在服务器的地址空间内执行客户端提供的脚本。这种脚本语言是Google开发出来进行数据处理的叫做Sawzall。目前，我们基于Sawzall的API不允许客户端脚本对BigTable执行回写操作，但是，它确实允许各种形式的数据转换、基于任意表达式的过滤以及通过各种各样的算子得出的总结。</p><p>BigTable可以和MapReduce[12]一起使用，MapReduce是Google开发的、用来运行大规模并行计算的框架。我们已经写了一套Wrapper的集合，可以让BigTable被用来作为一个MapReduce作业的输入源或者输出目标。</p><h1 id="4-构建块（Building-Blocks）"><a href="#4-构建块（Building-Blocks）" class="headerlink" title="4 构建块（Building Blocks）"></a>4 构建块（Building Blocks）</h1><p>BigTable是在其他几个Google基础设施之上构建的。BigTable使用了分布式Google文件系统（GFS）[17]来存储日志和数据文件。一个BigTable集群通常是在一个共享机器池中操作，这个共享机器池也运行着其他一些分布式应用程序。BigTable的进程通常和其他应用的进程共享同样的机器。BigTable依赖一个集群管理系统来进行调度作业、在共享机群上管理资源、处理机器失败和监控机器状态。</p><p>Google的<em>SSTable</em>的文件格式被用来作为存储BigTable数据的内部格式。一个SSTable提供一个持久化的、排序的、不可变的、从键到值的映射（map），其中，键和值都是任意的字节字符串。<strong>BigTable提供了查询一个指定key的值以及遍历一个指定的key区间内所有的键值对的操作。</strong>在内部，每个SSTable都包含一个块序列（通常每个块是64KB，但可配置）。使用一个块索引（存储在SSTable结尾的那个块）来快速定位块的位置。当SSTable被打开时，块索引就会被加载进内存。一个查询操作只需要进行一次磁盘扫描，我们首先在内存中的块索引中使用二分查找方法找到合适的块，然后再去从磁盘中读取相应的块。同样可选的，一个SSTable可以被完全映射到内存中，这样我们在进行查找操作时就不需要读取磁盘。</p><p>BigTable依赖一个高可用的、持久性的分布式锁服务叫<strong>Chubby</strong>[8]。一个Chubby服务包含5个活跃的副本，其中一个被选作主副本来对外提供服务。当大部分副本处于运行状态并且能够彼此通信时，就意味这个服务就是活着的。<strong>Chubby使用Paxos算法[9,23]来使它的副本在失败时保持一致性。</strong>Chubby提供了一个名字空间，它包含了一些目录和小文件。每个目录活着的文件都可以被用作一个锁，针对文件的读和写操作都是原子的。Chubby客户端函数库提供了针对Chubby文件的持久性缓存。每个Chubby客户端维护一个和 Chubby服务的session。如果租约过期以后不能及时更新session的租约，那么这个客户端的session就会过期。当一个客户端的session过期时，它将会丢失所有锁和开放的句柄。Chubby客户端也可以注册针对Chubby文件和目录的回调服务（callback），从而获取文件变化时候的通知或者session的过期。【Chubby就是类似于Zookeeper】</p><p>BigTable使用Chubby来处理大量的任务：保证在每个时间点只有一个主副本是活跃的；来存储BigTable数据的bootstrap的位置（见5.1节）；来发现tablet服务器以及宣告tablet服务器死亡；存储BigTable模式信息（即每个表的列族信息），以及存储访问控制列表。<strong>如果在一段时间以后，Chubby变得不可用，BigTable也会变的不可用了。</strong>我们最近对涵盖11个Chubby实例的14个BigTable集群进行了这方面的影响测试。由于Chubby的不可用（由于Chubby过时或者网络问题），而导致一些存储在BigTable中的数据变得不可用，这种情形占到BigTable服务小时的平均比例值是0.0047%。单个集群的百分比是0.0326%。</p><h1 id="5-实现（Implementation）"><a href="#5-实现（Implementation）" class="headerlink" title="5 实现（Implementation）"></a>5 实现（Implementation）</h1><p>BigTable实现包括三个主要的功能组件：一个用来链接到每个客户端的库，一个master服务，许多Tablet服务。Tablet服务器可以根据工作负载的变化，从一个集群中动态地增加或删除。master服务负责把Tablet分配到Tablet服务，探测Tablet服务的增加和过期，进行Table服务的负载均衡，以及GFS文件系统中的垃圾回收。除此以外，它还处理模式（schema）变化，比如表和列族创建。</p><p>每个Tablet服务管理一个Tablet集合（通常在每个Tablet服务器上我们会放置10到1000个Tablet）。Tablet服务会处理那些对已经加载的Tablet而提出的读写请求，并且会对变得太大的Tablet进行切分。</p><p><strong>和许多单master分布式存储系统一样[17,21]，客户端并不是直接从主服务器读取数据，而是直接从Tablet服务器上读取数据。</strong>因为BigTable客户端并不依赖于master服务来获得Tablet的位置信息，大多数客户端从来不和，aster服务器通信。这样在实际应用中master的负载很小。</p><p>一个BigTable集群存储了许多表。每个表都是一个Tablet集合，每个Tablet包含了位于某个域区间内的所有数据。在最初阶段，每个表只包含一个Tablet。随着表的增长，它会被自动分解成许多Tablet，每个Tablet默认尺寸大约是100到200MB。</p><h2 id="5-1-Tablet位置（Tablet-Location）"><a href="#5-1-Tablet位置（Tablet-Location）" class="headerlink" title="5.1 Tablet位置（Tablet Location）"></a>5.1 Tablet位置（Tablet Location）</h2><p>我们使用了一个类似于 B+树[10]的三层架构来存储Tablet位置信息（如图4所示）。</p><p><img src=":paper-bigtable/image-20200507151602754.png" alt="image-20200507151602754"></p><p>第一个层次是存储在Chubby中的一个文件，它包含了<code>root tablet</code>的位置信息。<code>root tablet</code>把Tablet的所有位置信息都保存在一个特定的元数据（METADATA）表中。每个METADATA表都包含了一个user tablet集合的位置信息。<code>root tablet</code>其实就是METADATA表当中的第一个Tablet，但是对待的方式有区别，它永远不会被拆分，这样就确保Tablet位置层次结构不会超过三层。</p><p>METADATA表存储了属于某个row key的Tablet的位置信息，row key是Tablet表标识符和它的最后一行这二者的一个编码。每个METADATA行，在内存中大约存储1KB的数据。由于METADATA Tablet128M大小的限制，我们的三层位置模式足够用来存放2^34的Tablet的位置信息（或者2^61字节在128M的tablets中）。</p><p>客户端的函数库会缓存Tablet位置信息。如果客户端不知道一个Tablet的位置，或者它发现它缓存的Tablet位置信息不正确，它就会在Tablet位置层次结构中依次向上寻找。如果客户端缓存是空的，那么定位算法就需要进行三次网络轮询，其中就包括一次从Chubby中读取信息。如果客户端的缓存是过期的，定位算法就要进行六次轮询，因为，只有在访问不存在的时候才会发现缓存中某个entry是过期的（假设METADATA Tablets不会频繁移动）。虽然，Tablets位置信息是保存在内存中，所以不需要访问GFS，我们仍然通过让客户端库函数预抓取tablet位置信息来进一步减少代价，具体方法是：每次读取METADATA表时，都要读取至少两条以上的Tablet位置信息。</p><p>我们也在METADATA表中存储了二级信息，包括一个日志，它记载了和每个tablet有关的所有事件（比如一个服务器什么时候开始提供这个tablet服务）。这些信息对于调试和性能分析是非常有用的。</p><h2 id="5-2-Tablet分配（Tablet-Assignment）"><a href="#5-2-Tablet分配（Tablet-Assignment）" class="headerlink" title="5.2 Tablet分配（Tablet Assignment）"></a>5.2 Tablet分配（Tablet Assignment）</h2><p>同一时间每个Tablet只可以被分配到一个tablet服务器。master服务保持跟踪tablet服务器们的情况，以及tablet被分配到tablet服务器的情况，这包括哪个tablet还没有被分配。当一个tablet没有被分配，并且有一个tablet服务器有足够空间可以容纳该tablet时，master服务器会向tablet服务器发送一个tablet加载请求来把当前这个tablet分配给这个tablet服务器。</p><p>BigTable使用Chubby来跟踪tablet服务器。当一个tablet服务器启动的时候，Chubby创建并且获得一个独占的排他锁，这个锁会锁住在指定的Chubby目录中的一个唯一命名的文件。<strong>master会监视这个目录（服务器目录），来发现tablet服务器。</strong>如果一个tablet服务器停止服务，它就会丢失这个锁，比如，由于网络故障，导致这个tablet服务器丢失了它与Chubby的会话。（Chubby提供了一个完善的机制，来允许一个tablet服务器检查自己是否已经丢失了这个独占排他锁）。如果丢失了锁，只要目录中的这个文件还存在，一个tablet服务器就会努力去获得这个锁。如果文件不再存在，那么这个tablet服务器就不能再够对外提供服务，因此它会自杀。每当tablet服务终止了（比如，集群管理系统把这个tablet服务器从集群中移除），它就会尝试释放锁，这样主服务器就可以更快地重新分配这个tablet。</p><p>master有责任去探测什么时候tablet服务器不再提供tablet服务，并且要尽快重新分配这些tablet。为了探测什么时候tablet服务器不再提供tablet服务，主服务器会周期性地询问每个tablet服务器，了解他们的锁的状态。如果一个tablet服务器报告它已经丢失了锁，或者在最近的几次尝试master都无法与tablet服务器取得联系，master就会努力获得一个针对这个服务器文件的独占排他锁。如果master可以获取这个锁，说明Chubby还活着只是tablet服务器已经死亡或者某些故障导致它无法连接到Chubby。因此master就要从Chubby中删除这个tablet服务器的文件，从而确保这个tablet服务器不再能够提供服务。一旦一个服务器文件被删除，主服务器就可以把所有以前分配给该服务器的tablet，都移动到待分配tablet的集合。为了保证一个BigTabl集群不会轻易受到主服务器和Chubby之间的网络故障的影响，如果一个主服务器的Chubby会话过期了，这个主服务器就会自杀。但是，正如上所述，主服务器失效并不会改变tablet到table的分配。</p><p>当一个master被集群管理系统启动时，它在能够改变tablet分配之前必须首先了解当前的tablet分配信息<strong>。因此在启动的时候，master会执行以下步骤：</strong>（1）master在Chubby中抓取一个独特的master lock，这就防止了多个master并发启动的情形。（2）master扫描Chubby中的服务器目录，从而发现当前可用的服务器。（3）master和当前每个可用的tablet服务器通信，来发现哪些tablets已经被分配到哪个tablet服务器。（4）主服务器扫描METADATA表来学习tablets表集合。一旦在扫描过程中，主服务器发现某个tablet还没有被分配，主服务器就把这个tablet放置到待分配tablet集合，这就让这些tablet可以分配到合理的地方。</p><p>一个比较复杂地方是在METADATA tablets被分配之前，我们是不能扫描METADATA表的。因此，在开始扫描（上述步骤4）之前，如果主服务器在步骤3的扫描中发现root tablet没有被发现，master会把root tablet增加到待分配tablet集合。这个增加确保root tablet一定会被分配。因为root tablet包含了所有METADATA tablets的名字。主服务器只有在扫描了root tablet以后，才可能知道所有这些METADATA tablets的名字。</p><p>现有的tablet集合，只有在以下情形才会发生改变：当一个tablet被创建或删除；对两个现有的tablet进行合并得到一个更大的tablet一个现有的tablet被分割成两个较小的tablet。master可以跟踪这些变化。因为Tablet分割是由一个tablet服务器发起的所以会被特殊对待。Tablet服务器通过把新的tablet信息记录在METADATA表中，来提交分割操作。当分割被提交以后，这个tablet服务器会通知master。为了防止分割通知丢失（或者由于master死亡或者由于tablet服务器死亡），当主服务器要求一个tablet服务器加载已经被分割的tablet时，主服务器会对这个新的tablet进行探测。Tablet服务器会把分割的情况通知给master，因为它在METADATA表中能所找到的tablet entry只是master要求它加载的数据的一部分。</p><h2 id="5-3-Tablet服务（Tablet-Serving）"><a href="#5-3-Tablet服务（Tablet-Serving）" class="headerlink" title="5.3 Tablet服务（Tablet Serving）"></a>5.3 Tablet服务（Tablet Serving）</h2><p><img src=":paper-bigtable/image-20200507151731029.png" alt="image-20200507151731029"></p><p>一个tablet的持久化状态是存储在GFS中的，如图5所示。更新被提交到一个提交日志，日志中记录了redo记录。在这些更新当中，最近提交的更新被存放到内存当中的一个被称为memtable的排序缓冲区，比较老的更新被存储在一系列SSTable中。为了恢复一个tablet，tablet服务器从METADATA表当中读取这个tablet的元数据。这个元数据包含了SSTable列表，其中每个SSTable都包括一个tablet和一个重做点（redo point）的集合，这些redo point是一些指针，它们指向那些可能包含tablet所需数据的重做日志。服务器把SSTable索引读入内存，执行重做点以后的所有已经提交的更新来重新构建memtable。</p><p>当一个写操作到达tablet服务器时，服务器首先检查它定义是否良好，并且发送者是否被授权执行该变更。执行授权检查时，会从一个Chubby文件中读取具有访问权限的写入者的列表，这个Chubby文件通常总能够在Chubby客户端缓存中找到。一个有效的变更会被写入提交日志中。分组提交是为了优化许多小变更[13,16]操作的吞吐量。在写操作已经被提交以后，它的内容就会被插入到memtable。</p><p>当一个读操作到达Tablet服务器，与写操作类似，服务器也会首先检查它是否是良好定义和得到授权的。一个有效地读操作是在一系列SSTable和memtable的合并的视图上执行的。由于SSTable和memtable是字典排序的数据结构，视图合并的执行效率很高。</p><p>当tablet发生合并或分割操作时，正在到达的读写操作仍然可以继续进行。</p><h2 id="5-4-压缩合并（Compactions）"><a href="#5-4-压缩合并（Compactions）" class="headerlink" title="5.4 压缩合并（Compactions）"></a>5.4 压缩合并（Compactions）</h2><p>随着写操作的执行，memtable的大小逐渐增加。当memtable的大小到达一个阈值的时候，memtable就会被冻结然后创建一个新的memtable，被冻结的memtable就转化成一个SSTable并被写入到GFS。这个<em>小压缩（minor compaction）</em>过程有两个目标：它缩小了tablet服务器的内存使用率；当发生服务器宕机需要恢复时可以减少了需要从重做日志中读取的数据量。当压缩过程正在进行时，正在到达的读写操作仍然可以继续进行。</p><p>每一次小压缩都会创建一个新的SSTable，如果这种行为没有约束地持续进行，读操作可能需要从任意数量的SSTable中合并更新。相反，我们会对这种文件的数量进行限制，我们在后台周期性地运行一个合并压缩程序。一个合并压缩程序从一些SSTable和memtable中读取内容，并且写出一个新的SSTable。一旦压缩过程完成，这个输入的SSTable和memtable就可以被删除。</p><p>一个合并压缩程序，把所有的SSTable的数据重写到一个SSTable，这个合并压缩被称为“主压缩”（major compaction）。非主压缩所产生的SSTable可以包含特殊的删除入口（entry），它把被删除的数据压缩在仍然存活的比较老的SSTable当中。另一方面，一个主压缩过程，产生一个不包含删除信息或被删除的数据的SSTable。BigTable定期检查它的所有tablet，并执行主压缩操作。这些主压缩过程可以允许BigTable收回被删除数据占用的资源，并且保证被删除数据在一定时间内就可以及时的从系统中消失，这对于一些存储敏感数据的服务来说是非常重要的。</p><h1 id="6-完善（Reﬁnements）"><a href="#6-完善（Reﬁnements）" class="headerlink" title="6 完善（Reﬁnements）"></a>6 完善（Reﬁnements）</h1><p>在前面章节描述的实现需要一系列完善措施来获取我们用户所需要的高的性能、可用性和可靠性。本节将更详细地描述实现的各个部分，以凸显这些完善。</p><h2 id="位置分组（Locality-groups）"><a href="#位置分组（Locality-groups）" class="headerlink" title="位置分组（Locality groups）"></a>位置分组（Locality groups）</h2><p>客户端可以把多个列族一起分组到一个<em>locality group</em>中。我们会为每个tablet中的每个locality group都生成一个单独的SSTable。把那些通常不会被一起访问的列族分割在不同的locality group，可以实现更高效的读。例如，在WebTable当中网页的元数据（比如语言和校验码），可以被放置到同一个locality group当中，网页的内容可以被放置到另一个locality group当中。这样想要读取页面元数据的应用就不需要去访问所有的页面内容。</p><p>另外，可以针对每个locality group来指定一些有用的参数。例如，一个locality group可以设置成存放在内存中。常驻内存的locality group的SSTable采用懒加载的方式加载到tablet服务器的内存中。一旦加载，属于这些locality group的列族，就可以被应用直接访问，而不需要读取磁盘。这个特性对于那些被频繁访问的小量数据来说是非常有用的：我们在内部使用其获取METADATA表的列族位置。</p><h2 id="压缩（Compression）"><a href="#压缩（Compression）" class="headerlink" title="压缩（Compression）"></a>压缩（Compression）</h2><p>客户端可以控制是否对相应于某个locality group的SSTable进行压缩，如果压缩，应该采用什么格式。用户自定义的压缩格式可以被应用到每个SSTable块中（块的大小可以通过locality group指定的参数来进行控制）。虽然对每个块进行单独压缩会损失一些空间，但是我们可以从另一个方面受益，当解压缩时只需要对小部分数据进行解压而不需要解压全部数据。许多客户端都使用两段自定义压缩模式。第一遍使用Bentley and McIlroy[6]模式，它对一个大窗口内的长的公共字符串进行压缩。第二遍使用一个快速的压缩算法，这个压缩算法在一个16KB数据量的窗口内寻找重复数据。两个压缩步骤都非常快，在现代计算机上运行，他们编码的速度是100-200MB/S，解码的速度在400-1000MB/S。</p><p>在选择我们的压缩算法时，即使我们强调速度而不是空间压缩率，这个两段压缩模式也表现出了惊人的性能。例如，在WebTable中，我们使用这种压缩模式来存储网页内容。在其中一个实验当中，我们在一个压缩后的locality group中存储了大量的文档。为了达到实验的目的，我们只为每个文档存储一个版本，而不是存储我们可以获得的所有版本。这个压缩模式获得了10：1的空间压缩率。这比传统的GZip方法的效果要好得多，GZip针对HTML数据通常只能获得3:1到4:1的空间压缩率。<strong>这种性能上的改进是和WebTable中的行的存储方式紧密相关的，即所有来自同一个站点的网页都存储在相近的位置。</strong>这就使得Bentley and McIlroy算法可以从同一个站点的网页中确定大量相似的内容。许多应用，不只是WebTable，都会很好地选择行名称，从而保证相似的数据可以被存放到同一个集群当中，这样就可以取得很好的压缩率。当我们在BigTable中存储同一个值的多个不同版本时，可以取得更好的压缩率。</p><h2 id="为读性能做缓存（Caching-for-read-performance）"><a href="#为读性能做缓存（Caching-for-read-performance）" class="headerlink" title="为读性能做缓存（Caching for read performance）"></a>为读性能做缓存（Caching for read performance）</h2><p>为了改进读性能，tablet服务使用了二级缓存。Scan缓存是一个高层次的缓存，它缓存着键值对，这些键值对是由tablet服务器代码的SSTable接口返回的。Block缓存是比较低层次的缓存，它缓存了从GFS当中读取的SSTable块。Scan缓存对于那些频繁读取相同数据的应用来说是非常有用的。Block缓存对于那些倾向于读取与自己最近读取数据位置临近的数据的应用来说，是比较有用的（比如顺序读取，或者随机读取属于同一个locality group中的不同的列）。</p><h2 id="布隆过滤器（Bloom-ﬁlters）"><a href="#布隆过滤器（Bloom-ﬁlters）" class="headerlink" title="布隆过滤器（Bloom ﬁlters）"></a>布隆过滤器（Bloom ﬁlters）</h2><p>正如5.3节描述的，一个读操作需要从构成一个tablet的当前状态的所有SSTable中读取数据。如果这些SSTable不在内存中，我们就不得不造成很多磁盘访问。我们通过下面的方式来减少磁盘访问，即允许客户端来确定，为某个特定locality group中的SSTable创建布隆过滤器（Bloom filter）[7]。一个Bloom filter允许我们询问，一个SSTabble是否包含属于指定的行/列对的数据。对于某个特定的应用，一个tablet服务器只需要使用少量内存空间来存储Bloom filter，都可以极大减少读操作的磁盘访问次数。我们使用Bloom filter也意味着，大多数对目前不存在的行或列的查询并不需要访问磁盘。</p><h2 id="提交日志实现（Commit-log-implementation）"><a href="#提交日志实现（Commit-log-implementation）" class="headerlink" title="提交日志实现（Commit-log implementation）"></a>提交日志实现（Commit-log implementation）</h2><p>如果我们为每个tablet都设置一个单独的文件来保存提交日志，那么就会在GFS中产生大量的文件并发写操作。这些写操作会引起大量的磁盘访问，这取决于每个GFS服务器底层文件系统的实现方式。除此以外，让每个tablet都有一个单独的日志文件，会降低分组提交优化的效率。为了解决这些问题，我们对每个tablet服务器具备一个独立日志文件这种方式进行了修改，不是把更新都写入相应tablet的独立文件中，而是把几个不同tablet的更新内容都添加到一个同样的物理日志文件中[18,20]。</p><p>使用单个日志文件，明显改善了普通操作时的性能收益，但是它使得故障恢复变得复杂起来。当一个tablet服务器死亡，它上面承载的tablet会被重新分配到其他多个tablet服务器，每个tablet服务器通常只接收一部分tablet。为了给一个tablet恢复状态，新的tablet服务器需要根据原来tablet服务器中记载的提交日志文件，为这个tablet重新执行一遍更新操作。但是，针对这些tablet的更新操作的日子记录，都混合存放在一个日志文件中。一种方法是，让新的tablet服务器完整地读取一份提交日志文件，然后，只把恢复这个tablet时所需要的日志记录读取出来，完成恢复。但是，在这种机制下，如果有100个机器，每个都被分配了来自死亡tablet服务器的一个单独的tablet，那么这个日志文件就必须被重复读取100遍（每个服务读一次）。</p><p>我们通过以下的方式来避免日志的重复读写：首先以键（表，行名称，日志顺序号）的顺序对日志文件的条目（entry）进行排序。在排序得到的结果中，所有针对某个特定tablet的更新都是连续存放的，因此，可以通过一次磁盘寻找，然后顺序批量读取数据，这种方法具有较高的效率。为了实现并行排序，我们把日子文本分割成64MB为单位的分段，在不同的tablet服务器上对每个分段进行并行排序。主服务器会对排序过程进行协调，当一个tablet服务器表示需要从一些提交日志文件中恢复数据时，就需要启动排序过程。</p><p>因为很多原因（例如，GFS服务器发生了许多写崩溃，或为到达某组三个GFS服务器经过的网络路径正在拥塞，或负载过重），将提交日志写入到GFS中去，有时会造成性能上的瓶颈。为了使得更新免受GFS延迟的影响，每个tablet服务器实际上有两个写日志线程，每个线程都写到它自己的日志中。在同一时间这2个线程只有一个线程在工作。如果当前活跃的写日志线程性能比较差，那么就切换到另一个写日志线程，提交日志队列中的更新就由这个新激活的线程来写入。日志条目包含了序号有助于在恢复过程删除那些由于线程切换而导致的重复的日志记录。</p><h2 id="加速tablet恢复（Speeding-up-tablet-recovery）"><a href="#加速tablet恢复（Speeding-up-tablet-recovery）" class="headerlink" title="加速tablet恢复（Speeding up tablet recovery）"></a>加速tablet恢复（Speeding up tablet recovery）</h2><p>如果master把一个tablet从一个tablet服务器转移到另一个tablet服务器，这个源tablet服务器首先会对这个tablet做一个minor compaction。这个压缩通过减少tablet服务器中的提交日志中的未压缩状态的数量可以减少了恢复时间。在完成这个压缩过程以后，这个源tablet服务器就停止提供针对这个tablet的服务。在源服务器完全卸载这个tablet之前，它要做另一个次压缩（通常很快）去删除该tablet服务器的中所有未压缩状态的日志，这些日志都是在第一个次压缩过程中产生的。当第二个次压缩完成时，这个tablet就可以被加载到另一个tablet服务器而不需要任何日志条目的恢复。</p><h2 id="利用不变性（Exploiting-immutability）"><a href="#利用不变性（Exploiting-immutability）" class="headerlink" title="利用不变性（Exploiting immutability）"></a>利用不变性（Exploiting immutability）</h2><p>除了SSTable缓存外，因为我们所产生的所有SSTable都是不变的，这也简化了BigTable系统的其他部分。例如，当我们从SSTable中读取数据时，不需要进行同步任何对文件系统访问。因此针对行级别的并发控制可以高效地执行。只有memtable在被同时被读操作和写操作访问数据结构会是变化的。为了减少读memtable的冲突，我们让每个memtable行进行<code>copy-on-write</code>方式来允许读和写操作并行执行。</p><p>因为SSTable是不可变的，永久性删除数据的问题就转变成收集过时的SSTable的问题。每个tablet的SSTable都在METADATA表注册了。master使用<code>标记-清除垃圾收器</code>来移除这些过时的SSTable集合，其中METADATA表中包含了这些SSTable的root集合。</p><p>最后，SSTable的不可变性让我们可以更快地分割tablet，而不是为每个子tablet生成一个新的SSTable集合。我们让子tablet和父tablet共享一个SSTable集合。</p><h1 id="7-性能评估（Performance-Evaluation）"><a href="#7-性能评估（Performance-Evaluation）" class="headerlink" title="7 性能评估（Performance Evaluation）"></a>7 性能评估（Performance Evaluation）</h1><p>我们建立了一个有N个tablet服务器的BigTable集群，通过不断修改N的值来衡量Bigtable的性能和可扩展性。Tablet服务器的配置为：1GB内存，并且把数据写到GFS单元中，这个GFS单元包含了1786个机器，每个机器具备400GB IDE硬盘。为了测试我们让N个客户端机器产生针对BigTable的负载（我们采用的客户端的数量和tablet服务器的数量是相等的，从而保证客户端不会成为瓶颈）。每个机器都有两个双核Opteron 2GHz芯片、充足的能够容纳工作集产生的数据的物理内存和一条千兆以太网链路。这些机器被安排为两层树型交换网络，在根部的带宽可以达到100-200Gbps。所有这些机器都采用相同的配置，都在同一个域内，因此任何两台机器之间的通讯时间都不会超过1微秒。</p><p>Tablet服务器、master、测试客户端和GFS服务器都在同样的机器集合上运行。每个机器都运行一个GFS服务器。其中一些机器还运行了一个tablet服务或一个客户端进程，或者运行了与这些实验同时使用池的其他作业的进程。</p><p>R是参与这次测试的BigTable的row key去重后的数量，R的选择需要确保每个benchmark在每个tablet服务器上读或写的数据量大约1GB。</p><p>负责顺序写的benchmark，使用名称从0到R-1的行键。行键的空间被分区成10N个等尺寸区间。这些区间被中央调度器分配到N个客户端，一旦，某个客户端已经处理完前面分配到的区间后，中央调度器就立即给这个客户端分配另一个区间。这种动态分配，可以减轻其他运行于当前机器上的进程对该机器的影响。我们在每个行键下面，写了一个单个字符串。每个字符串都是随机生成的，因此，不具备可压缩性。除此以外，在不同行键下面的字符串都是不同的，这样就不会存在跨行压缩。负责随机写的benchmark也是类似的，初了行键在写之前要以R为模数进行哈希操作，从而使得在整个benchmark期间，写操作负载可以均匀分布到整个行空间内。</p><p>负责顺序读的benchmark产生行键的方式，也和负责顺序写的benchmark类似。但是，顺序读benchmark不是在行键下面写数据，而是读取存储在行键下面的字符串，这些字符串是由前面的顺序写benchmark写入的。类似地，随机读benchmark会以随机的方式读取随机写benchmark所写入的数据。</p><p>Scan benchmark和顺序读benchmark类似，但是，使用了由BigTable API所提供的支持，来扫描属于某个域区间内的所有值。使用一个scan，减少了由benchmark所执行的RPC的数量，因为在Scan中，只需要一个RPC就可以从tablet服务器中获取大量顺序值。</p><p>随机读(mem) benchmark和随机读benchmark类似，但是，包含这个benchmark数据的局部群组，被标记为in-memory，因此，它是从内存中读取所需数据，而不是从GFS中读取数据。对这个benchmark而言，我们把每个tablet服务器所包含数据的数量，从1GB减少到100MB，从而可以很好地装入tablet服务器可用内存中。</p><p><img src=":paper-bigtable/image-20200507152024206.png" alt="image-20200507152024206"></p><p>图6显示了，当读或写1000字节的数据到BigTable中时，我们的benchmark的性能。表格内容显示了每个tablet服务器每秒的操作的数量。图显示了每秒的操作的总数量。</p><h2 id="单tablet服务性能（Single-tablet-server-performance）"><a href="#单tablet服务性能（Single-tablet-server-performance）" class="headerlink" title="单tablet服务性能（Single tablet-server performance）"></a>单tablet服务性能（Single tablet-server performance）</h2><p>让我们首先考虑只有一个tablet服务器的性能。随机读的性能要比其他类型的操作的性能都要差一个数量级，甚至差更多。每个随机读操作包含了，把一个64KB的SSTable块通过网络从GFS传输到一个tablet服务器，在这个块中，只有1000字节的数据会被使用。Tablet服务器大约每秒执行1200个操作，这会导致从GFS需要读取大约75MB/s。在这个过程中，带宽是足够的。许多具有这种访问模式的BigTable应用都把块大小设置为更小的值8KB。</p><p>从内存读取数据的随机读会更快一些，因为，每个1000字节读都是从tablet服务器内存当中读取，而不需要从GFS当中读取64KB的数据。</p><p>随机和顺序写执行的性能要好于随机读，因为，每个tablet服务器都把所有到达的写都追加到一个单独的提交日志中，并且使用分组提交，从而使得到GFS的写操作可以流水化。在随机写和顺序写之间，没有明显的性能差别。在两种情形下，所有的写都记录在同一个提交日志中。</p><p>顺序读的性能要好于随机读，因为从GFS中获取的每64KB SSTable块，会被存储到块缓存中，它可以为后面的64个读操作服务。</p><p>Scan操作会更快，因为针对单个RPC，tablet服务器可以返回大量的值，因此，RPC开销就可以分摊到大量的值当中。</p><h2 id="伸缩（Scaling）"><a href="#伸缩（Scaling）" class="headerlink" title="伸缩（Scaling）"></a>伸缩（Scaling）</h2><p>当我们在系统中把tablet服务器的数量从1增加到500的过程中，累计吞吐量急剧增加，通常以100倍的规模。例如，随着tablet服务器增长了500倍，针对内存的随机读的性能，增长了大约300倍，之所以会发生这种情况，因为这个benchmark的性能瓶颈是单个tablet服务器的CPU。</p><p>但是，性能不会线性增长。对于多数benchmark来说，当把tablet服务器的数量从1增加到50时，每个服务器的吞吐量有显著的降低。这个问题，是由多服务器环境中，负载不均衡引起的，通常由于有其他进程争抢CPU资源和网络带宽。我们的负载均衡算法努力解决这个问题，但是，无法实现完美的目标，主要原因在于：第一，重新负载均衡有时候会被禁止，这样做可以减少tablet迁移的数量（当一个tablet迁移时，在短时间内是不可用的，通常是一秒）；第二，我们的benchmark的负载是动态变化的。</p><p>随机读benchmark显示了最差的可扩展性。当服务器数量增加500倍时，累计吞吐量的增长只有100倍左右。导致这个问题的原因在于，对于每个1000字节的读操作，我们都会转移一个64KB的块。这个数据转移使得我们的GB级别的网络很快达到饱和，这样，当我们增加机器的数量时，单个服务器的吞吐量就会很快降低。</p><h1 id="8-实际应用（Real-Applications）"><a href="#8-实际应用（Real-Applications）" class="headerlink" title="8 实际应用（Real Applications）"></a>8 实际应用（Real Applications）</h1><p>截止到2006年8月，已经又388个非测试的BigTable集群运行在不同的Google服务器集群里，包括总共大约24500个tablet服务器。表1显示了每个集群中的tablet服务器的大概分布。许多集群都是用于开发目的，因此，在很多时间内都是空闲的。一组包含14个繁忙集群（包含8069个tablet服务器），每秒钟的累积负载是120万个请求。其中，到达的RPC流量是741MB/s，出去的RPC流量大约是16GB/s。</p><p>表2提供了一些关于当前正在使用的表的数据。一些表存储为用户服务的数据，而其他表则存储为批处理服务的数据。在总尺寸、平均单元格尺寸、从内存服务的数据的百分比以及表模式的复杂性方面，这些表区别很大。下面，我们将要分别描述三个产品团队如何使用BigTable。</p><p><img src=":paper-bigtable/image-20200507152117825.png" alt="image-20200507152117825"></p><p><img src=":paper-bigtable/image-20200507152148636.png" alt="image-20200507152148636"></p><h2 id="8-1-谷歌分析（Google-Analytics）"><a href="#8-1-谷歌分析（Google-Analytics）" class="headerlink" title="8.1 谷歌分析（Google Analytics）"></a>8.1 谷歌分析（Google Analytics）</h2><p>Google Analytics是一种服务，它帮助网站管理者分析网站流量模式。它提供了汇总分析，比如，每天不同访问者的数量，以及每天每个URL的网页视图的数量，以及网站流量报告，比如浏览了某个网页以后发生购买行为的用户的数量。</p><p>为了支持这项服务，网站管理员在网页中嵌入了一个小的Javascript程序。每当页面被访问时，都会触发这个Javascript程序。它把关于这个用户访问的所有相关信息都记录在Google Analytics中，比如用户标识以及被访问的网页的信息。Google Analytics分析这些数据，并提供给网站管理员访问。</p><p>我们简单地描述Google Analytics所使用的两个表。网站点击表（200TB）为每个用户访问都维护了一个行。行的名称是一个元祖，它包含了网站的名称以及session被创建的时间。这个表模式保证了，访问同一个WEB站点的session都是临近的，并且以时间顺序进行存储。这个表的压缩率可以达到14%。</p><p>汇总表（大约20TB）包含了针对每个网站的不同的汇总信息。这个表是从网站点击表中通过周期性地运行MapReduce作业而得到的。每个MapReduce作业从网站点击表当中抽取最近的session信息。系统的总吞吐量，受到了GFS吞吐量的限制。这个表的压缩率在29%左右。</p><h2 id="8-2-谷歌地球（Google-Earth）"><a href="#8-2-谷歌地球（Google-Earth）" class="headerlink" title="8.2 谷歌地球（Google Earth）"></a>8.2 谷歌地球（Google Earth）</h2><p>Google提供很多服务，它支持用户访问高清晰度的卫星图片，或者通过基于浏览器的Google Maps接口，或者通过定制的客户端软件Google Earth。这些产品允许用户在地球表面进行导航。该产品支持在不同清晰度下查看和标注地图信息。该系统采用一个表进行数据预处理，用另一个表位用户提供数据服务。</p><p>这个预处理管道使用一个表来存储卫星图片。在预处理过程中，影像数据被清洗并转换成最后的服务数据。这个表包含了大约70TB的数据，因此，是从磁盘访问的。影像数据都是经过压缩的，因此，BigTable就不存在数据压缩了。</p><p>影像表中的每行，都对应一个单个的地理片段。行被命名，以保证相邻的地理分段在存储时彼此接近。表包含了一个列家族来跟踪每个分段的数据。这个列家族包含了大量的列，基本上为每个原始影像创建一个列。因为，每个分段只是从少量几个列中的影像构建得到的，因此，这个列家族很稀疏。</p><p>这个预处理管道严重依赖于针对BigTable的MapReduce操作来转换数据。在这些MapReduce作业运行期间，整个系统的每个服务器大约处理1MB/秒的数据。</p><p>这个服务系统使用一个表来对存储在GFS中的数据建立索引。这个表相对比较小，大约500GB。但是，每个数据中心每秒钟必须以很低的延迟处理成千上万个请求。因而，这个表通常被重复存放到多个tablet服务器上，并且包含了内存列家族。</p><h2 id="8-3-个性化搜索（Personalized-Search）"><a href="#8-3-个性化搜索（Personalized-Search）" class="headerlink" title="8.3 个性化搜索（Personalized Search）"></a>8.3 个性化搜索（Personalized Search）</h2><p> Personalized search是一种服务，它记录了用户查询和点击数据，涵盖了各个方面的Google属性，比如，网页搜索、图片和新闻。用户可以浏览他们自己的访问历史，他们可以要求根据Google使用历史模式来获得个性化的搜索结果。</p><p>Personalized search把所有的用户数据都存放在BigTable中。每个用户都有一个独特的用户ID，并被分配了以这个用户ID为名字的行。每种类型的动作都会存储到特定的列家族中，比如，有一个列家族存储了所有的网页查询。每个数据元素都把这个动作发生的时间作为BigTable的时间戳。Personalized search在BigTable上使用MapReduce产生用户的profile。这些用户profile用来协助生成个性化的用户搜索结果。</p><p>Personalized Search的数据被分布到多个BigTable集群，来增加可用性，减少由距离而产生的延迟。Personalized Search最初构建了基于BigTable的客户端副本模式，从而保证所有副本的最终一致性。当前的系统使用了一个复制子系统，它是内嵌到服务器端的。</p><p>Personalized Search存储系统的设计，允许其他分组增加新的用户信息到他们自己的列中，这个系统当前正在被许多Google属性使用，这些属性需要存储用户的配置信息和设置。在多个分组之间共享一个表，导致了列家族数量比较大。为了帮助支持共享，我们为BigTable增加了一个简单的配额机制，从而对共享表当中某个特定应用可以使用的存储空间进行限制。这种机制为每个用户在使用本系统的不同的产品分组之间建立了隔离。</p><h1 id="9-教训（Lessons）"><a href="#9-教训（Lessons）" class="headerlink" title="9 教训（Lessons）"></a>9 教训（Lessons）</h1><p>在设计、实现、维护和支持BigTable的过程中，我们收获了有益的经验，并且获得了几个有意思的教训。</p><p>我们得到的一个教训是，大的分布式系统很发生多种错误，不仅是其他分布式系统经常遇到的标准的网络分割和故障。例如，我们已经遇到问题，他们是由以下原因引起的：内存和网络故障、大的时钟扭曲、机器挂起、我们所使用的其他系统（比如Chubby）中存在的故障、GFS配额当中的溢出以及计划或非计划之中的硬件维护。随着我们获得了更多的针对这些问题的经验，我们已经通过改变不同的协议来解决他们。例如，我们为RPC机制增加了checksumming。我们通过移除一部分系统针对另一部分系统所做的假设，也解决了一些问题。例如，我们取消了假设一个给定的Chubby操作只返回一个固定错误集合。</p><p>我们获得的另一个教训是，在很清楚地知道如何使用一个新特性之前，不要随便增加这个新特性。例如，我们最初计划在我们的应用API当中支持通用的事务。因为我们不会立即用到这种功能，所以，我们没有实现它。现在我们有许多应用都是运行在BigTable之上，我们就可以考察他们的应用需求，发现很多应用只需要针对单个记录的事务。当人们需要分布式事务时，最主要的用途就在于维护二级索引，我们就考虑增加一个特定的机制来满足这种需求。在通用性方面，这种特定机制会比通用事务模型差一些，但是，它更高效，尤其是对于那些需要跨越多个行的更新而言。</p><p>从支持BigTable运行中获得的一个比较实际的教训是，合适的系统级别的监视的重要性，即监视BigTable自己，也监视运行在BigTable上的进程。例如，我们扩展了我们的RPC系统，从而使得对一些RPC样本，它会详细跟踪记录针对该RPC的重要动作。这种特性已经允许我们探测和解决许多问题，比如针对tablet数据结构的锁冲突，当执行BigTable更新时的慢写，以及当METADATA tablet不可用时，访问METADATA表发生的阻塞。说明监视比较有用的另一个例子是，每个BigTable集群都是在Chubby中注册的。这就允许我们跟踪所有的集群，发现这些集群的大小，看看它们当前使用的是我们软件的哪个版本，以及是否存在一些预料之外的问题，比如很大的延迟。</p><p>我们所获得的最重要的教训就是简单设计的价值。假设我们系统的尺寸是大约10万行非测试代码，以及这些代码会随着时间演化，我们发现代码和设计的清晰性对于代码和系统维护具有重要的帮助。一个例子是我们的tablet服务器成员协议。我们的第一个协议很简单，主服务器周期性地发布租约给tablet服务器，如果tablet服务器发现租约到期就自杀。不幸的是，当存在网络问题时，这种协议极大降低了系统的可用性，并且对于主服务器的恢复时间也很敏感。我们先后几次更改了协议的设计，直到找到一个好的协议。但是，这个比较好的协议，太复杂，并且依赖于那些很少被其他应用使用的Chubby功能。我们发现，我们花费了大量的时间来调试各种晦涩的案例。最后，我们废弃了这个协议，转向采用一个比较简单的新的协议，它只依赖那些经常被使用的Chubby功能。</p><h1 id="10-相关工作（Related-Work）"><a href="#10-相关工作（Related-Work）" class="headerlink" title="10 相关工作（Related Work）"></a>10 相关工作（Related Work）</h1><p>Boxwood项目[24]具有一些和Chubby、GFS以及BigTable重叠的组件，因为Boxwood支持分布式协议和分布式B树存储。在每个发生重叠的领域中，Boxwood看起来似乎针对的是Google所提供服务的低一层次的服务。Boxwood项目的目标是提供构建高层次服务的基础架构，比如文件系统或数据库，而BigTable的目标是直接支持那些需要存储数据的客户端应用。</p><p>许多最近的计划，已经解决了在广域网内提供分布式存储和高层次服务的问题。这些计划包括，在分布式哈希表方面的工作，比如项目CAN[29]、CHORD[32]、Tapestry[37]和Pastry[30]。这些系统解决的问题，在BigTable中没有出现，比如高可用性带宽、不可信的参与者或频繁地重配置。分布式控制和理想的零错误不是BigTable的设计目标。</p><p>就可以提供给应用开发者的分布式存储模型而言，我们认为，分布式B树所提供的“键-值对”模型或者分布式哈希表具有很大的局限性。“键-值对”是一个有用的积木，但是，它不应该是可以提供给应用开发者的唯一的积木。我们所能提供的模型比简单的“键-值对”更加丰富，并且支持稀疏的办结构化数据。但是，它是非常简单的，并且足以支持非常高效的平面文件表示，并且它足够透明，允许用户来调节系统重要的行为。</p><p>有几个数据库开发商已经开发了并行数据库，它可以存储大量的数据。Oracle的Real Application Cluster数据库[27]，使用共享的磁盘来存储数据（BigTable使用GFS），并且使用一个分布式的锁管理器（BigTable使用Chubby）。IBM的DB2 Parallel Edition[4]是基于非共享[33]的体系架构，这一点和BigTable类似。每个DB2服务器负责表中行的一个子集，它被存储在一个局部关系数据库中。IBM的DB2 Parallel Edition和Oracle的Real Application Cluster都提供了完整的事务模型。</p><p>BigTable局部分组实现了和其他一些系统类似的压缩和磁盘读性能，这些系统在磁盘上存储数据室，采用基于列而不是基于行的存储，包括C-store[1,34]和商业化产品，比如Sybase IQ[15,36]，SenSage[31]，KDB+[32]，以及在MonetDB/X100[38]当中的ColumnBM存储层。另一个把数据垂直和水平分区到平面文件中并且取得了很好的数据压缩率的系统是，AT&amp;T的Daytona数据库[19]。局部分组不支持CPU缓存级别的优化，比如那些在Ailamaki[2]中描述的。</p><p>​        BigTable使用memtable和SSTable来存储tablet更新的方式，和Log-Structured Merge Tree[26]用来存储索引更新的方式是类似的。在这两个系统中，排序的数据在写入到磁盘之前是存放在内存之中的，读操作必须把来自磁盘和内存的数据进行合并。</p><p>C-Store和BigTable具有很多相似的特性：两个系统都使用非共享的体系架构，都有两个不同的数据结构，一个是为存放最近的写操作，一个是为存放长期的数据，并且设计了把数据从一个结构转移到另一个结构的机制。但是，这两个系统在API方面具有很大的区别：C-Store就像一个关系型的数据库，而BigTable提供了一个低层次的读和写接口，并且被设计成在每个服务器上每秒钟内提供成千上万的这种操作。C-Store也是一个读优化的关系型DBMS，而BigTable对读敏感和写敏感的应用都提供了很好的性能。</p><p>BigTable的负载均衡器，必须解决负载均衡和内存均衡问题，这也是非共享数据库[11,35]通常面临的问题。我们的问题更加简单一些：（1）我们不考虑同一个数据存在多个副本的情形，我们可能采用视图或索引的形式以另一种方式来看待副本；（2）我们让用户告诉我们，什么数据应该放在内存中，什么数据应该放在磁盘上，而不是系统自己尽力去猜测数据的存放位置；（3）我们没有复杂的查询优化机制，也不执行复杂的查询。</p><h1 id="总结（Conclusions）"><a href="#总结（Conclusions）" class="headerlink" title="总结（Conclusions）"></a>总结（Conclusions）</h1><p>我们已经描述了BigTable，一个为Google存储数据的分布式存系统。自从2005年4月开始，BigTable集群已经在实际生产系统中使用。在那之前，我们已经投入了7个person-years在设计和开发上。截止2006年8月，有超过60项计划正在使用BigTable。我们的用户喜欢BigTable 提供的高性能和高可用性。当用户的资源需求随着时间变化时，他们只需要简单地往系统中添加机器，就可以实现服务器集群的扩展。</p><p>鉴于BigTable的不同寻常的接口，一个有意思的问题是，我们的用户适应BigTable的过程是多么艰难。新的用户有时候不知道如何很好地使用BigTable提供的接口，尤其是，他们已经习惯使用支持通用事务的关系型数据库。但是，许多Google产品成功地使用BigTable来存放数据这样一个事实，已经说明BigTable设计的成功。</p><p>我们正在考虑增加其他几个额外的BigTable功能，比如支持二级索引，以及一个基础框架，它可以支持构建跨数据中心分布BigTable数据。我们已经开始把BigTable作为一个服务提供给产品组，从而使得每个组都不需要维护他们自己的集群。随着我们的服务集群的扩展，我们需要处理更多的BigTable内部的资源共享的问题。</p><p>最后值得一提的是，我们发现在google构建我们的存储解决方案有很大的优势。我们不仅有足够的灵活度为bigtable设计数据模型，我们还能够完全控制BigTable的实现,甚至Bigtable依赖的其它google基础设施。这些权限意味着一旦(bigtable或其依赖的基础设施)出现了瓶颈和低效之处，我们能够及时解决。</p><h1 id="致谢（Acknowledgements）"><a href="#致谢（Acknowledgements）" class="headerlink" title="致谢（Acknowledgements）"></a>致谢（Acknowledgements）</h1><p>We thank the anonymous reviewers, David Nagle, and our shepherd Brad Calder, for their feedback on this paper. The Bigtable system has benefited greatly from the<br>feedback of our many users within Google. In addition, we thank the following people for their contributions to Bigtable: Dan Aguayo, Sameer Ajmani, Zhifeng Chen,<br>Bill Coughran, Mike Epstein, Healfdene Goguen, Robert Griesemer, Jeremy Hylton, Josh Hyman, Alex Khesin, Joanna Kulik, Alberto Lerner, Sherry Listgarten, Mike<br>Maloney, Eduardo Pinheiro, Kathy Polizzi, Frank Yellin, and Arthur Zwiegincew.</p><h1 id="引用（References）"><a href="#引用（References）" class="headerlink" title="引用（References）"></a>引用（References）</h1><p>[1] ABADI, D. J., MADDEN, S. R., AND FERREIRA,M. C. Integrating compression and execution in columnoriented database systems. Proc. of SIGMOD (2006).</p><p>[2] AILAMAKI, A., DEWITT, D. J., HILL, M. D., AND SKOUNAKIS, M. Weaving relations for cache performance. In The VLDB Journal (2001), pp. 169–180.</p><p>[3] BANGA, G., DRUSCHEL, P., AND MOGUL, J. C. Resource containers: A new facility for resource management in server systems. In Proc. of the 3rd OSDI (Feb.1999), pp. 45–58.</p><p>[4] BARU, C. K., FECTEAU, G., GOYAL, A., HSIAO,H., JHINGRAN, A., PADMANABHAN, S., COPELAND, G. P., AND WILSON, W. G. DB2 parallel edition. IBM Systems Journal 34, 2 (1995), 292–322.</p><p>[5] BAVIER, A., BOWMAN, M., CHUN, B., CULLER, D., KARLIN, S., PETERSON, L., ROSCOE, T., SPALINK, T.,AND WAWRZONIAK, M. Operating system support for planetary-scale network services. In Proc. of the 1st NSDI (Mar. 2004), pp. 253–266.</p><p>[6] BENTLEY, J. L., AND MCILROY, M. D. Data compression using long common strings. In Data Compression Conference (1999), pp. 287–295.</p><p>[7] BLOOM, B. H. Space/time trade-offs in hash coding with allowable errors. CACM 13, 7 (1970), 422–426.</p><p>[8] BURROWS, M. The Chubby lock service for looselycoupled distributed systems. In Proc. of the 7th OSDI (Nov. 2006).</p><p>[9] CHANDRA, T., GRIESEMER, R., AND REDSTONE, J. Paxos made live — An engineering perspective. In Proc. of PODC (2007).</p><p>[10] COMER, D. Ubiquitous B-tree. Computing Surveys 11, 2 (June 1979), 121–137.</p><p>[11] COPELAND, G. P., ALEXANDER, W., BOUGHTER, E. E., AND KELLER, T. W. Data placement in Bubba. In Proc. of SIGMOD (1988), pp. 99–108.</p><p>[12] DEAN, J., AND GHEMAWAT, S. MapReduce: Simplified data processing on large clusters. In Proc. of the 6th OSDI (Dec. 2004), pp. 137–150.</p><p>[13] DEWITT, D., KATZ, R., OLKEN, F., SHAPIRO, L., STONEBRAKER, M., AND WOOD, D. Implementation techniques for main memory database systems. In Proc. of SIGMOD (June 1984), pp. 1–8.</p><p>[14] DEWITT, D. J., AND GRAY, J. Parallel database systems: The future of high performance database systems. CACM 35, 6 (June 1992), 85–98.</p><p>[15] FRENCH, C. D. One size fits all database architectures do not work for DSS. In Proc. of SIGMOD (May 1995), pp. 449–450.</p><p>[16] GAWLICK, D., AND KINKADE, D. Varieties of concurrency control in IMS/VS fast path. Database Engineering Bulletin 8, 2 (1985), 3–10.</p><p>[17] GHEMAWAT, S., GOBIOFF, H., AND LEUNG, S.-T. The Google file system. In Proc. of the 19th ACM SOSP (Dec.2003), pp. 29–43.</p><p>[18] GRAY, J. Notes on database operating systems. In Operating Systems — An Advanced Course, vol. 60 of Lecture Notes in Computer Science. Springer-Verlag, 1978.</p><p>[19] GREER, R. Daytona and the fourth-generation language Cymbal. In Proc. of SIGMOD (1999), pp. 525–526.</p><p>[20] HAGMANN, R. Reimplementing the Cedar file system using logging and group commit. In Proc. of the 11th SOSP (Dec. 1987), pp. 155–162.</p><p>[21] HARTMAN, J. H., AND OUSTERHOUT, J. K. The Zebra striped network file system. In Proc. of the 14th SOSP (Asheville, NC, 1993), pp. 29–43.</p><p>[22] KX.COM. kx.com/products/database.php. Product page.</p><p>[23] LAMPORT, L. The part-time parliament. ACM TOCS 16,2 (1998), 133–169.</p><p>[24] MACCORMICK, J., MURPHY, N., NAJORK, M., THEKKATH, C. A., AND ZHOU, L. Boxwood: Abstractions as the foundation for storage infrastructure. In Proc. of the 6th OSDI (Dec. 2004), pp. 105–120.</p><p>[25] MCCARTHY, J. Recursive functions of symbolic expressions and their computation by machine. CACM3, 4 (Apr. 1960), 184–195.</p><p>[26] O’NEIL, P., CHENG, E., GAWLICK, D., AND O’NEIL,E. The log-structured merge-tree (LSM-tree). Acta Inf.33, 4 (1996), 351–385.</p><p>[27] ORACLE.COM. www.oracle.com/technology/products/-database/clustering/index.html. Product page.</p><p>[28] PIKE, R., DORWARD, S., GRIESEMER, R., AND QUINLAN, S. Interpreting the data: Parallel analysis with Sawzall. Scientific Programming Journal 13, 4 (2005),227–298.</p><p>[29] RATNASAMY, S., FRANCIS, P., HANDLEY, M., KARP, R., AND SHENKER, S. A scalable content-addressable network. In Proc. of SIGCOMM (Aug. 2001), pp. 161–172.</p><p>[30] ROWSTRON, A., AND DRUSCHEL, P. Pastry: Scalable, distributed object location and routing for largescale peer-to-peer systems. In Proc. of Middleware 2001 (Nov. 2001), pp. 329–350.</p><p>[31] SENSAGE.COM. sensage.com/products-sensage.htm. Product page.</p><p>[32] STOICA, I., MORRIS, R., KARGER, D., KAASHOEK, M. F., AND BALAKRISHNAN, H. Chord: A scalable peer-to-peer lookup service for Internet applications. In Proc. of SIGCOMM (Aug. 2001), pp. 149–160.</p><p>[33] STONEBRAKER, M. The case for shared nothing. Database Engineering Bulletin 9, 1 (Mar. 1986), 4–9.</p><p>[34] STONEBRAKER, M., ABADI, D. J., BATKIN, A., CHEN, X., CHERNIACK, M., FERREIRA, M., LAU, E., LIN, A., MADDEN, S., O’NEIL, E., O’NEIL, P., RASIN, A., TRAN, N., AND ZDONIK, S. C-Store: A columnoriented DBMS. In Proc. of VLDB (Aug. 2005), pp. 553–564.</p><p>[35] STONEBRAKER, M., AOKI, P. M., DEVINE, R., LITWIN, W., AND OLSON, M. A. Mariposa: A new architecture for distributed data. In Proc. of the Tenth ICDE(1994), IEEE Computer Society, pp. 54–65.</p><p>[36] SYBASE.COM. www.sybase.com/products/databaseservers/sybaseiq. Product page.</p><p>[37] ZHAO, B. Y., KUBIATOWICZ, J., AND JOSEPH, A. D. Tapestry: An infrastructure for fault-tolerant wide-area location and routing. Tech. Rep. UCB/CSD-01-1141, CS Division, UC Berkeley, Apr. 2001.</p><p>[38] ZUKOWSKI, M., BONCZ, P. A., NES, N., AND HEMAN, S. MonetDB/X100—A DBMS in the CPU cache. IEEE Data Eng. Bull. 28, 2 (2005), 17–22.</p>]]></content>
    
    
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;Google三驾马车的最后一篇论文，最先发表于2006年的OSDI上。HBase的前身。&lt;/p&gt;
&lt;p&gt;最后一篇翻译了，这3篇真是把我翻译吐了。不是为了凑个整我就放弃了，翻译一篇文章要比读一篇文章要费劲的多得多！！！ 感觉纯粹是浪费自我的时间，头大-_-||后面还是就写笔记+总结形式吧。&lt;/p&gt;
&lt;/blockquote&gt;</summary>
    
    
    
    
  </entry>
  
  <entry>
    <title>【PaperReading】MapReduce（个人翻译）</title>
    <link href="http://lollipops.work/2020/05/10/Paper-MapReduce/"/>
    <id>http://lollipops.work/2020/05/10/Paper-MapReduce/</id>
    <published>2020-05-09T17:06:26.000Z</published>
    <updated>2020-08-14T09:51:03.456Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>Google三驾马车的第二篇论文，最先发表于2004年OSDI（操作系统领域的顶会）上。也是大数据最重要的一个数据处理方案。</p></blockquote><h2 id="MapReduce-Simplified-Data-Processing-on-Large-Clusters"><a href="#MapReduce-Simplified-Data-Processing-on-Large-Clusters" class="headerlink" title="MapReduce: Simplified Data Processing on Large Clusters"></a>MapReduce: Simplified Data Processing on Large Clusters</h2><p>Jeffrey Dean and Sanjay Ghemawat</p><h2 id="摘要（Abstract）"><a href="#摘要（Abstract）" class="headerlink" title="摘要（Abstract）"></a>摘要（Abstract）</h2><p>MapReduce准确的来说应该是一个编程模型，或者说是一个用来处理和生产大量数据集的相关实现。用户通过指定一个map函数来处理一个K/V键值对，这个键值对会被map生成一系列的中间键值对集。通过reduce函数，我们再将那些相同的中间key所关联的value值合并。这篇论文会展示在该模型下的一些真实场景下的任务。</p><p>那些用函数式的编写的程序会被自动的，并行的执行在一个很大的集群上。这个运行时系统会自己处理好如何分割输入的数据，程序执行资源的调度，机器失败的处理，管理必要的机器间的通信等这些细节问题。这让那些没有任何并行计算和分布式系统工作经验的程序员可以轻松的利用好一个很大的分布式系统的资源。</p><p>我们的集群是高度可扩展的：一个典型的MapReduce程序可以在上千台机器上来处理TB级的数据。程序员会觉得这个系统非常好用：在Google的每天都有数百个MapReduce程序被实现，并且上千个MapReduce任务运行在集群上。</p><h2 id="1-简介（Introduction）"><a href="#1-简介（Introduction）" class="headerlink" title="1. 简介（Introduction）"></a>1. 简介（Introduction）</h2><p>在过去的五年中，作者和许多Google的其他人已经实现了数百个用于特殊目的的计算：这些计算来处理大量原始数据，如爬虫抓取的文档，Web请求日志等，从而计算各种衍生的数据，例如倒排索引，Web文档的各种分析图像表示，每个主机爬取的页面数的结果汇总，在给定日期中最频繁请求的集合等。大多数此类计算从概念上讲是很直接。但是，输入数据量通常很大，为了在合理的时间内完成，这些计算必须分布在数百或数千台机器上。如何并行化计算，数据分布以及故障处理这些问题需要大量复杂的代码来处理他，这就让原来的简单的计算不那么容易了。</p><p>为了应对这种复杂性，我们设计了一个新的抽象，它允许我们只需要表达我想要执行的最简单的计算，但是隐藏了底层中对计算并行化，容错，数据分发和负载均衡的复杂的实现细节。这种抽象的灵感来自于Lisp和许多其他函数语言中的map和reduce原语。我们意识到，大部分我们的计算都涉及到使用map操作来作用于输入数据中每一个逻辑上的“记录”来生成一些列中间临时的kv键值对，然后我们在对这些键值对中相同key的值做reduce操作来适当的组合出派生数据。使用一个由用户来指定的map和reduce操作的这样一个函数式模型，让我们可以容易的并行化处理大型计算并可以使用重新执行来作为主要容错机制。</p><p>我们这项工作的主要贡献点是提供了简单而强大的接口，可以让大规模计算的自动并行化和分发，并通过此接口的实现，可以在大量的商用PC集群上获取高性能。</p><p>本文第2节描述了基本的编程模型，并给出了几个例子。第3节描述了一个针对基于群集的计算环境定制的MapReduce接口的实现。第4节描述了几个我们发现比较有用的编程模型。第5节使用各种任务的对我们的实现进行性能测试。第6节探讨了MapReduce在Google中的使用，包括我们使用它来重写生产索引系统的经验。第7节讨论相关以及未来的工作。</p><h2 id="2-编程模型（Programming-Model）"><a href="#2-编程模型（Programming-Model）" class="headerlink" title="2. 编程模型（Programming Model）"></a>2. 编程模型（Programming Model）</h2><p>计算通过输入一系列键值对，然后生成一些列键值对。MapReduce库的使用者将计算的表示为2种函数：Map和Reduce。</p><p>Map，由用户编写通过输入的键值对来生成新的一系列中间键值对。MapReduce库会将有相同中间Key：<code>I</code>的中间value全部都分组在一起，并将它们传递给Reduce函数。</p><p>Reduce函数也是由用户编写。它接受一个中间Key：<code>I</code>以及该key对应的一系列的的值。它会合并这些值的集合并大可能生成更小的集合。通常每次reduce调用后都只生成0个或者1个输出结果。这些中间值会通过迭代器提供给用户的reduce函数。这就可以使得我们处理那些太大而无法全部存储在内存中的值列表。</p><h3 id="2-1-例子（Example）"><a href="#2-1-例子（Example）" class="headerlink" title="2.1 例子（Example）"></a>2.1 例子（Example）</h3><p>思考下计算大量文档中每个单词出现次数的问题。用户可能会编写类似于以下伪代码的代码：</p><pre><code class="lang-pseudocode">map(String key, String value):// key: document name// value: document contentsfor each word w in value:EmitIntermediate(w, &quot;1&quot;);reduce(String key, Iterator values):// key: a word// values: a list of countsint result = 0;for each v in values:result += ParseInt(v);Emit(AsString(result));</code></pre><p>map函数将每个单词都加上一个关联数：该词出现的次数（这个简单的例子中就是1）。reduce函数将给定的单词的所有计数结果汇众求和。</p><p>此外，用户使用输入和输出文件名称以及一些可选的调参来编写的代码将数据写入MapReduce约定的对象中，然后用户就可以直接调用MapReduce的方法，并将上面生成好的特定对象传给MapReduce。这样用户的代码和MapReduce库（C++实现的）就可以连接起来了。附录A包含此示例的完整程序。</p><h3 id="2-2-类型（Types）"><a href="#2-2-类型（Types）" class="headerlink" title="2.2 类型（Types）"></a>2.2 类型（Types）</h3><p>尽管先前的伪代码是根据输入和输出都是以字符串类型进行编写的，但从概念上讲，用户提供的map和reduce函数的有下面的类型关联：</p><pre><code class="lang-pseudocode">map (k1,v1) → list(k2,v2)reduce (k2,list(v2)) → list(v2)</code></pre><p>也就是说，输入的keys和values和输出的keys和values的值不是同一个域【这里没太翻译明白】【就是说k2和v2和k1，v1不是同一个域？】。此外，中间的keys和values和结果的keys和values是属于同一个域。【v2】</p><p>我们C++实现的是使用字符串来和用户定义函数进行交互的，字符串转换为用户自己想要的类型是交给用户自己的代码去转换的。</p><h3 id="2-3-更多例子（More-Examples）"><a href="#2-3-更多例子（More-Examples）" class="headerlink" title="2.3 更多例子（More Examples）"></a>2.3 更多例子（More Examples）</h3><p>这里有一些有趣程序的简单示例，可以很容易地表现MapReduce计算。</p><p><strong>分布式的grep</strong>：如果一行数据匹配所给定的模式map函数就会将改行发出来，然后reduce函数是一个标识函数只负责将中间的数据拷贝到结果输出。</p><p><strong>计算URL访问频率</strong>：map函数处理web网页的请求日志并输出{URL,1}。然后reduce函数对一样的URL的值相加，并发出{URL,总和}的键值对。</p><p><strong>反转Web链接图</strong>：map函数将一个页面中所有能找到的叫source的链接和它的目标URL输出成{target，source}。然后reduce函数对相同target的source们聚合然后发出这个新的键值对{target, list(source)}。</p><p><strong>每个主机的Term-向量</strong>：【这里的term是搜索的概念，term就是一句话中的词组的意思】Term-向量是一个{word, frequency}键值对的一个列表，它总结了一个文档或者一组文档里面出现的最重要的单词。map函数对每个输入文档（其主机名是从文档的URL中抽取出来的）处理为{hostname, term vector}并发  出去。reduce函数传递一个主机所有文档的term-向量，然后将这些向量累加起来，丢弃不常用的term，最后发出{hostname, term vector}键值对。</p><p><strong>倒排索引</strong>：map函数解析每个文档，并发出一系列{word, 文档ID}的键值对，reduce函数会对一个给定的单词接受所有的键值对，然后对该单词的  文档ID进行排序并发出{word, list(文档ID)}的键值对。所有输出的键值对集合就形成了一个简单的倒排索引。通过增加这个计算来跟踪单词的位置是很容易的。</p><p><strong>分布式排序</strong>：map函数从每个记录中提取key，并发出{key，记录}这个键值对。 reduce函数会不做修改的发送所有的键值对。此计算要依赖第4.1节中描述的分区工具和第4.2节中描述的排序属性。</p><h2 id="3-实现（Implementation）"><a href="#3-实现（Implementation）" class="headerlink" title="3 实现（Implementation）"></a>3 实现（Implementation）</h2><p>MapReduce接口的可以有很多不同的实现。正确的选择取决于环境。例如，一种实现方式可能适用于小型共享存储器机器，另一种实现方式适用于大型NUMA多处理器，而另一种实现方式适用于甚至更大的联网机器集。</p><p>本节描述了一个在Google被广泛使用的计算环境的实现：一天用交换式以太网连接在一起的大型商用PC集群[4]。在我们的环境中：</p><p>（1）一般机器都是运行了Linux系统的双处理器x86处理器，每台机器有2-4 GB的内存。</p><p>（2）使用商用网络硬件 - 一般在单机器级别上为100M/秒或1G/秒，但在整体上带宽的平均值来要比这个带宽中位数要少得多。</p><p>（3）集群由数百或数千台机器组成，因此机器故障很常见。</p><p>（4）存储由廉价的直连在各个机器上的IDE磁盘提供。内部开发的分布式文件系统[8]用于管理存储在这些磁盘上的数据。文件系统通过复制在不可靠的硬件之上提供可用性和可靠性。</p><p>（5）用户将作业提交给调度系统。每个作业由一组任务组成，并由调度程序映射到集群中的一组可用计算机。</p><h3 id="3-1-执行概述（Execution-Overview）"><a href="#3-1-执行概述（Execution-Overview）" class="headerlink" title="3.1 执行概述（Execution Overview）"></a>3.1 执行概述（Execution Overview）</h3><p>Map函数的调用将被分布在多太机器上来自动将输入的数据分割为M份。这些被分割的输入可以在不同的机器上被并行处理。Reduce函数的调用也会被分布在多台机子上，它通过一个分区函数（比如hash(key) mod R）将map中生成的临时key来分片为R个片段。分区数量（R）以及分区函数由用户来指定。</p><p>图1显示了我们实现中MapReduce操作的总体流程。当用户程序调用MapReduce函数时，会发生以下一系列操作（图1中的编号标签对应于下面列表中的数字）：</p><p><img src=":paper-mapreduce/image-20200425145657218.png" alt="image-20200425145657218"></p><ol><li>用户程序中的MapReduce库首先将输入文件分成每块通常16M到64MB的M个块（这个可由用户通过可选参数控制）。然后，MapReduce会在一组计算机上启动该程序的许多副本【fork的过程】。</li><li>该程序的其中一个副本是特殊的-master。其余都是由主人分配工作的worker。有M个map任务和R个reduce任务要分配。master会挑选闲置的worker并为每个人分配一个map任务或reduce任务。</li><li><p>被分配了Map任务的worker要读取相应的被拆分的输入块的内容。它从输入数据中解析出键/值对，并将该键值对传递给用户定义的Map函数。Map函数生成的中间键/值对是缓存在内存中的。</p></li><li><p>周期性地，缓存的键值对会被写入本地磁盘，通过分区函数将数据划分为R个区域。这些被缓存的数据序列化到本地磁盘上这的磁盘位置将传回给master，master再负责将这些位置转发给reduce的workers。</p></li><li><p>当主服务器通知reduce workers这些位置时，它使用远程过程调用（RPC）从map的workers的本地磁盘读取缓冲数据。当reduce工作者读取了所有中间数据时，它会通过键来对这些数据进行排序，以便将所有出现的相同键组合在一起。之所以需要排序，因为通常很多不同的键都被映射到同一个reduce任务上。如果中间数据量太大而无法全存储在内存中，则使用外部排序。</p></li><li><p>reduce的worker对已排序的中间数据进行迭代，并且对于每个遇到的唯一key，它将key相应的中间value值集合传递给用户的Reduce函数。Reduce函数的输出结果被附加到这部分reduce分区上的一个最终输出文件上去。</p></li><li><p>当完成所有map任务和reduce任务后，master会唤醒用户程序。此时，用户程序中的MapReduce调用会return返回到用户代码中。</p></li></ol><p>成功完成后，MapReduce的执行结果就在那R个输出文件中（每个reduce任务一个，文件名由用户指定）。通常，用户不需要将这R个输出文件合并成一个文件，它们通常将这些文件作为输入传递给另一个MapReduce过程的调用，或者从另一个能够处理被分区为多个文件的来作为输入的分布式应用程序中使用它们。</p><h3 id="3-2-Master的数据结构（Master-Data-Structures）"><a href="#3-2-Master的数据结构（Master-Data-Structures）" class="headerlink" title="3.2 Master的数据结构（Master Data Structures）"></a>3.2 Master的数据结构（Master Data Structures）</h3><p>master拥有几中数据结构。对于每个map任务和reduce任务，它存储他们的状态（空闲，正在进行或已完成）以及正在工作机器的标识（用于非空闲任务）。</p><p>master是将这些文件区域的位置从map任务传播到reduce任务的管道。因此，对每一个完成的map任务，master会存储这些由map任务生成的R个中间文件块的位置和大小。当map任务完成是，这些文件位置和大小的变更也会被master接受。信息将逐步推送给正在进行reduce任务的workers。</p><h3 id="3-3-容错（Fault-Tolerance）"><a href="#3-3-容错（Fault-Tolerance）" class="headerlink" title="3.3 容错（Fault Tolerance）"></a>3.3 容错（Fault Tolerance）</h3><p>由于MapReduce库旨在帮助使用数百或数千台计算机处理大量数据，因此程序库必须能够优雅地容忍计算机故障。</p><h4 id="Worker失败"><a href="#Worker失败" class="headerlink" title="Worker失败"></a>Worker失败</h4><p>master会周期性的ping每个worker。如果在一个指定的时间范围内没有收到worker响应，master将把这个worker节点标记为失效。所有由这个失效的worker完成的Map任务被设回到初始的空闲状态，这样这些任务才能再被安排给其他的worker。同样的，失效worker上正在运行的Map或Reduce任务也将被重新置为空闲状态，等待重新调度。</p><p>当worker故障时，之所以要重新执行已经完成的map任务是因为这些任务的输出都存储在这台机器上，因此这些输出变成不可访问的了。已经完成的Reduce任务的输出存储在全局文件系统上【类似于GFS这样的系统上，是安全的】，因此不需要再次执行。</p><p>当一个Map任务先被worker A执行，之后又被调度到worker B执行（因为worker A失效了），这个重新执行的动作会被通知给所有执行Reduce任务的workers。任何还没有从worker A读取数据的Reduce任务将改为从worker B读取数据。</p><p>MapReduce可以适应大规模worker失效的情况。比如，在一个MapReduce操作中，在一个正在运行的集群上进行网络维护导致了80台机器在同一时间几分钟内无法访问了，MapReduce的master简单的再次执行下那些不可访问的worker已经完成的工作，之后继续向前执行，最终来完成这个MapReduce操作。</p><h4 id="Master失败"><a href="#Master失败" class="headerlink" title="Master失败"></a>Master失败</h4><p>master可以很容易的周期性的讲上面所描述的数据结构【master存储的那些信息】写入磁盘生成checkpoint检查点。如果master任务挂了，可以从最新的一个checkpoint来启动一个新的副本master进程。然而，鉴于只有一个master进程，master失败的可能性不大【这里笔误吧？”its failure is unlikely”应该表达的意思是如果master失败的结果是难以想象的】；因此我们当前的实现中如果master失效，就中止MapReduce计算。客户端可以检查此状态，并根据需要重新执行MapReduce操作。</p><h4 id="存在失败的语义【这段没怎么明白标题什么意思，这一段都不太明白】"><a href="#存在失败的语义【这段没怎么明白标题什么意思，这一段都不太明白】" class="headerlink" title="存在失败的语义【这段没怎么明白标题什么意思，这一段都不太明白】"></a>存在失败的语义【这段没怎么明白标题什么意思，这一段都不太明白】</h4><p>当用户提供的Map和Reduce算子是输入值确定的函数时，我们的分布式实现下的输出结果和整个程序没有任何错误并顺序执行产生的输出结果是一样的。【同样的输入应该同样的输出】</p><p>我们靠原子提交Map和Reduce任务的输出来完成这个特性。每个执行中的任务把它的输出写到私有临时文件中。一个Reduce任务会生成一个这样的文件，而一个Map任务则会生成R个这样的文件（每个Reduce任务对应一个）。当一个Map任务完成的时，worker发送一个包含了R个临时文件名的消息给master。如果master从一个已经完成的Map任务再次接收到到一个完成消息，master直接忽略这个消息；否则，master将这R个文件的名称记录在起一个数据结构中。</p><p>当reduce任务完成时，reduce worker会原子的把临时输出文件重命名为最终的输出文件。如果同一个Reduce任务在多台机器上执行，多个重命名操作会作用在同一个最终的输出文件上。我们依赖底层文件系统提供的原子性重命名操作的来保证最终的文件系统状态只是一个reduce任务产生的数据。</p><p>绝大多数的Map和Reduce<strong>算子</strong>【感觉这个翻译更好】是确定性的，以及在这种情况下我们在语义上和顺序执行情况下是等同的这样一个事实，让程序员们很容易推断他们程序的行为。当Map和/或Reduce算子是不确定性的时候，我们提供虽然弱但是依然合理的处理机制。当使用非确定算子情况下，一个特定reduce任务R1的输出和一个非确定性程序顺序执行产生时的输出是等价的。但是，另一个Reduce任务R2的输出也许对应着一个不同的非确定顺序程序执行产生的R2的输出。</p><p>考虑Map任务M和Reduce任务R1、R2的情况。设e(Ri)为Ri已经提交的执行（有且只有一个这样的执行）。较弱的语义会在当e(R1)读取了由M一次执行产生的输出，而e(R2)读取了由M的另一次执行产生的输出时出现。</p><h3 id="3-4-存储位置（Locality）"><a href="#3-4-存储位置（Locality）" class="headerlink" title="3.4 存储位置（Locality）"></a>3.4 存储位置（Locality）</h3><p>在我们的计算环境中网络带宽是一个相对稀缺的资源。我们通过尽量把输入数据(由GFS管理)存储在集群中机器的本地磁盘上来节省网络带宽。GFS将每个文件分割为64MB的小块，每个块有多个副本（一般是3个拷贝）保存在多台机器上。MapReduce的master在准备调度map任务时会考虑到输入文件的位置信息，尽量在存储了输入数据备份的机器上执行map任务；如果上面没有成功【比如存储的那个机器无法分配任务】，master将尝试在靠近有输入数据备份的机器上执行Map任务(例如，在一个和含有输入数据的机器在一个交换机里的worker机器上执行)。当在一个足够大的cluster集群上运行大型MapReduce操作的时候，大部分的输入数据都能从本地机器读取，因此基本不会消耗网络带宽。</p><h3 id="3-5-任务粒度（Task-Granularity）"><a href="#3-5-任务粒度（Task-Granularity）" class="headerlink" title="3.5 任务粒度（Task Granularity）"></a>3.5 任务粒度（Task Granularity）</h3><p>如上所述，我们把map拆分成了M个片段、把Reduce拆分成R个片段执行。理想情况下，M和R要比集群中worker的机器数量大的多。在每个worker机器执行大量的不同任务有助于集群的动态的负载均衡，并且一个worker失败是可以加快恢复速度：该失效机器上已经完成的许多map任务可以分布到任意其他的worker机器上去执行。</p><p>我们的具体实现中M和R可以取多大是有一定的实际界限的，因为master必须执行复杂度为O(M+R)次调度，并且在内存中保存O(MxR)个状态（但是对内存使用的影响因子还是很小的：O(MxR)块状态，大概每对Map任务/Reduce任务差不多只有1个字节）。此外，R值通常是用户指定的，因为每个Reduce任务最终都会生成一个单独的输出文件。实践中，我们也倾向于选择合适的M值，以便每一个独立任务处理的都是大约16M到64M的输入数据（这样上面说的存储位置优化才最有效），并且我们让R设为我们打算使用的worker机器数比较小的倍数。我们通常会用这样的比例来执行MapReduce计算：M=200,000，R=5,000，使用2000台worker机器。</p><h3 id="3-6-备份任务（Backup-Tasks）"><a href="#3-6-备份任务（Backup-Tasks）" class="headerlink" title="3.6 备份任务（Backup Tasks）"></a>3.6 备份任务（Backup Tasks）</h3><p>一个延长MapReduce总执行时间的常见原因是因为一个“落伍者”：在计算过程中一台机器花了很长的时间才完成最后几个Map或Reduce任务。出现“落伍者”的原因有很多。比如：如果一个机器的硬盘有问题就可能导致频繁的纠错操作，而让读取数据的速度从30M/s降低到1M/s。集群的调度系统可能给这台机器上又调度了其他的任务，由于CPU、内存、本地硬盘和网络带宽等问题，导致这台机器执行MapReduce代码的更慢。我们最近遇到的一个问题是因为机器的初始化代码有bug，导致处理器缓存被禁用：受影响的计算机的计算速度减慢了一百多倍。</p><p>我们有一个通用的机制来减少“落伍者”出现。当一个MapReduce操作快要完成的时候，master调度备份任务进程来执行剩下的还在处理中的任务。无论是最初的执行、还是备份任务执行结束都标记这个任务已经完成。我们已经调整了这个机制，让它所带来额外的计算资源不会超过几个百分点。我们发现这显着缩短了完成大型MapReduce操作的时间。例如，在5.3节描述的排序任务，在关闭掉备用任务的情况下要多花44%的时间才能完成排序任务。</p><h2 id="4-改进（Refinements）"><a href="#4-改进（Refinements）" class="headerlink" title="4 改进（Refinements）"></a>4 改进（Refinements）</h2><p>尽管通过编写简单的Map和Reduce函数所能提供的基本功能已经满足大部分的需求，我们还是发掘出了一些有用的功能扩展。本节将描述这些扩展。【标题叫扩展是不是更好？】</p><h3 id="4-1-分区功能（Partitioning-Function）"><a href="#4-1-分区功能（Partitioning-Function）" class="headerlink" title="4.1 分区功能（Partitioning Function）"></a>4.1 分区功能（Partitioning Function）</h3><p>MapReduce的使用者通常会指定他们想要的Reduce任务以及输出文件的数量（R）。数据会使用中间key通过分区函数被分区。一个默认的分区函数是使用hash分区(比如，<code>hash(key) mod R</code>)进行分区。hash分区能产生十分平衡的分区。然而，在某些情况下，使用其它的一些分区函数对key值进行的分区也非常有用。比如，输出的key结果是URLs，我们希望每个主机的所有记录都保存到同一个输出文件中。为了支持类似的情况，MapReduce库的用户可以提供一个专门的分区函数。例如，使用<code>hash(Hostname(urlkey)) mod R</code>作为分区函数让来自所有的来自同一个主机的URLs最终保存在同一个输出文件中。</p><h3 id="4-2-排序保证（Ordering-Guarantees）"><a href="#4-2-排序保证（Ordering-Guarantees）" class="headerlink" title="4.2 排序保证（Ordering Guarantees）"></a>4.2 排序保证（Ordering Guarantees）</h3><p>我们保证在给定的分区中，中间键值对数据会按照key值增量顺序来处理。这个顺序处理保证了可以很容易的让每个分区生成的输出文件也是有序的，当输出文件的格式需要能够支持高效的按key的随机访问查找的时候，将会变得很有用，亦或者是输出文件的使用者会发现对结果的数据集进行排序会很方便。</p><h3 id="4-3-组合功能（Combiner-Function）"><a href="#4-3-组合功能（Combiner-Function）" class="headerlink" title="4.3 组合功能（Combiner Function）"></a>4.3 组合功能（Combiner Function）</h3><p>在某些情况下，map函数产生的中间key值的重复数据会占很大的比重，并且用户给定的reduce函数满足交换律和结合律。2.1节中统计词频的例子就是个很好的例子。由于词频倾向于一个Zipf分布(齐夫分布)，每个map任务将产生成百上千<the,1>格式的记录。所有的这些计数将通过网络被发送到一个单独的reduce任务，然后通过reduce函数将这些值累加起来生成一个数字。我们允许用户指定一个可选的Combiner函数，可以让这些数据在发送到网络之前可以先部分进行合并。</p><p>Combiner函数是在每台执行Map任务的机器上执行的。通常Combiner和Reduce函数的实现使用的代码是一样的。唯一的区别是MapReduce库怎样处理函数的输出。reduce函数的输出被写入到最终输出文件里，而Combiner函数的输出是被写到中间文件里，然后会再被发送给一个reduce任务。</p><p>部分合并可以显著的加速一些MapReduce操作。附录A包含一个使用combiner函数的例子。</p><h3 id="4-4-输入输出类型（Input-and-Output-Types）"><a href="#4-4-输入输出类型（Input-and-Output-Types）" class="headerlink" title="4.4 输入输出类型（Input and Output Types）"></a>4.4 输入输出类型（Input and Output Types）</h3><p>MapReduce库支持几种不同的输入数据的格式。比如，文本模式下输入数据的每一行被视为是一个键值对：key是该行在文件中偏移量，value是那一行的内容。另外一种常见的格式是存储了一个按照key排序的键值对序列。每种输入类型的实现都需要能够把输入数据分割成数据片段，以使得可以让单独的map任务来进行后续处理（例如，文本模式分割的后范围必须确保只会在每行的边界进行范围分割）。尽管大多数使用者大部分情况下都只需要使用MapReduce预定义的少部分输入类型中的一个，但是使用者依然可以通过提供一个Reader接口简单的实现就可以对一个新的输入类型提供支持。</p><p>一个reader并不一定要从文件中读取数据，比如，我们可以很容易定义一个reader，让他从一个数据库里读取记录，或者从内存中的数据结构读取数据。</p><p>以类似的方式，我们提供了一系列预定义的输出数据的类型用来可以产生不同格式的数据，并且用户代码可以很容易增加对新的输出类型的支持。</p><h3 id="4-5-副作用（Side-effects）"><a href="#4-5-副作用（Side-effects）" class="headerlink" title="4.5 副作用（Side-effects）"></a>4.5 副作用（Side-effects）</h3><p>在某些情况下，MapReduce的使用者发现，如果在Map和/或Reduce操作过程中增加辅助的输出文件会比较省事。我们依靠程序writer把这种“副作用”变成原子的和幂等的3。通常应用程序首先把输出结果写到一个临时文件中，在输出全部数据之后，在使用系统级的原子操作rename重新命名这个临时文件。如果一个任务产生了多个输出文件，我们没有提供类似两阶段提交的原子操作支持这种情况。因此，对于会产生多个输出文件、并且对于跨文件有一致性要求的任务，都必须是确定性的任务。但是在实际应用过程中，这个限制还没有给我们带来过麻烦。</p><h3 id="4-6-跳过不好的记录（Skipping-Bad-Records）"><a href="#4-6-跳过不好的记录（Skipping-Bad-Records）" class="headerlink" title="4.6 跳过不好的记录（Skipping Bad Records）"></a>4.6 跳过不好的记录（Skipping Bad Records）</h3><p><strong>有时候，用户代码中的bug会导致Map或Reduce函数在处理某些记录的时候奔溃掉，这些bug让MapReduce操作无法完成。通常的做法是修复bug后再次执行MapReduce操作，但有时候这是不可行的；这些bug可能是在第三方依赖库里边，我们无法获取到源码。同时，我们可以接受跳过一些有问题的记录，比如在一个巨大的数据集上进行统计分析的时候。我们提供了一种可选的执行模式，在这种模式下，为了保证保证整个处理能继续进行，MapReduce会检测哪些记录导致确定性的crash，并且跳过这些记录不处理。</strong>【replace here new】</p><p>每个worker进程都配备了一个信号处理器用来捕获内存段异常（segmentation violation）和总线错误（bus error）。在调用一个用户的Map或Reduce操作之前，MapReduce库在一个全局变量中保存执行参数的序号【这里应该就是处理记录的序号】。如果用户程序产生了一个信号，消息处理器将发送“最后一发”包含了该序号的UDP包给master。当master看到在处理某条特定记录出现不止失败一次时，意味着下次重新执行相关的Map或者Reduce任务这条记录需要被跳过。</p><h3 id="4-7-本地执行（Local-Execution）"><a href="#4-7-本地执行（Local-Execution）" class="headerlink" title="4.7 本地执行（Local Execution）"></a>4.7 本地执行（Local Execution）</h3><p>调试Map和Reduce函数的问题可能非常的棘手，因为实际计算是在分布式系统中执行的，通常在几千台计算机上执行，具体执行任务的分配是由master进行动态调度决定的。为了简化调试、分析和小规模测试，我们额外开发了一套MapReduce库，可以让MapReduce任务在本机上顺序执行所有任务。控制权交给用户，让用户可以把计算限制到特定的Map任务上。用户通过设定特殊的标志来本地调用他们的程序，这样可以很容易的使用任意调试和测试工具（比如gdb）。</p><h3 id="4-8-状态信息（Status-Information）"><a href="#4-8-状态信息（Status-Information）" class="headerlink" title="4.8 状态信息（Status Information）"></a>4.8 状态信息（Status Information）</h3><p>master运行了一个内嵌的HTTP服务器并暴露一组状态信息页供人监控。状态页显示着计算的进度，比如已经完成了多少任务、有多少任务正在处理、输入的字节数、中间数据的字节数、输出的字节数、处理百分比等等。页面还包含了每个任务所生成的标准错误和标准输出文件的链接。用户利用这些数据预测计算需要执行大约多长时间、是否需要增加额外的计算资源。这些页面也可以被用来分析什么时候计算执行的比预期的要慢。</p><p>另外，处于最顶层的状态页显示了哪些worker失效了，以及他们失效的时候正在运行的Map和Reduce任务。这些信息在用户尝试诊断用户代码中的bug的时候十分有用。</p><h3 id="4-9-计数器（Counters）"><a href="#4-9-计数器（Counters）" class="headerlink" title="4.9 计数器（Counters）"></a>4.9 计数器（Counters）</h3><p>MapReduce库提供了一个计数器来统计各种事件发生次数。比如，用户可能想统计已经处理单词的总数、或者已经索引的德国文档的数量等等。</p><p>为了使用这个功能，用户要在程序中创建并命名一个的计数器对象，并在适当的时候在Map和Reduce函数中增加计数器的值。例如：</p><pre><code class="lang-pseudocode">Counter * uppercase;uppercase = GetCounter(&quot;uppercase&quot;);map(String name, String contents):    for each word w in contents:    if (IsCapitalized(w)):        uppercase - &gt; Increment();EmitIntermediate(w, &quot;1&quot;);</code></pre><p>这些计数器的值周期性的从各个单独的worker机器上传送到master（附加在ping的响应包中）。master将成功的Map和Reduce任务的计数器值聚合，并在MapReduce操作完成之后，返回给用户代码。当前计数器的值也会显示在master的状态页面上，这样用户就可以观察当前活着的计算的进度。在聚合计数器的值时，master要消除重复运行的Map或者Reduce任务的影响，避免重复累加（备份任务的使用和失效后重新执行任务这两种情况会导致重复执行）。</p><p>有些计数器的值是由MapReduce库自动维持的，比如已经处理的输入的键值对数量、输出的键值对的数量等。</p><p>使用者会发现计数器功能对于检查MapReduce操作的行为非常有用。比如，在某些MapReduce操作中，用户需要确保输出的键值对数量完全等于输入的键值对数量，或者处理的德国文档数量在处理的整个文档数量在可容忍的范围。</p><h2 id="5-性能（Performance）"><a href="#5-性能（Performance）" class="headerlink" title="5 性能（Performance）"></a>5 性能（Performance）</h2><p>在这节我们在一个大型集群上运行的两个计算来测量MapReduce的性能。一个计算来在大约1TB的数据中搜索出特定模式匹配的数据，另一个计算对大约1TB的数据进行排序。</p><p>这两个程序在大量MapReduce使用者的真实程序中是很有代表性 — 一类是将数据从一种表现形式转换为另外一种表现形式；另一类是从大的数据集中抽取少量用户感兴趣的数据。</p><h3 id="5-1-集群配置（Cluster-Configuration）"><a href="#5-1-集群配置（Cluster-Configuration）" class="headerlink" title="5.1 集群配置（Cluster Configuration）"></a>5.1 集群配置（Cluster Configuration）</h3><p>所有的程序都是在一个由大约1800台机器组成的集群上执行的。每个机器都配备两个2GHz的Intel Xeon处理器，支持超线程，4GB内存，两个160GB的IDE磁盘和一个千兆以太网卡。这些机器被部署在一个两层的树形交换网络中，在根节点大约有100-200 Gbps的总带宽可用。所有这些机器都采用相同的部署，因此任何一对机器之间的网络往返时间小于1毫秒。</p><p>在4GB的内存中，大约有1-1.5GB是由集群上运行的其他任务保留的。测试的程序是在周末下午执行的，这时的cpu、磁盘和网络大部分处于空闲状态。</p><h3 id="5-2-测试1-查找（Grep）"><a href="#5-2-测试1-查找（Grep）" class="headerlink" title="5.2 测试1 - 查找（Grep）"></a>5.2 测试1 - 查找（Grep）</h3><p>grep程序会扫描大概10的10次方个单个100个字节组成的记录，查找一个相对少见的3个字符的模式（这个模式在92337个记录有出现）。输入数据被分为差不多64M一片的若干分片（M=15000），整个输出数据存放在一个文件中（R=1）。</p><p><img src="D:\BLOG\hexo\source\_posts\PaperMapReduce\image-20200425220049080.png" alt="image-20200425220049080"></p><p>图2显示了这个运算随时间的处理过程。Y轴表示输入数据的处理速度。处理速度随着分配给MapReduce计算的机器数量的增加而增加，当1764台worker被分配参与计算的时，处理速度达到了30GB/s。当Map任务结束的时候，即在计算开始后80秒，输入的处理速度降到0。整个计算从开始到结束一共花了大概150秒。这包括了大概一分钟的初始启动阶段。初始启动阶段时间是因为要把这个程序传送到各个worker机器上的时间、等待GFS文件系统打开1000个输入文件集合的时间、获取相关的文件本地位置优化信息的时间。</p><h3 id="5-3-测试2-排序（Sort）"><a href="#5-3-测试2-排序（Sort）" class="headerlink" title="5.3 测试2 - 排序（Sort）"></a>5.3 测试2 - 排序（Sort）</h3><p>排序程序会排序10的10次方单个100个字节组成的记录（大概1TB的数据）。这个程序是模仿TeraSort的benchmark[10]。</p><p>排序程序由不到50行代码组成。只有三行的Map函数从文本行中抽取出一个10字节的key值作为排序的key，并且把这个key和原始文本行作为中间的键值对输出。我们使用了一个内置的恒等函数作为Reduce操作函数。这个函数把中间的键值对不作任何改变输出。最终排序结果输出到两路复制的GFS系统文件中（也就是说，程序输出2TB的数据）。</p><p>如前面提到的，输入数据被了分成64MB的片（M=15000）。我们把排序后的输出结果分区后存储到4000个文件中（R=4000）。分区函数使用key的原始字节来把数据分区到R个片段中。</p><p>我们这个benchmark测试中分区函数知道key的分布情况。对于一个通常的排序程序来说，我们会增加一个预处理的MapReduce操作来采样key的分布情况，通过采样key的分布来计算对最终排序处理的分区点。</p><p><img src="D:\BLOG\hexo\source\_posts\PaperMapReduce\image-20200425220159910.png" alt="image-20200425220159910"></p><p>图三（a）显示了这个排序程序一个正常的执行过程。左上图显示的是输入数据读取的速度。数据读取速度峰值会达到13GB/s，并且所有Map任务完成之后，即大约200秒之后迅速滑落到0。注意一点，排序程序输入数据读取速度小于上面的grep程序。这是因为排序程序的Map任务花了大约一半的处理时间和I/O带宽把中间输出结果写到本地硬盘。而对应的分布式grep程序的中间结果输出几乎可以忽略不计。</p><p>左边中间的图显示了中间数据从Map任务发送到Reduce任务的网络速度。这个过程从第一个Map任务完成之后就开始缓慢启动了。图中的第一个峰值是启动了第一批大概1700个Reduce任务（整个MapReduce分配到大概1700台机器上，每台机器1次最多执行1个reduce任务）。计算运行差不多300秒后，一些第一批启动的Reduce任务完成了，我们开始为剩下的Reduce任务shuffle数据。所有的处理在大约600秒后结束。</p><p>左下图表示Reduce任务把排序后的数据写到最终的输出文件的速度。在第一个排序阶段结束和数据开始写入磁盘之间有一个小的延时，这是因为worker机器正在忙于排序中间数据。写入的速度在2-4GB/s持续一段时间。整个写入时间大约持续850秒。再包括初始启动时间，整个运算消耗了891秒。这和TeraSort benchmark[18]的最好纪录1057秒很接近了。</p><p>还有一些东西要提一下：输入数据的读取速度比数据shuffle速度和输出数据写入磁盘速度要高，这是因为我们的输入数据本地化优化策略—大部分数据都是从本地硬盘读取的，从而绕过了网络带宽的限制。排序速度比输出数据写入到磁盘的速度快，这是因为输出数据写了两份（为了保证数据可靠性和可用性我们使用了双备份的GFS文件系统）。我们把输出数据写入到两个复制节点的原因是因为这是底层文件系统的保证数据可靠性和可用性的实现机制。在输出数据写入磁盘的时候如果底层文件系统使用编码容错技术(erasure coding)[14]的方式而不是复制的方式保证数据的可靠性和可用性，那么就可以减少对网络带宽的需求。</p><h3 id="5-4-备份任务的影响（Effect-of-Backup-Tasks）"><a href="#5-4-备份任务的影响（Effect-of-Backup-Tasks）" class="headerlink" title="5.4 备份任务的影响（Effect of Backup Tasks）"></a>5.4 备份任务的影响（Effect of Backup Tasks）</h3><p>图三（b）显示了关闭备份任务后排序程序执行情况。执行的过程和图3（a）很相似，除了输出数据写磁盘的动作在时间上拖了一个很长的尾巴，而且在这段时间里，几乎没有什么写入动作。在960秒后，除了5个reduce任务其他任务都完成。这些拖后腿的任务又执行了300秒才完成。整个计算消耗了1283秒，多了44%的执行时间。</p><h3 id="5-5-机器故障（Machine-Failures）"><a href="#5-5-机器故障（Machine-Failures）" class="headerlink" title="5.5 机器故障（Machine Failures）"></a>5.5 机器故障（Machine Failures）</h3><p>在图三（c）中演示的排序程序执行的过程中，在几分钟中内我们故意的杀死了1746个worker中的200个。集群底层的调度立刻在这些机器上重新开始新的worker处理进程（因为只是worker机器上的worker进程被kill了，机器本身还在工作）。</p><p>worker的死亡显示了一个负的输入数据读取率，因为一些以前完成的map任务丢失了了(因为相应的map任务的worker被kill了)，需要重新执行这些任务。这些Map任务很快就被会被重新执行。整个计算过程在933秒内完成，这包括启动开销(只比正常执行时间增加5%)。</p><h2 id="6-经验（Experience）"><a href="#6-经验（Experience）" class="headerlink" title="6 经验（Experience）"></a>6 经验（Experience）</h2><p>我们在2003年1月写了首个版本的MapReduce库，在2003年8月做了有了显著的增强，这包括了数据本地优化、worker机器之间任务的动态负载均衡等等。从那以后，我们惊讶的发现，MapReduce库能广泛应用于我们日常工作中遇到的各类问题。它现在广泛的被Google内部各个领域广泛的使用，包括：</p><ul><li>大规模机器学习问题，</li><li>Google News和Froogle产品的集群问题，</li><li>从流行的查询记录中抽取数据来生成报告（比如Google的Zeitgeist），</li><li>从大量的新应用和新产品的网页中提取有用信息（比如，从大量的位置搜索网页中抽取地理位置信息）,</li><li>大规模的图计算。</li></ul><p><img src="D:\BLOG\hexo\source\_posts\PaperMapReduce\image-20200425220359033.png" alt="image-20200425220359033"></p><p>图四显示了在我们的源代码管理系统中，这段时间内独立的MapReduce程序数量的巨大增长。从2003年早些时候的0个增长到2004年9月份的差不多900个不同的实例。MapReduce的成功是因为MapReduce库可以在不到半个小时时间内写出一个简单的程序，并能够有效的在上千台机器的组成的集群上运行，这大大的加速了开发和原形制作的周期。另外，MapReduce让完全没有分布式和/或并行系统开发经验的程序员很容易的利用大量的资源。</p><p><img src="D:\BLOG\hexo\source\_posts\PaperMapReduce\image-20200425220450179.png" alt="image-20200425220450179"></p><p>在每个任务结束的时候，MapReduce库日志分析出计算资源的使用状况。在表1，我们展示了2004年8月份在Google运行的MapReduce任务所占用的统计数据的一部分子集数据。</p><h3 id="6-1-大规模的索引（Large-Scale-Indexing）"><a href="#6-1-大规模的索引（Large-Scale-Indexing）" class="headerlink" title="6.1 大规模的索引（Large-Scale Indexing）"></a>6.1 大规模的索引（Large-Scale Indexing）</h3><p>目前MapReduce一个最成功的使用就是重写了Google网络搜索服务所使用到的索引构建系统。索引系统通过网络爬虫抓取回来的海量的文档作为输入数据，这些数据保存在GFS中。这些文档的原始内容有超过20TB的数据。索引程序是通过一系列大约5到10次的MapReduce操作来建立索引。使用MapReduce（而不是先前版本的分布式索引系统）带来了这些好处：</p><ul><li>索引的代码更加简单、小巧、并且容易理解，因为处理容错、分布式以及并行计算的代码都隐藏在MapReduce库中。例如，使用MapReduce库，一个计算阶段的代码行数从原来的3800行C++代码减少到大概700行代码。</li><li>MapReduce库的性能已经足够好了，因此我们可以把在概念上不相关的计算步骤分开，而不是混在一起来避免额外传递数据的开销。这样做也使得我们可以很容易去改变索引处理方式。比如，对之前的索引系统的一个修改可能要花好几个月的时间，但MapReduce只需要花几天时间就可以实现了。</li><li>索引过程变得更容易操作，因为由机器、机器运行缓慢和网络故障引起的问题都是由MapRe- duce库自动处理的，不需要操作员的干预。此外，通过向in- dexing集群中添加新机器，可以很容易地提高索引过程的性能。</li><li>构建索引的过程也变得更加容易操作了。因为大多数由机器故障、机器运行速度缓慢、以及网络故障阻塞等引起的问题都已经由MapReduce库解决了，不需要操作员的干预。此外，我们只需要通过向索引集群中添加新机器，就可以很容易地提高索引构建的性能。</li></ul><h2 id="7-相关工作（Related-Work）"><a href="#7-相关工作（Related-Work）" class="headerlink" title="7 相关工作（Related Work）"></a>7 相关工作（Related Work）</h2><p>很多系统都提供了严格的编程模式，并且通过对编程的严格限制来实现并行计算的自动化。比如，一个结合函数可以把N个元素的数组的前缀在N个处理器上在log N的时间内使用并行前缀算法计算完[6，9，13]【没懂这个啥例子】。MapReduce可以被认为是我们基于现实世界下海量计算的经验总结的这些模型的简化和萃取。更重要的是，我们还提供了基于上千台处理器的规模的容错实现。相对而言，大多数并行处理系统都只实现了小规模的集群的并行，并将处理机器失败的具体细节交给程序员来处理。</p><p>Bulk Synchronous Programming[17]和一些MPI原语[11]提供了更高级别的抽象来让程序员更容易写出并行处理程序。MapReduce和这些系统一个关键的不同之处在于，MapReduce利用受限的编程模型来让用户程序的自动并发处理，并且提供了透明的容错处理。</p><p>我们数据本地优化策略的灵感来自于如active  disks[12,15]这样的技术，这些技术尽量将处理中的数据元素推送到里距离计算处理节点本地磁盘的地方，来减少网络和IO子系统的吞吐量。我们在挂载几个硬盘的普通机器上执行我们的运算，而不是在磁盘处理器上执行我们的工作，但是这两种一般方法是相似的。</p><p>我们的备用任务机制和Charlotte系统[3]使用的eager调度机制比较类似。该机制的一个缺点是如果一个任务反复失效，那么整个计算就无法完成。我们通过跳过不好的记录的方式在某种程度上解决了这个问题。</p><p>MapReduce的实现依赖于一个内部的集群管理系统，这个管理系统负责在一个超大的共享机器的集群上分布并运行用户任务。虽然这个不是本论文的关注点，但有必要提下，这个集群管理系统在理念上和其它如Condor[16]系统是一样。</p><p>排序机制是MapReduce库的一部分，这个排序机制和NOW-Sort[1]在操作上很类似。源机器（map的workers）把待排序的数据进行分区后，发送到那R个Reduce workers中的一个进行处理。每个Reduce worker在本地对数据进行排序（尽可能在内存中排序）。当然，NOW-Sort没有MapReduce那样可以用户自定义的Map和Reduce函数所带来的广泛的适用性。</p><p>River[2]提供了一个处理进程通过分布式队列传送数据的方式进行互相通讯的编程模型。和MapReduce类似，River系统尝试在异构的硬件环境下，或者在系统扰动的情况下也试图提供良好的平均性能。River是通过精心调度硬盘和网络的通讯来平衡任务的完成时间。MapReduce库采用了其它的方法。通过限制编程模型，MapReduce框架把问题分解成为大量的细粒度的任务。这些任务在可用的worker集群上动态的调度，这样快速的worker可以执行更多的任务。通过对编程模型进行限制，我们可用在工作接近完成的时候调度冗余任务，这大大的缩短在硬件异构的情况下缩小整个操作完成的时间（比如慢的或者阻塞住的worker）。</p><p>BAD-FS[5]有一个和MapReduce完全不同的编程模式，它是面向广域网的任务执行。但是，这两个系统有两个基础功能很类似。（1）两个系统采用重新执行的方式来防止由于失效导致的数据丢失。（2）两个都使用数据本地化调度策略，减少网络通讯的数据量。</p><p>TACC[7]是一个设计用于简化构造高可用网络服务的系统。和MapReduce一样，它也依赖重新执行机制来实现的容错处理。</p><h2 id="8-结束语（Conclusions）"><a href="#8-结束语（Conclusions）" class="headerlink" title="8 结束语（Conclusions）"></a>8 结束语（Conclusions）</h2><p>MapReduce编程模型在Google内部成功应用于多个领域。我们把这成功归结为几个原因：首先，模型使用简单，即便对于完全没有并行或者分布式系统开发经验的程序员而言很很容易上手，因为MapReduce封装隐藏了并行处理、容错处理、数据本地化优化、负载均衡等底层实现细节。其次，大量各种类型的问题都可以通过MapReduce的计算形式来表示。比如，MapReduce用于在Google的web搜索服务所需的数据生成、用于排序、用于数据挖掘、用于机器学习，以及很多其它的系统；第三，我们开发了一·个MapReduce的实现，可以运行在一个包含了数千台机器组成的大规模集群。这个实现可以更加充分的利用这些丰富的计算资源，因此很适合用来解决Google遇到的那些需要大量计算的问题。</p><p>我们也从这份工作中学到了一些东西。首先，约束编程模式可以使并行和分布式计算变得容易，也易于让这样的计算可以容错；其次，网络带宽是稀有资源。我们系统中大量的系统都是为了减少网络的数据传输：本地优化策略使大量的数据从本地磁盘读取，并且写一份中间文件的副本在本地磁盘上也节省了网络带宽；第三，冗余的执行任务可以减少性能慢的机器带来的负面影响，并且解决由于机器失败导致的数据丢失问题。</p><h2 id="致谢（Acknowledgements）"><a href="#致谢（Acknowledgements）" class="headerlink" title="致谢（Acknowledgements）"></a>致谢（Acknowledgements）</h2><p>Josh Levenberg has been instrumental in revising and extending the user-level MapReduce API with a number of new features based on his experience with using MapReduce and other people’s suggestions for enhancements. MapReduce reads its input from and writes its output to the Google File System [8]. We would like to thank Mohit Aron, Howard Gobioff, Markus Gutschke, David Kramer, Shun-Tak Leung, and Josh Redstone for their work in developing GFS. We would also like to thank Percy Liang and Olcan Sercinoglu for their work in developing the cluster management system used by MapReduce. Mike Burrows, Wilson Hsieh, Josh Levenberg, Sharon Perl, Rob Pike, and Debby Wallach provided helpful comments on earlier drafts of this paper. The anonymous OSDI reviewers, and our shepherd, Eric Brewer, provided many useful suggestions of areas where the paper could be improved. Finally, we thank all the users of MapReduce within Google’s engineering organization for providing helpful feedback, suggestions, and bug reports.</p><h2 id="参考（References）"><a href="#参考（References）" class="headerlink" title="参考（References）"></a>参考（References）</h2><p> [1]  Andrea C. Arpaci-Dusseau, Remzi H. Arpaci-Dusseau,David E. Culler, Joseph M. Hellerstein, and David A. Pat-terson. High-performance sorting on networks of work-stations. In Proceedings of the 1997 ACM SIGMOD In-ternational Conference on Management of Data, Tucson,Arizona, May 1997.</p><p>[2] Remzi H. Arpaci-Dusseau, Eric Anderson, Noah Treuhaft, David E. Culler, Joseph M. Hellerstein, David Patterson, and Kathy Yelick. Cluster I/O with River: Making the fast casecommon. InProceedings of the Sixth Workshop on Input/Output in Parallel and Distributed Systems (IOPADS ’99), pages 10–22, Atlanta, Georgia,<br>May 1999.</p><p>[3] Arash Baratloo, Mehmet Karaul, Zvi Kedem, and Peter Wyckoff. Charlotte: Metacomputing on the web. In Pro-ceedings of the 9th International Conference on Parallel and Distributed Computing Systems, 1996.<br>[4] Luiz A. Barroso, Jeffrey Dean, and Urs Hölzle. Web searchfor aplanet: TheGooglecluster architecture. IEEE Micro, 23(2):22–28, April 2003.</p><p>[5] John Bent, Douglas Thain, Andrea C.Arpaci-Dusseau,Remzi H. Arpaci-Dusseau, and Miron Livny. Explicit control in a batch-aware distributed file system. In Pro-ceedings of the 1st USENIX Symposium on Networked Systems Design and Implementation NSDI, March 2004.</p><p>[6] Guy E. Blelloch. Scans as primitive parallel operations.IEEE Transactions on Computers, C-38(11), November 1989.</p><p>[7] Armando Fox, Steven D. Gribble, Yatin Chawathe, Eric A. Brewer, and Paul Gauthier. Cluster-based scal-<br>able network services. In Proceedings of the 16th ACM Symposium on Operating System Principles, pages 78–91, Saint-Malo, France, 1997.</p><p>[8] Sanjay Ghemawat, Howard Gobioff, and Shun-Tak Le-ung. The Google file system. In 19th Symposium on Op-erating Systems Principles, pages 29–43, Lake George, New York, 2003</p><p>[9] S. Gorlatch. Systematic efficient parallelization of scan and other list homomorphisms. In L. Bouge, P. Fraigni-aud, A. Mignotte, and Y. Robert, editors, Euro-Par’96. Parallel Processing, Lecture Notes in Computer Science 1124, pages 401–408. Springer-Verlag, 1996.</p><p>[10] Jim Gray. Sort benchmark home page. <a href="http://research.microsoft.com/barc/SortBenchmark/">http://research.microsoft.com/barc/SortBenchmark/</a>.</p><p>[11] William Gropp, Ewing Lusk, and Anthony Skjellum. Using MPI: Portable Parallel Programming with the<br>Message-Passing Interface. MIT Press, Cambridge, MA, 1999.</p><p>[12] L. Huston, R. Sukthankar, R. Wickremesinghe, M. Satya-narayanan, G. R. Ganger, E. Riedel, and A. Ailamaki. Di-amond: A storage architecture for early discard in inter-active search. In Proceedings of the 2004 USENIX File and Storage Technologies FAST Conference, April 2004.</p><p>[13] Richard E. Ladner and Michael J. Fischer. Parallel prefix computation. Journal of the ACM, 27(4):831–838, 1980.</p><p>[14] Michael O. Rabin. Efficient dispersal of information for security, load balancing and fault tolerance. Journal of the ACM, 36(2):335–348, 1989.</p><p>[15] Erik Riedel, Christos Faloutsos, Garth A. Gibson, and David Nagle. Active disks for large-scale data process-ing. IEEE Computer, pages 68–74, June 2001.</p><p>[16] Douglas Thain, Todd Tannenbaum, and Miron Livny. Distributed computing in practice: The Condor experi-ence. Concurrency and Computation: Practice and Ex-perience, 2004.</p><p>[17] L. G. Valiant. A bridging model for parallel computation.Communications of the ACM, 33(8):103–111, 1997.</p><p>[18] Jim Wyllie. Spsort: How to sort a terabyte quickly. <a href="http://alme1.almaden.ibm.com/cs/spsort.pdf">http://alme1.almaden.ibm.com/cs/spsort.pdf</a>..</p><h2 id="单词频率统计（A-Word-Frequency）"><a href="#单词频率统计（A-Word-Frequency）" class="headerlink" title="单词频率统计（A Word Frequency）"></a>单词频率统计（A Word Frequency）</h2><p>本节包含了一个完整的程序：统计命令行中敲入的一系列输入文件中，每个唯一单词出现的次数。</p><pre><code class="lang-C++">#include &quot;mapreduce/mapreduce.h&quot; // User’s map functionclass WordCounter: public Mapper &#123;    public: virtual void Map(const MapInput &amp; input) &#123;        const string &amp; text = input.value();        const int n = text.size();        for (int i = 0; i &lt; n;) &#123;            // Skip past leading whitespace            while ((i &lt; n) &amp;&amp; isspace(text[i]))                i++;            // Find word end            int start = i;            while ((i &lt; n) &amp;&amp; !isspace(text[i]))                i++;            if (start &lt; i)                Emit(text.substr(start, i - start), &quot;1&quot;);        &#125;    &#125;&#125;;REGISTER_MAPPER(WordCounter);// User’s reduce functionclass Adder: public Reducer &#123;    virtual void Reduce(ReduceInput * input) &#123;        // Iterate over all entries with the        // same key and add the values        int64 value = 0;        while (!input - &gt; done()) &#123;            value += StringToInt(input - &gt; value());            input - &gt; NextValue();        &#125;        // Emit sum for input-&gt;key()        Emit(IntToString(value));    &#125;&#125;;REGISTER_REDUCER(Adder);int main(int argc, char ** argv) &#123;    ParseCommandLineFlags(argc, argv);    MapReduceSpecification spec;    // Store list of input files into &quot;spec&quot;    for (int i = 1; i &lt; argc; i++) &#123;        MapReduceInput * input = spec.add_input();        input - &gt; set_format(&quot;text&quot;);        input - &gt; set_filepattern(argv[i]);        input - &gt; set_mapper_class(&quot;WordCounter&quot;);    &#125;    // Specify the output files:    // /gfs/test/freq-00000-of-00100    // /gfs/test/freq-00001-of-00100    // ...    MapReduceOutput * out = spec.output();    out - &gt; set_filebase(&quot;/gfs/test/freq&quot;);    out - &gt; set_num_tasks(100);    out - &gt; set_format(&quot;text&quot;);    out - &gt; set_reducer_class(&quot;Adder&quot;);    // Optional: do partial sums within map    // tasks to save network bandwidth    out - &gt; set_combiner_class(&quot;Adder&quot;);    // Tuning parameters: use at most 2000    // machines and 100 MB of memory per task    spec.set_machines(2000);    spec.set_map_megabytes(100);    spec.set_reduce_megabytes(100);    // Now run it    MapReduceResult result;    if (!MapReduce(spec, &amp; result)) abort();    // Done: ’result’ structure contains info    // about counters, time taken, number of    // machines used, etc.    return 0;&#125;</code></pre>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;Google三驾马车的第二篇论文，最先发表于2004年OSDI（操作系统领域的顶会）上。也是大数据最重要的一个数据处理方案。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2</summary>
        
      
    
    
    
    
  </entry>
  
  <entry>
    <title>【PaperReading】The Google File System（个人翻译）</title>
    <link href="http://lollipops.work/2020/05/10/Paper-GFS/"/>
    <id>http://lollipops.work/2020/05/10/Paper-GFS/</id>
    <published>2020-05-09T17:02:58.000Z</published>
    <updated>2020-08-14T09:51:03.452Z</updated>
    
    <content type="html"><![CDATA[<p>​        该paper是2003年Google发表在SOSP（Symposium on Operating Systems Principles-计算机系统领域的顶会）的论文《The Google File System》个人翻译。作为Hadoop时代的三架马车之一，对当前分布式系统诸多问题的解决方案具有很重要的意义。</p><blockquote><p>个人翻译不易，转载请备注！</p></blockquote><a id="more"></a><h1 id="谷歌文件系统"><a href="#谷歌文件系统" class="headerlink" title="谷歌文件系统"></a><strong>谷歌文件系统</strong></h1><p>Sanjay Ghemawat, Howard Gobioff, and Shun-Tak Leung （谷歌）</p><h2 id="摘要"><a href="#摘要" class="headerlink" title="摘要"></a><strong>摘要</strong></h2><p>​        我们已经设计并实现了google文件系统，它是一个适用于有着大量数据的数据密集型应用的文件系统。它提供了在低成本硬件设备上的运行的容错功能，以及可以在大量客户端面前呈现出高的整体性能。</p><p>​        尽管它与之前的一些分布式文件系统有着许多共同目标，但是通过对我们程序负载情况以及当前和未来的技术环境的考察，发现我们想要的设计和一些早期的文件系统所设想是不一样的，这让我们不得重新审视传统的设计选择，并探索出一些从根本上不同的设计观点。</p><p>​        目前这个文件系统成功的满足了我们的存储需求，他已经广泛的被应用部署于我们的一些存储平台（那些需要大数据集的研究和开发）服务上用于数据的生产和处理。迄今为止我们最大的集群已经在超过一千台的机器及数千块硬盘上提供上百TB的数据存储服务，同时可以被数百个客户端并发去访问。</p><p>​        在本论文中，我们将提供为了支持分布式应用程序的文件系统扩展接口设计，并对我们设计的许多方面展开讨论，展示微基准（微基准是一个旨在衡量非常小以及特定代码性能的基准，基准测试是实现对一类测试对象的某项性能指标进行定量的和可对比的测试）和现实中使用的一些测量数据报告。</p><h2 id="1-简介"><a href="#1-简介" class="headerlink" title="1.简介"></a><strong>1.简介</strong></h2><p>​        我们已经设计和实现了GFS（谷歌文件系统）去满足谷歌飞速增长的数据处理需求，GFS和之前的许多分布式系统有许多共同的目标，比如：性能，可扩展，可靠性，可用性。但是，驱动GFS的设计的还有对我们程序的工作负载和技术环境的观察，所以说现在包括可预见的将来，GFS都和早期分布式系统所设计的设想有显著的不同。我们重新审视了传统的一些设计选择，并探索了一些从根本上不同的设计点。</p><p>​        第一，<strong>组件的失效应该是常态的而不是意外事件。</strong>文件系统是建立在由成百上千的廉价机器构成的存储介质，并被成百上千的机器访问着。这些机器的组件无论数量还是质量都很难保证在任意时间都能提供功能，以及一些组件可以自动从失败状态恢复过来。我们已经见过很多问题：比如程序的bug，人为的错误以及磁盘的错误，操作系统的bug，内存，连接器，网络，以及电力供应。因此，持续的监控，错误检测，容错，以及自动恢复这些特性必须集成在整个文件系统里面。</p><p>​        第二，以传统的标准来看，我们的<strong>文件十分的大</strong>，数GB的文件是家常便饭。每个文件通常包含许多的程序对象如web文档，当我们定期操作由数十亿数据对象快速增长构成的数TB的数据集时候，即使文件系统支持，操作数十亿的kb大小的文件也是很慢的很笨重的。所以，<strong>设计的假设条件和某些模块的参数，比如IO的操作，数据块大小这些设计元素都需要被重新考虑</strong>。</p><p>​        第三，<strong>大部分文件变更都是通过追加文件内容而不是重新写入的</strong>。对一个文件随机写这种事件几乎不会发生！一旦写入，这个文件就是只读的，并且通常是顺序读（大量的数据其实都符合这种特性）。 这些数据文件可能是大量的数据分析和程序扫描的仓库组成，可能是运行中的应用程序不断产生的数据流，可能是一些打包归档的文件，也可能是一些由一个机器生成的结果（该结果需要立刻实时或者稍有延迟的被另一个机器处理）。如果针对这些大数据都具备这样的访问模式（append模式），那么当对客户端请求的数据块缓存失效了，数据的append操作就是性能优化和操作原子性的关键。</p><p>​        第四，应用程序和文件系统API的协同设计，提高了整个系统的灵活性。比如我们通宽松GFS的一致性模型从而极大的简化的文件系统并且没有给应用程序带来繁重的负担。我们也<strong>引进了一个原子性的append操作</strong>，这样多个客户端就可以并发的对文件append数据而不需要额外的同步。这些都会在论文的后面做详细的讨论。</p><p>​        大量的GFS集群目前已经部署并服务于不同的目的任务。其中最大的一个集群有超过1000个存储节点，超过300T的磁盘。并且被数百个客户端在不同的机器上大量的持续不断的访问着。</p><h2 id="2-设计概述"><a href="#2-设计概述" class="headerlink" title="2.设计概述"></a><strong>2.设计概述</strong></h2><h3 id="2-1设计概述"><a href="#2-1设计概述" class="headerlink" title="2.1设计概述"></a><strong>2.1设计概述</strong></h3><p>​        为了设计一个满足我们需求的文件系统，我们需要以前面提到的那些具有挑战和机遇的假设为指南。上面我们已经提到了一些关键的关注点，现在我们将更加详细的描述我们的假设。</p><ul><li>首先，这个文件系统<strong>应该是建立在大量廉价的机器组件上的</strong>，当然这些组件也经常会失败。所以就必须不停的自我监控从而能在日常基本工作中可以快速的对组件失败进行检测-&gt;容错-&gt;恢复。</li><li>该文件系统<strong>只存储适量的大文件</strong>，我们希望有数百万个文件，每个基本在100M或者更大点。对于上GB的文件也会是一个普遍的情况应该可以被高效的管理，小文件当然也必须支持，只不过我们不需要去优化他们。</li><li>文件系统的主要负载主要包含这两种读请求：<strong>大的顺序流式读取和小的随机读取</strong>。在大的流式读取中，单个操作一般读取几百KB或者上M的数据。来自同一个客户端的连续请求通常是通过不断读取一个文件的临近区域。而一个小的随机读取一般是读取某个任意偏移位置上几KB数据。一个具有性能意识的程序会将这些小的读取批化，并且重新编排这些读取操作使得读取可以高效稳定的顺序穿过文件，而不是来回跳跃的读取。</li><li>文件的系统的负载还有一部分来自于很多大量的、顺序的对文件进行追加写。通常这些写操作的数据量大小和读类似。<strong>一旦写入，文件就很少被修改。</strong>我们需要支持随机写请求但是不需要保证其效率性能。</li><li>系统必须对<strong>多个客户端同时并发的追加同一个文件提供良好的实现</strong>。我们的文件经常被用于如生产者-消费者队列或者做<strong>多路归并</strong>。数百个生产者，一人一个机器，将会并发的对一个文件进行追加。所以<strong>最小化的同步机制的原子性是很有必要的</strong>。文件应该稍后可以被读取，或者消费者同时的进行读取到文件。</li><li><strong>高持久的带宽比低延迟更加重要</strong>，我们大多数程序的目的是能够高速率的批量处理数据，很少对单个读写操作的响应时间有严格的要求。</li></ul><h3 id="2-2接口"><a href="#2-2接口" class="headerlink" title="2.2接口"></a><strong>2.2接口</strong></h3><p>​        GFS提供了给人很熟悉一个的文件系统接口，尽管他并没有实现一个像POSIX那样的统一标准的API 【Portable Operating System Interface of UNIX（可移植操作系统接口），POSIX标准定义了不同操作系统对应上层应用程序统一标准的接口】。GFS中文件是按照目录分层级组织的，并通过文件路径名来标识。我们支持文件的常用操作如：创建，删除，打开，关闭，读取，写入操作。</p><p>​        此外，GFS有快照和操作记录追加机制。<strong>快照是一个以最低开销创建一个文件和目录树的拷贝。记录追加允许多个客户端同时向同一个文件并发的追加数据，并能保证每个客户端追加操作的原子性。</strong>对于实现一个多路合并结果及生产者消费者队列这很有用，客户端可以并发的添加数据而不需要做额外的加锁操作。我们发现GFS所设计的这种文件类型，对于构建大型的分布式应用程序有无法估计的价值。快照 和 记录追加 将会在3.4和3.3节分别作详细讨论。</p><h3 id="2-3架构"><a href="#2-3架构" class="headerlink" title="2.3架构"></a><strong>2.3架构</strong></h3><p>​        一个GFS集群包含一个master，多个chunkservers服务，可以被多个客户端访问。如图一所示：</p><p><img src=":paper-gfs/1.png" alt="1"></p><p>​        这上面每一个服务通常都运行在一个单独的Linux机器的用户级进程中。当然我们也可以运行客户端和chunkserver在一个机器上面，只要机器的资源允许以及可以接受运行可能不稳定的应用程序代码所带来的低可靠性。（意思就是部署的程序可能可靠性差，把机子搞崩了就连带chunkserver也炸了）。</p><p>​        <strong>文件被划分成固定大小的chunk</strong>。每个chunk是由master在该chunk被创建时分配的一个不可变的、全局唯一的64bit句柄（chunk handle）来标识。Chunkserver将chunk作为linux文件存储在本地，对于chunk数据的读写通过chunk handle和指定字节范围来确定。为了可靠性，每个chunk将会被备份到多个chunkserver上，默认的我们存储三个备份。当然用户也可以对文件不同的命名空间区域自定义不同的备份级别。</p><p>​        <strong>Master维护所有文件系统的元数据</strong>。包括：名字空间，访问控制信息，文件与chunks的映射信息，chunks的当前位置。它也控制系统范围内的一些活动，比如chunk租赁管理，孤立无引用的chunk的垃圾回收，chunkserver间chunk的迁移。Master与chunkserver通过周期性的心跳进行通信，从而发送指令和获取chunkserver的状态。</p><p>​        实现了文件系统API的应用程序通过<strong>GFS客户端</strong>和GFS的master和chunkserver进行连接进行通信进行数据读写操作。客户端如果需要操作元数据则需要与master通信，其他的所有的纯数据的通信直接与chunksever通信。我们没有提供POSIX API，因此也就不需要与linux vnode layer关联【unix文件节点】。</p><p>​        <strong>客户端和chunkserver都不会进行文件数据缓存。</strong>客户端做缓存只能提供很小的好处，因为大部分的应用需要流式读取整个文件或者工作的数据集太大了根本没法缓存。没有这些缓存简化了客户端和整个系统，消除缓存一致性的问题(但实际上客户端会缓存元数据)。Chunkserver也不需要缓存文件数据，是因为chunk是作为本地存储文件，Linux自带机制已经帮我们把那些经常访问的数据缓存在内存中了。</p><h3 id="2-4-单Master"><a href="#2-4-单Master" class="headerlink" title="2.4 单Master"></a><strong>2.4 单Master</strong></h3><p>​        一个单独的master极大的简化了我们的设计，使得master可以通过它对集群全局的理解来合理的安排chunks和副本位置的选择。但是我们必须最小化master在读写操作中的相关性，使得master不会成为一个性能的瓶颈。 客户端不会从master节点来读取和写入文件而是向master询问它应该联系哪一个chunkservers。并且这个交互的关联信息会被在一个限定的时间内缓存以用于随后的许多操作可以直接使用缓存的关联信息。</p><p>根据图1我们来描述一次简单的读流程是怎么进行交互的：</p><ul><li><p>首先，通过固定大小的chunk size，客户端从程序中指定的文件名和偏移的字节量解析出文件的chunk index。</p></li><li><p>然后，他会向master发送一个包含文件名和chunk index的请求。master会返回请求对应的chunk句柄（后面都翻译为chunk handle）和chunk副本的位置信息。客户端会缓存这些信息，以文件名和chunk index为key。</p></li><li><p>随后，客户端会向其中一个副本节点发送请求（通常是离客户端最近的那个）。该请求指定了chunk handle和一个该chunk的字节范围。后面的所有对该chunk的请求就不在需要和master再进行交互了直到缓存过期或者该chunk的文件被重新打开过。</p></li></ul><p>实际上，客户端通常一次性请求多个chunks的信息，master也能将这些chunks信息一并返回回来。这些额外的信息几乎没有任何额外消耗，但可以很好的免除将来master和client许多次通信。</p><h3 id="2-5-Chunk大小"><a href="#2-5-Chunk大小" class="headerlink" title="2.5 Chunk大小"></a><strong>2.5 Chunk大小</strong></h3><p>​        Chunk 大小是设计参数中一个关键点。我们选择了<strong>64M为默认大小</strong>，它远远大于通常的文件系统块。每一个chunk的副本是一个简单的Linux文件被存在chunkserver上，并只有在需要的时候才扩展。这种惰性空间分配（lazy space allocation）避免了因为内部空间碎片造成的空间的浪费，碎片最大可能有一个chunk size那么大。</p><p><strong>大的chunk size具有几个很重要的优点：</strong></p><p>​        第一点，他减少了客户端和master通信的次数。因为在一个chunk上的读和写只需要最初的一次和master请求的chunk位置信息【chunk越大，那么包含的内容也就越多，需要的chunk location信息也就越少】，这种交互的减少对我们系统负载来说是十分的重要的。因为我们的上层程序一般操作都是顺序读取大文件。即使是对那些少量的随机读，客户端也可以很轻松的缓存一个几TB的工作数据集的所有的chunk位置信息。</p><p>​        第二点，因为chunk很大，一个客户端就很大可能只会在一个chunk上执行全部的操作了，这样可以通过保持一个持久的TCP长连接从而减少网络的开销。</p><p>​        第三点，他减少了在master中存储的元数据的体积，这样可以运行我们保持所有的元数据在内存中，这所带来的好处我们会在2.6.1节作讨论。</p><p>​        另一方面，一个大的chunk size，即便采用了lazy space allocation也有着他的缺点。比如对于一个小文件只有几个chunk，或者只有一个chunk的，如果有大量的客户端需要访问这个文件那么这个chunk所在的servers就会成为热点。但实际实操中，热点并不是我们一个主要的问题毕竟我们的程序大部分都是顺序读大的多chunk的文件。</p><p>​        但是，<strong>热点问题</strong>在GFS在第一次被一个批处理队列系统使用的时候确实暴露出来了：一个可执行文件被写入GFS的一个单chunk文件中，随后同时启动了上百台机器，那个存储了这个执行文件的chunkserver很快就被数百个同时请求给整超载了。我们当时通过给该执行文件提高更高的副本数量并将那批处理队列系统的启动时间进行错开解决了这个问题。但是长远的解决方法应该是在这种情况下，允许客户端从其他的客户端读取数据【而不是一窝蜂的压力都给在chunkserver上】。</p><h3 id="2-6-元数据"><a href="#2-6-元数据" class="headerlink" title="2.6 元数据"></a><strong>2.6 元数据</strong></h3><p>​        <strong>master节点主要存储三种类型的元数据：文件和chunk的命名空间（namespace），文件和chunk的映射关系（mapping），每个chunk副本的位置（location）。</strong>所有的元数据都存在master的内存中的。前两中（namespace和mapping）还被被持久化到一个记录了变更操作的操作日志中并存储在master的本地磁盘上同时备份到远程机子上去。使用这样的一个日志允许我们更新master的状态简单、可信赖、并且不需要担心master宕机后状态的不一致【得益于log副本】。master节点并不持久化chunk的位置信息而是在启动的时候主动去询问每一个chunk，以及每个chunkserver加入集群的时候获取。</p><h4 id="2-6-1-内存数据结构"><a href="#2-6-1-内存数据结构" class="headerlink" title="2.6.1 内存数据结构"></a><strong>2.6.1 内存数据结构</strong></h4><p>​        因为元数据是存在内存中的，所以master的操作是十分快的。因此，master可以很容易并且高效的在后天对他整个状态做周期性的扫描，这个周期性一般是做chunk的垃圾回收，chunkserver失败时候重新备份，以及为平衡负载和磁盘空间在chunkserver之间chunk的迁移。在4.3，4.4节我们会进一步讨论这些活动。</p><p>​        这种纯内存方式的设计存在一个潜在的限制就是chunk的数量，因此整个集群的的容量受限于master有多大的内存。但实际上这并不是一个严重的限制，<strong>master对每个64M的chunk保存的元数据小于64KB</strong>。大部分的的chunk是满的，因为大部分的文件都包含多个chunk，所以也就只有最后一个chunk可能是不满的。类似的，每个文件所对应的namespace也通常小于64KB因为他保存时候会采用前缀压缩算法进行压缩。</p><p>​        如果需要支持更大的文件系统，只需要对master添加额外的内存就行了，这点开销和元数据全部存在到内存所带来的简单，可靠，性能和灵活性比起来只是一笔很小的代价。</p><h4 id="2-6-2-chunk位置"><a href="#2-6-2-chunk位置" class="headerlink" title="2.6.2 chunk位置"></a><strong>2.6.2 chunk位置</strong></h4><p>​        master并不会对一个给定的chunk有哪些chunkserver保存了他的一个副本的信息保存一个持久化的记录。他只会在他启动的时候，简单的从chunkserver拉取过来这些信息。master通过周期性的心跳检测来监控chunkserver从而控制所有chunk的放置，并且master可以保证自己信息是更新了的。</p><p>​        一开始我们也尝试了把chunk的位置信息给持久化到master上去，但是我们发现如果在启动的时候以及后面周期性的去获取这些信息这样更加简单，因为这样避免了当chunkserver在加入和离开集群，改名，失败，重启等情况下master和chunkserver之间的需要进行同步。在一个数百台服务器的集群中，这些事故太经常了发生了。</p><p>​        理解这个设计决定的另一个方式是明白一个chunkserver对他磁盘上的chunks是否有最终的发言权，在master去维护一套这些信息的一致性视图是没有意义的：因为一个chunkserver上的发生的错误可能会让他上面的chunk不可见（比如一个磁盘可能损坏了或者不可用了），又或者是一个操作可能重新命名了一个chunkserver。【这段比较难以理解：主要阐述了为什么不在master上持久化位置信息？主要因为这些信息不稳定，不像之前提的那两种那么稳定，对每个chunkserver自己的状态依赖太大，一旦chunkserver出了故障，master没法及时或者自动的感知到哪些chunk有问题了，这时候按照持久化记录的那个再去获取chunk就有问题了。所以持久化没有太大意义，不如定时去获取最新的】</p><h4 id="2-6-3-操作日志"><a href="#2-6-3-操作日志" class="headerlink" title="2.6.3 操作日志"></a><strong>2.6.3 操作日志</strong></h4><p>​        <strong>操作日志包含了关键的元数据变更的历史记录，这是GFS的核心</strong>。他不止是元数据变化的唯一持久化记录，他也定义了那些并发操作在逻辑时间上的时间线。文件，chunks以及他们的版本（见4.5节）这些都在他们被创建时唯一且永久的被对应的逻辑时间所标识。</p><p>​        所以操作日志是十分关键的，我们必须可靠的保存。不能在元数据变更还没有被持久化之前对客户端可见。否则，我们将会丢失整个文件系统或者客户端最近的操作，即使chunkserver还保存着。所以我们将其备份到多个远程的机器上并且在对一个客户端作回应前必须保证该操作的日志已经被刷写到本地和远程磁盘上了。master在刷写前会批处理多个日志记录一起进行刷写，从而减少刷写和复制对整个系统吞吐量的影响。</p><p>​        Master通过重放操作日志来恢复文件系统。为了最小化启动时间 ，我们必须保持log在很小的规模。<strong>master会在日志增长超过一定的大小的时候对当前状态设置一个检查点（checkpoints）</strong>，master就可以从本地磁盘上最近的那一次checkpoint进行恢复，这样读取的日志记录就在一个被限制的范围内了。<strong>checkpoint是一个压缩的类B-Tree结构</strong>，可以被直接映射到内存中并用于namespace的查找，都不需要额外的解析。这个大大的提高了GFS的恢复速度和可用性。</p><p>​        因为创建一个checkpoint需要消耗一些时间，所以master内部状态的结构是按这么样的一个设计来的：可以既无延时接受不断的变化又可以去创建一个新的检查点。master会启动一个新的线程切到一个新的日志文件去创建检查点。这个检查点包含了系统所有的变更，对一个包含了百万文件的集群只要几分钟就能完成。在创建结束后，它会被同时写到本地和远程磁盘上。</p><p>​        恢复只需要最新的检查点和之后的日志文件。更老的检查点和日志文件可以随便删除，尽管我们会保存一些来预防一些事故灾难。在创建检查点的时候发生失败并不会影响系统的准确性，因为恢复的代码会检查并跳过不完整的检查点。</p><h3 id="2-7-一致性模型"><a href="#2-7-一致性模型" class="headerlink" title="2.7 一致性模型"></a><strong>2.7 一致性模型</strong></h3><p>​        GFS使用了一个<strong>宽松的一致性模型</strong>来支持我们高分布式的应用程序，但实现起来也相对于简单和高效。现在我们讨论下GFS的一致性保证，以及这对程序来说意味着什么。我们也会着重讲GFS是如何维护这些保证的，但是这些细节会留在论文的其他部分。</p><h4 id="2-7-1-GFS的保证"><a href="#2-7-1-GFS的保证" class="headerlink" title="2.7.1 GFS的保证"></a><strong>2.7.1 GFS的保证</strong></h4><p>​        文件的namespace的变更（比如文件的创建）是原子性的。他们只被master处理：命名空间锁保证了原子性和准确性（4.1节）；master的操作日志定义了全局全部操作的顺序（2.6.3节）。</p><p>​        在一个数据变更后，文件区域的状态取决于该变更操作的类型。不管他是成功还是失败，或者是并发的变更的。表1是对结果的一个概述。如果所有的客户端总是看见同样的数据，不管他们是从哪一个副本读取的数据那么该文件区域便是一致的。一个文件数据变更后是一致的那么所有的客户端应该都能看到该变更的写入，这时候该文件区域就已定义的。当一个变更成功并没有被其他的并发写操作影响，那么这个被影响的区域就是已定义的（也就是一致的）：所有的客户端会看到变更的写入。<strong>并发的成功更改会让区域处于未定义但是是一致的，</strong>所有的客户端看见的是同样的数据，但这不意味着任何一个变更已经写入了(如果变更是针对相同的数据写这样有的变更就会被新的变更所覆盖，这样用户就无法看到最先的变更了，同时发生在跨chunk的操作会被拆分成两个操作，这样这个操作的一部分可能会被其他操作覆盖，而另一部分则保留下来，如3.1节末尾所述)。通常的，他看到的是多个变更组合后的结果。一个失败的变更会使区域在一个非一致状态（因此也是未定义）：不同的客户端可能会看见不同的数据。我们下面会描述我们的程序如何已定义的区域和未定义的区域。应用程序不需要进一步去区分未定义区域的不同种类。</p><p>​        数据的变更可能是写入或者记录的追加。一个写操作的数据会写入到程序指定的偏移位置上。一个记录的追加会将数据原子的追加，即使是并发的变更也会至少被追加一次，但是偏移位置由GFS来选择（见3.3节）。（而客户端会认为一个通常的追加是在当前文件的尾部写入一个偏移量位置的数据）偏移会返回给客户端，来标记包含了那条记录的已定义区域的开始位置。另外，GFS可能会在他们之间插入一些padding或者记录的副本。他们会占据被认为是不一致的区域，并且通常比用户数据小的多。</p><p>​        在一系列成功变更后，变更的文件区域可以被保证是已定义的并包含了最后一次变更记录的写入。GFS通过两步来实现：a.将这些变更以相同的顺序应用到该chunk的所有副本上（见3.2节），b. 使用chunk的版本号来检测所有已经变旧的副本（可能是因为chunkserver挂了的时候导致chunk丢失了变更信息，见4.5节）。旧的副本永远都不会参与任何变更以及客户端向master询问chunk位置的信息的返回。他们会被优先进行垃圾回收掉。</p><p>​        因为客户端缓存了chunk的位置信息，他们可能会在本地信息更新前读到旧的副本上去。这个本地缓存信息（原文window）由缓存的超时时间和文件下一次打开的时间所限制，文件打开会清除该文件在缓存中所有的chunk信息。此外，我们的大部分操作都是only-append操作，一个旧的副本通常会返回一个chunk早期append结束位置的数据而不是一个已经发生更新过时的数据。当读方重试和master联系的时候，可以理解得到当前chunk的位置信息。</p><p>​        在一个变更成功很久之后，组件失败仍然可能污染和损坏数据。GFS通过周期性的在master和所有的chunkserver间握手来找到那些失败的chunkserver，并且通过校验和（checksum）来检测数据的污染（见5.2节）。一旦问题暴露，数据会尽可能快的从正确的副本中恢复（4.3节）。只有当一个chunk在GFS对污染做出反应之前该chunk的所有副本数据已经全部丢失，这个chunk的丢失才是不可逆的，而通常GFS的反应时间在分钟内的。即便是在这种情况下：chunk已经不可用，但是没有被污染：应用程序还是可以收到清楚的错误信息而不是污染了的数据。</p><p><img src=":paper-gfs/2.png" alt="2"></p><h4 id="2-7-2-应用程序的影响"><a href="#2-7-2-应用程序的影响" class="headerlink" title="2.7.2 应用程序的影响"></a><strong>2.7.2 应用程序的影响</strong></h4><p>​        GFS应用程序可以通过使用一些简单的技术来适应这种宽松的一致性模型，这些技术点都是为了某些目的：依赖append而不是覆盖写，checkpoints，自我写校验，自我记录标识。</p><p>​        事实上，所有我们的程序修改文件都是通过append而不是overwrite。在一个典型的使用场景：一个writer会从头到尾生成一个文件。当它写完所有的数据的时候会对该文件原子性的重命名为一个永久的名字，或者周期性的通过检查点来检查有多少数据已经被成功写入。checkpoints可能会也包含应用级的校验和。reader只验证和处理最后一个checkpoints之前的文件区域，因为这些区域是处于已定义的状态了。尽管存在一致性和并发性问题，但这种方法对我们很有用。与随机写入相比，append要高效得多，对应用程序故障的响应能力也更强。checkpoints允许writer增量性地重新启动写（不需要每次都从头开始），并防止reader处理虽然成功写入了但是从应用程序的角度来看仍然不完整的文件数据。</p><p>​        在另一种典型用法中，许多writer并发的append到一个文件以合并结果或作为生产者 - 消费者队列。 记录append操作的append-at-least-once语义保证了每个writer的输出。reader处理偶尔的填充和重复，如下所示。 writer为每条记录准备了比如校验和等额外的信息，以便验证其准确性。 reader可以使用校验和来识别和丢弃额外的填充和记录片段。 如果它不能容忍偶然的重复数据（例如，如它们可能触发非幂等操作），可以使用记录中的唯一标识符来过滤它们，这通常需要命名相应的应用程序实体（例如一个web文档）。 这些记录的I/O功能函数（除了重复删除）都在我们的应用程序共享的库代码中，并适用于Google的其他文件接口的实现。 有了这个，相同的记录序列，加上罕见的重复记录，总是会被传递给reader。</p><p>​        在以上的描述中，存在一个基本的假定：数据是以record形式存储的，而且通常这些record都是可以重复的，比如一个网页文档我们可以重复存，这对于数百亿的网页文档来说，存储少数多余的很正常，也就是说这些数据通常是文本，而不是二进制，所以我们才可以在append或者写时用记录的副本来覆盖非一致的区域，所以提供了append的append-at-least-once语义，因为append二次也是可以的。如果我们要保证唯一性，可以在应用层增加逻辑。<em>【这一段基本没看懂】</em></p><h2 id="3-系统交互"><a href="#3-系统交互" class="headerlink" title="3. 系统交互"></a><strong>3. 系统交互</strong></h2><p>我们设计这个系统的目的是为了最小化master在所有操作中的相关度。在这个前提下，我们现在来讨论下client，master以及chunkservers 是如何交互来实现数据的变更，record的原子append，以及快照的。</p><h3 id="3-1-租约（Lease）和变更顺序"><a href="#3-1-租约（Lease）和变更顺序" class="headerlink" title="3.1 租约（Lease）和变更顺序"></a><strong>3.1 租约（Lease）和变更顺序</strong></h3><p>一个变更就是一个修改chunk的内容或者让元数据的操作，比如一个写或者append操作。每个变更都需要在所有的副本上执行。我们使用租约来维持多个副本间变更顺序的一致性。Master授权给其中一个chunk租约，我们叫做主副本（primary）。主副本会对该chunk的所有变更选择一个顺序，然后所有的副本就会按照这个顺序去应用这些变更。因此，全局的变更顺序首先由master来选择租约授权的顺序来确定的（如果多个chunk修改，master顺序对给他们进行租约授权，授权其实就是选一个primary来进一步处理），然后在一个租约里面的是交给primary副本来定义操作顺序的。</p><p>这个授权机制是为了最小化master的管理开销所设计的。一个租约有60s的超时时间设置。但是只要这个chunk正在发生变更，主副本就可以和master请求延长租约。这些扩展的请求和授权通常是通过master和chunkserver的心跳信息一起发送的。master有时候可能想在租约到期前撤销租约（比如master想要停止对一个被重命名的文件进行变更操作）。即使master和主副本失去通信，该机制可以让master安全的在旧的租约过期后，授权一个新的租约给新的副本。</p><p>在图2中，我们按照下面的顺序步骤来阐述一个写操作的过程。</p><p><img src=":paper-gfs/3.png" alt="3"></p><p>\1. 客户端会请求master哪一个chunkserver持有当前的租约，以及其他的副本位置。如果没有副本持有租约，master会选一个副本授权租约给它（这里没有展示）。</p><p>\2. master返回该主副本的标识一起其他副本的位置信息。客户端缓存这些数据用于未来的变更。只有当主副本没有了响应或者发现租约已经到期了，client才会和master联系。</p><p>\3. 客户端向所有的副本推送数据。客户端可以按照任意的顺序推送数据，么个chunkserver会将数据缓存在内部的LRU的buffer里面直到数据需要被使用或者过期。 通过控制流和数据流的解耦，我们可以将重要的数据流基于网络拓扑来进行调度，而不是去考虑哪一个是主副本，从而提高性能。这在3.2节我们会深入讨论。</p><p>\4. 一旦副本确认收到了数据，客户端会发一个写请求给主副本。该请求标识了之前推送给所有副本的数据。然后主副本会分配一个连续的序列号给所有她接受到的变更（可能来自于多个client）。然后主副本会将按照前面的序列好将变更应用爱本地的副本上。</p><p>\5. 主副本将写请求发送给所有的其他副本，每个副本都按主副本分配的序列号应用这些变更。</p><p>\6. 等其他副本都写入完成了，会返回给主副本操作完成。</p><p>\7. 主副本会响应给客户端。任何副本碰到的错误信息都会返回给client。出现错误时，写操作可能已经在主副本以及部分副本上执行成功了（如果是主副本失败了，就不会有变更的序列号分配给其他的副本也就是不会有后面的操作了）。如果遇到错误，那么客户端的请求会被认为是失败的，修改的region认为是处于不一致的状态。我们客户端的代码代码会通过重试这些变更来处理这样的错误。它会先在3-7步骤进行一些尝试后再从头重试写操作。</p><p>如果app的一个写操作是很大的操作或者跨chunk边界的，GFS客户端代码会将他们分为多个写操作。这些操作都遵循上面的控制流，但是可能会被其他client的并发操作插入或者覆盖。因此共享的文件区域最终可能会包含了来自不同clinet的片段，虽然这些副本之间是一致的（因为他们所有的操作都是按照相同的顺序执行的），但是文件区域会处于一种一致但是未定义的状态，如2.7提到的那样。【这个黄东旭好像提到过】</p><h3 id="3-2数据流"><a href="#3-2数据流" class="headerlink" title="3.2数据流"></a><strong>3.2数据流</strong></h3><p>为了高效的使用网络，我们将数据流和控制流分离。当控制流从client到主副本再到其他的所有次副本节点时，数据流则是通过一个仔细选择出来的chunkserver链，将数据线性的流水线的方式进行推送。我们的目的是充分利用每个机器的带宽，避免网络瓶颈和高延迟的连接，最小化推送数据的延迟。</p><p>为了充分利用每个机器的网络带宽，数据是在一个chunkserver链线性的推送的，而不是以其他的拓扑结构进行分布的（比如：树结构）。因此，每个机器的带宽可以全部用来发送数据，而不是被多个接收者所切分。</p><p>为了避免网络瓶颈和搞延迟的连接（比如：交换机的网络链路就经常会这样），每个机器都会将数据传输到在网络拓扑中距离最近的，并且没有收到数据的那个机器推送数据。我们假设客户端要推送数据到chunkserver S1到S4。他先推送给最近的机器S1，然后S1推送给最近的S2，类似的S2给S3或者S4（距它最近的那个就行），以此类推。我们的网络拓扑很简单，所以距离可以通过IP地址精确的被估算出来。</p><p>最后，为了最小化延迟，我们将数据通过TCP进行连接。一旦一个chunckserver收到一些数据，它就会立刻开始推进这些数据。流水线对我们来说十分有用，因为我们使用了一个全双工链路的交换网络，这样在接受的时候立刻进行发送数据不会影响到我们接受数据的速度。如果没有网络拥塞，理想化的向R个副本传输B字节的数据需要的时间是B/T+RL（T代表网络吞吐率，L是机器间的网络延迟）。我们网络的链路通常是100Mbps（T）的吞吐，L也远远低于1ms，因此1M的数据在理想情况下80ms就可以分发完成。</p><h3 id="3-3原子性的记录append"><a href="#3-3原子性的记录append" class="headerlink" title="3.3原子性的记录append"></a><strong>3.3原子性的记录append</strong></h3><p>GFS提供一个原子性的append操作叫做record append(注意这与传统的append操作也是不同的)。在一个传统的写操作中，client来指定数据需要写的偏移位置。对于相同区域的并行写操作是无法串行的：该区域最终可能包含来自多个client的数据片段。但在一个record append操作中，client只需要指定数据。GFS会将它至少一次的原子性地append到文件中（比如：像一个持续不断的字节流一样），数据偏移的位置由GFS确定，同时将该偏移位置返回给client。这很类似于unix文件打开模式中的O_APPEND模式，当多个writer并发写操作时不会产生竞争条件。</p><p>Record append在我们分布式应用程序中被重度使用，很多在不同机器的client会并发地向同一个文件进行append。如果使用传统的写操作，client就会需要进行复杂而又昂贵的同步化操作，比如通过一个分布式锁管理器。在我们的工作中，这样的文件通常是被用来服务于一个多生产者/单消费者队列或者保存来自多个不同client的归并结果。</p><p>Record append是一种变更操作，除了一点在主副本上的额外的逻辑外依然遵循3.1节的控制流逻辑。Client先将所有的数据推送给所有的副本后，再向主副本发送请求，主副本会检查将记录append到该chunk是否会导致该chunk超过它的最大值(64MB)。如果超过了，它就将该chunk填充到最大值，并告诉次副本做同样的工作，然后通知Client下面的操作应该在下一个trunk上重试了。(append的record大小需要控制在trunk最大值的四分之一以内，以保证最坏情况下的碎片在一个可以被接受的等级)。如果append后chunk没有超过最大值，那就是通常的情况，主副本会将数据append到它的副本上，然后告诉次副本将数据写在相同的偏移位置上，最后向client返回成功响应。</p><p>如果record append在任何一个副本上操作失败，client会重试这个操作。那么相同chunk的多个副本就有可能包含不同的数据，这些数据可能包含了相同记录的整个或者部分的重复值。GFS并不保证所有的副本在比特级别上的一致性，它只保证数据作为一个原子单元最少被写入了一次。这个属性可以很容易地从一个简单的观察中得出：为了让操作返回成功，数据肯定要被写入到某个trunk的所有副本的并在相同偏移位置上了。此外，所有的副本都应该达到了记录尾部，因此未来的记录将会被放置在更高的偏移位置，或者是另一个不同的chunk，即使以后另一个副本变成了主副本。在我们的一致性保证里，record append操作成功的数据区域就是已定义的(因此肯定是一致的)，而介于其间的数据则是不一致的(因此也是未定义的)。我们的应用程序可以处理这样的不一致区域，如我们在2.7.2里讨论的那样。【这里看的也是一脸懵逼】</p><h3 id="3-4快照"><a href="#3-4快照" class="headerlink" title="3.4快照"></a><strong>3.4快照</strong></h3><p>快照操作可以几乎实时的制作文件或者目录树的一个拷贝，同时可以最小化对于正在执行的变更操作的中断。我们的用户用它来创建大数据集的分支拷贝(以及拷贝的拷贝)，或者用来创建当前状态的checkpoint，在试验以后可以轻松提交修改或着回滚的更改之前的状态。</p><p>像AFS[5]，我们使用标准的写时拷贝技术来实现快照。当master收到一个快照请求时，它首先撤销那些要进行快照的的文件对应的chunks所有已发出的租约。这让这些chunk的后续写操作需要与master交互来得到租约持有者。这样就可以给master一个优先的机会创建该chunk的新的拷贝。</p><p>当这些租约被撤销或者过期后，master会将这些操作以日志形式写入磁盘。然后复制源文件或者目录树的元数据到内存中，再将这些日志记录应用到这些元数据上去，新创建的快照文件与源文件一样指向相同的chunk。</p><p>当client在快照操作后第一次要对一个chunk C进行写入时，它会发送请求给master找到当前租约拥有者。Master发现到对chunk C的引用计数大于1。它会推迟响应客户端的请求，并选择一个新的chunk我们称为handle C’。然后让每个拥有C的那些chunkserver创建一个新的叫做C’的chunk。通过在相同的chunkserver上根据原始的chunk创建新chunk，就保证了数据拷贝是本地的，而不是通过网络传输来的(我们的硬盘比100Mbps网络快大概三倍)。从这一点来看，对于任何chunk的请求处理都没有什么不同：master为新创建的chunk C’的副本中的一个授权租约，然后返回给client，这样client就可以正常的写这个chunk了，而不知道该chunk实际上是从一个现有的chunk刚刚被创建出来的。</p><h2 id="4-master操作"><a href="#4-master操作" class="headerlink" title="4. master操作"></a><strong>4. master操作</strong></h2><p>master执行所有的namespace操作，另外他还管理着整个系统的chunk副本：如决定如何放置，创建一个新的chunk以及其副本，协调整个系统的活动来保证chunk都被完整的备份的，对所以的chunkserver负载均衡，以及回收未使用的存储空间。现在我们将分别讨论这些主题。</p><h3 id="4-1-namespace管理和锁"><a href="#4-1-namespace管理和锁" class="headerlink" title="4.1 namespace管理和锁"></a><strong>4.1 namespace管理和锁</strong></h3><p>master的很多操作都十分的耗时间：比如，一个快照操作需要撤销所有的该快照相关的chunkserver的租约。我们不想在运行这个的时候耽误到别的master操作。因此，我们通过在namespace的region上使用锁来保证正确的串行化来允许多个操作可以同时运行。</p><p>不同于很多传统的文件系统，GFS没有一个用于列出该目录下所有文件的一种目录数据结构。也不支持对同一个文件或者目录起别名（比如unix系统里面的硬链接和软链接）。GFS将文件的全路径到元数据的映射作为一个查找表逻辑上表示namespace。通过前缀压缩，这个表可以在内存中高效的表示出来。每个在namespace树下的节点（要么是一个文件的绝对路径要么是一个目录的绝对路径）都有一个相关联的读写锁。</p><p>每个master的操作在其运行前都需要一系列锁来配合。比如，如果要操作/d1/d2/…/dn/leaf（叶子），那么他就需要从/d1，/d1/d2，… 到/d1/d2/…/dn这些目录的读锁，以及d1到叶子leaf节点的全路径的读锁或者写锁。说明下leaf可能是个文件或者目录，这取决于是什么操作。</p><p>我们现在来说明下该锁机制是如何防止在/home/user正在被快照到/save/user时，/home/user/foo被创建。首先快照操作获取/home和/save上的read-lock，/home/user和 /save/user上的写锁。而文件创建操作需要/home和/home/user上的读锁，在/home/user/foo上的写锁。这两个操作将会被准确的串行化执行，因为他们都要获取/home/user上的锁。文件创建操作并不需要父级目录的写锁，是因为我们没有目录或者类似于inode的数据结构来防止被修改，使用读锁已经足够有效防止父级目录被删除了。</p><p>这种锁模式的一个好处就是它允许对同一个目录进行并发的变更操作。比如多个文件创建操作可以并发的在同一个目录里面执行。每个操作需要一个目录的读锁和一个文件名的写锁。在目录上的读锁可以防止目录被删除，重命名或者快照。针对文件的写锁可以保证相同文件名的只会被创建一次。</p><p>因为namespace有很多节点，读写锁对象是懒加载分配的并且一旦不再使用了就会被删除。并且，这些锁需要一个全局一致的获取顺序来避免死锁：这个顺序先按namespace树的等级来排，同一个等级按照字典序来排。</p><h3 id="4-2-备份放置"><a href="#4-2-备份放置" class="headerlink" title="4.2 备份放置"></a><strong>4.2 备份放置</strong></h3><p>一个GFS集群在很多层面上都是高度分布式的。他一般有数百个chunkserver分布在若干个机架上。这些chunkserver可能被来自于相同或者不同的机架上的很多个客户端访问。在不同机架上的两个机器通信可能会跨一个或多个网络交换。另外，进出一个机架的带宽可能会小于机架内所有机器的宽带总和。多级的分布式对数据的分布发出了扩展性，可靠性和可用性的挑战。</p><p>chunk副本的放置策略出于练歌目的：最大化数据的可靠和可用性，最小化网络带宽的使用。出于这两个目的，不仅仅只是把副本分散到不同的机器上，这只能应对机器或者磁盘的失败，以及最大化利用每个机器的网络带宽。我们必须将这些副本分散在这些机架上（而不是机器上）。这样可以保证当一个机架整个损坏或者离线的时候（比如因为网络交换机故障或者电路问题）chunk的某些副本依然是存活的可用的。这也意味着对于一个chunk的流量，尤其是读操作可以充分利用多个不同机架的带宽。另一方面，写操作流量得需要经过多个机架间进行，但权衡下我们是可以接受的。</p><h3 id="4-3-创建、重备份、重平衡"><a href="#4-3-创建、重备份、重平衡" class="headerlink" title="4.3 创建、重备份、重平衡"></a><strong>4.3 创建、重备份、重平衡</strong></h3><p>创建chunk副本有三个原因：chunk的创建，重备份，重平衡。</p><p>当master创建一个chunk时，需要选择哪里去存放这些空的副本。会考虑下面几个因素：（1）我们要将新的副本存放在那些磁盘使用率低于平均水平的磁盘上，这样随着时间的推移整个chunkservers上的磁盘使用率会大致相同（2）我们要限制每个chunkserver上的最近创建的次数，尽管创建它本身是个廉价操作，但是创建后往往跟随的大量的写操作，因为创建就是为了去写副本数据的嘛，在我们 一次append-多次读的工作负载下，一旦数据被写入，那么这些数据都是只读的。（3）如上面提到的，我们要将chunk的副本分布在整个机架群上。</p><p>一旦可用的chunk备份数量低于用户指定的数量，master就需要重新备份一个chunk。这个可能有多个原因导致：一个chunkserver变得不可用了，chunkserver报告他的备份已经破坏了，磁盘因为某些错误变得不可用了，或者副本的目标数量被用户提高了。每个需要被重备份的chunk的优先级由下面几个因素决定：一个是它与目标的备份的目标值差多少，比如相对于丢失了1个副本的chunk我们会给一个丢失了2个副本的chunk更高的优先级。另外，我们会率先重备份那些仍然活着的chunk而不是那些最近已经被删除的文件（见4.4节）。最后，我们为了最小化对运行中的程序的影响，我们会提升任何阻塞了用户进程的chunk的优先级。</p><p>master挑选优先级最高的chunk并且通过通知一些chunkserver去直接从一个有效的副本上复制chunk数据来进行clone。这些新副本的存放和前面新创建的有相似的目标：平均磁盘使用率，限制单台chunkserver上的clone操作数量，跨机架副本分布。为了避免clone的流量淹没client的流量，master同时限制集群和每个chunkserver的clone操作数量。另外，每个chunkserver会通过限制源chunkserver的读请求来限制它们在每个clone操作上花费的带宽（我的理解是：clone需要从源chunk复制数据，他控制该读来控制带宽使用）。</p><p>最后，master会周期性的对副本进行重平衡：它会检查当前的副本分布，然后移动副本到更好的磁盘空间来进行负载平衡。这个过程是一步步的填充一个新的chunkserver的，而不是一下子大量的写入这些新的chunk。对于一个新的副本的存放标准和上面讨论的相似。另外，master必须还要选择移除哪个已经存在的副本。总的来说，master更倾向于移动这些chunk到低于磁盘平均利用率的chunkserver上来平衡磁盘使用率。</p><h3 id="4-4-垃圾回收"><a href="#4-4-垃圾回收" class="headerlink" title="4.4 垃圾回收"></a><strong>4.4 垃圾回收</strong></h3><p>在一个文件被删除后，GFS不会立即释放可用的物理存储，而是会延迟处理等替代文件和chunk级别的垃圾回收的时候来操作。我们发现这种方式可以让系统变的更加简单和可靠。</p><h4 id="4-4-1-机制"><a href="#4-4-1-机制" class="headerlink" title="4.4.1 机制"></a><strong>4.4.1 机制</strong></h4><p>当一个文件被应用程序删除后，master会立刻将该删除记录计入日志，就和其他的一些更变操作一样。但是却不会立刻释放资源，该删除的文件只是被重命名为一个包含了删除时间戳的隐藏文件名。在master对namesoace进行常规扫描的时候，如果发现这中隐藏的文件已经存在了3天以上了就才会被移除（这个时间间隔是可以配置的）。在这个之前，该文件仍然是可以通过这个新的被重命名的特殊名字读取的，也可以通过重新命名它为正常名字来撤销删除操作。当那些隐藏文件名的文件被从namespace中删除后，他在内存中的元数据就会被擦除了。这样就有效的切断了所有和他的chunk的关联了。</p><p>在master对namespace的一次常规扫描中，master会标识出那些孤立的chunk（那些无法从任何文件可以引用到达的）并在元数据中擦除这些chunks。在chunkserver和master的每次心跳中，chunkserver会向master报告一个集合，该集合是该chunkserver包含了那些孤立chunks的子集（是吗？还是说就是该chunkservers有的chunks的一个任意子集？）。然后master会返回给哪些已经不存在master元数据中的chunks的标识。然后chunkserver就可以自由的删除这些chunk的副本了。 </p><h4 id="4-4-2-讨论"><a href="#4-4-2-讨论" class="headerlink" title="4.4.2 讨论"></a><strong>4.4.2 讨论</strong></h4><p>尽管分布式垃圾回收是一个很难的问题，它对于程序语言上下文需要复杂的解决方案。但是在我们的例子中就十分简单了。我们可以很容易标识chunk的所有的引用：他们在由master维护的一个文件-chunk的映射表上。我们也能很容易的标识所有的chunk副本：他们都是一些在chunkserver上指定目录的linux文件。任何对于master来说是未知的chunk的副本就认为是“垃圾”。</p><p>使用垃圾回收的方式来回收存储空间比直接删除有几个优点：第一，这对于组件失败很常见的大规模的分布式系统来说十分的简单和可信赖。chunk的创建可能在一些chunkserver上已经成功创建了，但是在另外一些失败，这样就有master不知道的存在的副本了（没懂这什么意思啊，两者有什么关联吗）。副本的删除信息也可能在通信中丢失，master需要能够记住信息并重新发送给这些失败的节点。不管是master还是chunkserver的垃圾回收都要提供一种统一的方式去清理那些不知道有没有用的副本。第二，master会将这些垃圾回收活动和其他的常规后台活动合并，比如常规的namespace扫描和chunkservers的握手。所以这些活动是批处理的并且开销是分摊的。此外，这些活动只会在master相对空闲的时候执行，master需要能够迅速的响应客户端的请求。第三，这种垃圾回收延迟删除的方式给那些不可逆转的删除操作提供了一个安全的保护网。</p><p>根据我们的经验，这种延迟删除的主要坏处就是会有时候阻碍用户在磁盘紧张的时候对磁盘使用调整。那些不断创建和删除的临时文件的程序可能不能立刻就能重新利用磁盘空间。我们通过当已删除的文件再次被删除的时候加速他的垃圾回收来解决这个问题。我们也允许用户在不同的命名空间使用不同的重备份和垃圾回收策略。例如，用户可以指定某些目录树下的所有chunk文件不存储备份，任何删除文件的操作都是即时的并会永久的从文件系统状态中移除。</p><h3 id="4-5-过期副本检测"><a href="#4-5-过期副本检测" class="headerlink" title="4.5 过期副本检测"></a><strong>4.5 过期副本检测</strong></h3><p>当一个chunkserver失败了或者停机下，该机的chunk的副本可能就丢失了一些变更从而变的过期。对于每个chunk，master维护了一个chunk的版本号来区分最新的和过期的副本。</p><p>一旦master对一个chunk授权一个新的租约，就会增加该chunk的版本号并通知所有的副本更新。master和这些副本都会在他们持久化的状态中记录这个新的版本号。这发生在任何客户端被通知前，也就是客户端可以开始往chunk写数据前。如果另一个副本当前是不可用的，他的chunk的版本号就不会被更新。当该chunkserver重新启动并报告他的chunk集合和他们的版本号信息的时候，master会检测到这个chunkserver有一个过期的副本。如果master发现有些版本号大于他记录的，master就认为他授权租约的时候失败了，就会用这个更公告的版本号来进行更新。</p><p>master通过周期性的垃圾回收来移除这些过期的副本，在这之前，客户端请求有关该过期的chunk的信息都会直接被认为不存在来回应。作为另外一种保护措施，当通知客户端哪一个chunkserver持有租约的时或者指示某个chunkserver执行clone操作，从另一个chunkserver读取chunk数据时候，master会包含这些chunk的版本号信息。客户端和chunkserver在执行这些操作的时候会验证这些版本号来保证他们所操作的chunk是最新的数据。</p><h2 id="5-容错和诊断"><a href="#5-容错和诊断" class="headerlink" title="5. 容错和诊断"></a><strong>5. 容错和诊断</strong></h2><p>我们在设计这个系统时其中一个最大的挑战就是处理频繁的组件失败。组件的数量和质量使得这些问题成为一个常态而不是异常：我们不能完全的信任机器，也不能完全相信磁盘。组件的失败会导致系统不可用，更糟糕的话会损坏数据。我们现在就来讨论下我们遇到的这些挑战，以及我们为系统构建的一些工具来诊断那些不能避免发生的失败情况。</p><h3 id="5-1-高可用"><a href="#5-1-高可用" class="headerlink" title="5.1 高可用"></a><strong>5.1 高可用</strong></h3><p>在GFS数百台的服务器中，任意给定时间总是会有一些机器变得不可用。我们使用两个简单但是有效的策略来保证整个系统的高可用：快速回复和备份。</p><p><strong>5.1.1 快速恢复</strong></p><p>master和chunkserver两者都设计成无论他们如何被终止都可以在几秒内恢复他们的状态并启动。事实上，我们并不区分正常和不正常的终止；服务器通常都是通过杀死进程来关闭的。客户端和其他的服务器在其未完成的请求超时的时候会有一个小的停顿，然后去重新连接那个重启后的服务器并重试。6.2.2节会给出观测时间的报告。</p><h4 id="5-1-2-chunk备份"><a href="#5-1-2-chunk备份" class="headerlink" title="5.1.2 chunk备份"></a><strong>5.1.2 chunk备份</strong></h4><p>正如之前所讨论的，每个chunk会备份在不同的机架的多个chunkserver上。用户可以对不同的namespace指定不同的备份等级，默认值是3。master会根据需要克隆已经存在的副本来保持每个chunk在一些chunkserver掉线或者发现备份数据已经损坏情况下（通过校验和，见5.2节）这些chunk的完全备份。尽管副本已经很好的满足了我们需求，我们依然探索其他形式的一些跨机器的冗余方案，比如使用<strong>同等或者更少的代码【原文就没看懂</strong>parity or erasure codes<strong>】</strong>来满足我们不断增长的只读存储请求。我们认为在我们松耦合的系统中实现这些更复杂的冗余模式是很有挑战，但是需要可以被管理的，因为我们的流量主要是append操作和读操作而不是随机写操作。</p><h4 id="5-1-3-master备份"><a href="#5-1-3-master备份" class="headerlink" title="5.1.3 master备份"></a><strong>5.1.3 master备份</strong></h4><p>为了可靠，master的状态也会被备份。master的操作日志和检查点都被备份在多台机器上。一个对master状态的变更只有在日志记录已经完全刷写到本地磁盘和其他所有的副本之后才会认为是变更提交了。为了简单起见，master依然掌控着所有的变更操作和后台活动比如垃圾回收这些内部修改着系统的活动。当这些活动或者变更失败了，几乎可以立即重启。如果master所在的机器或者磁盘失败了，在GFS外部的一个监控基础设施就会在别的地方通过备份的操作日志重新开启一个新的master进程。客户端可以使用master的一个权威的名字（其实就是别名，比如 gfs-test）来访问，这个名字应该一个DNS别名，如果master被重新分配到别的机器上了，我们可以修改DNS别名的映射来重定向。</p><p>此外，甚至在主master已经挂掉了的情况下，“影子”master依然可以为这个文件系统提供只读访问。他们是影子，并不是镜像，所以他们可能会比主master稍微落后一些，通常是几秒。对于那些不经常变更文件或者应用程序不介意获取到的是有点旧的结果，这提高了读的可用性。实际上，因为文件内容是从chunkserver上读的，程序并不能看到过期文件的内容。真正可能在短时间内过期的是文件的元数据，如目录内容或者访问控制信息。</p><p>影子master为了保证自身的实时性，一个影子master会读取一个不断增长的操作日志的副本，然后将这些操作序列应用在自己的数据结构上，这就和主master做的一样。和主master一样，它在启动后会轮询chunkserver（之后很少轮询）来定位chunk的副本位置，并通过频繁的握手交换信息来监控他们的状态。只有在master决定创建或者删除一个副本导致副本位置更新时候，影子master才会依赖主master。</p><h3 id="5-2-数据完整性【整个这章我对校验和这个东西都不是清楚，到底是怎么个形式】"><a href="#5-2-数据完整性【整个这章我对校验和这个东西都不是清楚，到底是怎么个形式】" class="headerlink" title="5.2 数据完整性【整个这章我对校验和这个东西都不是清楚，到底是怎么个形式】"></a><strong>5.2 数据完整性【整个这章我对校验和这个东西都不是清楚，到底是怎么个形式】</strong></h3><p>每个chunkserver使用校验和来检测存储的数据是否被破坏了，一个GFS集群经常有数千台磁盘在数百台机器，这就很容易发生磁盘损坏从而在读和写路径上发生数据的损坏或者丢失（见第7节的一个原因）。我们可以通过其他的chunk的副本来恢复损坏的数据，但是通过跨chunkserver的副本比较来检测chunk的损坏是不切实际的。另外，有分歧的副本仍然可能是合法的：GFS语义的变更特别是前面讨论过的原子性的append记录操作，并不能保证所有的副本是完全一致的。因此，每个chunkserver必须要通过维持一个校验和来独立验证他和自己的拷贝的完整性。</p><p>一个chunk被分割成64KB大小的块，每个块相应的有32bit的校验和。和其他的元数据一样，校验和被和用户数据分离保存在内存中，并被持久化存储。</p><p>对于读操作，chunkserver 在返回任何数据给请求者之前，会校验那些读取边界重叠的数据块的校验和。不管请求者是client还是其他的一个chunkserver。因此chunkserver就不会传播损坏的数据给其他的机器。如果一个块记录的校验和和记录的不一致，chunkserver会返回一个错误给请求者，并报告这个错误给master。作为回应，请求者就会从其他的副本来读取，同时master会从其他的副本clone这个chunk。让一个有效的新的副本创建好了，master会指示那个报告失败的chunkserver删除那个失败的副本。</p><p>校验和对于读的性能影响很小，有下面几个原因：因为大部分我们的读操作至少跨了几个块，我们只需要读取相对很少的额外数据来验证。GFS客户端通过尝试在块校验和边界上排序这些读操作来大大减少开销（是不是就是让读操作对于块来说更加的顺序性，而不是太随机）。此外，在chunkserver上查找和比较校验和不需要通过任何IO就可以完成，校验和的计算操作也可以和IO操作同时的进行。</p><p>对于一个chunk尾部append操作（而不是写覆盖已存在的文件）的校验和计算经过了深度的优化，因为他们在我们的工作负载中占有统治地位。我们只要增量的更新最后一部分校验块的校验和，并为那些append在尾部已经填充好了的新的校验块计算校验值。即使最后一部分的校验块已经损坏了并且我们当前没有检测到他，这个新的校验值也不会和存储的数据匹配，当下一次这个块被读的时候就会检测到这个损坏【就是说我们只是增量计算新块的校验值，但是不会验证其准确性，坏的数据会保存在数据校验和里面，下次读的时候验证就会发现不匹配—这块没咋懂，好像就是这么个意思：我们只是增量的更新append操作满了而新生成的块的校验和，即使有损坏我们也不管也不检测，而是在下次读这个块的时候和存储的数据对比（这个数据是不是master的元数据？），一对比就能发现数据损坏了】。</p><p>相对的，如果一个写操作覆盖了一个已经存在的chunk范围区域，我们必须读取和验证这个范围的第一个块和最后一个块。然后执行写操作，最后再计算和记录新的校验和。如果我们在覆写这些chunk前不验证第一个和最后一个块，那么新的校验和可能会隐藏那些没有被复写区域的数据的损坏。【因为这里没有采用增量计算方式，因为它是覆盖不是append所以现有的检验和就是整个块的没法从中取出部分数据的校验和，必须重新计算】</p><p>在空间的时期，chunkserver可以扫描并验证那些没有在活动状态的chunk。这让我们可以检测那些很少被读取的文件的是否有损坏。一旦发现了损坏，master可以创建一个新的没有损坏的副本并删除了已经损坏的副本。这就避免了一个不活跃但是损坏了的trunk骗过master，让master认为该chunk有足够的好的副本。</p><h3 id="5-3诊断工具"><a href="#5-3诊断工具" class="headerlink" title="5.3诊断工具"></a><strong>5.3诊断工具</strong></h3><p>全面而详细的诊断性的日志以最低的成本来帮助我们对问题分解，调试和性能分析。没有日志，我们就很难清楚那些机器间短暂的，不可重复的交互。GFS生成诊断日志记录了很多珍贵的时间（比如chunkserver的启动和关闭）以及所有的RPC请求和响应。这些诊断日志可以自由的删除而不会影响系统的准确性。但是我们在空间允许的情况下应该尽可能的保存这些日志。</p><p>RPC的日志包含了线路上所有的请求和响应的信息，除了读写文件的数据。通过匹配响应的请求和整理在不同机器上的RPC记录，我们可以重新构建出整个交互历史过程来诊断一个问题。这些日志也可以用来跟踪负载测试和性能分析。</p><p>日志带来的性能影响是很小的（和他带来的好处比这些性能的损耗不算什么），因为日志是异步并顺序的被写出的。那些最近发生的事件依然保留在内存中，用于持续的在线监控。</p><h2 id="6-测量"><a href="#6-测量" class="headerlink" title="6.测量"></a><strong>6.测量</strong></h2><p>在这节，我们将展示一些小规模基准测试来说明GFS的架构和实现的固有瓶颈，有些实验数据来自原google实际在使用的集群。</p><h3 id="6-1小规模基准测试"><a href="#6-1小规模基准测试" class="headerlink" title="6.1小规模基准测试"></a><strong>6.1小规模基准测试</strong></h3><p>我们在一个包含1个master节点，2个master备份，16个chunkserver，16个client组成的GFS集群来测试GFS的性能。需要说明下，这个配置是为了方便测试，实际情况中集群通常会有数百个chunkserver和数百个client。</p><p>所有的机器都配备双核1.4GHz PIII处理器，2G的内存，2个80G5400转的硬盘，和一个100兆的全双工以太网带宽连接到HP2524交换机上。所有的19台GFS服务器都连接到了一个交换机上，其他16台客户端连接到另一个上。这两个交换机之间使用1G的线路连接。</p><h4 id="6-1-1读操作"><a href="#6-1-1读操作" class="headerlink" title="6.1.1读操作"></a><strong>6.1.1读操作</strong></h4><p>N个客户端同时从文件系统读。每个客户端随机的在320G的文件集合中选择一个4M的region区。每个客户端最后要读到1个G的数据，所以需要重复该步骤256次。chunkservers一共就32G的内存，所以我们预计至少有10%的命中率到了linux缓存。我们的结果应该跟接近一个无缓存的结果。</p><p>图3展示了展示了该N个客户端的读取速率的聚合结果和理论上的极限值。125M/s是在1GB的交换机连接下的峰值，类似的，所以客户端100M带宽的情况下饱和时候每个客户端的流量应该是12.5M/s。观测的结果是如果只有单个client在读时候，读取速度在10M/s，在客户端极限的80%左右。对于16个客户端一起在读的时候，读取速度在94M/s。在125M的峰值下大概利用率在75%左右，也就是单台6M/s左右。因为reader个数的增加效率从80%降低到75%。多个客户端同时访问同一个chunkserver的概率也同时变大。</p><p><img src=":paper-gfs/4.png" alt="4"></p><h4 id="6-1-2写操作"><a href="#6-1-2写操作" class="headerlink" title="6.1.2写操作"></a><strong>6.1.2写操作</strong></h4><p>N个客户端同时想N个不同的文件写数据。每个客户端以单次1M的一系列写操作向一个新文件写入1GB的数据。这统计的写速度和他的理论极限值如图3-b。这个极限值为67M/s，因为我们需要把每个字节写入到16个chunkserver中的3个，并且每个只有12.5M/s的输入网络连接。</p><p>单个客户端的写速度是6.3M/s差不多是极限值的一半，主要是因为我们的网络协议栈，他不能很好的利用我们用于chunkserver副本推送所有设计的流水线模型。在传输数据从一个副本到另一个的时候的延迟降低了整体的写性能。</p><p>16台客户端整体的写速度达到了35M/s（大概每个2.2M/s），大概也是理论值的一半。和读操作一样，我们在并发读取的客户端增多的时候就增大了读到同一个chunkserver的可能性，此外，比起16个reader，16个writer的还会增大冲突，因为每个写操作都需要关联三个不同的副本。</p><p>写操作比我们想要的要慢，但在实际中这还没有成为一个主要的问题，因为尽管它增加了每个客户端的延迟，但是他并不会显著影响系统对大量客户端的写入总带宽。</p><h4 id="6-1-3记录追加"><a href="#6-1-3记录追加" class="headerlink" title="6.1.3记录追加"></a><strong>6.1.3记录追加</strong></h4><p>图3-c展示了record的append性能。N个客户端同时append到一个文件上。性能取决于保存该文件最后那个chunk的chunkserver的网络带宽，和客户端的的数量无关。该性能从一个客户端的6M/s降低到16个客户端的4.8M/s，大部分是因为网络拥塞和不同客户端的不同的网络传输速率导致的。</p><p>我们程序倾向于冰箱的创建多个这样的文件。或者说，N个客户端并行的向M个分片append，并且这个N和M都是数百个的级别。因此我们实验中网路的拥塞不是一个主要的问题，因为客户端可以让自己在让自己处理流程，在一个chunkserver比较忙的时候去append另一个。</p><h3 id="6-2现实世界的集群"><a href="#6-2现实世界的集群" class="headerlink" title="6.2现实世界的集群"></a><strong>6.2现实世界的集群</strong></h3><p>我们现在检查谷歌内部的两个正在使用中，具有相似集群代表性的集群。集群A被上百个工程师正常来用作研究和开发使用。一个典型的任务就是被一个真人用户初始化然后运行数个小时。它读取从几M到几个TB的数据，并转换和分析这些数据，然后将这些结果写回到集群。集群B主要用于生产环境，任务时间通常更加长，并且是不断的生成和处理数TB的数据集，但是很少有认为的参与。在这两个案例下，一个任务包括了很多在许多机器上并行读和写的很多文件的处理流程。</p><p><img src=":paper-gfs/5.png" alt="5"></p><h4 id="6-2-1存储"><a href="#6-2-1存储" class="headerlink" title="6.2.1存储"></a><strong>6.2.1存储</strong></h4><p>如上面表格展示的5个条目的第一个，两个集群都有数百个chunkserver，拥有数TB的磁盘空间并且使用率都是相对的平等而不是完全满了的。“已使用空间”包含所有的chunk的副本。事实上所有的文件都被备份3次，因此这两个集群分别存储了18T和52T的数据。</p><p>这两个集群有很相似数量的文件，尽管B集群有大量的死文件，那些已经被删除或者被新版本文件替换，但是还没有被回收的文件。但是他有更多的chunk，因为他的文件一般都很大。</p><h4 id="6-2-2元数据"><a href="#6-2-2元数据" class="headerlink" title="6.2.2元数据"></a><strong>6.2.2元数据</strong></h4><p>chunkserver总共存储了数十GB的元数据，大部分都是对那64KB的用户使用块的校验和。chunkserver上唯一的其他的元数据就是4.5节讨论过的那些chunk的版本号。</p><p>保存在master中的元数据要更小，只有数十M。或者每个文件平均大概100字节。这刚好满足我们实际中master的内存大小不会限制系统的容量的设想。大部分的文件元数据都是以前缀压缩形式存储的。其他的元数据包括文件的所有者，权限，到chunk的映射，以及chunk的当期版本。另外对于每个chunk我们存储了当前副本的位置以及用户实现写时拷贝的引用计数。</p><p>每个独立的服务器，包括chunkserver和master，只有50M到100M的元数据。因此恢复是十分快的，在服务器可以回应请求之前只需要几秒钟时间从磁盘中读取这些元数据。但是master有时可能会有点慢-通常30到60s，知道他从所有的chunkserver上拉取到chunk的位置信息。</p><h4 id="6-2-3读写速率"><a href="#6-2-3读写速率" class="headerlink" title="6.2.3读写速率"></a><strong>6.2.3读写速率</strong></h4><p>表3显示了在各个时期的读写速度。在测量之前，所有集群都已经启动1周的时间。（集群最近被重启以升级新版本的GFS）</p><p>自从从起后，平均的写速度小于30M/s。当我们拿到这些测量值时候，集群B正在处于一个密集的写操作活动中：大概100/s的数据生成速度，因为又要写副本这又造成了300M/s的网络负载。</p><p>读的速度远高于写的速度。集群的整体负载和写操作比起来包含了更多读操作这和我们预测的一样。所有的集群都在一个很重的读活动中。尤其是，A已经在之前的一个星期中一直维持着580MB/s的读速率。它配置的网络可以支持750MB/s，所以它已经充分利用了资源。B集群可支持1300 MB/s的峰值读速率，但是应用只使用了380 MB/s。</p><p><img src=":paper-gfs/6.png" alt="6"></p><h4 id="6-2-4-master负载"><a href="#6-2-4-master负载" class="headerlink" title="6.2.4 master负载"></a><strong>6.2.4 master负载</strong></h4><p>表3同时也展示了操作被发送到master的速率，大概200-500个/s。master可以很容易的保持这个速率，所以这些对系统负载来说不是一个瓶颈。</p><p>在GFS的早些版本，master偶尔对一些工作负载产生瓶颈。master花费大量的时间在巨大的目录(包含上千万的文件)中进行线性扫描。因此，我们改进了master的数据结构使其可以通过namespace来进行二分搜索。现在他可以很容易的支持每妙数千个文件访问请求。如果必要的话，我们可以再namespace数据结构前加一个name查找缓存来进一步加速。</p><h4 id="6-2-5-恢复时间"><a href="#6-2-5-恢复时间" class="headerlink" title="6.2.5 恢复时间"></a><strong>6.2.5 恢复时间</strong></h4><p>在一个chunkserver失败了，在该server上的chunk的副本数就会低于要求，这时候就必须clone来位置正常副本数量。恢复这些chunk的时候取决于资源的数量。在一个实验中，我们在集群B杀死了一个chunkserver。这个chunkserver包含了15000个chunks差不多600G的数据。为了减少恢复对运行的应用的影响和为调度决策提供余地。我们默认的参数限制集群并发的clone操作在91个（是chunkserver数据量的40%左右）。并且每个clone操作只允许消费最多6.25M/s(50Mbps)。所有的chunk在23.2分钟才被恢复，备份速度在440M/s。</p><p>在另一个实验中，我们杀死了两个chunkserver，每个有大概16000个chunk和660G数据。这个双失败让266个chunk变的只有一个副本了。所以这266个chunk在clone的时候有一个更高的优先级。我们在两分钟内恢复了这些副本至少2份。从而让集群可以容忍另外一个chunkserver如果也失败了不会有数据丢失。</p><h3 id="6-3-工作负载剖析"><a href="#6-3-工作负载剖析" class="headerlink" title="6.3 工作负载剖析"></a><strong>6.3 工作负载剖析</strong></h3><p>这一节，我们详细介绍了两个GFS群集上的工作负载细分，这两个集群与6.2节中相当但不完全相同。群集X用于研发，而群集Y用于生产数据处理。</p><h4 id="6-3-1-方法和说明"><a href="#6-3-1-方法和说明" class="headerlink" title="6.3.1 方法和说明"></a><strong>6.3.1 方法和说明</strong></h4><p>这些结果只包含客户端发起的请求，因为他们可以反映由我们应用程序对整个文件系统产生的工作负载。他们不包括为了执行客户端的请求或者服务器内部的后台活动（比如写推送或者重平衡）而在服务器内部的请求。</p><p>IO操作的统计数据是从我们在GFS上真实的RPC请求日志记录信息重新构建而来的。比如，GFS客户端的代码可能将一个读请求拆分了多个RPC请求来提高并行度，我们就通过日志推断出他的原始请求。因为我们的访问模式是程式化的，我们希望任何的错误都可以出现在日志中。应用程序显式的日志可能可以提供更精准的数据，但是重新编译和启动数千个在运行的客户端来做这个日志统计操作在逻辑上是不可能的，从那么多机器上搜集信息是十分的笨重的。</p><p>需要注意的一点是，不要去过度的概括我们的工作负载。因为GFS和应用程序都是由google完全控制的，应用程序倾向于为GFS调整自己，而相反的GFS是为这些应用程序设计的。这种相互的作用可能也存在一些广泛的文件系统和应用程序中，但是这种影响在我们的case中可能更加明显。</p><h4 id="6-3-2-chunkserver负载"><a href="#6-3-2-chunkserver负载" class="headerlink" title="6.3.2 chunkserver负载"></a><strong>6.3.2 chunkserver负载</strong></h4><p><img src=":paper-gfs/7.png" alt="7"></p><p>​        表4展示根据操作的大小的分布数据。读操作展现出了一个双峰分布。小量的读（低于64kb的）来自于那些大量文件中查找一些小片数据的随机读的客户端。大量读的操作（超过512kb）来自于那些需要长时间线性读取整个文件的程序。</p><p>对于集群Y有大量的读操作没有返回任何数据。我们的程序，特别是那些生产系统，经常使用文件来作为生产者和消费者队列。生产者并发的向一个文件append数据而消费者从文件的地步读取文件。有时候，消费者的消费超过了生产者就不会返回数据。集群X显示其很少出现这种情况，因为他通常是用来做短期的数据分析任务而不是长期的分布式应用程序。</p><p>写的规模也显示出双峰分布。大的写操作（大于256K）通常是来自于写操作者的缓冲。那些缓冲很少数据的写操作者，通常是检查点或者是同步操作，也或者是那些占用较小写入量（小于64Kb）的产生较少数据的写操作者。</p><p>随着记录的append，可以发现集群Y比X有更大的record append比率。因为我们使用Y的那个生产系统针对GFS做了更多的优化。</p><p>表5显示了数据在各种操作规模下的数据传输总量。对于所有种类的操作，大的操作（超过256KB）通常占传输的大多数字节。由于随机查找工作量需要，小读取（小于64 KB）的确只传输小量数据，但占了读操作的一大部分。</p><p><img src=":paper-gfs/8.png" alt="8"></p><h4 id="6-3-3-append与write"><a href="#6-3-3-append与write" class="headerlink" title="6.3.3 append与write"></a><strong>6.3.3 append与write</strong></h4><p>记录的append操作在我们生产系统中被大量使用。对于集群X，按字节传输来算write和append的比例是108:1，按照操作次数计算他们比例是8:1。对于集群Y，比例分别是3.7:1和2.5:1。一次，这些比例告诉我们两个集群他们的append操作都会比write操作要大。对于X，测量期间记录append操作整体要低，这可能是由于一两个有特殊缓冲大小设置的程序导致的结果倾斜。</p><p>正如期望的，我们数据的变更操作的负载是由append操作控制的而不是重写控制的。我们测量了在主副本上的数据重写数量，这个数量很接近那些客户端特地去重写先前写入的数据而不是追加新数据的小部分案例数量。对于集群X，以字节大小计算的话重写大概占了整个数据变更的0.0001%，以操作个数计算，大概小于0.0003%。对于Y集群，这两个比例都是0.05%，尽管这也不算大，但是还是要高于我们的期望。结果发现大部分重写的操作都是来因为错误或者超时客户端的重试操作。他们不算是工作负载的一部分，而是重试机制的结果。</p><h4 id="6-3-4-master负载"><a href="#6-3-4-master负载" class="headerlink" title="6.3.4 master负载"></a><strong>6.3.4 master负载</strong></h4><p><img src=":paper-gfs/9.png" alt="9"></p><p>表6展示对master各种请求类型的拆分。大部分的请求都是为了读查询chunk的位置以及数据变更来获取租约持有信息的。（找chunk位置和获取租约锁）</p><p>集群X和集群Y可以看见在删除数量上的明显差别，因为集群Y存储的生产数据会定期的重新生产并替换为更新的版本。其中还有一些差异隐藏在open请求中，因为老版本的文件可能在被重新打开的时候给隐式删除（unix打开模式的’w’操作）。</p><p>findMatchingFiles是一个支持ls和类似文件系统操作的匹配模式。不同于其他的master的请求，他可能要处理大部分的命名空间，所以他的操作是很昂贵的。集群Y更经常出现这种请求因为自动处理数据的任务更倾向于检查文件系统的各个部分以了解应用程序全局的状态。相反，集群X的程序更明显处于用户的控制下，所以很多操作都预先知道了所需要的文件的名字（用户自己决定的）。</p><h2 id="7-经验"><a href="#7-经验" class="headerlink" title="7.经验"></a><strong>7.经验</strong></h2><p>在构建和部署GFS的过程中，我们经历了大量的问题，一些是操作性的和一些是技术性的。</p><p>起初，GFS只是被构思用于我们的生产系统的后台文件系统。随着时间的推移，该用途进化为研究和开发任务所使用。开始的他还不支持如权限，配额等这些东西，但是现在他们已经基本都包含了。但是生产系统是可以被很好的控制和训练的，但是用户的操作就不是了，所以我们需要跟多的基础设置来避免用户和用户间的互相干扰。</p><p>我们一些最大的问题是磁盘和linux相关的。很多我们的硬盘都声称支持一定范围的IDE协议版本的linux驱动，但实际上根据反馈来看他们是在最新的一些版本上是可以信赖的。因为协议的版本都很相似，所以这些驱动大部分是可以正常工作的，但是某些场合下，这种不匹配会让驱动和内核在硬盘驱动状态下产生分歧。这会因为内核的错误导致数据默默地被污染了。这个问题激发了我们使用校验和来检测数据污染，当发生这种情况，我们就需要去修改内核来处理这种协议不匹配的问题。</p><p>早先，我们使用linux2.2内核的时候发现了一些由于fsync()函数的花费导致的问题。他的花费是和文件的大小而不是被修改的部分成比例增加的。这对于我们大量的操作日志是一个问题，特别是在我们实现检查点之前。我们当时通过使用同步写来绕过这个问题，最后迁移到linux2.4解决了这个问题。</p><p>另一个linux问题是：对于一个读写锁，在一个地址空间的任意线程在从磁盘中读页数据（读锁）或者在mmap()方法调用中修改地址空间（写锁）都必须持有一个读写锁。我们发现在系统在系统在轻负载下短暂超时，并曾努力寻找是否是资源瓶颈或零星的硬件故障。最终，我们发现这个读写锁在读磁盘线程在处理前面map的数据的时候，会阻塞主网络线程将新的数据映射到内存中。由于我们的工作瓶颈主要在于网络带宽而不是内存拷贝带宽，我们多花费一额外的copy操作使用pread()替换了mmap来绕过这个问题。</p><p>尽管有一些问题，linux的可用性有助于我们探索和理解系统的行为。在恰当的时候，我们也会改进内核并和开源社区分享这些变化。</p><h2 id="8-相关工作"><a href="#8-相关工作" class="headerlink" title="8.相关工作"></a><strong>8.相关工作</strong></h2><p>像其他的大型分布式文件系统比如AFS[5]，GFS提供了一个本地独立的namespace，让数据可以为了负载平衡或者容错而透明的移动。不同于AFS，为了提升整体的性能和容错能力，GFS将文件数据在多个存储服务器上存储，这点更类似于xFS[1]或者Swift[3]。</p><p>由于硬盘相对便宜，且与复杂的RAID策略相比，使用副本策略更简单就可以。由于GFS当前采用副本只是进行冗余因此它会比xFS或者Swift消耗更多的原始存储。</p><p>与AFS,xFS,Frangipani,Intermezzo这些系统相比，GFS在文件系统接口下并不提供任何缓存。我们的目标工作的负载类型对于单应用程序运行很少是可重用的，因为他们要么流式的读取大量数据集或者在里面进行随机的seek，而每次只读少量的数据。</p><p>一些如xFS,Frangipani,Minnesota’s GFS和GPFS的分布式文件系统删除了中央服务节点，依赖于分布式的算法来实现一致性和管理。我们选择中央化这个方法是为了简化设计，增加可靠性，获取灵活性。尤其是，一个中央化的master更容易实现复杂的chunk放置和备份策略，因为master已经具有大部分的相关信息来控制了它们如何改变。我们通过让master状态很小以及在其他机器上进行备份来处理容错。我们的影子master机制当期提供了可扩展性和可用性（对于读）。对于master状态的更新，会append到write-ahead这样的日志里来进行持久化。因此我们可以通过类似于Harp里的primary-copy模式来提供一个比我们当前模式具有更强一致性的高可用性保证。</p><p>我们未来将处理类似于Lustre的一个问题：向大量客户端提供整体性能。然而我们通过注重于我们自己的需求而不是构建一个POSIX兼容文件系统来简化了这个问题。另外，GFS假设大量的组件都是不可靠的，因此容错是我们设计的中心。</p><p>GFS十分类似于NASD架构。只是NASD是基于网络连接的硬盘驱动器，而GFS则使用普通机器作为chunkserver，就像在NASD原型中所做的那样。与NASD工作不同的是，我们的chunkserver在需要时分配固定大小的chunk而不是变长的对象。此外，GFS还实现了诸如重平衡，副本，快速恢复这些在生产环境需要的机制。</p><p>不像Minnesota’s GFS和NASD，我们并没有寻求改变存储设备的模型。我们更专注使用现有商品化组件来组成的复杂分布式系统去解决日常的数据处理需求。</p><p>生产者消费者队列使用原子record append操作解决了与分布式队列-River一个相似的问题。River使用跨多机器基于内存的分布式队列以及仔细的数据流控制来解决这个问题，而GFS只使用了一个可以被很多生产者并发地进行append的持久化文件。River模型支持m对n的分布式队列，但缺乏持久化存储带来的容错力，而GFS只能高效地支持m-对-1队列。多个消费者可以读取相同文件，但是它们必须协调来划分传进的负载。</p><h2 id="9-总结"><a href="#9-总结" class="headerlink" title="9.总结"></a><strong>9.总结</strong></h2><p>谷歌文件系统展示了在普通硬件上支持大规模数据处理工作负载的要素。尽管一些设计决定是针对我们特殊设置的，但是大部分可以应用在一个类似规模和成本意识的数据处理任务。</p><p>根据我们当前和预期的应用程序工作负载和技术环境，我们重新审视传统的文件系统的一些假设。通过观察我们的在设计中有一些与传统设计根本上不同的观点：我们将组件失败看做常态而不是异常，为经常进行的在大文件上的append进行优化（可能是并发的），然后是读(通常是顺序的)，扩展并且放松了标准文件系统接口来改进整个系统。</p><p>我们通过不断的监控，备份关键数据，快速和自动恢复来提供系统的容错。Chunk备份让我们可以容忍chunkserver的失败发生。这些经常性的失败，驱动了一个优雅的在线修复机制的产生，它尽可能快的周期性透明的修复那些丢失的副本。另外，我们通过使用校验和来检测磁盘或者IDE子系统级别的数据损坏，对于系统中硬盘数目很大的时候，这种损坏就变得很正常。</p><p>我们的设计对于很多执行大量任务的并发读者和写者实现了高的整体吞吐率。我们这是通过分离文件系统控制实现的，这个控制让master直接来处理，对于数据传输则在chunkserver和客户端之间进行。通过增大chunk的大小以及chunk的租约机制，减少master在普通操作中的参与度。这让一个简单的，中央化的的master不会成为瓶颈成为可能。我们相信网络协议栈的改进将解除当前对单个客户端看到的写入吞吐量的限制。</p><p>GFS成功地满足了我们的存储需求，并广泛的被作为一个数据存储平台用于google的研究和开发以及生产数据的处理。它是一个重要的工具，使我们能够在整个互联网继续创新和解决问题。</p><h2 id="致谢"><a href="#致谢" class="headerlink" title="致谢"></a><strong>致谢</strong></h2><p>We wish to thank the following people for their contributions to the system or the paper. Brain Bershad (our shepherd) and the anonymous reviewers gave us valuable comments and suggestions. Anurag Acharya, Jeff Dean, and David des- Jardins contributed to the early design. Fay Chang worked on comparison of replicas across chunkservers. Guy Ed- jlali worked on storage quota. Markus Gutschke worked on a testing framework and security enhancements. David Kramer worked on performance enhancements. Fay Chang, Urs Hoelzle, Max Ibel, Sharon Perl, Rob Pike, and Debby Wallach commented on earlier drafts of the paper. Many of our colleagues at Google bravely trusted their data to a new file system and gave us useful feedback. Yoshka helped with early testing.</p><h2 id="引用"><a href="#引用" class="headerlink" title="引用"></a><strong>引用</strong></h2><p>[1] Thomas Anderson, Michael Dahlin, Jeanna Neefe, David Patterson, Drew Roselli, and Randolph Wang. Serverless network file systems. In Proceedings of the 15th ACM Symposium on Operating System Principles, pages 109–126, Copper Mountain Resort, Colorado, December 1995.</p><p>[2] Remzi H. Arpaci-Dusseau, Eric Anderson, Noah Treuhaft, David E. Culler, Joseph M. Hellerstein, David Patterson, and Kathy Yelick. Cluster I/O with River: Making the fast case common. In Proceedings of the Sixth Workshop on Input/Output in Parallel and Distributed Systems (IOPADS ’99), pages 10–22, Atlanta, Georgia, May 1999.</p><p>[3] Luis-Felipe Cabrera and Darrell D. E. Long. Swift: Using distributed disk striping to provide high I/O data rates. Computer Systems, 4(4):405–436, 1991.</p><p>[4] Garth A. Gibson, David F. Nagle, Khalil Amiri, Jeff Butler, Fay W. Chang, Howard Gobioff, Charles Hardin, Erik Riedel, David Rochberg, and Jim Zelenka. A cost-effective, high-bandwidth storage architecture. In Proceedings of the 8th Architectural Support for Programming Languages and Operating Systems, pages 92–103, San Jose, California, October 1998.</p><p>[5] John Howard, Michael Kazar, Sherri Menees, David Nichols, Mahadev Satyanarayanan, Robert Sidebotham, and Michael West. Scale and performance in a distributed file system. ACM Transactions on Computer Systems, 6(1):51–81, February 1988.</p><p>[6] InterMezzo. <a href="http://www.inter-mezzo.org">http://www.inter-mezzo.org</a>, 2003.</p><p>[7] Barbara Liskov, Sanjay Ghemawat, Robert Gruber, Paul Johnson, Liuba Shrira, and Michael Williams.</p><p>Replication in the Harp file system. In 13th Symposium on Operating System Principles, pages 226–238, Pacific Grove, CA, October 1991.</p><p>[8] Lustre. <a href="http://www.lustreorg">http://www.lustreorg</a>, 2003.</p><p>[9] David A. Patterson, Garth A. Gibson, and Randy H.</p><p>Katz. A case for redundant arrays of inexpensive disks (RAID). In Proceedings of the 1988 ACM SIGMOD International Conference on Management of Data, pages 109–116, Chicago, Illinois, September 1988.</p><p>[10] Frank Schmuck and Roger Haskin. GPFS: A shared-disk file system for large computing clusters. In Proceedings of the First USENIX Conference on File and Storage Technologies, pages 231–244, Monterey, California, January 2002.</p><p>[11] Steven R. Soltis, Thomas M. Ruwart, and Matthew T. O’Keefe. The Gobal File System. In Proceedings of the Fifth NASA Goddard Space Flight Center Conference on Mass Storage Systems and Technologies, College Park, Maryland, September 1996.</p><p>[12] Chandramohan A. Thekkath, Timothy Mann, and Edward K. Lee. Frangipani: A scalable distributed file system. In Proceedings of the 16th ACM Symposium on Operating System Principles, pages 224–237, Saint-Malo, France, October 1997.</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;​        该paper是2003年Google发表在SOSP（Symposium on Operating Systems Principles-计算机系统领域的顶会）的论文《The Google File System》个人翻译。作为Hadoop时代的三架马车之一，对当前分布式系统诸多问题的解决方案具有很重要的意义。&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;个人翻译不易，转载请备注！&lt;/p&gt;
&lt;/blockquote&gt;</summary>
    
    
    
    
  </entry>
  
  <entry>
    <title>【Java】ConcurrentHashMap源码分析</title>
    <link href="http://lollipops.work/2020/05/10/Java-JDK-ConcurrentHashMap/"/>
    <id>http://lollipops.work/2020/05/10/Java-JDK-ConcurrentHashMap/</id>
    <published>2020-05-09T16:59:49.000Z</published>
    <updated>2021-01-16T09:05:23.397Z</updated>
    
    <content type="html"><![CDATA[<p>ConcurrentHashMap源码分析</p><a id="more"></a><h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>JDK中提供了HashTable和ConcurrentHashMap两个类面向并发情况下使用HashMap的结构（或者使用Collections提供的synchronizedMap方法）。其中HashTable是通过对关键的操作全部加上synchronize同步锁来解决并发问题的，这种做法使用锁的粒度太大了，操作的时候基本就是串行化并发操作来保证并发安全，性能自然十分的差。所以JDK在1.5版本提供了ConcurrentHashMap来解决高并发场景下使用HashMap的问题。</p><p>JDK从1.7 到1.8引入了很多东西，HashMap发生了很多的变化，同时ConcurrentHashMap也做了较大的改动，本文就2个版本分别做分析及对比。</p><h1 id="预备"><a href="#预备" class="headerlink" title="预备"></a>预备</h1><h2 id="Unsafe"><a href="#Unsafe" class="headerlink" title="Unsafe"></a>Unsafe</h2><p>ConcurrentHashMap里面大量使用了Unsafe的东西，所以先了解下Unsafe。</p><p>Unsafe是位于<code>sun.misc</code>包下的一个类，主要提供一些用于执行<strong>低级别、不安全</strong>操作的方法，如直接访问系统内存资源、自主管理内存资源等。Unsafe类使Java语言拥有了类似C语言指针一样操作内存空间的能力。不正确使用Unsafe类会使得程序出错的概率变大，使得Java这种安全的语言变得不安全，所以一定要慎重使用Unsafe。</p><p>java的Unsafe主要提供的功能有：</p><p><img src="https://p1.meituan.net/travelcube/f182555953e29cec76497ebaec526fd1297846.png" alt="img"></p><p>在ConcurrentHashMap中的CAS功能就是通过Unsafe来实现的，当然ConcurrentHashMap不止就使用其CAS的功能。</p><p>我们自己平时一般不会使用Unsafe里面的东西，而是用JDK为我们包装好的工具，但如果使用Unsafe提供的方法。但是注意一点：</p><p>我们使用Unsafe类去getUnsafe实例的时候是获取不到的。因为我们get的时候：</p><pre><code class="lang-java">private static final sun.misc.Unsafe UNSAFE; UNSAFE = sun.misc.Unsafe.getUnsafe();</code></pre><p>可以查看下getUnsafe()这个方法：</p><pre><code class="lang-java">@CallerSensitivepublic static Unsafe getUnsafe() &#123;    Class var0 = Reflection.getCallerClass();    if (var0.getClassLoader() != null) &#123;        throw new SecurityException(&quot;Unsafe&quot;);    &#125; else &#123;        return theUnsafe;    &#125;&#125;</code></pre><p>其中var0其实就是调用这个getUnsafe的类。</p><p>注意一点：我们正常代码是无法使用这个getUnsafe方法的，因为我们一般代码的类的ClassLoader都是ApplicationClassLoader，所以var0.getClassLoader() != null判断为true会直接抛异常。而ConcurrentHashMap的类加载器是BootStrapClassLoader，而java代码中是无法获取到BootStrapClassLoader的（BootStrap是C实现的），所以会直接返回theUnsafe对象。</p><p>所以只有BootStrap加载的那些类才能直接使用Unsafe，其他的都不能直接使用，我们可以通过2种方法去使用它：</p><ol><li>可以通过反射来获取theUnsafe对象。</li><li>使用<code>-Xbootclasspath/a</code>把调用Unsafe相关方法的类A所在jar包路径追加到默认的bootstrap路径</li></ol><p>Unsafe的各种功能这里不多赘述，<a href="https://tech.meituan.com/2019/02/14/talk-about-java-magic-class-unsafe.html">网上资料很多</a>。</p><h2 id="HashMap"><a href="#HashMap" class="headerlink" title="HashMap"></a>HashMap</h2><p>ConcurrentHashMap里面很多小细节都和其对应版本的HashMap是一样的，这里就不做赘述了。</p><h1 id="JDK1-7实现"><a href="#JDK1-7实现" class="headerlink" title="JDK1.7实现"></a>JDK1.7实现</h1><h2 id="结构"><a href="#结构" class="headerlink" title="结构"></a>结构</h2><p>先看看结构</p><h3 id="类结构"><a href="#类结构" class="headerlink" title="类结构"></a>类结构</h3><p><img src=":java-jdk-concurrenthashmap/ConcurrentHashMap.png" alt="ConcurrentHashMap"></p><h3 id="内部结构"><a href="#内部结构" class="headerlink" title="内部结构"></a>内部结构</h3><p> <img src=":java-jdk-concurrenthashmap/struct.png" alt="image-20200219111015592"></p><p>存储结构和1.7的HashMap一样采用了<strong>数组+链表</strong>的方式；</p><p>并发控制采用的是<strong>分段锁</strong>的结构，每个Segment就是一把锁（ReentrantLock）。相对于HashTable来说，ConcurrentHashMap对于并发控制的锁的细粒度更细。</p><p>ConcurrentHashMap的一个kv存储单元叫<strong>HashEntry</strong>（类似于HashMap的Entry），Segment相当于一个仓库房间，HashEntry就是房间中的货物。多个线程访问同一个房间只能排队，但是整个ConcurrentHashMap可以排Segment.length个队伍。</p><h2 id="源码"><a href="#源码" class="headerlink" title="源码"></a>源码</h2><h3 id="重要结构类"><a href="#重要结构类" class="headerlink" title="重要结构类"></a>重要结构类</h3><h4 id="Segment"><a href="#Segment" class="headerlink" title="Segment"></a>Segment</h4><pre><code class="lang-java">static final class Segment&lt;K,V&gt; extends ReentrantLock implements Serializable &#123;    transient volatile HashEntry&lt;K,V&gt;[] table;    transient int threshold;    final float loadFactor;    Segment(float lf, int threshold, HashEntry&lt;K,V&gt;[] tab) &#123;        this.loadFactor = lf;        this.threshold = threshold;        this.table = tab;    &#125;      //方法体略    final V put(K key, int hash, V value, boolean onlyIfAbsent)&#123;&#125;;    private void rehash(HashEntry&lt;K,V&gt; node) &#123;&#125;;    private void scanAndLock(Object key, int hash) &#123;&#125;;    final V remove(Object key, int hash, Object value) &#123;&#125;;    final boolean replace(K key, int hash, V oldValue, V newValue) &#123;&#125;;    final V replace(K key, int hash, V value) &#123;&#125;;    final void clear() &#123;&#125;;&#125;</code></pre><p>因为Segment是继承了ReentrantLock，所以Segment也是一个可重入锁。每个Segment自带了一把锁。我们从代码可见：<strong>Segment不仅仅是一把锁，其内部自成一个小型的HashMap</strong>。</p><h4 id="HashEntry"><a href="#HashEntry" class="headerlink" title="HashEntry"></a>HashEntry</h4><pre><code class="lang-java">static final class HashEntry&lt;K,V&gt; &#123;    final int hash;    final K key;    volatile V value;    volatile HashEntry&lt;K,V&gt; next;    HashEntry(int hash, K key, V value, HashEntry&lt;K,V&gt; next) &#123;        this.hash = hash;        this.key = key;        this.value = value;        this.next = next;    &#125;    final void setNext(HashEntry&lt;K,V&gt; n) &#123;        UNSAFE.putOrderedObject(this, nextOffset, n);    &#125;    //....&#125;</code></pre><p>HashEntry是实际封装key/value对的类。其结构就是一个简单的<strong>链表结构</strong>。</p><h3 id="构造函数"><a href="#构造函数" class="headerlink" title="构造函数"></a>构造函数</h3><pre><code class="lang-java">public ConcurrentHashMap(int initialCapacity,                             float loadFactor, int concurrencyLevel) &#123;        if (!(loadFactor &gt; 0) || initialCapacity &lt; 0 || concurrencyLevel &lt;= 0)            throw new IllegalArgumentException();        if (concurrencyLevel &gt; MAX_SEGMENTS)            concurrencyLevel = MAX_SEGMENTS;        // Find power-of-two sizes best matching arguments        int sshift = 0; //位移的次数，也是ssize 2的次幂数        int ssize = 1;        //ssize就是segment数组大小，这里将传来的concurrencyLevel变为2的次幂数（因为put的时候也是要&amp;来计算放在哪个segment）        while (ssize &lt; concurrencyLevel) &#123;            ++sshift;            ssize &lt;&lt;= 1;        &#125;        this.segmentShift = 32 - sshift; //这个在put的时候使用，为了将key的hashcode右移多少位，然后计算位于哪个segment中。32是因为hashcode是int的是32位        this.segmentMask = ssize - 1;//segment掩码        if (initialCapacity &gt; MAXIMUM_CAPACITY)            initialCapacity = MAXIMUM_CAPACITY;        int c = initialCapacity / ssize;//每个segment存放entry数量        //防止除法是去尾的，总和可能会小于initialCapacity。比如17/16=1 16*1&lt;17        if (c * ssize &lt; initialCapacity)            ++c;        int cap = MIN_SEGMENT_TABLE_CAPACITY; //每个segment存放entry数量有个最小值为2，如果小于2就*2，也必须是2的倍数        while (cap &lt; c)            cap &lt;&lt;= 1;        // create segments and segments[0]        // 创建segment的数组和单个segment内部带entry数组的s0        // -- 所以1.7 不是懒加载的        Segment&lt;K,V&gt; s0 =            new Segment&lt;K,V&gt;(loadFactor, (int)(cap * loadFactor),                             (HashEntry&lt;K,V&gt;[])new HashEntry[cap]);//segment内部其实又相当于是一个小的hashmap        Segment&lt;K,V&gt;[] ss = (Segment&lt;K,V&gt;[])new Segment[ssize];        UNSAFE.putOrderedObject(ss, SBASE, s0); // ordered write of segments[0]        this.segments = ss;    &#125;</code></pre><h4 id="三个构造参数"><a href="#三个构造参数" class="headerlink" title="三个构造参数"></a>三个构造参数</h4><pre><code class="lang-java">int initialCapacity;     //设定entry的个数，默认16float loadFactor;         //负载因子，默认0.75fint concurrencyLevel; //segment的个数，默认16</code></pre><p>上面2个参数和HashMap的一样，concurrencyLevel（并发等级）就是上面结构图中Segment的个数，即Segment多少个就能支持多少个线程并发的访问。</p><h4 id="执行流程"><a href="#执行流程" class="headerlink" title="执行流程"></a>执行流程</h4><ol><li>参数校验和再次调整，具体做了下面的操作</li></ol><ul><li>concurrencyLevel 调整为2次幂数</li><li>计算每个Segment中应该分配的HashEntry数组长度（<strong>每个Segment中HashEntry的长度不能小于2</strong>）<ul><li>首先通过计算公式：initialCapacity / ssize计算初步长度</li><li>MIN_SEGMENT_TABLE_CAPACITY=2是系统指定的每个Segment最小分配的HashEntry长度</li><li>长度小于2就乘以2（也要保证是2的倍数）</li></ul></li></ul><ol><li>先创建一个Segment，并初始化好内部HashEntry数组</li><li>再创建Segment数组，并将创建好的Segment放入Segment[0]</li></ol><h4 id="构造总结"><a href="#构造总结" class="headerlink" title="构造总结"></a>构造总结</h4><ul><li>每个Segment中HashEntry的长度不能小于2，并且是2的倍数</li><li>1.7的ConcurrentHashMap不是懒加载的，而是先初始化了Segment数组以及Segment数组中第一个位置的HashEntry数组</li></ul><p>我们这里不要将ConcurrentHashMap的结果给理解为以segment为单位的，而是还是按照HashMap一样以entry为单位，而segment只是在entry上的一个修饰。</p><h3 id="put函数"><a href="#put函数" class="headerlink" title="put函数"></a>put函数</h3><pre><code class="lang-java">public V put(K key, V value) &#123;    Segment&lt;K,V&gt; s;    if (value == null)        throw new NullPointerException();    int hash = hash(key);    //将hash值右移保留高位，因为要和segment长度&amp;计算，所以hashcode保留位数和segment长度的位数应该一致。    int j = (hash &gt;&gt;&gt; segmentShift) &amp; segmentMask;    if ((s = (Segment&lt;K,V&gt;)UNSAFE.getObject          // nonvolatile; recheck         (segments, (j &lt;&lt; SSHIFT) + SBASE)) == null) //  in ensureSegment        //segment为空就去生成一个        s = ensureSegment(j);    return s.put(key, hash, value, false);&#125;</code></pre><h4 id="执行流程-1"><a href="#执行流程-1" class="headerlink" title="执行流程"></a>执行流程</h4><ol><li>参数校验，<strong>key和value都不能为null</strong>。</li><li>计算key的hash值，并确定该key存放在哪个Segment中。</li><li>判断需要放入Segment位置是否初始化？如果没有使用ensureSegment进行初始化</li><li>调用Segment的put存入元素</li></ol><h4 id="如何确定存储Segment位置？"><a href="#如何确定存储Segment位置？" class="headerlink" title="如何确定存储Segment位置？"></a>如何确定存储Segment位置？</h4><p>主要理解代码中的：</p><pre><code class="lang-java">(hash &gt;&gt;&gt; segmentShift) &amp; segmentMask;</code></pre><p>segmentShift和segmentMask是我们在构造过程中计算出来的：</p><pre><code class="lang-java">this.segmentShift = 32 - sshift;this.segmentMask = ssize - 1;</code></pre><ul><li>ssize就是Segment数组的长度</li><li>sshift是Segment长度对应2的次幂的次幂数，比如ssize为8，那么sshift就为3</li></ul><p>segmentMask掩码很好理解，HashMap都是和掩码&amp;运算来确定位置的。</p><p><code>32-sshift</code> <strong>则相当于HashMap的位干扰，具体作用是保留hash值的高位来和掩码&amp;运算。</strong></p><p>因为int是32个字节，减去sshift其实就是相当于保留高sshift的位数，这样和ssize的二进制位的掩码&amp;运算可以保证位置落在0~ssize中。</p><h4 id="ensureSegment"><a href="#ensureSegment" class="headerlink" title="ensureSegment"></a>ensureSegment</h4><p>上面put函数确定了我们的k/v应该存放在哪个Segment中去了，但是我们构造的时候只是初始化了第一个位置的Segment。所以如果为空需要先初始化一下Segment。</p><pre><code class="lang-java">private Segment&lt;K,V&gt; ensureSegment(int k) &#123;    final Segment&lt;K,V&gt;[] ss = this.segments;    long u = (k &lt;&lt; SSHIFT) + SBASE; // raw offset    Segment&lt;K,V&gt; seg;    if ((seg = (Segment&lt;K,V&gt;)UNSAFE.getObjectVolatile(ss, u)) == null) &#123;        Segment&lt;K,V&gt; proto = ss[0]; // use segment 0 as prototype 复用segment0的对象来创建新的segment        int cap = proto.table.length;        float lf = proto.loadFactor;        int threshold = (int)(cap * lf);        HashEntry&lt;K,V&gt;[] tab = (HashEntry&lt;K,V&gt;[])new HashEntry[cap];        if ((seg = (Segment&lt;K,V&gt;)UNSAFE.getObjectVolatile(ss, u))            == null) &#123; // recheck            Segment&lt;K,V&gt; s = new Segment&lt;K,V&gt;(lf, threshold, tab);            //自旋，不断的判断操作过程中是否有其他线程的修改            while ((seg = (Segment&lt;K,V&gt;)UNSAFE.getObjectVolatile(ss, u))                   == null) &#123;                //cas操作                if (UNSAFE.compareAndSwapObject(ss, u, null, seg = s))                    break;            &#125;        &#125;    &#125;    return seg;&#125;</code></pre><h5 id="执行流程-2"><a href="#执行流程-2" class="headerlink" title="执行流程"></a>执行流程</h5><ol><li>都是使用的Unsafe的方法来判断需要put的位置上的Segment是否为null。</li><li>如果为null，复用Segment[0]位置上的（构造函数已经初始化的）属性来构建一个Segment对象。</li></ol><p>上面步骤基本上没到关键的涉及真实内存操作的时候都会走Unsafe调用，是为了保证在执行的时候，没有其他线程的操作影响。</p><h4 id="Segment的put函数"><a href="#Segment的put函数" class="headerlink" title="Segment的put函数"></a>Segment的put函数</h4><p>上面确定了所在元素是位于哪个Segment了，最后一步就是调用Segment的put方法，将元素放入HashEntry数组里面。</p><pre><code class="lang-java">final V put(K key, int hash, V value, boolean onlyIfAbsent) &#123;    //尝试获取锁，node返回可能是null就在下面put的时候初始化，    //不是null说明在获取锁的时候，scanAndLockForPut帮我们预热完成    HashEntry&lt;K,V&gt; node = tryLock() ? null :        scanAndLockForPut(key, hash, value);    V oldValue;    try &#123;        // Segment内部的HashEntry数组        HashEntry&lt;K,V&gt;[] tab = table;        // 定位该hash值需要存储的HashEntry数组位置        int index = (tab.length - 1) &amp; hash;        // 获取需要存入的数组位置上拉链的头结点        HashEntry&lt;K,V&gt; first = entryAt(tab, index);        // 遍历链表        for (HashEntry&lt;K,V&gt; e = first;;) &#123;            // 如果当前位置上有值            if (e != null) &#123;                K k;                // 有值并且key和hash都相等。直接替换旧值                if ((k = e.key) == key ||                    (e.hash == hash &amp;&amp; key.equals(k))) &#123;                    oldValue = e.value;                    if (!onlyIfAbsent) &#123;                        e.value = value;                        ++modCount;                    &#125;                    break;                &#125;                // 有值，但是key不等就往下遍历                e = e.next;            &#125;            else &#123; //当前位置为null了，说明要么就是链表是空的，要么就是遍历到尾部了。                if (node != null)                   // 如果当前node在获取锁的已经初始化完成了，那么就直接头插法，将其next指向原来的头结点                    node.setNext(first);                else                    // 如果node还为null，就在这里进行创建HashEntry对象并头插。                    node = new HashEntry&lt;K,V&gt;(hash, key, value, first);                int c = count + 1;                // 判断是否需要扩容，ConcurrentHashMap只扩Segment的容量                if (c &gt; threshold &amp;&amp; tab.length &lt; MAXIMUM_CAPACITY)                    rehash(node);                else                    //设置为头节点                    setEntryAt(tab, index, node);                ++modCount;                count = c;                oldValue = null;                break;            &#125;        &#125;    &#125; finally &#123;        // 不要忘了解锁        unlock();    &#125;    return oldValue;&#125;</code></pre><h5 id="执行流程-3"><a href="#执行流程-3" class="headerlink" title="执行流程"></a>执行流程</h5><ol><li>获取锁</li><li>扫描链表，采用头插法插入元素，或者key存在就更新元素</li><li>判断是否需要扩容</li></ol><h4 id="rehash扩容"><a href="#rehash扩容" class="headerlink" title="rehash扩容"></a>rehash扩容</h4><p>这个没什么好交代的，和1.7的HashMap一样，双倍扩容，链表重新分配位置。</p><h4 id="scanAndLockForPut函数"><a href="#scanAndLockForPut函数" class="headerlink" title="scanAndLockForPut函数"></a>scanAndLockForPut函数</h4><p>这个函数的主要作用就是不断的尝试去获取锁，并且帮我们做了很多预热工作。</p><pre><code class="lang-java">private HashEntry&lt;K,V&gt; scanAndLockForPut(K key, int hash, V value) &#123;    //获取链表的头部    HashEntry&lt;K,V&gt; first = entryForHash(this, hash);    HashEntry&lt;K,V&gt; e = first;    HashEntry&lt;K,V&gt; node = null;    int retries = -1; // negative while locating node    //循环尝试获取锁，如果获取到了就返回node    while (!tryLock()) &#123;        HashEntry&lt;K,V&gt; f; // to recheck first below        // 这里和0判断，主要保证创建node只会走一次        if (retries &lt; 0) &#123;            if (e == null) &#123;                // 遍历发现该链表没有key，就大胆的先初始化好它备用。                if (node == null) // speculatively create node                    node = new HashEntry&lt;K,V&gt;(hash, key, value, null);                // 后面就不再执行这里了。                retries = 0;            &#125;            // 如果发现有key相同的node，那么代表要覆盖，就不创建新node            else if (key.equals(e.key))                retries = 0;            else                //继续往下遍历节点                e = e.next;        &#125;        //MAX_SCAN_RETRIES：单核是1，多核是64，        //如果尝试的次数大于MAX_SCAN_RETRIES，则调用lock()方法阻塞，使当前线程进入同步队列中等待被唤醒        else if (++retries &gt; MAX_SCAN_RETRIES) &#123;            lock();            break;        &#125;        // 链表发生了变化，就需要重新遍历了        else if ((retries &amp; 1) == 0 &amp;&amp;                 (f = entryForHash(this, hash)) != first) &#123;            e = first = f; // re-traverse if entry changed            retries = -1;        &#125;    &#125;    // 返回node，可能是null（获取到锁的时候，没有发现满足node的条件）也可能不是null。    return node;&#125;</code></pre><h5 id="做了哪些预热工作？"><a href="#做了哪些预热工作？" class="headerlink" title="做了哪些预热工作？"></a>做了哪些预热工作？</h5><ul><li>在不断尝试获取锁的过程中，扫描当前链表，判断该链表是否包含该key。</li><li>如果不包含就这里先初始化好HashEntry返回。否则还是返回null。</li></ul><h3 id="get函数"><a href="#get函数" class="headerlink" title="get函数"></a>get函数</h3><p>get函数就比较简单：</p><pre><code class="lang-java">public V get(Object key) &#123;    Segment&lt;K,V&gt; s; // manually integrate access methods to reduce overhead    HashEntry&lt;K,V&gt;[] tab;    int h = hash(key);    //定位Segment的位置    long u = (((h &gt;&gt;&gt; segmentShift) &amp; segmentMask) &lt;&lt; SSHIFT) + SBASE;    if ((s = (Segment&lt;K,V&gt;)UNSAFE.getObjectVolatile(segments, u)) != null &amp;&amp;        (tab = s.table) != null) &#123;        // 定义HashEntry数组中位置，并遍历该位置的链表判断是否有该key        for (HashEntry&lt;K,V&gt; e = (HashEntry&lt;K,V&gt;) UNSAFE.getObjectVolatile                 (tab, ((long)(((tab.length - 1) &amp; h)) &lt;&lt; TSHIFT) + TBASE);             e != null; e = e.next) &#123;            K k;            if ((k = e.key) == key || (e.hash == h &amp;&amp; key.equals(k)))                return e.value;        &#125;    &#125;    return null;&#125;</code></pre><h4 id="执行流程-4"><a href="#执行流程-4" class="headerlink" title="执行流程"></a>执行流程</h4><ol><li>获取Segment位置</li><li>获取HashEntry的位置</li><li>遍历链表查看是否有元素，有则返回，无则返null</li></ol><h3 id="remove函数"><a href="#remove函数" class="headerlink" title="remove函数"></a>remove函数</h3><p>逻辑也很清晰和简单，直接看代码很清楚。</p><pre><code class="lang-java">public V remove(Object key) &#123;    int hash = hash(key);    //定位Segment    Segment&lt;K,V&gt; s = segmentForHash(hash);    return s == null ? null : s.remove(key, hash, null);&#125;</code></pre><pre><code class="lang-java">final V remove(Object key, int hash, Object value) &#123;    //进入Segment先获取锁    if (!tryLock())        scanAndLock(key, hash);    V oldValue = null;    try &#123;        HashEntry&lt;K,V&gt;[] tab = table;        //定位HashEntry        int index = (tab.length - 1) &amp; hash;        HashEntry&lt;K,V&gt; e = entryAt(tab, index);        HashEntry&lt;K,V&gt; pred = null;        while (e != null) &#123;            K k;            HashEntry&lt;K,V&gt; next = e.next;            //找到了待删除节点，进行删除            if ((k = e.key) == key ||                (e.hash == hash &amp;&amp; key.equals(k))) &#123;                V v = e.value;                if (value == null || value == v || value.equals(v)) &#123;                    if (pred == null)                        setEntryAt(tab, index, next);                    else                        pred.setNext(next);                    ++modCount;                    --count;                    oldValue = v;                &#125;                break;            &#125;            pred = e;            e = next;        &#125;    &#125; finally &#123;        unlock();    &#125;    return oldValue;&#125;</code></pre><h3 id="size-方法"><a href="#size-方法" class="headerlink" title="size 方法"></a>size 方法</h3><p>看很多博客都加了size分析，我也掺一脚吧。</p><pre><code class="lang-java">public int size() &#123;    // Try a few times to get accurate count. On failure due to    // continuous async changes in table, resort to locking.    final Segment&lt;K,V&gt;[] segments = this.segments;    int size;    boolean overflow; // size大小是否溢出    long sum;         // 当前modCount数量    long last = 0L;   // 上次modCount数量    int retries = -1; // first iteration isn&#39;t retry    try &#123;        for (;;) &#123;            //RETRIES_BEFORE_LOCK=2            //先不走锁的方案            if (retries++ == RETRIES_BEFORE_LOCK) &#123;                // 如果试了2次发现，两个modCount数量不一样，说明有修改，就将所有Segment进行加锁计算                for (int j = 0; j &lt; segments.length; ++j)                    ensureSegment(j).lock(); // force creation            &#125;            sum = 0L;            size = 0;            overflow = false;            //不加锁计算出所有Segment的count和modCount            for (int j = 0; j &lt; segments.length; ++j) &#123;                Segment&lt;K,V&gt; seg = segmentAt(segments, j);                if (seg != null) &#123;                    sum += seg.modCount;                    int c = seg.count;                    if (c &lt; 0 || (size += c) &lt; 0)                        overflow = true;                &#125;            &#125;            // 如果2次比较相同，说明期间没有修改，size是准确的。            if (sum == last)                break;            last = sum;        &#125;    &#125; finally &#123;        // 如果是加锁的话，最后记得解锁        if (retries &gt; RETRIES_BEFORE_LOCK) &#123;            for (int j = 0; j &lt; segments.length; ++j)                segmentAt(segments, j).unlock();        &#125;    &#125;    return overflow ? Integer.MAX_VALUE : size;&#125;</code></pre><p>因为并发操作获取size的过程中可能有修改的操作，所以获取的可能不准，想要精确的值就需要将所有的Segment的加锁，然后分别求出每个Segment中HashEntry的数量。但是全部加锁会严重影响ConcurrentHashMap的性能。所有作者提出了另外一种解决方案：在不加锁的情况下先求出所有modCount的总和2次，比对2次结果，如果相同，则认为在获取期间ConcurrentHashMap没有发生变化。否则，就只能走加锁的模式。</p><h1 id="JDK1-8实现"><a href="#JDK1-8实现" class="headerlink" title="JDK1.8实现"></a>JDK1.8实现</h1><p>JDK1.8 的类就很复杂了，一大堆的内部类就看晕了(✖人✖)。</p><h2 id="结构图"><a href="#结构图" class="headerlink" title="结构图"></a>结构图</h2><h3 id="内部结构-1"><a href="#内部结构-1" class="headerlink" title="内部结构"></a>内部结构</h3><p><img src="/Users/zhaohaoren/workspace/blog/hexo/source/img/hashmap/ds.png" alt=""></p><p>其底层结构和HashMap是一样的，采用<strong>数组+链表/红黑树</strong>来存储数据。只是不再使用分段锁，而是采用了<strong>CAS + synchronized</strong>来保证并发安全。</p><h2 id="源码-1"><a href="#源码-1" class="headerlink" title="源码"></a>源码</h2><h3 id="内部类"><a href="#内部类" class="headerlink" title="内部类"></a>内部类</h3><p><img src="https://images2018.cnblogs.com/blog/1394959/201806/1394959-20180606194916727-1631558402.png" alt="img"></p><p><img src="https://images2018.cnblogs.com/blog/1394959/201806/1394959-20180606194941108-1310305878.png" alt="img"></p><h3 id="重要结构类-1"><a href="#重要结构类-1" class="headerlink" title="重要结构类"></a>重要结构类</h3><h4 id="Node"><a href="#Node" class="headerlink" title="Node"></a>Node</h4><p>ConcurrentHashMap中最基本的类，最终包装存入的key和value的对象。</p><pre><code class="lang-java">static class Node&lt;K,V&gt; implements Map.Entry&lt;K,V&gt; &#123;    final int hash;    final K key;    volatile V val;    volatile Node&lt;K,V&gt; next;&#125;</code></pre><p>可以看出这是一个链表结构，还需要注意的是hash这个在ConcurrentHashMap中有更多的作用：</p><pre><code class="lang-java">//hash值为-1的时候代表节点是fwdstatic final int MOVED     = -1; // hash for forwarding nodes //hash值为-2的时候，代表这是一个树的根static final int TREEBIN   = -2; // hash for roots of trees//hash值为-3的，代表是保留节点，用于computeIfAbsent等static final int RESERVED  = -3; // hash for transient reservations</code></pre><h4 id="TreeNode"><a href="#TreeNode" class="headerlink" title="TreeNode"></a>TreeNode</h4><p>红黑树的节点，当链表转为树的时候Node节点会转为树节点</p><pre><code class="lang-java">static final class TreeNode&lt;K,V&gt; extends Node&lt;K,V&gt; &#123;    TreeNode&lt;K,V&gt; parent;  // red-black tree links    TreeNode&lt;K,V&gt; left;    TreeNode&lt;K,V&gt; right;    TreeNode&lt;K,V&gt; prev;    // needed to unlink next upon deletion    boolean red;&#125;</code></pre><h4 id="TreeBin"><a href="#TreeBin" class="headerlink" title="TreeBin"></a>TreeBin</h4><p>ConcurrentHashMap内部的Hash表不是直接存储TreeNode节点的，而是存储的TreeBin，通过TreeBin来维护这棵红黑树</p><pre><code class="lang-java">static final class TreeBin&lt;K,V&gt; extends Node&lt;K,V&gt; &#123;    TreeNode&lt;K,V&gt; root;    volatile TreeNode&lt;K,V&gt; first;&#125;</code></pre><h4 id="ForwardingNode"><a href="#ForwardingNode" class="headerlink" title="ForwardingNode"></a>ForwardingNode</h4><p>扩容时候用到的类，在扩容时候，操作的Node会变为该Node状态，这时候有put操作，遇到该节点就会去先协助扩容。</p><p>ForwardingNode对象来源有两个：</p><ol><li>原来的位置为null，但是此时复制操作已经到当前位置的后面了，会将这个原来的桶的这个位置置为ForwardingNode对象；</li><li>2.原来位置不为null，但是已经操作过这个位置了。</li></ol><pre><code class="lang-java">static final class ForwardingNode&lt;K,V&gt; extends Node&lt;K,V&gt; &#123;    final Node&lt;K,V&gt;[] nextTable;    ForwardingNode(Node&lt;K,V&gt;[] tab) &#123;        super(MOVED, null, null, null);        this.nextTable = tab;    &#125;&#125;</code></pre><h3 id="重要属性"><a href="#重要属性" class="headerlink" title="重要属性"></a>重要属性</h3><h4 id="table"><a href="#table" class="headerlink" title="table"></a>table</h4><p>hash表</p><pre><code class="lang-java">transient volatile Node&lt;K,V&gt;[] table;</code></pre><h4 id="nextTable"><a href="#nextTable" class="headerlink" title="nextTable"></a>nextTable</h4><p>当扩容的时候，新的hash表，其他时候该表为null</p><pre><code class="lang-java">private transient volatile Node&lt;K,V&gt;[] nextTable;</code></pre><h4 id="baseCount"><a href="#baseCount" class="headerlink" title="baseCount"></a>baseCount</h4><p>基本计数器，主要在没有多线程并发竞争更新的时候使用，CAS原子更新。ConcurrentHashMap不希望并发的时候再更新计数器的时候也阻塞住，所以计数方式采用了baseCount+counterCells。</p><pre><code class="lang-java">private transient volatile long baseCount;</code></pre><h4 id="counterCells"><a href="#counterCells" class="headerlink" title="counterCells"></a>counterCells</h4><p>并发情况下，计数通过更新这个数组来增加计数，这是一个数组，并且长度是2的次幂</p><pre><code class="lang-java">private transient volatile CounterCell[] counterCells;</code></pre><h4 id="sizeCtl"><a href="#sizeCtl" class="headerlink" title="sizeCtl"></a>sizeCtl</h4><p>最核心的一个属性，控制这表的扩容初始化等。后面会详细介绍各个状态及作用。</p><pre><code class="lang-java">private transient volatile int sizeCtl;</code></pre><h3 id="构造函数-1"><a href="#构造函数-1" class="headerlink" title="构造函数"></a>构造函数</h3><p>1.8中无论是HashMap还是ConcurrentHashMap都是采用了懒加载的方式，即在构造函数调用的时候，并不会初始化内部table，而是在第一次put的时候才会申请内存。</p><pre><code class="lang-java">// 使用默认参数构造// 初始容量 (16)、加载因子 (0.75)  concurrencyLevel (16) public ConcurrentHashMap() &#123;&#125;public ConcurrentHashMap(int initialCapacity,float loadFactor, int concurrencyLevel) &#123;        if (!(loadFactor &gt; 0.0f) || initialCapacity &lt; 0 || concurrencyLevel &lt;= 0)            throw new IllegalArgumentException();        //initialCapacity和concurrencyLevel取最大的使用        if (initialCapacity &lt; concurrencyLevel)   // Use at least as many bins            initialCapacity = concurrencyLevel;   // as estimated threads        long size = (long)(1.0 + (long)initialCapacity / loadFactor);        int cap = (size &gt;= (long)MAXIMUM_CAPACITY) ?            MAXIMUM_CAPACITY : tableSizeFor((int)size); //确保表是2的次幂        this.sizeCtl = cap;&#125;</code></pre><p>这里只选择了2个构造函数，其他的构造函数和这个类似，我们可以看到构造函数的主要作用就是计算sizeCtl这个值。</p><h4 id="sizeCtl（表初始化和扩容的控制位）"><a href="#sizeCtl（表初始化和扩容的控制位）" class="headerlink" title="sizeCtl（表初始化和扩容的控制位）"></a>sizeCtl（表初始化和扩容的控制位）</h4><p>sizeCtl是很重要的一个属性，它用于table的初始化和扩容，有几种情况（from注释）：</p><ol><li>0，表示未指定初始容量</li><li>-1，表示table正在初始化</li><li>-N，表示有N-1个线程正在进行扩容操作</li><li>其他情况<ol><li>如果table未初始化，表示table需要初始化的大小</li><li>如果table初始化完成，表示扩容阈值（0.75*容量），当实际容量&gt;=sizeCtl，则扩容</li></ol></li></ol><h4 id="注意点"><a href="#注意点" class="headerlink" title="注意点"></a>注意点</h4><ul><li><code>loadFactor</code> 如果指定这个值只会在构造的时候起作用，在后面的操作中将不再起作用（构造里面并没有赋值给内部属性）。</li><li>计算公式是先计算<code>(1.0 + (long)initialCapacity / loadFactor);</code>然后取这个值的最近2次幂数来作为初始容量的。具体缘由，个人认为因为这里负载因子是一次性的，作者认为用户指定的<code>initialCapacity</code>是用户估算出要存储的数量。所以依据这个值，为了尽量避免扩容，所以就反向求出一个size来保证这个size的在该负载因子下，存储<code>initialCapacity</code>个元素都不会扩容。</li><li><strong>这里的<code>concurrencyLevel</code>和jdk1.7的含义已经不一样了</strong>，他表示估计的参与并发更新的线程数量。其实和并发控制没啥太大关系，只有在构造函数中使用了它和initialCapacity比较下，如果初始容量小于估计并发数情况下，我们则选用并发数作为初始容量。</li></ul><h3 id="put函数-1"><a href="#put函数-1" class="headerlink" title="put函数"></a>put函数</h3><p>put只是简单调用了下putVal方法</p><pre><code class="lang-java">public V put(K key, V value) &#123;    return putVal(key, value, false);&#125;</code></pre><p>我们主要来看putVal</p><pre><code class="lang-java">final V putVal(K key, V value, boolean onlyIfAbsent) &#123;    // key和value都不能为空    if (key == null || value == null) throw new NullPointerException();    // 使用位干扰计算hash值    int hash = spread(key.hashCode());    //用来计算在这个table[index]（也可以称为一个bin）已经有多少个元素，用来控制扩容或者转移为树    int binCount = 0;    for (Node&lt;K,V&gt;[] tab = table;;) &#123;        Node&lt;K,V&gt; f; int n, i, fh;        if (tab == null || (n = tab.length) == 0) //如果table为null就初始化table            tab = initTable();        else if ((f = tabAt(tab, i = (n - 1) &amp; hash)) == null) &#123; //定位该key存放table的位置，判断该位置是否有值            // 如果没有值就CAS插入该key的Node，就结束了（该node就是该table位置上头结点，这里没有使用锁方式）            if (casTabAt(tab, i, null,                         new Node&lt;K,V&gt;(hash, key, value, null)))                break;                   // no lock when adding to empty bin        &#125;        //f是table[index]的头结点，这里检测该节点是否是MOVE的状态，MOVE表示正在进行数组扩张的数据复制阶段        else if ((fh = f.hash) == MOVED)            //当前线程也会去帮助参与扩容的复制            tab = helpTransfer(tab, f);        else &#123;            /*            上面排除了table为空，table[index]为空，数组正在扩容的情况，这些情况处理的时候都不需要同步来控制并发的。            但是如果该位置上有值（可能是链表可能是红黑树），那么该put操作就需要走同步。             */            V oldVal = null;            // *core:            synchronized (f) &#123;                if (tabAt(tab, i) == f) &#123;                    //fh为f.hash值。                    if (fh &gt;= 0) &#123; //fh&gt;=0 说明是链表                        binCount = 1;                        for (Node&lt;K,V&gt; e = f;; ++binCount) &#123; //遍历链表                            K ek;                            if (e.hash == hash &amp;&amp;                                ((ek = e.key) == key ||                                 (ek != null &amp;&amp; key.equals(ek)))) &#123; //如果已存在该key就替换                                oldVal = e.val;                                if (!onlyIfAbsent) ////当是putIfAbsent的时候，只有在这个key没有设置值得时候才设置                                    e.val = value;                                break;                            &#125;                            Node&lt;K,V&gt; pred = e;                            if ((e = e.next) == null) &#123; //遍历到了尾结点就将该Node添加到最后                                pred.next = new Node&lt;K,V&gt;(hash, key,                                                          value, null);                                break;                            &#125;                        &#125;                    &#125;                    //table上该位置是已经转为红黑树了                    else if (f instanceof TreeBin) &#123;                        Node&lt;K,V&gt; p;                        binCount = 2;                        //走树的put逻辑                        if ((p = ((TreeBin&lt;K,V&gt;)f).putTreeVal(hash, key,                                                       value)) != null) &#123;                            oldVal = p.val;                            if (!onlyIfAbsent)                                p.val = value;                        &#125;                    &#125;                &#125;            &#125;            //处理完成后看看是否符合树化条件            if (binCount != 0) &#123;                if (binCount &gt;= TREEIFY_THRESHOLD) //个数大于8就树化                    treeifyBin(tab, i);                if (oldVal != null)                    return oldVal;                break;            &#125;        &#125;    &#125;    //计数    addCount(1L, binCount);    return null;&#125;</code></pre><h4 id="执行流程-5"><a href="#执行流程-5" class="headerlink" title="执行流程"></a>执行流程</h4><ol><li>定位该key在<code>table</code>中位置，判断hash表是否为null及定位的位置上是否有数据。</li><li>当上面条件都不是的时候，则进入<code>synchronized</code>同步块，对树或者链表遍历并插入或者更新该key。</li><li>然后判断下当前bin位置上节点数量是否需要转为红黑树。</li></ol><h4 id="表初始化"><a href="#表初始化" class="headerlink" title="表初始化"></a>表初始化</h4><p>1.8的内部的Hash表是在put的时候才进行初始化的，就是调用上面代码的<code>initTable</code>方法。</p><pre><code class="lang-java">private final Node&lt;K,V&gt;[] initTable() &#123;    Node&lt;K,V&gt;[] tab; int sc;    while ((tab = table) == null || tab.length == 0) &#123; //table还没被初始化，进入while        if ((sc = sizeCtl) &lt; 0) //sizeCtl小于0的时候表示在别的线程在初始化表或扩展表            Thread.yield(); // lost initialization race; just spin        else if (U.compareAndSwapInt(this, SIZECTL, sc, -1)) &#123; //开始准备初始化表，先用CAS将sizeCtrl设置为-1            try &#123;                if ((tab = table) == null || tab.length == 0) &#123;                    //如果指定了大小的时候就创建指定大小的Node数组，否则创建默认大小(16)的Node数组                    int n = (sc &gt; 0) ? sc : DEFAULT_CAPACITY;                    @SuppressWarnings(&quot;unchecked&quot;)                    Node&lt;K,V&gt;[] nt = (Node&lt;K,V&gt;[])new Node&lt;?,?&gt;[n];                    table = tab = nt;                    sc = n - (n &gt;&gt;&gt; 2);                &#125;            &#125; finally &#123;                sizeCtl = sc; //初始化完成后，sizeCtl长度为数组长度的3/4【n-(n &gt;&gt;&gt; 2)】            &#125;            break;        &#125;    &#125;    return tab;&#125;</code></pre><p>代码里面看出，除了初始化表之外，就是不断的调整sizeCtrl的值，来标识当前的状态。如果当前线程发现有线程已经对其初始化了，就让出CPU空闲。最后初始化完成后，sizeCtrl的值设置为table数组大小的0.75倍（<code>n-(n &gt;&gt;&gt; 2)</code>）。</p><h4 id="多线程扩容"><a href="#多线程扩容" class="headerlink" title="多线程扩容"></a>多线程扩容</h4><p>当ConcurrentHashMap并发地添加元素时，如果发现map正在扩容（当前hash 值对应的槽位有值了，且如果这个值是 -1:MOVED），其他线程还会帮助其扩容，以加快速度，这就是1.8添加的多线程扩容。这部分就是上面调用的helpTransfer方法，主要是在扩容时将table表中的结点转移到nextTable中。</p><pre><code class="lang-java">final Node&lt;K,V&gt;[] helpTransfer(Node&lt;K,V&gt;[] tab, Node&lt;K,V&gt; f) &#123;    Node&lt;K,V&gt;[] nextTab; int sc;    // 表不为空 &amp;&amp; 结点类型使ForwardingNode转移类型 &amp;&amp; 结点的nextTable不为空    if (tab != null &amp;&amp; (f instanceof ForwardingNode) &amp;&amp;        (nextTab = ((ForwardingNode&lt;K,V&gt;)f).nextTable) != null) &#123;        int rs = resizeStamp(tab.length);        // 如果nextTab没有被并发修改且tab也没有被并发修改且sizeCtl&lt;0（说明还在扩容）        while (nextTab == nextTable &amp;&amp; table == tab &amp;&amp;               (sc = sizeCtl) &lt; 0) &#123;            // 扩容结束的条件            // 如果 sizeCtl 无符号右移  16 不等于 rs （ sc前 16 位如果不等于标识符，则标识符变化了）            // 或者 sizeCtl == rs + 1  （扩容结束了，不再有线程进行扩容）（默认第一个线程设置 sc ==rs 左移 16 位 + 2，当第一个线程结束扩容了，就会将 sc 减一。这个时候，sc 就等于 rs + 1）            // 或者 sizeCtl == rs + 65535  （如果达到最大帮助线程的数量，即 65535）            // 或者转移下标正在调整 （扩容结束）            if ((sc &gt;&gt;&gt; RESIZE_STAMP_SHIFT) != rs || sc == rs + 1 ||                sc == rs + MAX_RESIZERS || transferIndex &lt;= 0)                break;            // 每有一个线程来帮助迁移，sizeCtl就+1            if (U.compareAndSwapInt(this, SIZECTL, sc, sc + 1)) &#123;                //转移                transfer(tab, nextTab);                break;            &#125;        &#125;        return nextTab;    &#125;    return table;&#125;</code></pre><h4 id="更新count"><a href="#更新count" class="headerlink" title="更新count"></a>更新count</h4><p>在put完成的最后，表示添加了一个Node操作完成了，这时候需要调用addCount来更新baseCount。该方法主要作用：</p><ol><li>就是CAS增加baseCount的值，增加值为x。<ol><li>如果更新失败，或者counterCells为null会调用fullAddCount方法进行循环更新。</li></ol></li><li>判断当前bin桶是否需要扩容，判断依据check值。</li></ol><p>详细分析注释如下：</p><pre><code class="lang-java">private final void addCount(long x /*需要增加的数*/, int check /*一个bin下node的个数*/) &#123;        CounterCell[] as; long b, s;        // 如果计数盒子counterCell不是空 ||  CAS更新baseCount的值失败        if ((as = counterCells) != null ||            !U.compareAndSwapLong(this, BASECOUNT, b = baseCount, s = b + x)) &#123;            CounterCell a; long v; int m;            boolean uncontended = true;            // 如果counterCells是空，表示尚未出现并发            // || 如果counterCells数组上随机取余一个位置为空            // || 修改这个cell的变量失败，代表出现并发            // 就调用fullAddCount 来将将数字加到counterCells上去。            if (as == null || (m = as.length - 1) &lt; 0 ||                (a = as[ThreadLocalRandom.getProbe() &amp; m]) == null ||                !(uncontended =                  U.compareAndSwapLong(a, CELLVALUE, v = a.value, v + x))) &#123;                //fullAddCount方法的作用就是去更新cell的value值。来保证元素个数正确的更新                fullAddCount(x, uncontended);                return;            &#125;            // 如果check还小于1的话，就这了退出            if (check &lt;= 1)                return;            s = sumCount();        &#125;        //如果check值大于等于0，则需要检验是否需要进行扩容操作        if (check &gt;= 0) &#123;            Node&lt;K,V&gt;[] tab, nt; int n, sc;            while (s &gt;= (long)(sc = sizeCtl) &amp;&amp; (tab = table) != null &amp;&amp;                   (n = tab.length) &lt; MAXIMUM_CAPACITY) &#123;                int rs = resizeStamp(n);                if (sc &lt; 0) &#123;                    if ((sc &gt;&gt;&gt; RESIZE_STAMP_SHIFT) != rs || sc == rs + 1 ||                        sc == rs + MAX_RESIZERS || (nt = nextTable) == null ||                        transferIndex &lt;= 0)                        break;                    //是否已经有其他线程在执行扩容操作                    if (U.compareAndSwapInt(this, SIZECTL, sc, sc + 1))                        transfer(tab, nt);                &#125;                //当前线程是唯一的或是第一个发起扩容的线程  此时nextTable=null                else if (U.compareAndSwapInt(this, SIZECTL, sc,                                             (rs &lt;&lt; RESIZE_STAMP_SHIFT) + 2))                    transfer(tab, null);                s = sumCount();            &#125;        &#125;    &#125;</code></pre><p>put这个流程到这里就先结束了，这个流程中还有一些比较重要点先不扯到这个流程里面了，后面会单独分析。</p><h3 id="扩容操作"><a href="#扩容操作" class="headerlink" title="扩容操作"></a>扩容操作</h3><p>我们在put的最后一步addCount的时候，会去check一下是否需要进行扩容操作。其调用的就是transfer方法。该扩容有2种情况：</p><ol><li>sizeCtl&lt;0，即有别的线程正在进行扩容，此时当前这个线程需要参与帮助扩容的任务中来；</li><li>该线程是唯一的扩容线程。</li></ol><p>扩容代码应该算是ConcurrentHashMap里面最复杂的部分了，扩容主要是2个步骤，首先创建扩容后的2倍数组，然后将老table的数据移动到新的table上去。</p><p><code>transfer</code>方法很复杂，这里先总结下大概步骤：</p><ul><li>通过当期机器CPU核心数及Hash表长度得到每个线程（CPU）要帮助处理多少个桶，确定每个线程可以处理的数量</li><li>如果发现扩容后的数据没有初始化就初始化</li><li>开始进入循环，多线程扩容的逻辑主要都在这里面<ul><li>先计算i和bound，即分配一个线程处理Hash表的区间范围</li><li>里面还有许多的判断逻辑用来处理各种情况，比如该桶其他线程已经处理过了等。</li></ul></li><li>链表扩容节点转移<ul><li>为了加速扩容，将链表节点分为2个部分，链表尾部连续相同的节点我们可以直接拽过来，然后再从头开始一直到刚在拽的那个点，遍历链表，头插的方式分为2个链表。</li><li>然后将2个链表存储到新table的新位置上去。</li></ul></li><li>树扩容节点转移</li></ul><p>详细代码注释如下：</p><pre><code class="lang-java">private final void transfer(Node&lt;K,V&gt;[] tab, Node&lt;K,V&gt;[] nextTab) &#123;    int n = tab.length, stride;    // 这里主要意思是单个线程允许处理的最少table桶数量不能小于16，如果小于16就设置为16    if ((stride = (NCPU &gt; 1) ? (n &gt;&gt;&gt; 3) / NCPU : n) &lt; MIN_TRANSFER_STRIDE/*=16*/)        stride = MIN_TRANSFER_STRIDE; // subdivide range    // 如果是首次扩容，需要初始化扩容后的数组    if (nextTab == null) &#123;            // initiating        try &#123;            // 扩容2倍            @SuppressWarnings(&quot;unchecked&quot;)            Node&lt;K,V&gt;[] nt = (Node&lt;K,V&gt;[])new Node&lt;?,?&gt;[n &lt;&lt; 1];            nextTab = nt;        &#125; catch (Throwable ex) &#123;      // try to cope with OOME            sizeCtl = Integer.MAX_VALUE;            return;        &#125;        nextTable = nextTab;        // 指向最后一个桶，方便从后向前遍历        transferIndex = n;    &#125;    int nextn = nextTab.length;    //定义ForwardingNode来标记迁移完成的桶    ForwardingNode&lt;K,V&gt; fwd = new ForwardingNode&lt;K,V&gt;(nextTab);    boolean advance = true; //是否继续向前查找下一个桶    boolean finishing = false; //控制扩容何时结束，以及完成前重新扫描下数组，查看有没有完成的    // 该for循环用于处理一个 stride 长度的任务    // i后面会被赋值为该 stride 内最大的下标，而 bound 后面会被赋值为该 stride 内最小的下标    for (int i = 0, bound = 0;;) &#123;        Node&lt;K,V&gt; f; int fh;       // 如果当前线程可以向后推进，主要是控制i递减。且每个线程都会进入这里取得自己需处理的桶的区间       // 从table最后一个位置开始，主要就是为了计算当前线程需要处理的桶区间        while (advance) &#123;            int nextIndex, nextBound;            if (--i &gt;= bound || finishing)                advance = false;            else if ((nextIndex = transferIndex) &lt;= 0) &#123;                i = -1;                advance = false;            &#125;            else if (U.compareAndSwapInt                     (this, TRANSFERINDEX, nextIndex,                      nextBound = (nextIndex &gt; stride ?                                   nextIndex - stride : 0))) &#123;                bound = nextBound;                i = nextIndex - 1;                advance = false;            &#125;        &#125;        if (i &lt; 0 || i &gt;= n || i + n &gt;= nextn) &#123;            //当前线程所有任务完成            int sc;            //扩容结束后做后续工作，将 nextTable 设置为 null，表示扩容已结束，将 table 指向新数组，sizeCtl 设置为扩容阈值            if (finishing) &#123;                nextTable = null;                table = nextTab;                sizeCtl = (n &lt;&lt; 1) - (n &gt;&gt;&gt; 1);                return;            &#125;            //每当一条线程扩容结束就会更新一次 sizeCtl 的值，进行减 1 操作            if (U.compareAndSwapInt(this, SIZECTL, sc = sizeCtl, sc - 1)) &#123;                if ((sc - 2) != resizeStamp(n) &lt;&lt; RESIZE_STAMP_SHIFT)                    return;                finishing = advance = true;                //除了修改结束标识之外，还得设置 i = n; 以便重新检查一遍数组，防止有遗漏未成功迁移的桶                i = n; // recheck before commit            &#125;        &#125;        else if ((f = tabAt(tab, i)) == null)            // 如果当前槽没有Node，CAS插入fwd表示该槽已经处理过了            advance = casTabAt(tab, i, null, fwd);        else if ((fh = f.hash) == MOVED)            // 遇到了别的线程已经处理过的线程，直接跳过            advance = true; // already processed        else &#123;            // 移动节点的操作            synchronized (f) &#123;                if (tabAt(tab, i) == f) &#123;                    //ln 新数组原来位置上的节点list                    //hn 新数组原来位置+n的的节点list                    Node&lt;K,V&gt; ln, hn;                    // 对链表进行迁移                    if (fh &gt;= 0) &#123;                        // 这里我迷了一阵子，这里不是计算原来的table中的位置（应该是fh&amp;(n-1)）                        // 这里和数组长度来&amp;，计算出的结果就2个：0和非0，因为数组的长度是2的次幂，即判断依据就是hash值在那个位上的bit是0还是1。                        // 保留最后一个连续相同链的首节点的hash&amp;n值                        int runBit = fh &amp; n;                        // 保留最后一个连续相同链的首节点                        Node&lt;K,V&gt; lastRun = f;                        /* 第一次遍历整个链表                         * 这里是很有意思的一个地方，解释了runBit和lastRun的作用：                         * 这里把一个链表分为了2种：1个是需要移动的，一个是还会在原来位置的                         * 这个循环就是找出该链表最后一个位置，在该位置（lastRun）往后所有的节点，都和该位置节点hash &amp; n值一样                         * */                        for (Node&lt;K,V&gt; p = f.next; p != null; p = p.next) &#123;                            int b = p.hash &amp; n;                            if (b != runBit) &#123;                                // 每次不一样就重新设置一下                                runBit = b;                                lastRun = p;                            &#125;                        &#125;                        // 如果runBit为0，将lastRun 设置为ln的首节点，表示这往后的链表不需要移动                        if (runBit == 0) &#123;                            ln = lastRun;                            hn = null;                        &#125;                        // 不为0，说明要移动到+n位置，先放入hn                        else &#123;                            hn = lastRun;                            ln = null;                        &#125;                        // 第二次遍历链表，只遍历到lastRun位置                        for (Node&lt;K,V&gt; p = f; p != lastRun; p = p.next) &#123;                            int ph = p.hash; K pk = p.key; V pv = p.val;                            if ((ph &amp; n) == 0)                                // 采用的是头插法，也就是说遍历这个链表的时候，这些头插的元素都是反转的。                                ln = new Node&lt;K,V&gt;(ph, pk, pv, ln);                            else                                hn = new Node&lt;K,V&gt;(ph, pk, pv, hn);                        &#125;                        //ln设置新数组原来位置                        setTabAt(nextTab, i, ln);                        //hn设置新数组+n位置                        setTabAt(nextTab, i + n, hn);                        //原来表该位置都设置为fwd                        setTabAt(tab, i, fwd);                        advance = true;                    &#125;                    else if (f instanceof TreeBin) &#123;                        // 对红黑树进行迁移，这里就不多交代了                        // ........                    &#125;                &#125;            &#125;        &#125;    &#125;&#125;</code></pre><h3 id="size方法"><a href="#size方法" class="headerlink" title="size方法"></a>size方法</h3><p>ConcurrentHashMap中针对并发通过baseCount和CounterCell来计算获取size的大小，在<code>addCount()</code> CAS 更新 baseCount的时候，可能因为并发问题更新失败，会调用 fullAddCount 将这些失败的结点包装成一个 CounterCell 对象，保存在 CounterCell 数组中。那么整张表实际的 size 其实是 baseCount 加上 CounterCell 数组中元素的个数。</p><pre><code class="lang-java">public int size() &#123;    long n = sumCount();    return ((n &lt; 0L) ? 0 :            (n &gt; (long)Integer.MAX_VALUE) ? Integer.MAX_VALUE :            (int)n);&#125;final long sumCount() &#123;    CounterCell[] as = counterCells; CounterCell a;    long sum = baseCount;    if (as != null) &#123;        for (int i = 0; i &lt; as.length; ++i) &#123;            if ((a = as[i]) != null)                sum += a.value;        &#125;    &#125;    return sum;&#125;</code></pre><h3 id="remove函数-1"><a href="#remove函数-1" class="headerlink" title="remove函数"></a>remove函数</h3><p>remove比较简单，简单说下流程：</p><ol><li>如果表还未初始化或者通过key无法hash定位表的位置则返回null。</li><li>如果该位置桶结点类型是ForwardingNode节点，则执行helpTransfer 协助扩容。</li><li>如果一切ok，就给桶加锁，并删除一个节点。</li><li>最后调用addCount 方法去CAS更新baseCount 的值。</li></ol><h3 id="get函数-1"><a href="#get函数-1" class="headerlink" title="get函数"></a>get函数</h3><p>很简单，直接上代码</p><pre><code class="lang-java">public V get(Object key) &#123;    Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; e, p; int n, eh; K ek;    // 确定key的数组位置    int h = spread(key.hashCode());    if ((tab = table) != null &amp;&amp; (n = tab.length) &gt; 0 &amp;&amp;        (e = tabAt(tab, (n - 1) &amp; h)) != null) &#123;        if ((eh = e.hash) == h) &#123;            if ((ek = e.key) == key || (ek != null &amp;&amp; key.equals(ek)))                return e.val;        &#125;        else if (eh &lt; 0)            return (p = e.find(h, key)) != null ? p.val : null;        // 遍历该位置的所有节点        while ((e = e.next) != null) &#123;            if (e.hash == h &amp;&amp;                ((ek = e.key) == key || (ek != null &amp;&amp; key.equals(ek))))                return e.val;        &#125;    &#125;    //找不到就返回null    return null;&#125;</code></pre><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><h2 id="1-7和1-8的区别"><a href="#1-7和1-8的区别" class="headerlink" title="1.7和1.8的区别"></a>1.7和1.8的区别</h2><h3 id="结构上"><a href="#结构上" class="headerlink" title="结构上"></a>结构上</h3><p>两者都大量使用Unsafe的方法进行元素的操作。并发控制上：</p><ul><li>1.7：Segment + HashEntry，通过Segment的分段锁来获取Segment粒度的并发。</li><li>1.8：Bin+CAS+Synchronize，通过CAS+Synchronize将粒度缩减到Bin（Hash槽）级别。</li><li>为什么要使用CAS+Synchronized取代Segment+ReentrantLock？<ul><li>减少内存资源消耗：表如何很大的话，就会有很多ReentrantLock对象</li><li>synchronized底层做了优化，性能不差</li><li>锁的粒度可以控制的更细</li></ul></li></ul><p>初始状态：</p><ul><li>1.7：有初始化一个Segment和hash表</li><li>1.8：采用懒加载，第一次put的时候才会初始化hash表。</li></ul><p>存储结构：</p><ul><li>1.7：数组+链表</li><li>1.8：数组+链表、红黑树</li></ul><h3 id="size上"><a href="#size上" class="headerlink" title="size上"></a>size上</h3><ul><li>1.7：不加锁计算2次，对比是否相同，如果不同则加锁来统计总数。</li><li>1.8：使用baseCounter+CellCounter来计数。</li></ul><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><p><a href="https://blog.csdn.net/ZOKEKAI/article/details/90051567">https://blog.csdn.net/ZOKEKAI/article/details/90051567</a></p><p><a href="https://tech.meituan.com/2019/02/14/talk-about-java-magic-class-unsafe.html">https://tech.meituan.com/2019/02/14/talk-about-java-magic-class-unsafe.html</a></p><p><a href="https://www.cnblogs.com/banjinbaijiu/p/9147434.html">https://www.cnblogs.com/banjinbaijiu/p/9147434.html</a></p>]]></content>
    
    
    <summary type="html">&lt;p&gt;ConcurrentHashMap源码分析&lt;/p&gt;</summary>
    
    
    
    
  </entry>
  
  <entry>
    <title>什么是SaaS、PaaS和IaaS</title>
    <link href="http://lollipops.work/2020/05/10/%E5%85%B6%E4%BB%96-%E5%90%84%E7%A7%8D_aas/"/>
    <id>http://lollipops.work/2020/05/10/%E5%85%B6%E4%BB%96-%E5%90%84%E7%A7%8D_aas/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2020-08-14T09:51:03.462Z</updated>
    
    <content type="html"><![CDATA[<p>刚进公司领导让我参与构建公司的paas，只知道这是个云计算领域的词汇，具体啥含义完全没概念，所以抽空了解下。</p><hr><a id="more"></a><blockquote><p>IaaS就是台服务器。<br>PaaS就是Tomcat加MySQL。<br>SaaS就是三千块一套的加个Logo就能开业的电商网站。</p></blockquote><p>上面是转自<a href="https://www.zhihu.com/question/21641778/answer/62523535">知乎</a>的三句话，个人感觉比较好就直接引用了。</p><h1 id="云"><a href="#云" class="headerlink" title="云"></a>云</h1><p>​        自从06年Google提出云计算的概念之后，这个概念便一直火热着。现在以及可预见的将来，可以说cloud将在会渗透到我们生活的方方面面。现在不仅云计算，云服务，云存储，云安全……一堆概念应“云“而生！对于目前的云计算来说，可以分为这么几层：基础设施层(infrastructure)、平台层(platform)和软件层(software)，也即对应着所谓的iaas，paas，saas。云计算很多概念个人看的都比较虚，毕竟没有实操体验。所以只简要了解这三层的概念。</p><h1 id="本地部署"><a href="#本地部署" class="headerlink" title="本地部署"></a>本地部署</h1><p>​        先来说说本地部署，他不是云计算概念的一部分，他属于传统的软件硬件体系架构。在过去（也就5，6年前），你想要做一个网站什么的，项目的编码，服务器等都是需要自己去采购完成的。你需要自己去买服务器硬件资源，服务器的操作系统，可能收费的服务器软件，最后才是自己的软件代码。这个缺点就是对于中小用户来说成本高昂，不论是资金成本还是人力维护成本。如果再遇上个什么创业倒闭的那就砸手里了，大都贱卖了。</p><h1 id="IAAS"><a href="#IAAS" class="headerlink" title="IAAS"></a>IAAS</h1><p>​        基础设施即服务 <strong>(Infrastructure-as-a-Service)</strong>。</p><p>​        IaaS层主要是指用户无需购置存储、网络设备等，云计算典型支持产品有云主机，使用者较多为运维/研发等。总结起来就是，由一些云厂商给我提供基础硬件作为服务，比如你想要GPU做你的深度学习训练，可以随时租用一段时间，而不一定要买个全套。或者你要硬盘存储什么东西等等。这些服务只限于你要执行程序的一些基础设施，比如给你分配一台主机或者直接是一台虚拟机。硬件和操作系统平台都是由云厂商提供的。</p><p>​        提供该服务的厂商有：Amazon, Microsoft, VMWare, Alibaba等，这些云厂商基本都提供了该服务，就相当于租台服务器。所以大部分云厂商其实都是IAAS为主要服务的吧。</p><h1 id="PAAS"><a href="#PAAS" class="headerlink" title="PAAS"></a>PAAS</h1><p>​        平台即服务 <strong>(Platform-as-a-Service)</strong>。</p><p>​        PaaS层服务商主要提供SDK、API，他们不必自己购买专用的开发软件、测试软件、服务器等，实现软件的轻松快速发布，使用者为软件开发商。这个其实就是在IAAS上做平台了，很多公司都会有这样的功能：它向外开发了一些公共接口。用户可以通过该厂商提供的SDK来使用该平台能提供的一些服务。总结来说就是你写了一个服务，别人通过你提供的SDK或者什么的可以来使用你这个服务提供的功能。</p><p>​        这种服务典型的厂商有：七牛云，Google App Engine，阿里的EDAS等。就比方说你用七牛云的sdk，然后将文件保存到七牛云上去。</p><h1 id="SAAS"><a href="#SAAS" class="headerlink" title="SAAS"></a>SAAS</h1><p>​        软件即服务<strong>(Software-as-a-Service)</strong>。</p><p>​        SAAS就更简单粗暴了，一切都由服务商给你提供好了。你只要直接使用，或者将其定制化你想要的样子就可以了。</p><p>​        这种厂商现在也很多：dropbox，百度云，Elastic云等等。他们都是整包成一个软件+服务+基础设置整体，对用户来说就知道这是个软件，而不在乎里面底层细节怎么实现的，所以叫软件即服务。</p><p>​        其实我的感觉，我们所有的互联网产品，那都是一个SAAS产品！无非有两个阵营：一个2C，一个2B。2B的就是正常网站啊，APP给用户用的，用户就相当于使用APP的服务不是吗？2C可能就更接近于云厂商提供的那些牛逼服务了。网上有很多卖软件的，什么课程管理系统，图书管理系统，这些业务逻辑相似，只要有对应源码，依据客户要求换个log，UI啥的，往云上一部署，那么我就是个saas不是吗？当然这只是个人粗略的见解。</p><p><strong>网上有个参考图：</strong>    </p><p>IaaS-&gt;Paas-&gt;SaaS 越往后整个软件服务的集成度越高。对于使用者所要求的技术也越来越低。SAAS更是拿来即用，不需要懂技术，只要像学如何使用word一样知道点哪里就行了。            <img src=":others-various-Xaas/iaas-paas-saas.jpg" alt="iaas-paas-saas"></p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>除了上面三个基本的服务分层，还有很多由此衍生的一些服务，比如CAAS（Communications-as-a-Service），MaaS（Machine as a Service）…以及未来又可能出现的?aaS等等。</p><p>举个例子：</p><ul><li>小明在阿里云上租了个服务器，这个服务器除了一个基本的操作系统环境什么都没有。那么阿里云这时候提供给小明的服务就是IAAS。</li><li>小明突然要使用上传文件功能，这时候去买了阿里云的OSS服务来做对象存储，这样就可以直接调用OSS的SDK就直接把文件存起来了，不用自己写IO去巴拉巴拉的读写文件来传输了。这时候阿里云的OSS服务就是PAAS的。</li><li>小明写代码写了没两天，就不想干了，想看网上有没有可以直接用的模板去套用下，然后发现阿里云的一款产品A，刚好就是他想要的那种功能。然后他就直接买了A产品。然后把A换了个颜色，换个log，设置下布局（这些都是A产品提供的自选功能）。然后他就把A当做自己的东西对外开放了。那么这个产品A就是SAAS的。</li></ul>]]></content>
    
    
    <summary type="html">&lt;p&gt;刚进公司领导让我参与构建公司的paas，只知道这是个云计算领域的词汇，具体啥含义完全没概念，所以抽空了解下。&lt;/p&gt;
&lt;hr&gt;</summary>
    
    
    
    
    <category term="云计算" scheme="http://lollipops.work/tags/%E4%BA%91%E8%AE%A1%E7%AE%97/"/>
    
  </entry>
  
  <entry>
    <title>【设计模式】中介者模式</title>
    <link href="http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E4%B8%AD%E4%BB%8B%E8%80%85%E6%A8%A1%E5%BC%8F/"/>
    <id>http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E4%B8%AD%E4%BB%8B%E8%80%85%E6%A8%A1%E5%BC%8F/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2021-01-16T09:06:15.830Z</updated>
    
    <content type="html"><![CDATA[<h1 id="【设计模式】中介者模式"><a href="#【设计模式】中介者模式" class="headerlink" title="【设计模式】中介者模式"></a>【设计模式】中介者模式</h1><p>本文主要介绍中介者模式的概念和用法。</p><h2 id="模式背景"><a href="#模式背景" class="headerlink" title="模式背景"></a>模式背景</h2><p>实际开发中，我们存在很多广播性质的需求。比如说当我们发送一个消息，希望很多人同时收到，所以有了QQ群这个功能。有了QQ群，我们就不需要通过自己一个个去发送消息，也就是说这个QQ群帮我和其他的用户进行了解耦。类似于QQ群这样一个引入中间者思想的模式就是中介者模式。</p><p>如果不使用中介者模式的话，所有相关的人员全部都耦合在一起的，这种大量的耦合势必有很多缺点。比如：</p><ol><li>很难复用，消息的传播都在消息发送方，如果一个用户也想发送给这个群组，那就又要写一套一样的逻辑代码。</li><li>扩展性差！添加一个成员什么的，就需要修改源代码，违反开闭原则。</li></ol><h2 id="定义-amp-概念"><a href="#定义-amp-概念" class="headerlink" title="定义&amp;概念"></a>定义&amp;概念</h2><p><strong>中介者模式</strong>：使用一个中介对象来封装一系列对象之间的交互，使得各个对象间不需要显式的互相引用，从而减低耦合度。中介者模式又叫调停者模式，是一种对象行为型模式。</p><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>系统中有大量的多对多等相互关系，这些关系让系统的类或者组件之间的耦合度就很高，这时候就<strong>运用迪米特法则，引入一个中间的类</strong>，来维系这些关系，大大降低系统的耦合度。这个引入的中间类就是中介者。</p><p>引入了中介者之后，可以让系统中对象的关系由网状结构转为星型结构，如下图所示。系统中的对象交互都通过中介者对象来进行调度。</p><p><img src="/Users/zhaohaoren/workspace/mycode/blog-docs/docs/设计模式/Mediator2.png" alt="image-20200828105920238"></p><p><strong>中介者模式的核心就是这个中介类</strong>，他的主要职责：</p><ol><li>中转作用：调用先发送给中介者，中介者转发给需要的接受者。</li><li>协调作用：中介者可以有自己的内部逻辑，将这些逻辑写在中介者类中，也就是说中介者可以辅助调用者做一些操作。</li></ol><p><strong>组成要素</strong></p><ul><li><strong>抽象中介者：</strong>一个接口，用于各个同事独享之间进行通信。<ul><li>这是<strong>核心</strong>，里面有2个核心的部分：<ul><li><strong>内部持有一个list用来存储转发的对象</strong></li><li><strong>提供一个注册方法，可以动态的添加成员。</strong></li></ul></li></ul></li><li>具体中介者：抽象中介的实现，维持各个同事对象的关系和引用的实现。</li><li>抽象同事类：各个同事类的公有方法，申明和中介者通信的抽象方法。<ul><li><strong>内部持有一个中介者引用</strong>，我们还是希望让同事自己能发起调用，相当于将之前其他对象耦合的逻辑抽取到中介者中。</li></ul></li><li>具体同事类：是抽象同事类的子类。每个同事对象需要通信的时候先和中介者通信，间接来和其他同事类通信。</li></ul><h2 id="UML"><a href="#UML" class="headerlink" title="UML"></a>UML</h2><p><img src="/Users/zhaohaoren/workspace/mycode/blog-docs/docs/设计模式/Mediator.png" alt="image-20200828103614979"></p><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><p>抽象中介者</p><pre><code class="lang-java">public abstract class Mediator &#123;    /**     * 维系着同事对象的引用     */    protected ArrayList&lt;Colleague&gt; colleagues = new ArrayList&lt;&gt;();    /**     * 往中介者中添加同事对象     */    public void register(Colleague colleague) &#123;        colleagues.add(colleague);    &#125;    /**     * 中介的关系逻辑     */    public abstract void operation(Colleague colleague);&#125;</code></pre><p>具体中介者</p><pre><code class="lang-java">public class ConcreteMediator extends Mediator &#123;    @Override    public void operation(Colleague colleague) &#123;        //这里将注册的同事类的关系联系起来        //这里只是瞎写了一个逻辑。        if (colleague == colleagues.get(0)) &#123;            colleague.op();            colleagues.get(1).op();        &#125; else &#123;            colleagues.get(1).op();            colleague.op();        &#125;    &#125;&#125;</code></pre><p>抽象同事类</p><pre><code class="lang-java">public abstract class Colleague &#123;    protected Mediator mediator;    public Colleague(Mediator mediator) &#123;        this.mediator = mediator;    &#125;    /**     * 同事自己的方法，一般也是其他同事所耦合的向要调用的方法。     */    public abstract void op();    /**     * 自己定义依赖方法，与中介者进行通信     */    public void send() &#123;        mediator.operation(this);    &#125;&#125;</code></pre><p>具体同事类</p><pre><code class="lang-java">public class ColleagueX extends Colleague&#123;    public Colleague1(Mediator mediator) &#123;        super(mediator);    &#125;    @Override    public void op() &#123;        System.out.println(&quot;i am X&quot;);    &#125;&#125;</code></pre><p>使用</p><pre><code class="lang-java">//定义中介者ConcreteMediator concreteMediator = new ConcreteMediator();//定义同事Colleague1 colleague1 = new Colleague1(concreteMediator);Colleague2 colleague2 = new Colleague2(concreteMediator);//同事注册到中介者上concreteMediator.register(colleague1);concreteMediator.register(colleague2);//触发改动colleague1.send();colleague2.send();</code></pre><h2 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h2><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul><li>降低了类的复杂度，将多对多转化成了一对多，更方便理解类对象之间的关系。 </li><li>对相互交互的类进行了解耦。 </li><li>可复用，一个中介者可以说是一个群组，这个群组可以被多个</li><li>符合迪米特原则。</li></ul><h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul><li>如果各个同事间的交互非常多并且复杂情况下，中介者可能会变得十分复杂，不易维护和管理。</li></ul><h2 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h2><p>系统中对象之间存在复杂的引用关系，让系统看着很复杂，可以使用中介者来清理交互的脉络。或者就如需要实现QQ群这样的广播功能，引入中介者降低耦合。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>中介者其实也是我之前提过的，基于代理的思想，只是他们服务的具体细节目标有所不同（但是他们本质都还是代理嘛）。中介者模式多用来处理多对多这种关系复杂的系统，为了让这种关系松耦合才使用的。我们需要一个中介类，因为中介类需要管理一系列的同事类，所以同事类也需要有统一抽象。所以整个中介者模式就是围绕中介类抽象，同事类抽象进行的延伸。</p><h1 id="附"><a href="#附" class="headerlink" title="附"></a>附</h1><p>相关代码：<a href="https://github.com/zhaohaoren/design_pattern_tutorial">https://github.com/zhaohaoren/design_pattern_tutorial</a></p><p>如有代码和文章问题，还请指正！感谢！</p>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;【设计模式】中介者模式&quot;&gt;&lt;a href=&quot;#【设计模式】中介者模式&quot; class=&quot;headerlink&quot; title=&quot;【设计模式】中介者模式&quot;&gt;&lt;/a&gt;【设计模式】中介者模式&lt;/h1&gt;&lt;p&gt;本文主要介绍中介者模式的概念和用法。&lt;/p&gt;
&lt;h2</summary>
        
      
    
    
    
    
    <category term="设计模式" scheme="http://lollipops.work/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>【设计模式】享元模式</title>
    <link href="http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E4%BA%AB%E5%85%83%E6%A8%A1%E5%BC%8F/"/>
    <id>http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E4%BA%AB%E5%85%83%E6%A8%A1%E5%BC%8F/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2021-01-16T09:07:43.999Z</updated>
    
    <content type="html"><![CDATA[<h1 id="【设计模式】享元模式"><a href="#【设计模式】享元模式" class="headerlink" title="【设计模式】享元模式"></a>【设计模式】享元模式</h1><p>本文主要介绍享元模式概念和用法，以及有状态享元模式，复合享元模式。</p><h2 id="模式背景"><a href="#模式背景" class="headerlink" title="模式背景"></a>模式背景</h2><p>享元模式算是一个比较复杂的模式。在一些系统中，可能会需要大量的对象。比如游戏中的小兵，围棋系统中的黑子白子，如果我们每需要一个这样的对象的时候都去new一个，那么系统会创建大量的对象。这会加重系统的负担，如果是Java的话还会触发大量的GC。<strong>所以就如何优化设计，避免系统中去创建大量相同或者相似的对象就是享元模式的目标。</strong></p><p>一个最好的例子就是：在一个人很多城市，每个人都需要学习自行车。如果人手都配一个自行车，那么成本得多大。但是有了<strong>共享单车</strong>就不一样了。我们只需要少量的共享单车就能让所有人都能使用到自行车。这个共享单车就是享元模式的思想。</p><h2 id="定义-amp-概念"><a href="#定义-amp-概念" class="headerlink" title="定义&amp;概念"></a>定义&amp;概念</h2><p>运用共享技术有效地支持大量细粒度对象的复用。系统只使用少量的对象，而这些对象都很相似，状态变化很小，可以实现对象的多次复用。由于 <strong>享元模式要求能够共享的对象必须是细粒度对象，因此它又称为轻量级模式，它是一种对象结构型模式。</strong></p><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>享元模式的享元我理解为：<strong>共享元数据的元</strong>。什么是元数据？元数据描述数据属性信息的数据叫元数据，那元就是数据的属性。也就是对所有的数据来说，这个元都一样的（比如文件都有大小，类型，文件名这些属性）。</p><p>享元模式也是将系统中这些大量相似或者相同的对象中状态不变的部分给抽取出来，让状态变化的部分都共享这部分。所以享元模式能做到<strong>可以共享的关键</strong>是区分出该对象的<strong>内部状态</strong>和<strong>外部状态</strong>。</p><p><strong>内部状态（不变）</strong>：那些不会因为环境改变而改变的。比如说描述一段文字，文字内容ABC就是ABC。</p><p><strong>外部状态（变）</strong>：那会随着环境改变而跟着改变的那些无法共享的状态。比如ABC文字是宋体，黑体等。</p><p><strong>我们用享元模式将不变的内部状态创建单例或者少量。让变化的无法共享的外部状态通过传参等形式传递给共享对象。</strong>这也是为什么能共享的对象必须是细粒度的，一般一堆对象中，那些相同的部分都比较小，这些部分能提出来做成共享对象就提出来做。</p><p>享元模式结构比较复杂，一般还需要结合工厂，单例模式使用。</p><p><strong>组成要素</strong></p><ul><li><p>抽象享元类（Flyweight）</p><ul><li>一个接口或者抽象类，申明公共方法，提供那些不变的内部状态，已经设置外部状态的方法。</li></ul></li><li><p>具体享元类（ConcreteFlyweight）</p><ul><li>就是那些需要被共享的，状态不变属性和方法构建的对象。通常<strong>使用单例模式来设计具体享元类。</strong></li></ul></li><li><p>非共享具体享元类（UnsharedConcreteFlyweight）</p><ul><li>有些享元抽象类的子类我们并不需要共享，这些类就直接走new的。</li></ul></li><li><p>享元工厂类（FlyweightFactory）</p><ul><li><strong>创建所有享元对象的工厂</strong>，内部有个<strong>缓存池</strong>，缓存了共享的享元对象。当需要对象的时候先从池中取，取不到再创建。其主要作用是创建充当池。</li><li>就是2个作用：创建享元对象和做享元池缓存。</li></ul></li><li><p>外部状态类（非必须，如果享元对象有外部状态时候才需要，如果只是共享就不需要）</p><ul><li>那些无法被共享的状态，这些对象代表着享元对象变化的状态，需要传给享元对象使用。</li></ul></li></ul><p>依据享元对象定制化需求，享元模式分为两种：</p><ul><li><p><strong>单纯享元模式</strong>：所有的具体享元类都是可以共享的，不存在非共享的具体享元类。</p></li><li><p><strong>复合享元模式</strong>：有时候我们希望享元池中的多个享元对象，都设置同时的外部状态，此时可以使用组合模式将这些享元对象组合起来。这样让这些享元对象同时拥有相同的外部状态。</p></li></ul><h2 id="UML"><a href="#UML" class="headerlink" title="UML"></a>UML</h2><p>这是一个带状态的享元模式：</p><p><img src="/Users/zhaohaoren/workspace/mycode/blog-docs/docs/设计模式/Flyweight.png" alt="image-20200821171242170"></p><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><p>抽象享元类</p><pre><code class="lang-java">public interface Flyweight &#123;    /**内部变化*/    void printIntrinsicState();    /**@param extrinsicState 外部变化 让外部来控制*/    void op(String extrinsicState);&#125;</code></pre><p>具体享元类</p><pre><code class="lang-java">public class ConcreteFlyweight1 implements Flyweight &#123;    /** 这个内部状态 就是该对象能被享元的分到的最小细粒度了。    在这种状态下，系统需要多少个对象就要创建多少这样的享元类。*/    @Override    public void printIntrinsicState() &#123;        System.out.println(&quot;intrinsicState1&quot;);    &#125;    /** 显示外部变化*/    @Override    public void op(String extrinsicState) &#123;        printIntrinsicState();        System.out.println(extrinsicState);    &#125;&#125;</code></pre><p>非共享具体享元类</p><pre><code class="lang-java">public class UnsharedConcreteFlyweight implements Flyweight &#123;    /**不是享元内部的那些状态。*/    private String otherStates;    @Override    public void printIntrinsicState() &#123;        System.out.println(&quot;intrinsicState&quot;);    &#125;    @Override    public void op(String extrinsicState) &#123;        //输出内部状态        printIntrinsicState();        //输出其他的状态        System.out.println(otherStates);        //输出外部控制的状态        System.out.println(extrinsicState);    &#125;&#125;</code></pre><p>享元工厂类</p><pre><code class="lang-java">public class FlyweightFactory &#123;    //单例    private static FlyweightFactory instance = new FlyweightFactory();    //缓存池    private HashMap&lt;String, Flyweight&gt; flyweights = new HashMap&lt;&gt;();    //存入共享享元对象    public FlyweightFactory() &#123;        Flyweight fw1 = new ConcreteFlyweight1();        Flyweight fw2 = new ConcreteFlyweight2();        flyweights.put(&quot;1&quot;, fw1);        flyweights.put(&quot;2&quot;, fw2);    &#125;    //获取    public Flyweight getFlyweight(String key) &#123;        if (flyweights.containsKey(key)) &#123;            //缓存存在则直接返回            return flyweights.get(key);        &#125; else &#123;            //不存在则创建并缓存再返回            Flyweight fw = new ConcreteFlyweight1();            flyweights.put(key, fw);            return fw;        &#125;    &#125;    public static FlyweightFactory getInstance() &#123;        return instance;    &#125;&#125;</code></pre><p>加入外部变化：外部变化可以是具体类的对象，这里为了简化，只是使用字符串传进去。</p><pre><code class="lang-java">Flyweight f1 = FlyweightFactory.getInstance().getFlyweight(&quot;1&quot;);f1.op(&quot;you are one&quot;);//这时候f1的op行为就被改变了，但是f1还是原来的f1f1.op(&quot;you are not one&quot;);</code></pre><h2 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h2><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul><li>减少内存中的对象数量，使得相同相似的对象只保留一个。</li></ul><h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul><li>提高了系统的复杂度，需要分离内部外部状态，让人无法整体的去看待。让程序的逻辑复杂化，也让人更难以理解。</li><li>缺点主要就是在内部状态和外部状态的关系上。</li></ul><h2 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h2><ol><li>当系统中存在大量相同或者重复对象的时候，可以尝试使用享元模式。主要出发点是为了节约内存，减少对象对内存的开销。</li><li>需要缓冲池的场景</li></ol><p><strong>JDK的String类就使用的是享元模式</strong>。String定义的字符串，两个一个样的字符串其实都是一个对象，都是同样的地址。相关的池技术其实都和享元有这相近的思想。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p><strong>当系统中大量相同相似的对象的时候，尝试使用享元模式。第一件事情先将这些对象的内部状态和外部状态分开。内部状态就是享元对象，使用抽象工厂来创建。外部状态通过参数传给享元对象来让享元具备不同的状态（这个外部状态也是有可能不需要传给享元对象的）。</strong>注意点就是：需要一个单例工厂创建享元对象。</p><h1 id="附"><a href="#附" class="headerlink" title="附"></a>附</h1><p>相关代码：<a href="https://github.com/zhaohaoren/design_pattern_tutorial">https://github.com/zhaohaoren/design_pattern_tutorial</a></p><p>如有代码和文章问题，还请指正！感谢！</p>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;【设计模式】享元模式&quot;&gt;&lt;a href=&quot;#【设计模式】享元模式&quot; class=&quot;headerlink&quot;</summary>
        
      
    
    
    
    
    <category term="设计模式" scheme="http://lollipops.work/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>【设计模式】代理模式</title>
    <link href="http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E4%BB%A3%E7%90%86%E6%A8%A1%E5%BC%8F/"/>
    <id>http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E4%BB%A3%E7%90%86%E6%A8%A1%E5%BC%8F/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2021-01-16T09:07:48.575Z</updated>
    
    <content type="html"><![CDATA[<h1 id="【设计模式】代理模式"><a href="#【设计模式】代理模式" class="headerlink" title="【设计模式】代理模式"></a>【设计模式】代理模式</h1><p>本文主要介绍：通用的代理模式（即静态代理），Java动态代理，CGLib动态代理。</p><h2 id="模式背景"><a href="#模式背景" class="headerlink" title="模式背景"></a>模式背景</h2><p>在某些场景下，处于某些原因，一个系统模块不希望或者不能直接访问另一个模块。比如现有的项目下，已经有了一个接口，但是他只提供了服务。在后续的开发中，发现有一些其他的模块需要调用它，但是需要对这个接口进行一个修改。但是使用方的不同，对接口所需要的修改的形式也不同（可能模块A需要对调用添加日志拦截，模块B需要对调用进行权限校验）。此时我们就可以引入一个第三放的代理类来完成。</p><p>代理模式应该属于思想相对简单的设计模式，应用范围也极广。</p><h2 id="定义-amp-概念"><a href="#定义-amp-概念" class="headerlink" title="定义&amp;概念"></a>定义&amp;概念</h2><p>给某一个对象提供一个代理，并由代理对象控制对原对象的引用。</p><p>代理模式是一种<strong>对象结构型模式</strong>。</p><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>代理模式就是通过<strong>在系统中引入一个新的代理对象</strong>。唯一需要注意一点：在标准的设计模式中，被代理的对象<strong>需要有接口的实现</strong>。并且这种实现的方式称为<u><strong>静态代理</strong></u>。</p><p><strong>组成要素</strong></p><ul><li>抽象主题角色（Subject）<ul><li>申明真实角色和代理角色的共同接口，让真实角色和代理角色符合里式替换原则，任何使用真实角色的地方都可以使用代理角色。客户端主要使用这个抽象进行编程。</li></ul></li><li>代理主题角色（Proxy）<ul><li><strong>内部包含对真实角色的引用</strong>，来操作真实对象。有一个和真实角色一样的接口，用来可以替换真实角色。</li></ul></li><li>真实主题角色（RealSubject）<ul><li>真正的那个被代理的对象。</li></ul></li></ul><h2 id="UML"><a href="#UML" class="headerlink" title="UML"></a>UML</h2><p><img src="D:\workspace\blog-docs\docs\设计模式\Proxy.png" alt="Proxy"></p><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><p>抽想主题角色</p><pre><code class="lang-java">public interface Subject &#123;    void request();&#125;</code></pre><p>真实主题角色</p><pre><code class="lang-java">public class RealSubject implements Subject &#123;    @Override    public void request() &#123;        System.out.println(&quot;subject is working...&quot;);    &#125;&#125;</code></pre><p>代理角色</p><pre><code class="lang-java">public class Proxy implements Subject &#123;    // 引用的真实主题    private Subject subject;    public Proxy(Subject subject) &#123;        this.subject = subject;    &#125;      // 代码前加强    public void preRequest() &#123;        System.out.println(&quot;prepare...&quot;);    &#125;    @Override    public void request() &#123;        preRequest();        subject.request();        postRequest();    &#125;      // 代码后加强    public void postRequest() &#123;        System.out.println(&quot;finish...&quot;);    &#125;&#125;</code></pre><p>客户端</p><pre><code class="lang-java">Subject s = new Proxy(new RealSubject());s.request();</code></pre><h2 id="装饰模式区别"><a href="#装饰模式区别" class="headerlink" title="装饰模式区别"></a>装饰模式区别</h2><p>代理模式主要是为真实角色增加一些全新的职责，如权限控制，缓冲处理等，这些职责和真实角色的原始职责<strong>不是同一个领域的职责</strong>。它的目的主要是控制外部对对象的访问。</p><p>装饰模式是对原有职责的一个扩展，<strong>扩展的职责属于同一领域的</strong>。它的目的主要是为对象扩充功能。</p><h2 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h2><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul><li>协调调用者和被调用者，一定程度的对系统解耦，符合迪米特法则。</li><li>对于新需求，只需要添加代理类，不要修改源代码。符合开闭原则。</li></ul><h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul><li>加入了一个代理类，增加了系统的复杂度。</li><li>静态代理，一个真实对象，就需要有一个静态代理类与之对应。</li></ul><h2 id="扩展"><a href="#扩展" class="headerlink" title="扩展"></a>扩展</h2><p>代理模式一些其他的知识点</p><h3 id="代理模式种类"><a href="#代理模式种类" class="headerlink" title="代理模式种类"></a>代理模式种类</h3><p>代理模式可以分为很多种类（随便看看就成）：</p><ul><li>远程代理<ul><li>不同进程（地址空间）之间的代理。进程可以分布在不同的主机上，通过网络RPC进行通信。</li></ul></li><li>虚拟代理<ul><li>创建一个资源消耗大的对象，可以先创建一个较小的代理对象，具体对象等需要的时候再创建。</li></ul></li><li>保护代理<ul><li>控制对一个对象的访问，可以给不同级别的用户提供权限。</li></ul></li><li>缓冲代理<ul><li>为一个目标操作提供临时缓存，以便更多客户端可以共享这个结果。</li></ul></li><li>智能引用代理<ul><li>记录对象被调用的次数等。</li></ul></li></ul><h3 id="Java动态代理"><a href="#Java动态代理" class="headerlink" title="Java动态代理"></a>Java动态代理</h3><p>上面说到，静态代理类的缺点：每一个需要被代理的实体类，都需要编写一个代理类。这样无疑会大大加重系统复杂度。</p><p>思考一下：所谓代理，我们完全可以剥离出<code>RealSubject</code>！ 就好比商城试穿衣服，每一个人穿了衣服的人就是一个被代理的类，但是人是人，衣服是衣服，这个衣服可以给很多人去穿，我们不需要为每个人去单独私人定制造一件衣服，然后才能试穿。<strong>我们将代理的类的功能性剥离出来，形成一个模板，然后通过传入实体对象动态的给其创建出代理类。</strong>这样，我们只需要实现一个代理模板，就可以满足很多不同的实体对象了，而不是为这些实体对象一一创建代理类。</p><p>java有一种机制是在系统运行中动态创建代理类。<strong>使用的反射机制</strong>，通常代理的是一个接口下面的所有的类，因为他<strong>只能代理接口</strong>。</p><h4 id="实现-1"><a href="#实现-1" class="headerlink" title="实现"></a><strong>实现</strong></h4><p>就是实现一个代理类，然后使用的时候，像这个代理类传入对象通过反射动态创建代理对象。</p><pre><code class="lang-java">public class JavaProxy implements InvocationHandler &#123;    private Subject subject;    public JavaProxy(Subject subject) &#123;        this.subject = subject;    &#125;    @Override    public Object invoke(Object proxy, Method method, Object[] args) throws Throwable &#123;        System.out.println(&quot;invoke java proxy&quot;);        if (&quot;request&quot;.equals(method.getName())) &#123;            System.out.println(&quot;invoke request method&quot;);            return method.invoke(subject, args);        &#125; else &#123;            System.out.println(&quot;调用的其他方法&quot;);            return method.invoke(subject, args);        &#125;    &#125;&#125;</code></pre><p>客户端</p><pre><code class="lang-java">//JDK动态代理Subject real = new RealSubject();//传入实体JavaProxy proxy = new JavaProxy(real);//创建代理对象Subject proxyClass = (Subject) java.lang.reflect.Proxy.newProxyInstance(ClassLoader.getSystemClassLoader(), new Class[]&#123;Subject.class&#125;, proxy);proxyClass.request();</code></pre><h4 id="原理-1"><a href="#原理-1" class="headerlink" title="原理"></a><strong>原理</strong></h4><p>涉及java两个关键的类：</p><ul><li><p>java.lang.reflect.Proxy</p><ul><li>用来生成代理类和对象的</li></ul></li><li><p>java.lang.reflect.InvocationHandler</p><ul><li>代理的实现逻辑。</li></ul></li></ul><h4 id="缺点-1"><a href="#缺点-1" class="headerlink" title="缺点"></a>缺点</h4><ul><li>要求原实体对象（<code>RealSubject</code>）必须实现接口。</li></ul><h4 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a><strong>注意事项</strong></h4><ul><li><strong>Java动态代理只能代理接口，要代理类需要使用第三方的CLIGB等类库。</strong></li></ul><h3 id="CGLIB动态代理"><a href="#CGLIB动态代理" class="headerlink" title="CGLIB动态代理"></a>CGLIB动态代理</h3><p>CGLIB是一个Java字节码的生成工具，它会为原类动态生成一个被代理类的子类。</p><h4 id="实现-2"><a href="#实现-2" class="headerlink" title="实现"></a>实现</h4><p>也需要先实现一个抽象的代理层。</p><pre><code class="lang-java">public class ProxyInterceptor implements MethodInterceptor &#123;    private Object object;    public ProxyInterceptor(Object object) &#123;        this.object = object;    &#125;    @Override    public Object intercept(Object o, Method method, Object[] args, MethodProxy methodProxy) throws Throwable &#123;        System.out.println(&quot;invoke cglib proxy&quot;);        if (&quot;request&quot;.equals(method.getName())) &#123;            System.out.println(&quot;invoke request method [cglib]&quot;);            methodProxy.invokeSuper(o, args);        &#125; else &#123;            System.out.println(&quot;调用的其他方法&quot;);            methodProxy.invokeSuper(o, args);        &#125;        return null;    &#125;&#125;</code></pre><p>一个没有接口的被代理类</p><pre><code class="lang-java">public class CGLibRealSubject &#123;    public void request() &#123;        System.out.println(&quot;cglib subject is working...&quot;);    &#125;&#125;</code></pre><p>客户端使用</p><pre><code class="lang-java">//CGLIB动态代理ProxyInterceptor interceptor = new ProxyInterceptor(new CGLibRealSubject());CGLibRealSubject cgLibRealSubject = (CGLibRealSubject) Enhancer.create(CGLibRealSubject.class,interceptor);cgLibRealSubject.request();</code></pre><p><strong>原理</strong></p><p>Cglib 动态代理是针对代理的类, 动态生成一个子类, 然后子类覆盖代理类中的方法, 如果是private或是final类修饰的方法,则不会被重写。</p><p>详细请看：—-</p><h1 id="附"><a href="#附" class="headerlink" title="附"></a>附</h1><p>相关代码：<a href="https://github.com/zhaohaoren/design_pattern_tutorial">https://github.com/zhaohaoren/design_pattern_tutorial</a></p><p>如有代码和文章问题，还请指正！感谢！</p>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;【设计模式】代理模式&quot;&gt;&lt;a href=&quot;#【设计模式】代理模式&quot; class=&quot;headerlink&quot;</summary>
        
      
    
    
    
    
    <category term="设计模式" scheme="http://lollipops.work/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>【设计模式】单例模式</title>
    <link href="http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E5%8D%95%E4%BE%8B%E6%A8%A1%E5%BC%8F/"/>
    <id>http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E5%8D%95%E4%BE%8B%E6%A8%A1%E5%BC%8F/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2021-01-16T09:07:54.809Z</updated>
    
    <content type="html"><![CDATA[<h1 id="【设计模式】单例模式"><a href="#【设计模式】单例模式" class="headerlink" title="【设计模式】单例模式"></a>【设计模式】单例模式</h1><p>单例模式属于<strong>创建型模式</strong>。它应该是接触到的各种模式中最简单的，也是最普遍能见到的了。类似于Spring等很多框架里面都大量的采用了单例模式。本文将介绍所有网络上流传的单例模式的各种写法，如有不足欢迎补充！</p><h2 id="模式背景"><a href="#模式背景" class="headerlink" title="模式背景"></a>模式背景</h2><p>在某些情况下，我们需要系统中的某些对象，在<strong><code>全局只有唯一的存在</code></strong>，就是整个程序只会有一个实例。比如windows的任务管理器，我们只能打开一个，无同时打开多个。对于面向对象的语言来讲，单例模式就是一个类创建唯一实例之后，在当前系统（严谨点应该是和该系统关联的所有相关系统）都无法再创建该实例。</p><h2 id="UML"><a href="#UML" class="headerlink" title="UML"></a>UML</h2><p><img src="D:\workspace\blog-docs\docs\设计模式\singleton_pattern_uml_diagram.jpg" alt="https://www.tutorialspoint.com/design_pattern/singleton_pattern.htm"></p><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>对于Java来说，单例模式需要这3个基本的要素：</p><ol><li><strong>构造函数private</strong><ul><li>让外界无法通过new的方式来创建新的对象。</li></ul></li><li><strong>内部能够持有一个自己的实例</strong><ul><li>因为我们没法new对象，所以我们可以认为该对象和其类一样的唯一。所以这个对象就最好交给这个类来保管。</li><li>所以整个类内部需要一个能保管自己实例对象的引用。</li></ul></li><li><strong>对外使用静态方法获取单例的实例</strong><ul><li>我们没法使用new了，需要对外提供一个静态方法，让外部通过静态方法获取单例对象。</li></ul></li></ol><p>但是考虑程序在初始化和使用该单例时候的性能问题，单例可以有很多种实现方式，但是他的思想基础就是这三个基本的要素。</p><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><p>单例模式有4种写法，各有优劣，下面逐个说明。</p><h3 id="饿汉式"><a href="#饿汉式" class="headerlink" title="饿汉式"></a>饿汉式</h3><p>这是最简单单例写法，我们假设使用者很”饥饿”，<strong>在单例类初始化的时候，我们就要给他这个对象</strong>。如果在资源开销不是很大的时候，且没有明确延迟加载需求的时候我们可以直接使用饿汉式。</p><p><strong>实现</strong></p><pre><code class="lang-java">public class EagerSingleton &#123;    //2: 内置一个单例对象    private static final EagerSingleton singleton = new EagerSingleton();    //1: 构造函数私有化    private EagerSingleton() &#123;    &#125;    //3: 提供静态方法获取数据    public static EagerSingleton getInstance() &#123;        return singleton;    &#125;&#125;</code></pre><ul><li>优点<ul><li>实现简单，因为类加载就已经new对象了，所以单例是<strong>线程安全的</strong>。</li></ul></li><li>缺点<ul><li>不是懒加载的，会消耗过多资源。<strong>比如系统大量的单例，80%都不一定用到，那么在系统加载的时候会很耗时间和资源</strong>。</li></ul></li></ul><blockquote><p>多数情况下<strong>不推荐</strong>（当然不绝对，如果只是少量该类的话，实现这个时间成本最低了。）</p></blockquote><h3 id="懒汉式"><a href="#懒汉式" class="headerlink" title="懒汉式"></a>懒汉式</h3><p>我们需要对饿汉式存在的问题改良：所以设定使用者不那么饿，还很懒，让类在在我们使用的时候创建这个对象就可以了。</p><h4 id="简单懒汉"><a href="#简单懒汉" class="headerlink" title="简单懒汉"></a>简单懒汉</h4><p>简单懒汉就是对该单例实现延迟加载，<strong>将创建的过程放到 <code>getInstance()</code> 过程中！</strong> 因为第一次获取的时候没有对象，所以我们先要判断该类是否已经初始化了一个对象在里面。</p><p><strong>实现</strong></p><pre><code class="lang-java">public class LazySingleton &#123;    //1: 内置单例对象引用    private static LazySingleton singleton = null;    //2: 构造函数私有化    private LazySingleton() &#123;    &#125;    //3: 提供获取对象的静态方法    public static LazySingleton getInstance() &#123;        // 4：判断如果还没有创建就先创建        if (singleton == null) &#123;            singleton = new LazySingleton();        &#125;        return singleton;    &#125;&#125;</code></pre><ul><li>优点<ul><li>确实懒加载的，解决了饿汉式可能占资源的问题。</li></ul></li><li>缺点<ul><li>多线程会出问题。比如当线程t1已经创建了单例，线程t2也同时调用了<code>getInstance</code>方法，此时因为没有同步机制，t1方法走完了创建了实例，t2再走一次，对象会被创建2次。</li><li><strong>线程不安全。</strong></li></ul></li></ul><blockquote><p><strong>不推荐</strong>（该写法是<strong>线程不安全的</strong>。但是他不能保证获取的单例都是唯一的）</p></blockquote><h4 id="简单线程安全懒汉"><a href="#简单线程安全懒汉" class="headerlink" title="简单线程安全懒汉"></a>简单线程安全懒汉</h4><p>在简单懒汉基础上加了线程同步机制。只不过这个同步是直接<strong>对 <code>getInstance()</code>方法进行同步</strong>。</p><p><strong>同步静态方法体的实现</strong></p><pre><code class="lang-java">//3: 提供获取对象的静态方法synchronized public static LazySingleton getInstance() &#123;    // 判断如果还没有创建就先创建    if (singleton == null) &#123;        singleton = new LazySingleton();    &#125;    return singleton;&#125;</code></pre><ul><li>优点<ul><li>线程安全，较为简单。</li></ul></li><li>缺点<ul><li>get对象完全串行化，并发访问效率差。</li></ul></li></ul><blockquote><p><strong>不推荐</strong>（并发性能差）</p></blockquote><h4 id="双重校验锁懒汉（DCL）"><a href="#双重校验锁懒汉（DCL）" class="headerlink" title="双重校验锁懒汉（DCL）"></a>双重校验锁懒汉（DCL）</h4><p><code>DCL：double-checked locking</code>。上面的并发性能差，其<strong>主要原因是我们加锁同步的范围太大了</strong>。我们可以对同步的代码块进一步缩小到new Singleton的时候。但是当两个线程都执行到singleton == null的代码块里面了，这时候同步阻塞了，那么当第一个线程创建完成后，第二个线程进入的时候还是需要进一步判断当前系统中是否已经有了单例，这样才能保证线程安全。</p><p><strong>疑问</strong></p><ul><li>既然有第二重还要判断，那为啥还要第一重？<ul><li>如果去掉了和上面方法体同步。</li><li>这个二重校验锁其实应该这样看：我们确实只是对上面那个整个方法体<code>synchronized (LazySingletonV2.class)</code>加了锁。这样性能和上面的方法同步就没区别了。所以锁和第二重判断是保证我们创建单例安全的。 而第一重，是做了一个优化！<strong>第一重的核心作用并不是重复检验，而是优化！</strong></li><li>第一重判断保证一旦实例创建好了，后面的线程不需要走同步块就可以直接返回实例，但是上面那个单例不可以（执行必须都走同步）。</li><li>而第二重才是阻止并发创建单例的时候。</li><li><strong>也就是说该单例，加了第一重判断后，只有在创建单例对象的那段时间效率会差些，一旦创建完，效率还是很高，后面都不会走同步区域</strong>。</li></ul></li></ul><p><strong>二重校验实现</strong></p><ul><li>这里主要需要注意到两点：<ul><li><strong>二重检验</strong></li><li><strong><code>singleton</code>引用需要加<code>volatile</code>关键字，保证内存可见性。</strong></li></ul></li></ul><pre><code class="lang-java">//6: 因为线程可见性，需要使用volatile修饰。private volatile static LazySingletonV2 singleton = null;//3: 提供获取对象的静态方法public static LazySingletonV2 getInstance() &#123;    //4: 第一重判断    if (singleton == null) &#123;        synchronized (LazySingletonV2.class) &#123;            //5: 第二重判断            if (singleton == null) &#123;                singleton = new LazySingletonV2();            &#125;        &#125;    &#125;    return singleton;&#125;</code></pre><ul><li>优点<ul><li>这是饿汉式最完美的实现方案了。<strong>线程安全</strong>又懒加载。</li></ul></li><li>缺点<ul><li>比同步方法效率要高些，但是依然性能不是很高，特别用了<code>volatile</code>关键字后，会屏蔽jvm的底层一些优化，性能也不是很完美。</li></ul></li></ul><blockquote><p><strong>不推荐</strong>（实现麻烦，而且后面有更好的！）</p></blockquote><h3 id="静态内部类（IoDH）"><a href="#静态内部类（IoDH）" class="headerlink" title="静态内部类（IoDH）"></a>静态内部类（IoDH）</h3><p><code>IoDH：Initialization on Demand Holder</code></p><p>有没有一种可以懒加载，还线程安全的？这就要说这个IoDH了。该单例主要利用了java的<strong>内部类和静态内部类都是延时加载</strong>的这一加载机制。</p><p><strong>实现</strong></p><pre><code class="lang-java">public class IoDHSingleton &#123;    //1: 构造私有化    private IoDHSingleton() &#123;    &#125;    //2: 静态内部类    private static class HolderClass &#123;        private final static IoDHSingleton instance = new IoDHSingleton();        /*         * 利用了java的类加载机制：         * 加载IoDHSingleton的时候不会初始化HolderClass，而是在getInstance的时候才会加载。         * */    &#125;    //3: 静态方法    public static IoDHSingleton getInstance() &#123;        return HolderClass.instance;    &#125;    public static void main(String[] args) &#123;        IoDHSingleton s1 = IoDHSingleton.getInstance();        IoDHSingleton s2 = IoDHSingleton.getInstance();        System.out.println(s1 == s2);    &#125;&#125;</code></pre><ul><li>优点<ul><li>懒加载，<strong>线程安全</strong>。</li></ul></li><li>缺点<ul><li>对语言依赖性很强，不是java或者和java类似的加载机制的语言可能不支持。</li><li>序列化不能保证单例。也就是我将一个对象序列化到磁盘上，然后再读取到内存，这样内存中还是会出现2个对象。只不过这种场景就太少见了。</li></ul></li></ul><blockquote><p><strong>推荐</strong>（比较完美的方案了，在内部类被使用而加载的时候创建了单例，不仅是懒加载还自然保证单例。）</p></blockquote><h3 id="枚举"><a href="#枚举" class="headerlink" title="枚举"></a>枚举</h3><p>这是《Effective Java》里面推荐的方式。</p><pre><code class="lang-java">public enum EnumSingleton &#123;    INSTANCE;&#125;</code></pre><ul><li>优点<ul><li>线程安全，序列化单例保证</li></ul></li><li>缺点<ul><li>没啥缺点</li></ul></li></ul><blockquote><p><strong>强烈推荐</strong>（单例能用枚举就是用枚举吧。如果不习惯就用IoDH。）</p></blockquote><h2 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h2><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul><li>可以严格控制外部如何访问这个对象。</li><li>系统中只存在一个，节省系统资源。</li></ul><h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul><li>单例模式没有抽象层，不好对单例类进行扩展。</li><li>单例模式职责太重，既负责创建又负责业务方法。一定程度违背单一职责原则。</li></ul><h2 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h2><ol><li>网站计数器</li><li>线程池实例</li><li>windows资源管理器等等</li><li>主要系统中需要的东西是唯一的，就会使用这个。</li><li>如果一个东西创建和销毁也很耗性能，考虑单例让他常驻内存。</li></ol><h1 id="附"><a href="#附" class="headerlink" title="附"></a>附</h1><p>相关代码：<a href="https://github.com/zhaohaoren/design_pattern_tutorial">https://github.com/zhaohaoren/design_pattern_tutorial</a></p><p>如有代码和文章问题，还请指正！感谢！</p>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;【设计模式】单例模式&quot;&gt;&lt;a href=&quot;#【设计模式】单例模式&quot; class=&quot;headerlink&quot;</summary>
        
      
    
    
    
    
    <category term="设计模式" scheme="http://lollipops.work/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>【设计模式】原型模式</title>
    <link href="http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E5%8E%9F%E5%9E%8B%E6%A8%A1%E5%BC%8F/"/>
    <id>http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E5%8E%9F%E5%9E%8B%E6%A8%A1%E5%BC%8F/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2021-01-16T09:08:00.835Z</updated>
    
    <content type="html"><![CDATA[<h1 id="【设计模式】原型模式"><a href="#【设计模式】原型模式" class="headerlink" title="【设计模式】原型模式"></a>【设计模式】原型模式</h1><p>又是一个相对简单的模式，原型模式属于<strong>对象创建型</strong>模式。他主要就是通过一个原型对象克隆出多个一模一样的对象。</p><p>在我个人看来，设计模式作用方面可以分为两类：一类是用来优化代码结构的，比如系统解耦，让系统可扩展等等。另一类的模式对系统结构并无帮助，他不是结构型的，而是功能型的，就像单例，原型这些。</p><h2 id="模式背景"><a href="#模式背景" class="headerlink" title="模式背景"></a>模式背景</h2><p>在某些情况下，我们需要完整的复制某一个对象。比如游戏中的小兵，我的世界里面的土块，他们都是以一个模板为原型的相同的对象。如何方便高效的完成对象的复制，就是原型模式要解决的。</p><h2 id="UML"><a href="#UML" class="headerlink" title="UML"></a>UML</h2><p><img src="D:\workspace\blog-docs\docs\设计模式\Prototype.jpg" alt="Prototype"></p><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>让原型类自己提供一个<code>clone</code>方法，使用者直接调用原型类对象的该<code>clone</code>方法，就可以克隆出一个与该原型对象一样的克隆对象。</p><p>注意克隆后的对象是全新的对象，是新创建的内存空间，不是引用这种。<strong>他和原来的原型对象相互独立，修改各自的属性不会相互影响。</strong></p><p><strong>基本要素</strong></p><ul><li>一个代表原型模式的接口，申明clone方法。</li><li>一个实现了接口的原型类。</li><li>提供clone方法。</li></ul><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><p>根据不同的语言的特性，会有一些不同的实现方式。这里只说Java的实现和一种通用的实现。</p><h3 id="通用实现"><a href="#通用实现" class="headerlink" title="通用实现"></a>通用实现</h3><p>通用的实现就是自己创建一个新对象，然后将原型对象的属性重新赋值给这个新对象。这种方式是所有面向对象语言都通用的。</p><p>定义一个Prototype接口，表明原型模式：</p><pre><code class="lang-java">public interface Prototype &#123;    Prototype clone();&#125;</code></pre><p><strong>原型类通用实现</strong></p><pre><code class="lang-java">public class BasePrototypeDemo implements Prototype &#123;    private String attr;    public String getAttr() &#123;        return attr;    &#125;    public void setAttr(String attr) &#123;        this.attr = attr;    &#125;    //内部克隆一个新的对象    @Override    public Prototype clone() &#123;        BasePrototypeDemo instance = new BasePrototypeDemo();        instance.setAttr(this.attr);        return instance;    &#125;&#125;</code></pre><h4 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h4><ul><li>适用于所有的语言，都可以实现。</li></ul><h4 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h4><ul><li>代码较为复杂。通过new再赋值的方式，如果属性很多势必要大量代码，效率也不会太高。</li></ul><h3 id="使用Java的-Cloneable"><a href="#使用Java的-Cloneable" class="headerlink" title="使用Java的 Cloneable"></a>使用Java的 Cloneable</h3><p>Java的Object里面有个clone方法，可以实现对象的赋值（想当于Java语言自身特性），但是使用clone的类需要实现<code>Cloneable</code>接口。否则会抛<code>CloneNotSupportedException</code>异常。</p><p>Java的实现方式如下：</p><pre><code class="lang-java">public class JavaPrototypeDemo implements Cloneable &#123;    @Override    protected JavaPrototypeDemo clone() &#123;        try &#123;            return (JavaPrototypeDemo) super.clone();        &#125; catch (CloneNotSupportedException e) &#123;            e.printStackTrace();            return null;        &#125;    &#125;&#125;</code></pre><h4 id="优点-1"><a href="#优点-1" class="headerlink" title="优点"></a>优点</h4><ul><li>Java实现起来很方便</li></ul><h4 id="缺点-1"><a href="#缺点-1" class="headerlink" title="缺点"></a>缺点</h4><ul><li>只支持Java</li><li>只是<strong>浅复制</strong>（对象内组合的对象还是原来的对象，复制只是复制了引用，只会复制基本数据类型，引用数据类型只复制地址）</li></ul><h2 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h2><p>原型模式主要可以简化我们创建对象的过程。它适合于来<strong>创建对象成本较大</strong>的情况。当我们需要一个比较复杂的对象的一个副本做操作的时候，我们可以试着考虑一下原型模式。</p><h2 id="深复制-amp-浅复制"><a href="#深复制-amp-浅复制" class="headerlink" title="深复制&amp;浅复制"></a>深复制&amp;浅复制</h2><p>原型模式携带着一个问题：就是上面提到的浅复制问题。</p><ul><li>浅复制：原型对象的属性也是一个对象，复制的时候如果<strong>复制的是该属性对象引用</strong>，那么这就是浅复制。</li><li>深复制：原型对象的属性也是一个对象，复制的时候<strong>将该属性对象也复制一份到克隆对象上</strong>，那么这就是深复制。</li></ul><p>也就是说，<strong>深复制才是真正的完全克隆。浅复制只是复制了最外面的一层壳。</strong></p><h3 id="实现深复制"><a href="#实现深复制" class="headerlink" title="实现深复制"></a>实现深复制</h3><p>深复制需要将内部的引用数据类型的属性也要复制一份全新的对象。我们可以递归式的向下找出每个引用对象然后复制一份再赋值，但是那样太麻烦了，效率也太差了。我们一般可以<strong>使用序列化的方式来实现</strong>：将对象二进制流写到内存中，然后再从流中反序列化这个对象。</p><pre><code class="lang-java">// 原型类@Dataclass SerialObj implements Serializable &#123;    private String name;    private Attachment attachment;    public SerialObj deepClone() throws IOException, ClassNotFoundException &#123;        // 将对象写入流中        ByteArrayOutputStream baos = new ByteArrayOutputStream();        ObjectOutputStream oos = new ObjectOutputStream(baos);        oos.writeObject(this);        // 从流中取出对象        ByteArrayInputStream bais = new ByteArrayInputStream(baos.toByteArray());        ObjectInputStream ois = new ObjectInputStream(bais);        return (SerialObj) ois.readObject();    &#125;&#125;// 内置组合对象class Attachment implements Serializable &#123;    private String name;    public String getName() &#123;        return name;    &#125;    public void setName(String name) &#123;        this.name = name;    &#125;&#125;</code></pre><h4 id="优点-2"><a href="#优点-2" class="headerlink" title="优点"></a>优点</h4><ul><li>可以深复制</li></ul><h4 id="缺点-2"><a href="#缺点-2" class="headerlink" title="缺点"></a>缺点</h4><ul><li>代码量大些，但必要用时候只能用这个。</li></ul><h2 id="原型管理器"><a href="#原型管理器" class="headerlink" title="原型管理器"></a>原型管理器</h2><p>将多个原型对象存储在一个集合中给客户端使用。相当于是一个工厂，这个工厂里面已经有了一堆的模具，谁要对象和这个工厂一提，工厂不会吧自己的模具给你，而是以这个模具克隆一份给你。<strong>原型管理器就相当于原型类的工厂。</strong></p><p>简而言之，就是创建一个原型管理器，里面存放着一堆原型对象，外界通过原型管理器去克隆原型对象，而不是直接操作原型对象。</p><p><strong>UML</strong></p><p><img src="D:\workspace\blog-docs\docs\设计模式\PrototypeManager.jpg" alt="PrototypeManager"></p><p><strong>基本要素</strong></p><ol><li>原型对象的统一接口</li><li>原型对象</li><li>原型对象管理器类，这个工厂一般肯定是单例的，因为不是单例也没啥意义。</li></ol><h3 id="实现-1"><a href="#实现-1" class="headerlink" title="实现"></a>实现</h3><pre><code class="lang-java">//1: 一个该管理器管理原型类的公共接口interface IObj extends Cloneable &#123;    IObj clone();    void say();&#125;//2: 原型类定义，需要自己实现cloneclass Obj1 implements IObj &#123;    @Override    public IObj clone() &#123;        try &#123;            return (IObj) super.clone();        &#125; catch (CloneNotSupportedException e) &#123;            e.printStackTrace();            return null;        &#125;    &#125;    @Override    public void say() &#123;        System.out.println(&quot;i am 1&quot;);    &#125;&#125;class Obj2 implements IObj &#123;    @Override    public IObj clone() &#123;        try &#123;            return (IObj) super.clone();        &#125; catch (CloneNotSupportedException e) &#123;            e.printStackTrace();            return null;        &#125;    &#125;    @Override    public void say() &#123;        System.out.println(&quot;i am 2&quot;);    &#125;&#125;//3: 原型对象管理器class PrototypeManager &#123;    /*使用饿汉式单例。*/    private HashMap&lt;String, IObj&gt; hm = new HashMap&lt;&gt;();    private static final PrototypeManager manager = new PrototypeManager();    private PrototypeManager() &#123;        hm.put(&quot;1&quot;, new Obj1());        hm.put(&quot;2&quot;, new Obj2());    &#125;    public IObj getObj(String key) &#123;        return (hm.get(key)).clone();    &#125;    public static PrototypeManager getManager() &#123;        return manager;    &#125;&#125;</code></pre><h1 id="附"><a href="#附" class="headerlink" title="附"></a>附</h1><p>相关代码：<a href="https://github.com/zhaohaoren/design_pattern_tutorial">https://github.com/zhaohaoren/design_pattern_tutorial</a></p><p>如有代码和文章问题，还请指正！感谢！</p>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;【设计模式】原型模式&quot;&gt;&lt;a href=&quot;#【设计模式】原型模式&quot; class=&quot;headerlink&quot;</summary>
        
      
    
    
    
    
    <category term="设计模式" scheme="http://lollipops.work/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>【设计模式】命令模式</title>
    <link href="http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E5%91%BD%E4%BB%A4%E6%A8%A1%E5%BC%8F/"/>
    <id>http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E5%91%BD%E4%BB%A4%E6%A8%A1%E5%BC%8F/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2021-01-16T09:08:08.102Z</updated>
    
    <content type="html"><![CDATA[<h1 id="【设计模式】命令模式"><a href="#【设计模式】命令模式" class="headerlink" title="【设计模式】命令模式"></a>【设计模式】命令模式</h1><p>本文主要介绍命令模式的概念和用法，以及一些针对特定需要的变种形式。</p><h2 id="模式背景"><a href="#模式背景" class="headerlink" title="模式背景"></a>模式背景</h2><p>在面向对象的程序中，调用的过程即耦合的过程，这不可避免。但是存在特殊的情况，调用方（我们称为请求者）和被调方（称为接收者）并<strong>不是调用关系很明确</strong>！又或者说<strong>一个请求者对应多个接收者</strong>，并且需要根据不同的环境选择不同的接受者。比如我们自己定义快捷键：我们的快捷键就是请求者，对应要实现的功能（如：最小化窗口，关闭页面）就是接受者，这些快捷键需要依据用户配置的功能的映射来选择对应的功能。</p><p>在正常的思路中，我们一般都直接将<strong>调用者和接受者耦合在一起</strong>。即快捷键类组合一个功能的引用。代码即：</p><pre><code class="lang-java">class Invoker&#123;  private Receiver rev;  public void invoke()&#123;    rev.execute();  &#125;&#125;</code></pre><p>这么做，在一般的情况还行。但是如果<strong>Receiver太多</strong>了，并且<strong>Receiver无法提取共同的抽象</strong>！（比如rev1是execute方法，rev2是process方法，rev3要调用一连串方法）这就很糟糕了：</p><ol><li><p>如果需要修改接收者，需要修改这个类，用户也可定制化设置。</p></li><li><p>这个调用关系是被绑死了，如果添加新的接收者，需要创建一个新的Invoker类，并且格式还差不多。这会造成大量的类。</p></li><li><p>总之，就是因为这2个关系的耦合度高，导致了系统不易扩展，会违背开闭原则。</p><p>所以我们需要一种松耦合的方式来<strong>解决这个扩展性的问题</strong>，这就是命令模式的目的。</p></li></ol><h2 id="定义-amp-概念"><a href="#定义-amp-概念" class="headerlink" title="定义&amp;概念"></a>定义&amp;概念</h2><p>命令模式（Command）：<strong>将一个请求封装为一个对象</strong>，从而可以使用不同的请求对象对客户进行参数化；对请求排队或者记录请求日志，以及支持可撤销的操作。命令模式是一种<strong>对象行为型模式</strong>，别名有行为模式或者事务模式。</p><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>定义说的很复杂，其实思想也很简单。既然希望调用者和接收者接口，那么<strong>调用者中引用的必然是要一个抽象层</strong>的东西。我们让<strong>调用者全部面向抽象编程</strong>，就能完全解耦了。但是上面也说了，接受者的具体执行方法逻辑五花八门，是没有办法提取共同抽象的。命令模式所以就在中间添加了一层：<strong>命令层</strong>。这就是定义所说的，<code>将一个请求封装为一个对象</code>，这个对象就是命令类对象。通过对每个接收者包装一层命令类，我们就可以通过命令类再去调用接收者方法。</p><p><strong>组成要素</strong></p><ul><li>抽象命令类<ul><li>一个抽象类或者接口，提供统一的命令的抽象，目的让调用者面向抽象编程。</li></ul></li><li>具体命令类<ul><li>抽象命令类的子类，即使封装的命令对象，该命令对象内部依赖具体的执行者。</li><li>我的理解就是在每个接受者上面包了一层。让他们具有统一的对外访问入口。</li></ul></li><li>调用者<ul><li>请求的发送者，它通过命令对象来执行请求。该类设计的时候并不会确定其接受者是谁。而是在程序运行中将一个具体的命令对象注入其中。动态的来指定调用者和接收者的关系。</li></ul></li><li>接受者<ul><li>具体的功能，请求的具体业务处理逻辑。</li></ul></li></ul><p>因为我们的实际的需求可能是多变的，命令模式也有很多变种：</p><ul><li><p><strong>命令队列</strong></p><p>如果请求者需要一连串的接受者来处理，我们可以使用命令队列的形式。就是添加一个命令队列类，命令队列中持有一个命令的List。</p></li><li><p><strong>宏命令</strong></p><p>是组合模式和命令模式的联用，添加一个宏命令类，这个命令类里面也是一个命令的list，和命令队列差不多。（我感觉一样就是叫法不同，不知道书上作者为啥分开讲）</p></li><li><p><strong>增加撤销操作</strong></p><p>执行命令，当然也可以撤销，我们可以在抽象命令类中，添加一个撤销的方法，用来undo执行的命令。</p></li></ul><h2 id="UML"><a href="#UML" class="headerlink" title="UML"></a>UML</h2><p><img src="/Users/zhaohaoren/workspace/mycode/blog-docs/docs/设计模式/Command.png" alt="image-20200825140300622"> </p><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><h3 id="一般命令模式"><a href="#一般命令模式" class="headerlink" title="一般命令模式"></a>一般命令模式</h3><p>调用者</p><pre><code class="lang-java">public class Invoker &#123;    /**     * 需要被注入给该调用者的命令     */    Command command;    public Invoker(Command command) &#123;        this.command = command;    &#125;    public void call() &#123;        command.execute();    &#125;&#125;</code></pre><p>接受者</p><pre><code class="lang-java">public class Receiver &#123;    public void action() &#123;        System.out.println(&quot;processing...&quot;);    &#125;&#125;</code></pre><p>抽象命令类</p><pre><code class="lang-java">public interface Command &#123;    /**     * 所有命令都走这个方法来执行 接收者的调用     */    void execute();&#125;</code></pre><p>具体命令类</p><pre><code class="lang-java">public class ConcreteCommand implements Command &#123;    /**     * 一个命令绑定一个接受者     */    private Receiver receiver;    public ConcreteCommand() &#123;        this.receiver = new Receiver();    &#125;    /**     * 通过execute调用接受者     */    @Override    public void execute() &#123;        receiver.action();    &#125;&#125;</code></pre><p>客户端</p><pre><code class="lang-java">Command command = new ConcreteCommand();Invoker invoker = new Invoker(command);invoker.call();</code></pre><h3 id="命令队列"><a href="#命令队列" class="headerlink" title="命令队列"></a>命令队列</h3><p>如果调用者需要完成的功能是需要一些列的命令来执行，那么久需要一个队列命令。我们可以使用list来存放一个命令列表，但是最常用，灵活性最好的是加一个CommandQueue类。</p><pre><code class="lang-java">public class CommandQueue &#123;    private ArrayList&lt;Command&gt; commands = new ArrayList&lt;&gt;();    public void addCommand(Command command) &#123;        commands.add(command);    &#125;    public void removeCommand(Command command) &#123;        commands.remove(command);    &#125;    /**     * 循环调用每一个命令     */    public void execute() &#123;        for (Command command : commands) &#123;            command.execute();        &#125;    &#125;&#125;</code></pre><h2 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h2><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul><li>降低系统的耦合度，请求者和接收者之间的解耦。</li><li>方便扩展新命令，而不需要修改代码。</li><li>可以方便的实现一个命令队列或者宏命令。</li></ul><h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul><li>没有解决会造成系统中大量的类的问题。</li></ul><h2 id="适用场景"><a href="#适用场景" class="headerlink" title="适用场景"></a>适用场景</h2><p>那些希望使用者和接收者不直接交互，在前期设计的时候，这2者的关系无法确定，后面可能需要动态的去配置的时候尝试使用命令模式。还有某个使用者如果想要执行一系列的操作的时候也考虑使用命令队列来操作。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>命令模式的<strong>主要目的就是为了让请求者和接收者解耦</strong>，做法就是添加一个命令层，每个命令类对应着一个接受者，从而让接收者有抽象命令类的统一外观。然后请求者只需要面向抽象命令类编程即可。</p><h1 id="附"><a href="#附" class="headerlink" title="附"></a>附</h1><p>相关代码：<a href="https://github.com/zhaohaoren/design_pattern_tutorial">https://github.com/zhaohaoren/design_pattern_tutorial</a></p><p>如有代码和文章问题，还请指正！感谢！</p>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;【设计模式】命令模式&quot;&gt;&lt;a href=&quot;#【设计模式】命令模式&quot; class=&quot;headerlink&quot;</summary>
        
      
    
    
    
    
    <category term="设计模式" scheme="http://lollipops.work/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>【设计模式】备忘录模式</title>
    <link href="http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E5%A4%87%E5%BF%98%E5%BD%95%E6%A8%A1%E5%BC%8F/"/>
    <id>http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E5%A4%87%E5%BF%98%E5%BD%95%E6%A8%A1%E5%BC%8F/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2021-01-16T09:08:12.798Z</updated>
    
    <content type="html"><![CDATA[<h1 id="【设计模式】备忘录模式"><a href="#【设计模式】备忘录模式" class="headerlink" title="【设计模式】备忘录模式"></a>【设计模式】备忘录模式</h1><p>本文主要介绍备忘录模式概念和用法。</p><h2 id="模式背景"><a href="#模式背景" class="headerlink" title="模式背景"></a>模式背景</h2><p>备忘录模式是为了解决系统中状态回退，撤销问题的。比如系统功能中的回到上一步，下棋悔棋等。我们可能因为当前的状态存在了问题，需要回滚到上一个状态去。</p><h2 id="定义-amp-概念"><a href="#定义-amp-概念" class="headerlink" title="定义&amp;概念"></a>定义&amp;概念</h2><p>在不破坏封装的前提下，捕获一个对象的内部状态，并在该对象之外保存这个状态，这样可以在以后将对象恢复到原先保存的状态。他是一种对象行为型模式，其别名为Token。</p><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>备忘录模式也是一个相对简单的设计模式，他的主要思想就是在<strong>外部</strong>再创建一个对象专门来保存一个对象的上一个状态，这个类我们就叫做备忘录类。原始对象需要自己提供对象状态的创建和恢复，但是他内部不引用这个对象（如果在内部自己引用，那么该对象职责会偏重），所以我们需要外部一个专门管理负责人的负责人类。</p><p>并且为了状态的安全考虑，我们需要对状态类对象的访问做控制：</p><ol><li><strong>状态类只能原始类对象来创建，以及set等方法</strong>，其他的类不允许调用。<ul><li>java使用package访问级别来控制，状态类和原始类在同一个包中。</li></ul></li><li><strong>负责人类只负责保存备忘录对象</strong>。</li></ol><p><strong>组成要素</strong></p><ul><li>原发器：需要被保存状态的类</li><li>备忘录：用来存储原发器的内部状态，根据原发器来决定保存哪些内部状态。注意备忘录只能被原发器来使用，不能给其他的对象修改其内部状态。</li><li>负责人：管理者，它负责保存备忘录，但是不能对备忘录的内容进行操作或者检查。只负责存储备忘录对象，不可以修改它，也不需要知道备忘录内部细节。</li></ul><p>备忘录模式中，如果负责人只管理一个对象，那么就只能撤销一步，如果管理多个状态（List），就可以撤回多次。</p><h2 id="UML"><a href="#UML" class="headerlink" title="UML"></a>UML</h2><p><img src="/Users/zhaohaoren/workspace/mycode/blog-docs/docs/设计模式/Memento.png" alt="image-20200907111342192"></p><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><p>单次和多次撤销的实现。</p><h3 id="单次撤销"><a href="#单次撤销" class="headerlink" title="单次撤销"></a>单次撤销</h3><p>原发器</p><pre><code class="lang-java">public class Originator &#123;    private String state;    /**     * 创建一个备忘录对象     */    public Memento createMemento() &#123;        return new Memento(this);    &#125;    /**     * 根据备忘录对象恢复到之前的状态。     */    public void restoreMemento(Memento m) &#123;        state = m.getState();    &#125;    public String getState() &#123;        return state;    &#125;    public void setState(String state) &#123;        this.state = state;    &#125;&#125;</code></pre><p>备忘录</p><pre><code class="lang-java">class Memento &#123;    private String state;    Memento(Originator originator) &#123;        state = originator.getState();    &#125;    public String getState() &#123;        return state;    &#125;    public void setState(String state) &#123;        this.state = state;    &#125;&#125;</code></pre><p>负责人</p><pre><code class="lang-java">public class Caretaker &#123;    private Memento memento;    public Memento getMemento() &#123;        return memento;    &#125;    public void setMemento(Memento memento) &#123;        this.memento = memento;    &#125;&#125;</code></pre><p>使用</p><pre><code class="lang-java">// 创建负责人Caretaker caretaker = new Caretaker();Originator o = new Originator();o.setState(&quot;1&quot;);// 创建一个备忘录对象，保存状态，并初始化负责人的备忘录对象caretaker.setMemento(o.createMemento());o.setState(&quot;2&quot;);System.out.println(o.getState());// 恢复o.restoreMemento(caretaker.getMemento());System.out.println(o.getState());</code></pre><h3 id="多次撤销"><a href="#多次撤销" class="headerlink" title="多次撤销"></a>多次撤销</h3><p>就是在负责人类中定义一个集合来存储多个备忘录。</p><pre><code class="lang-java">public class MementosTaker &#123;    private List&lt;Memento&gt; mementos = new ArrayList&lt;&gt;();    public Memento getMemento(int index) &#123;        return mementos.get(index);    &#125;    public void setMementos(Memento memento) &#123;        this.mementos.add(memento);    &#125;&#125;</code></pre><h2 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h2><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul><li>提供了一种状态恢复机制，使用户可以方便的恢复到一个特定的历史状态。</li><li>实现了信息的封装，使得用户不需要关心状态的保存细节。</li></ul><h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul><li>如果类的成员变量过多，会占用比较大的内存资源，而且每一次保存都会消耗一定的内存。</li></ul><h2 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h2><ul><li>一个对象需要保存该对对象在某一时刻的全部状态或者部分状态的。</li><li>想要实现撤销回滚等操作的。</li><li>不想让外界破坏一个对象的历史状态，并不希望将其实现细节暴露给外界的。</li></ul><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>原始类就不用多说了，主要记住该模式还有其他的2个类：专门存储状态的状态类，专门负责管理状态的负责人类。</p><ul><li>原始类，状态类放在一个包下，状态类设置访问级别为默认的package级别。</li><li>原始类中包含状态对象的创建和恢复方法，创建完成了，和需要恢复的时候，都需要负责人对象来存储或者返回之前的状态对象。</li></ul><h1 id="附"><a href="#附" class="headerlink" title="附"></a>附</h1><p>相关代码：<a href="https://github.com/zhaohaoren/design_pattern_tutorial">https://github.com/zhaohaoren/design_pattern_tutorial</a></p><p>如有代码和文章问题，还请指正！感谢！</p>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;【设计模式】备忘录模式&quot;&gt;&lt;a href=&quot;#【设计模式】备忘录模式&quot; class=&quot;headerlink&quot; title=&quot;【设计模式】备忘录模式&quot;&gt;&lt;/a&gt;【设计模式】备忘录模式&lt;/h1&gt;&lt;p&gt;本文主要介绍备忘录模式概念和用法。&lt;/p&gt;
&lt;h2</summary>
        
      
    
    
    
    
    <category term="设计模式" scheme="http://lollipops.work/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>【设计模式】外观模式</title>
    <link href="http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E5%A4%96%E8%A7%82%E6%A8%A1%E5%BC%8F/"/>
    <id>http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E5%A4%96%E8%A7%82%E6%A8%A1%E5%BC%8F/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2021-01-16T09:08:16.010Z</updated>
    
    <content type="html"><![CDATA[<h1 id="【设计模式】外观模式"><a href="#【设计模式】外观模式" class="headerlink" title="【设计模式】外观模式"></a>【设计模式】外观模式</h1><p>本文主要介绍外观模式及其抽象外观的原理和实现。</p><h2 id="模式背景"><a href="#模式背景" class="headerlink" title="模式背景"></a>模式背景</h2><p>首先，使用外观模式之前就已经拥有一个很好的起点，系统业务中某些模块已经封装好了，而且职责单一。但是最终使用避免不了模块与模块，类与类之间的交互。这种交互该如何优化？就是外观模式要解决的。</p><p>书上举了一个例子，系统中有三个子模块：1.负责加密，2.负责读文件，3负责写文件。这三个模块各司其职，职能已经划分好了。但是使用使用的时候，我们会有大量的地方需要成批的调用这1-2-3的逻辑，每个地方都写这些逻辑毫无复用而言。所以我们应该<strong>将这些组合的单一职责逻辑封装到一个新类上，让外界统一通过这个新类来访问</strong>，这就是外观模式。</p><h2 id="定义-amp-概念"><a href="#定义-amp-概念" class="headerlink" title="定义&amp;概念"></a>定义&amp;概念</h2><p><strong>外观模式（Facade Pattern）：</strong>为子系统中的一组接口提供一个统一的入口。外观模式定义了一个高层接口，这个接口使得这一子系统更加容易使用。（外观模式又称为门面模式，是一种对象结构型模式）</p><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>外观模式的目的是为了简化类于类之间的交互，其本质思想来源于封装。子系统是已经设计好的单一职责类，外观模式是将这些单一的职责聚合起来，提供一套模板。让需要使用这套模板的类，直接使用外观类进行访问。这样来提高代码的复用性。</p><p><strong>必要条件</strong></p><ul><li>Facade（外观角色）<ul><li>客户端直接调用的那个外观，负责和其他多个子系统进行交互。</li></ul></li><li>SubSystem（子系统角色）<ul><li>每个子系统分别有自己对应的功能，可以被客户端直接调用，也可以被外观角色调用。他即使客户端想要实现功能的具体办事人。</li></ul></li></ul><h3 id="抽象外观模式"><a href="#抽象外观模式" class="headerlink" title="抽象外观模式"></a>抽象外观模式</h3><p>外观模式分为<strong>简单外观</strong>和<strong>抽象外观</strong>。简单外观就是对子模块添加外观类。但是<strong>这么会有一个问题，就是对子模块不可扩展</strong>。如果我们需要替换，增加，移除子模块，那么就需要去修改外观类，或者添加外观类去修改客户端代码，这违背了开闭原则。这时候可以<strong>针对外观类添加抽象层，来实现外观类的扩展</strong>，而客户端使用抽象外观进行编程。</p><h2 id="UML"><a href="#UML" class="headerlink" title="UML"></a>UML</h2><p>简单外观模式</p><p><img src="/Users/zhaohaoren/workspace/mycode/blog-docs/docs/设计模式/Facade_Simple.png" alt="image-20200821105902188"></p><p>抽象外观模式</p><p><img src="/Users/zhaohaoren/workspace/mycode/blog-docs/docs/设计模式/FacadeAbstract.png" alt="image-20200821110256652"></p><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><h3 id="简单外观"><a href="#简单外观" class="headerlink" title="简单外观"></a>简单外观</h3><p>直接将子系统的功能包装成一个外观类。</p><p><strong>实现</strong></p><p>子系统</p><pre><code class="lang-java">//X: 0,1,2....Npublic class SubSystemX &#123;    public void op() &#123;        System.out.println(&quot;system 0X is doing x ...&quot;);    &#125;&#125;</code></pre><p>外观</p><pre><code class="lang-java">public class Facade &#123;    private SubSystem01 s1;    private SubSystem02 s2;    public Facade(SubSystem01 s1, SubSystem02 s2) &#123;        this.s1 = s1;        this.s2 = s2;    &#125;    public void op() &#123;        s1.op1();        s2.op2();    &#125;&#125;</code></pre><p>客户端</p><pre><code class="lang-java">Facade facade = new Facade(new SubSystem01(),new SubSystem02());facade.op();</code></pre><h3 id="抽象外观类"><a href="#抽象外观类" class="headerlink" title="抽象外观类"></a>抽象外观类</h3><p>上面简单外观有个问题：如果想要修改外观类的实现，比如增加减少和外观类交互的子系统，那么就要修改该外观类以及客户端了。所以需要引入一个抽象，这样以后只需要添加新的外观类就可以了。客户端具体使用哪个外观类通过配置就可以搞定。</p><p><strong>实现</strong></p><pre><code class="lang-java">public abstract class AbstractFacade &#123;    public abstract void op();&#125;public class FacadeNew extends AbstractFacade &#123;    private SubSystem03 s3;    public FacadeNew(SubSystem03 s3) &#123;        this.s3 = s3;    &#125;    @Override    public void op() &#123;        s1.op1();    &#125;&#125;//使用AbstractFacade abstractFacade = new FacadeNew(new SubSystem01());// 可以通过配置来加载具体的外观类abstractFacade.op();</code></pre><h2 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h2><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul><li>简化了调用过程，无须了解、深入子系统，防止带来风险。</li><li>减少系统依赖、松散耦合。</li><li>更好的划分访问层次，外观层次和下面子模块层次分开，还可以外观套外观。</li><li>符合迪米特法则，即最少知道原则。</li></ul><h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul><li>没法很好的限制客户端直接使用子系统，这个缺点在某些情况才存在。</li></ul><h2 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h2><ul><li>为了节约系统资源，外观角色可以<strong>设计为一个单例类</strong>，并确保系统中只有唯一一个访问的入口。</li><li>一个系统<strong>可以设计多个外观类</strong>，分别提供不同的功能。</li><li>不要在外观类中对子系统的功能有添加，外观类只是集中这些功能，不是扩充这些功能。<strong>不要加入新的行为</strong>，如果新行为应该在子系统中添加，或者直接添加子系统。不要使用外观类来添加。</li></ul><h2 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h2><p>这类场景还是比较多的。当你写代码的时候，在写某一块业务的时候，你发现写的过程中，这边需要调用A的东西，又需要调用B的东西，然后这样的代码在很多地方又都要用到，你就可以考虑使用外观模式。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>外观模式也是一个很简单的模式（其实很多时候我们已经这么用了，只是不知道官方话怎么讲）。这个模式主要就2个要点：</p><ol><li>当我们需要将多个职责封装在一起的时候，使用外观模式提供一个统一的访问入口。 </li><li>当外观模式中的子系统可能会有扩展和替换的时候，使用抽象外观去支持其扩展性。</li></ol><h1 id="附"><a href="#附" class="headerlink" title="附"></a>附</h1><p>相关代码：<a href="https://github.com/zhaohaoren/design_pattern_tutorial">https://github.com/zhaohaoren/design_pattern_tutorial</a></p><p>如有代码和文章问题，还请指正！感谢！</p>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;【设计模式】外观模式&quot;&gt;&lt;a href=&quot;#【设计模式】外观模式&quot; class=&quot;headerlink&quot; title=&quot;【设计模式】外观模式&quot;&gt;&lt;/a&gt;【设计模式】外观模式&lt;/h1&gt;&lt;p&gt;本文主要介绍外观模式及其抽象外观的原理和实现。&lt;/p&gt;
&lt;h2</summary>
        
      
    
    
    
    
    <category term="设计模式" scheme="http://lollipops.work/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>【设计模式】工厂模式</title>
    <link href="http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E5%B7%A5%E5%8E%82%E6%A8%A1%E5%BC%8F/"/>
    <id>http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E5%B7%A5%E5%8E%82%E6%A8%A1%E5%BC%8F/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2021-01-16T09:08:19.427Z</updated>
    
    <content type="html"><![CDATA[<h1 id="【设计模式】工厂模式"><a href="#【设计模式】工厂模式" class="headerlink" title="【设计模式】工厂模式"></a>【设计模式】工厂模式</h1><p>通常我们说的工厂模式是指<strong>工厂方法模式</strong>。工厂模式的目的是<strong>将对象的创建和使用分离</strong>，用来移除一些类创建对象 和对象操作行为之间的耦合，让客户端类不创建对象交给工厂来创建对象。如果一个类没有太多变数，构造也简单就不要使用工厂类，不然只会导致工厂泛滥。</p><p>工厂模式是一种<strong>创建型模式</strong>。</p><h2 id="模式背景"><a href="#模式背景" class="headerlink" title="模式背景"></a>模式背景</h2><p>开发中经常会出现需要根据不同的条件，去创建不同的对象并调用其特有方法的情况。比如我要实现一个需求，依据用户需求创建不同的图形，然后打印出来。在不使用工厂模式的情况下，我们代码可能会长成这样：</p><pre><code class="lang-java">public class BadCase &#123;    private String name;    public BadCase(String shape) &#123;        if (&quot;circle&quot;.equals(shape)) &#123;            this.name = &quot;circle&quot;;        &#125; else if (&quot;rect&quot;.equals(shape)) &#123;            this.name = &quot;rect&quot;;        &#125; else if (&quot;triangle&quot;.equals(shape)) &#123;            this.name = &quot;triangle&quot;;        &#125;        //else if ...    &#125;    public void draw() &#123;        if (&quot;circle&quot;.equals(this.name)) &#123;            System.out.println(&quot;i am circle&quot;);        &#125; else if (&quot;rect&quot;.equals(this.name)) &#123;            System.out.println(&quot;i am rect&quot;);        &#125; else if (&quot;triangle&quot;.equals(this.name)) &#123;            System.out.println(&quot;i am triangle&quot;);        &#125;        //else if ...    &#125;    public static void main(String[] args) &#123;        BadCase bad = new BadCase(&quot;rect&quot;);        bad.draw();    &#125;&#125;</code></pre><p>当然这是故意这么写成这么最糟糕样子的，这种写法显然毫无任何优雅可言，完全面向过程式写法。他存在哪些问题呢？</p><ol><li>毫无对象的概念！完全是面向过程的写法。</li><li>太多的<code>if-else</code>判断，在创建的时候使用<code>if-else</code>来判断进行创建，输出的时候又一次判断再输出，不仅性能差，代码还冗余。</li><li>如果此时我们希望加入五角星形状，我们就需要修改该类。这违反了开闭原则。</li><li>这个<code>BaseCase</code>的类，职责过重，既负责创建又负责使用，违反了单一职责原则。</li><li>main函数相当于客户端，客户端需要使用new来创建，每要一个图像，就要new一个该<code>BaseCase</code>实例。在实际中new实例可能需要很多初始化工作，这就会在代码中耦合大量创建对象的代码，而且每使用一次就需要耦合一堆初始化代码，对象的创建和使用都在一起，编码复杂且耦合性强。</li></ol><p>工厂模式就是为了一定程度上解决该问题的，一般工厂模式有下面3种：</p><ul><li>简单工厂模式（Simple Factory），又称静态工厂方法模式（Static Factory Method Pattern）。</li><li><p>工厂方法模式（Factory Method），又称多态性工厂模式（Polymorphic Factory）；</p></li><li><p>抽象工厂模式（Abstract Factory），又称工具箱模式（Kit 或Toolkit）。</p></li></ul><p><strong>先解决问题1：</strong></p><p>我们一般都不会像上面那么写。一般我们都会将每个图形，对应成一种类型的对象。既然说工厂模式，我们设定这些对象就是一个个的商品：</p><pre><code class="lang-java">// 产品Aclass ProductA &#123;    public void printName() &#123;        System.out.println(&quot;i am A&quot;);    &#125;&#125;// 产品Bclass ProductB&#123;    public void printName() &#123;        System.out.println(&quot;i am B&quot;);    &#125;&#125;</code></pre><p>这才是Java工程中最常见的，我们的项目中散落着大量的各种实体类，并且他们之间有关联。下面就使用那三种工厂模式来解决其他的问题。</p><h2 id="简单工厂模式"><a href="#简单工厂模式" class="headerlink" title="简单工厂模式"></a>简单工厂模式</h2><p>简单工厂模式不属于GoF的23种设计模式里面，但他是学习其他工厂模式的基础。他的设计思想比较简单：主要<strong>目的是将对象的创建和使用分离</strong>（即解决问题5）。使用的所有对象都是通过工厂类来创建的，然后客户端来使用。</p><h3 id="UML"><a href="#UML" class="headerlink" title="UML"></a>UML</h3><p><img src="D:\workspace\blog-docs\docs\设计模式\SimpleFactory.jpg" alt="SimpleFactory"></p><h3 id="原理"><a href="#原理" class="headerlink" title="原理"></a><strong>原理</strong></h3><ol><li>将需要创建的对象，封装到不同的类中，这些类我们设定为产品类。</li><li>对这些产品类的共性部分，我们需要抽象出来，封装到抽象产品类中。</li><li>提供一个工厂类，并提供一个静态方法，该方法依据传入参数，创建不同的对象。</li></ol><p><strong>基本要素</strong></p><ol><li>产品的抽象接口或类</li><li>工厂类</li><li>静态工厂方法</li></ol><h3 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h3><p><strong>先抽象封装好产品类</strong></p><pre><code class="lang-java">// 所有工厂里面的产品，抽象一个接口。便于产品的扩展不会影响到工厂类和客户端类去改动。interface Product &#123;    void printName();&#125;// 产品Aclass ProductA implements Product &#123;    @Override    public void printName() &#123;        System.out.println(&quot;i am A&quot;);    &#125;&#125;// 产品Bclass ProductB implements Product &#123;    @Override    public void printName() &#123;        System.out.println(&quot;i am B&quot;);    &#125;&#125;</code></pre><p><strong>实现工厂类</strong></p><pre><code class="lang-java">public class SimpleFactory &#123;      // 静态工厂方法    public static Product getProduct(String name) &#123;        switch (name.toLowerCase()) &#123;            case &quot;a&quot;:                return new ProductA();            case &quot;b&quot;:                return new ProductB();            default:                return null;        &#125;    &#125;    // 模拟客户端使用    public static void main(String[] args) &#123;        SimpleFactory.getProduct(&quot;a&quot;).printName();    &#125;&#125;</code></pre><h3 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h3><h4 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h4><ul><li>对象的创建和使用分离：创建由工厂类完成，客户端只是使用。</li><li>一定程度上减轻了类的职责，对象的使用封装到对象的内部。</li></ul><h4 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h4><ul><li>工厂类职责依然过重：还是太多判断，并且一旦不能正常工作，所有该工厂的对象都将受到影响。</li><li>系统扩展的话，需要修改工厂类：如果添加产品，还需要修改工厂类的代码。</li><li>因为使用了静态工厂方法，所以工厂类不能形成基于继承的等级结构。</li><li>会增加系统中的类个数：需要引入工厂类，这可能会增加系统的复杂度。</li></ul><h3 id="适用场景"><a href="#适用场景" class="headerlink" title="适用场景*"></a>适用场景*</h3><ul><li>工厂类负责<strong>创建的对象比较少</strong>。因为比较少的时候，工厂类内部的业务不会太复杂。</li><li>客户端只想传参得对象，对如何创建对象不关心。</li></ul><blockquote><p>Tips：</p><p>我们可能感觉在客户端中使用工厂类需要传递参数这种方式不好，每次想要更换的时候，需要修改客户端代码。这个问题可以通过将该参数通过读取配置文件的方式来解决。这也正是Spring的思想。</p></blockquote><h2 id="工厂方法模式"><a href="#工厂方法模式" class="headerlink" title="工厂方法模式"></a>工厂方法模式</h2><p><strong>简单工厂模式存在一个问题：</strong>当要引入新的产品的时候，就需要修改工厂类，违背开闭原则。而工厂方法模式就是为了解决该问题做的简单延伸。目的是要<strong>解决工厂职责太重问题</strong>。</p><p>思考一下：想要通过动态添加类，而不是修改原代码的方式来修改工厂类，该怎么做？没错！就是再抽取一个抽象层，然后只要添加子类就完成了动态的扩展。工厂方法模式也是基于这个思想，让工厂可以动态扩展。</p><h3 id="UML-1"><a href="#UML-1" class="headerlink" title="UML"></a>UML</h3><p><img src="D:\workspace\blog-docs\docs\设计模式\MethodFactory.jpg" alt="MethodFactory"></p><h3 id="原理-1"><a href="#原理-1" class="headerlink" title="原理"></a><strong>原理</strong></h3><p>上面我重点抽象的是产品类，而工厂方法模式我们重点<strong>是对工厂也进行抽象</strong>。</p><p>我们将工厂的工厂方法抽象出一层<strong>抽象工厂类</strong>，该类是该模式的核心。具体产品的创建交给了工厂的子类去完成。</p><p><strong>每个产品类都对应着一个具体工厂实现类</strong>，后面将通过该工厂类来创建该产品类对象。</p><p>这个工厂方法相当于对简单工厂模式的工厂类进行进一步的减轻职责：多创建一些工厂让他们分别负责自己的产品，客户端使用时相当于在产品外面都包了一层抽象工厂类，直接面向抽象工厂编程。客户端就不需要知道产品的类，只需要知道产品的工厂类就行了。那么产品类就和客户端隔离了。我们就可以查询代码中有哪些工厂就行了。</p><p><strong>基本要素</strong></p><ol><li>产品及产品的抽象</li><li>工厂的抽象（接口或者类）</li><li>具体的工厂，每个产品对应一个工厂，工厂的具体实现类。</li></ol><h3 id="实现-1"><a href="#实现-1" class="headerlink" title="实现"></a><strong>实现</strong></h3><pre><code class="lang-java">// 抽象工厂interface ProductFactory &#123;    Product getProduct();&#125;// 生产A的抽象工厂class AProductFactory implements ProductFactory &#123;    @Override    public Product getProduct() &#123;        return new ProductA();    &#125;&#125;// 生产B的抽象工厂class BProductFactory implements ProductFactory &#123;    @Override    public Product getProduct() &#123;        return new ProductB();    &#125;&#125;// 客户端使用ProductFactory factory = new AProductFactory(); // 可以使用配置文件来指定加载哪个工厂Product product = factory.getProduct();product.printName();</code></pre><blockquote><p>这种模式，当添加产品时候，不需要该代码，但是需要添加大量的类。</p></blockquote><h3 id="优缺点-1"><a href="#优缺点-1" class="headerlink" title="优缺点"></a>优缺点</h3><h4 id="优点-1"><a href="#优点-1" class="headerlink" title="优点"></a>优点</h4><ul><li>更加符合开闭原则，新加产品不需要修改工厂类。<ul><li>我们只需要对新加的产品，添加一个对应的工厂类，然后加入项目中即可。</li></ul></li><li>没有了静态方法，可以形成基于继承体系的工厂了。</li><li>每个产品只负责创建一个产品，符合单一职责。</li></ul><h4 id="缺点-1"><a href="#缺点-1" class="headerlink" title="缺点"></a>缺点</h4><ul><li>加一个（或者说一类）产品，就要加一个对应的工厂类，很烦，会产生大量工厂类！</li><li>虽然不用修改工厂类代码，但是新加的工厂实现类，还是需要通过配置的方式才能指定客户端用哪个具体实现类加载。所以客户端代码一般都要使用DOM，反射等技术来支持具体实例化配置，会增加系统的理解难度。<ul><li>注意：我们开闭原则，不代表我们什么都不改动！这是不可能的！</li><li>我们不要改动的应该是封装的部分，而客户端使用方肯定还是需要有改动的。之所以不能改，设想一下，你写的功能是以第三方包的形式给别人用的，你一旦改动出问题了，影响的是所有用你第三方包的人，你再让他们去为你改动再修改，明显不可能，所以对于第三方包这种性质的类，我们只允许添加不允许修改。</li></ul></li></ul><h3 id="适用场景-1"><a href="#适用场景-1" class="headerlink" title="适用场景*"></a>适用场景*</h3><ul><li><strong>这个模式是工厂方法使用频率最高的模式！</strong>所以一般情况下，你都应该尝试看是否适合使用它。</li><li>我们不希望把产品类的信息交给客户端（其实就是客户端知道的参数是和具体的实体对象有关的），而是希望客户端感知的只有专门用于创建该对象的工厂类。这样即创建封装在工厂层，使用封装在产品实体层。</li></ul><h2 id="抽象工厂模式"><a href="#抽象工厂模式" class="headerlink" title="抽象工厂模式"></a>抽象工厂模式</h2><p>上面工厂方法模式，会导致系统中<strong>存在大量的工厂类</strong>。我们可以将一些相关产品组成一个产品组，然后交给同一个工厂来统一生产。</p><p><strong>解决问题</strong></p><ul><li>添加新产品需要添加大量的类，如何减少系统类的个数。</li><li>同一种风格的产品，希望他们可以绑定在一起。</li></ul><h3 id="UML-2"><a href="#UML-2" class="headerlink" title="UML"></a>UML</h3><p><img src="D:\workspace\blog-docs\docs\设计模式\AbstractFactory.jpg" alt="AbstractFactory"></p><h3 id="原理-2"><a href="#原理-2" class="headerlink" title="原理"></a><strong>原理</strong></h3><p>先了解两个概念：</p><p><strong>产品等级：</strong>产品等级结构即产品的继承结构，如一个抽象类是电视机，其子类有海尔电视机、海信电视机、TCL电视机，则抽象电视机与具体品牌的电视机之间构成了一个产品等级结构，抽象电视机是父类，而具体品牌的电视机是其子类。</p><p><strong>产品族：</strong>在抽象工厂模式中，产品族是指由<strong>同一个工厂生产的</strong>，位于不同产品等级结构中的一组产品，如海尔电器工厂生产的海尔电视机、海尔电冰箱，海尔电视机位于电视机产品等级结构中，海尔电冰箱位于电冰箱产品等级结构中。海尔产的电视，电冰箱就是一个产品族。</p><p>上面两个概念看不懂我觉得也无所谓，没要扣字眼。<strong>理解的话，就是系统中有几类产品，同时有几种风格。每种风格都可以当做是一个工厂，这个工厂就生产该风格下的所有类的产品。</strong>（再举个例子：<code>UI</code>界面有明亮风格，暗黑风格等。<code>UI</code>组成有button，text框，文字等控件。那么抽象工厂就是相当于有一个暗黑工厂，这个工厂的工厂方法生产出的是一套控件，他们可以看做一个模板）。</p><p>抽象工厂模式相当于是对工厂方法模式的那些工厂类做进一步收缩。将一个系列的产品绑定在一起形成一个产品族。然后只要该产品族的工厂类就行了。</p><p><strong>基本要素</strong></p><ol><li>产品的抽象，可以是若干个。以及这些抽象的具体产品。 比如：<code>UI</code> 的按钮是抽象，红色按钮，黄色按钮是具体实现。</li><li>一个产品族的工厂抽象。以及具体实现。比如：按钮，文本框，选择框这三个当做一个产品族，工厂抽象就要有创建这三个的抽象方法。在一个族里面的产品需要有共同的约束，比如上面的那些控件都是依据不同<code>UI</code>会有不同变化的。</li></ol><h3 id="实现-2"><a href="#实现-2" class="headerlink" title="实现"></a><strong>实现</strong></h3><p><strong>产品族</strong></p><p>所有的类如果全的话就是：种类*风格 的个数</p><pre><code class="lang-java">//A1 A2 和 A 是不同的产品等级。 A是某一种产品 A1 A2 是该种产品的不同风格（不同厂家或者不同UI风格）interface Product_A &#123;&#125;//A1 B1 是产品族class ProductA1 implements Product_A &#123;&#125;class ProductA2 implements Product_A &#123;&#125;//....//B 同理interface Product_B &#123;&#125;class ProductB1 implements Product_B &#123;&#125;class ProductB2 implements Product_B &#123;&#125;</code></pre><p><strong>抽象工厂</strong></p><p>顶层抽象是包含了创建所有种类的抽象。</p><p>具体工厂的工厂方法是去创建属于该风格下的所有类型产品。</p><pre><code class="lang-java">// 产品族工厂抽象interface IAbstractFactory &#123;    Product_A createA();    Product_B createB();&#125;// 产品族为1的所有产品class Factory1Impl implements IAbstractFactory &#123;    @Override    public Product_A createA() &#123;        return new ProductA1();    &#125;    @Override    public Product_B createB() &#123;        return new ProductB1();    &#125;&#125;// 产品族为2的所有产品class Factory2Impl implements IAbstractFactory &#123;    @Override    public Product_A createA() &#123;        return new ProductA2();    &#125;    @Override    public Product_B createB() &#123;        return new ProductB2();    &#125;&#125;</code></pre><p>客户端</p><pre><code class="lang-java">public static void main(String[] args) &#123;    IAbstractFactory factory = new Factory1Impl();    Product_A a = factory.createA();    Product_B b = factory.createB();    a.printName();    b.printName();&#125;</code></pre><h3 id="优缺点-2"><a href="#优缺点-2" class="headerlink" title="优缺点"></a>优缺点</h3><h4 id="优点-2"><a href="#优点-2" class="headerlink" title="优点"></a>优点</h4><ul><li>减少了工厂方法模式中工厂类的个数：多个根据特征合成一个工厂。然后一个工厂来创建这些对象。</li><li>可以很方便的添加新的产品族</li></ul><h4 id="缺点-2"><a href="#缺点-2" class="headerlink" title="缺点"></a>缺点</h4><ul><li>添加新的产品很费劲，可能需要修改很多代码。</li></ul><h3 id="适用场景-2"><a href="#适用场景-2" class="headerlink" title="适用场景*"></a>适用场景*</h3><p>首先我们业务需要使用工厂模式来创建对象，然后我们这些对象还有一个特性：这些对象若干个可以做为一个整体的产品的模板。那么就可以考虑使用抽象工厂模式。</p><ul><li>UI换肤，组件布局都一样，只是每个小控件UI不一样，这个布局就是一个族。（<strong>这个模式很看重族的概念</strong>）</li><li>为不同操作系统生成程序。长得都一样，无非操作系统api层调用不同。</li></ul><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><h3 id="速记"><a href="#速记" class="headerlink" title="速记"></a>速记</h3><p>方便方便理清和记忆这些工厂模式，粗糙的总结下：</p><p>他们三个都是需要产品有抽象层。</p><ul><li>简单工厂<ul><li>一个工厂类，有个静态工厂方法。</li><li>工厂方法通过<code>if-else</code>或者<code>switch-case</code>来new对象。</li></ul></li><li>工厂方法<ul><li>对工厂进行抽闲，有很多工厂实现类，每个工厂实现类创建一个产品。</li><li>一个产品一个工厂类，每个工厂类对应一个产品，工厂方法new一个对象。</li></ul></li><li>抽象工厂<ul><li>将多个不同类型产品合入一个工厂类中构成一个套件或者模板，每个工厂实现类就是一套不同的套件，这个工厂实现类专门生产属于该风格下的各类产品。</li><li>一套风格对应一个工厂类，每个工厂类对应多个产品，提供多个new对象的工厂方法。</li></ul></li></ul><h1 id="附"><a href="#附" class="headerlink" title="附"></a>附</h1><p>相关代码：<a href="https://github.com/zhaohaoren/design_pattern_tutorial">https://github.com/zhaohaoren/design_pattern_tutorial</a></p><p>如有代码和文章问题，还请指正！感谢！</p>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;【设计模式】工厂模式&quot;&gt;&lt;a href=&quot;#【设计模式】工厂模式&quot; class=&quot;headerlink&quot;</summary>
        
      
    
    
    
    
    <category term="设计模式" scheme="http://lollipops.work/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>【设计模式】建造者模式</title>
    <link href="http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E5%BB%BA%E9%80%A0%E8%80%85%E6%A8%A1%E5%BC%8F/"/>
    <id>http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E5%BB%BA%E9%80%A0%E8%80%85%E6%A8%A1%E5%BC%8F/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2021-01-16T09:08:23.974Z</updated>
    
    <content type="html"><![CDATA[<h1 id="【设计模式】建造者模式"><a href="#【设计模式】建造者模式" class="headerlink" title="【设计模式】建造者模式"></a>【设计模式】建造者模式</h1><p>建造者模式主要针对复杂对象的创建，是一种比较复杂的<strong>对象创建型模式</strong>。本文主要介绍：建造者模式带Director实现，无Director实现，Lombok实现、</p><h2 id="模式背景"><a href="#模式背景" class="headerlink" title="模式背景"></a>模式背景</h2><p>我们为什么要有建造者模式呢？在我们项目中可能存在一些比较复杂的对象的创建，比如一个对象里面组合了大量的其他的对象，如果将创建这些对象的代码放在使用方来编写。那么所有使用到该对象的地方都要编写巨冗长的代码，这显然不够优雅！本着公共的重复的代码就应该抽取在同一个地方的原则，我们应该将创建这个对象的职责抽取出来，交给一个专门的类来做！所以创建者模式就是为此而生的。</p><p>我们使用构建者模式可以将创建的逻辑放在一个对应的Builder类里面。比如我们现在前端需要一个返回的VO对象。这个VO对象需要根据浏览器还是移动端等情况，返回不同的结构类型，但是整体大概都是相同的一个大的VO对象。这时候我们就可以创建2个Builder：一个针对电脑的Builder，一个针对移动端的Builder。这样我们只需要编写好对应的Builder以后使用的时候就可以直接复用。</p><h2 id="UML"><a href="#UML" class="headerlink" title="UML"></a>UML</h2><p><img src="D:\workspace\blog-docs\docs\设计模式\Builder.jpg" alt="Builder"></p><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>构建者模式，即将创建一个复杂对象的逻辑抽取到Builder类中进行，将一个复杂的对象的构建和他的表示分离。</p><p>又因为这个对象可能需要多个表示，所以我们需要对这些Builder进行抽象，使得可以扩展Builder实现类来完成不同的表示。</p><p>又因为不同Builder的实现类，只是负责复杂对象各个部分的具体实现，我们可能需要对复杂对象进行装配的时候的次序有要求：比如先装<code>Part2</code>再装<code>Part1</code>，又或者说我们某些情况不需要装配<code>Part2</code>了，所以这时候需要提供一个Director类来配置装配秩序。</p><p>客户端只需要和Director进行交互。</p><blockquote><p>当然我个人遇到的大部分情况，都可以将Director去掉，构建的过程都放在Builder实现类中即可。</p><p>但是这个是针对构建次序要求不多的情况的（很多时候我们只需要装配好就行，并没有次序要求），如果装配次序要求的多，那么每次新的次序要求的时候，我们就要修改Builder类，这样就违背了开闭原则。</p><p>建造者模式和抽象工厂模式有点相似：都是按照一个模板来构建表示的对象。区别是建造者模式是构建一个大的复杂对象整体作为返回，抽象工厂则是返回一系列的零件对象。（返回一个车和返回车胎，车灯等的区别）</p></blockquote><p><strong>必要条件</strong></p><ol><li>需要被构建的实体类（一般是复杂的实体对象）。</li><li>抽象Builder<ul><li>接口或者抽象类；</li><li>让各种Builder统一约束。申明<code>buildPartX</code>方法以及<code>getResult</code>方法。</li></ul></li><li>Builder实现类。<ul><li>为各个<code>Part</code>提供具体的实现逻辑。没一个Builder实现类都应当对应着一个业务需求Bean。</li></ul></li><li>一个构建器<ul><li>目的1：让build的过程和客户端进行隔离。</li><li>目的2：真正的控制复杂对象的构建逻辑，Builder只是把方法都确定好了，Director负责调用来装配。</li></ul></li></ol><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><p>实体类（我们这里以一个汽车为例子）</p><pre><code class="lang-java">public class Car &#123;    private String light;    private String wheel;    private String chair;&#125;</code></pre><p>构建抽象类</p><pre><code class="lang-java">public abstract class CarBuilder &#123;    /**    我们将Car对象在这里创建，实现类继承就好了。    为什么Car对象在Builder中创建，我想是为了和Director进行隔离吧。因为从对象-&gt;Builder-&gt;Director中间有层次关系，尽量将每一层的隔离做好。     */    Car car = new Car();    public abstract void buildLight();    public abstract void buildWheel();    public abstract void buildChair();    /**这个可以是一个静态方法*/    public Car createCar() &#123;        return car;    &#125;&#125;</code></pre><p>具体Builder实现</p><pre><code class="lang-java">public class BigCarBuilder extends CarBuilder &#123;    @Override    public void buildLight() &#123;        this.car.setLight(&quot;big light&quot;);    &#125;    @Override    public void buildWheel() &#123;        this.car.setWheel(&quot;big wheel&quot;);    &#125;    @Override    public void buildChair() &#123;        this.car.setChair(&quot;big chair&quot;);    &#125;&#125;</code></pre><p>指挥类</p><pre><code class="lang-java">public class Director &#123;   /** 构造者模式第二个关键点：     * 指挥类里面内持一个builder，让客户端只在乎使用哪个builder来构建就行。     * 通过这样客户端就完全只需要知道自己用哪个builder就行了，然后初始化好builder直接往指挥类里面塞。 */    public CarBuilder builder;    public Director(CarBuilder builder) &#123;        this.builder = builder;    &#125;    public CarBuilder getBuilder() &#123;        return builder;    &#125;    /**构建对象*/    public Car build() &#123;        builder.buildLight();        builder.buildWheel();        builder.buildChair();        return builder.createCar();    &#125;&#125;</code></pre><p>使用</p><pre><code class="lang-java">CarBuilder builder = new BigCarBuilder();//具体使用哪个builder来构造对象，可以使用配置文件来配置，增加灵活度。Director director = new Director(builder);Car car = director.build();System.out.println(car);</code></pre><h3 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h3><ul><li>优点<ul><li>职责拆分细，扩展性相对就高些。</li></ul></li><li>缺点<ul><li>结构复杂，代码量大，你是不是也觉得为了创建一个对象都一下子就延伸了2个步骤很繁琐。</li></ul></li></ul><blockquote><p>就是上面全部必要条件的实现。如果构建一个对象很复杂，还是推荐使用该方式进行结构设计。</p></blockquote><h3 id="无Director实现"><a href="#无Director实现" class="headerlink" title="无Director实现"></a>无Director实现</h3><p>之前说过，在某些情况下，我们可以将Director类和Builder进行融合，将Director的职责交给Builder，构建的过程让Builder自己也同时完成。</p><p><strong>实现</strong></p><pre><code class="lang-java">abstract class CarBuilder2 &#123;    protected Car car = new Car();    public abstract void buildLight();    public abstract void buildWheel();    public abstract void buildChair();      /*builder自己来构建*/    public Car build() &#123;        this.buildWheel();        this.buildChair();        this.buildLight();        return car;    &#125;&#125;public class NoDirectorCarBuilder extends CarBuilder2 &#123;    @Override    public void buildLight() &#123;        this.car.setLight(&quot;no director light&quot;);    &#125;    @Override    public void buildWheel() &#123;        this.car.setWheel(&quot;no director wheel&quot;);    &#125;    @Override    public void buildChair() &#123;        this.car.setChair(&quot;no director chair&quot;);    &#125;    /*验证*/    public static void main(String[] args) &#123;        NoDirectorCarBuilder builder = new NoDirectorCarBuilder();        Car car = builder.build();        System.out.println(car);    &#125;&#125;</code></pre><blockquote><p>这么做简化了 Director类，但是也将职责全交给了Builder，这也必然会加重了Builder的职责。</p><p>但是当一个对象要求十分复杂的时候，还是推荐使用Director的方式来进行构建。 扩展性更高，也更加符合单一职责原则。</p></blockquote><h3 id="Lombok实现"><a href="#Lombok实现" class="headerlink" title="Lombok实现"></a>Lombok实现</h3><p>Java开发中经常使用的插件：<code>lombok</code> 中有一个<code>@Builder</code>注解用来很方便，他也是利用了构建者模式。使用这个注解来创建对象还是很方便的。其内部实现方式如下：</p><pre><code class="lang-java">class Person &#123;    private String name;    private String age;    /*lombok开始*/    /**     * 关键4 构建对象     */    public static PersonBuilder bulder() &#123;        return new PersonBuilder();    &#125;    /**     * 关键1 构造     */    public Person(PersonBuilder builder) &#123;        this.name = builder.name;        this.age = builder.age;    &#125;    /**     * 关键2 构建类     */    public static class PersonBuilder &#123;        /**         * 关键3 属性copy         */        private String name;        private String age;        public PersonBuilder name(String name) &#123;            this.name = name;            return this;        &#125;        public PersonBuilder age(String age) &#123;            this.age = age;            return this;        &#125;        public Person build() &#123;            return new Person(this);        &#125;    &#125;    public static void main(String[] args) &#123;        Person p = Person.bulder().name(&quot;justin&quot;).age(&quot;24&quot;).build();        System.out.println(p.getName());    &#125;&#125;</code></pre><p>其内部会为当前类创建一个Builder类，该类拥有和原始类同样的属性，我们通过builder来对属性就行赋值。最后通过builder的对象来构造原始类的对象。</p><h2 id="优缺点-1"><a href="#优缺点-1" class="headerlink" title="优缺点"></a>优缺点</h2><p>整体来看，构建者模式的优缺点如下：</p><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul><li>对象的创建和表示分离，符合解耦的思想。</li><li>每个构建者相互独立，可以动态的添加或者替换构建者来创建不同的表示，扩展性高。</li><li>一定程度上，可以让我们更清晰的了解复杂对象的创建逻辑（只需要盯准Director类就行了）。</li></ul><h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul><li>适用性是那种大致相同，只是各个组成部分都有自己不同的表示的。如果差异太大不适合该模式。</li><li>如果对复杂对象需求变化太多，会增加很多构建类，增加了系统复杂度。</li></ul><h2 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h2><ol><li>构建复杂的对象，一个对象内部包含了很多其他的对象。</li><li>复杂对象对外的表示需要能扩展，很有可能产品会对这个复杂对象的表示提出新需求。</li><li>生成的复杂对象，对象创建过程中，内部的对象有相互依赖的关系，使用Director可以很好的控制和让我们理解这些顺序。</li></ol><h1 id="附"><a href="#附" class="headerlink" title="附"></a>附</h1><p>相关代码：<a href="https://github.com/zhaohaoren/design_pattern_tutorial">https://github.com/zhaohaoren/design_pattern_tutorial</a></p><p>如有代码和文章问题，还请指正！感谢！</p>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;【设计模式】建造者模式&quot;&gt;&lt;a href=&quot;#【设计模式】建造者模式&quot; class=&quot;headerlink&quot;</summary>
        
      
    
    
    
    
    <category term="设计模式" scheme="http://lollipops.work/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>【设计模式】序</title>
    <link href="http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E5%BA%8F/"/>
    <id>http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E5%BA%8F/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2021-01-16T09:16:36.427Z</updated>
    
    <content type="html"><![CDATA[<h1 id="【设计模式】序"><a href="#【设计模式】序" class="headerlink" title="【设计模式】序"></a>【设计模式】序</h1><p>在豆瓣上搜索了一下评价最好的一本书了</p><p><img src="D:\workspace\blog-docs\docs\设计模式\begin.png" alt="image-20200806211011660"></p><p>下面文章主要从这本书出发，通过自己的实践和复习以及再参考一些其他的数据，去不断的丰富它。</p><p>设计模式好多乱七八糟的概念定义，这些有些好记，有些记不住，很多人对设计模式的感觉：一看就动，一用就想不起来。我先简单谈一谈在学习过程中我对设计模式的一些理解：</p><h4 id="1-模式灵活多变"><a href="#1-模式灵活多变" class="headerlink" title="1. 模式灵活多变"></a>1. 模式灵活多变</h4><p>没有说书上的模式是最好的，所有的模式都是服务于场景。永远选择你场景下你觉得最适合的你模式。也就是说，你如果做抽象，可能没必要做到设计模式书上那么高的抽象，你可以适当的降低抽象层。这就和数据库范式设计一样，适当的时候反范式也是必要的，只要对系统结构优化达到目标了就行。模式是方便你自己的，但是代码也是写给人看的，太高的抽象很多时候没必要，也让别人不好理解。</p><h4 id="2-勤设计，勤反思"><a href="#2-勤设计，勤反思" class="headerlink" title="2. 勤设计，勤反思"></a>2. 勤设计，勤反思</h4><p>我们很容易就陷入过度设计的泥坑，但是人总是想整点东西上去，所以每次我们对一个模块进行重构之后，自己再梳理一遍：我这么改了之后，对整个系统或者模块是否有了改进？方便了我们的开发吗？有益了代码的维护吗？如果没有那么大可不必，如果还变坏了那么就可能是过度设计。</p><h4 id="3-思维不要太僵化于固定模式"><a href="#3-思维不要太僵化于固定模式" class="headerlink" title="3. 思维不要太僵化于固定模式"></a>3. 思维不要太僵化于固定模式</h4><p>有些场景下我们会立刻想到某个设计模式，他们完美的契合。但是大部分场景可能找不到对应的设计模式，亦或者是你一时间没想起来。需要认清，所有（大部分吧）的设计模式也都是为了解决系统中存在的不合理的点，也就是你觉得很复杂，写的很费劲的地方。而设计模式的大部分做法就是进行抽象然后试着剥离解耦。其实我们完全可以不用将思想僵化在书上固定的设计模式里面，我们可以按照自己的思维对系统进行抽象解耦，也能得到比较好的效果（只要对系统提升一点点那就是有帮助的）。</p><h4 id="4-设计模式并不高深"><a href="#4-设计模式并不高深" class="headerlink" title="4. 设计模式并不高深"></a>4. 设计模式并不高深</h4><p>我们只要懂一些基础的原则性的东西：比如开闭原则，里式替换等。更粗糙点讲：<strong>如果想要扩展就尝试抽象一层，如果想要复用能用关联组合就不要使用继承！</strong> 心中有了这些基本的原则，随便一写程序，里面多多少少都已经有了某种模式的味道在里面了，只是可能不标准。但是一贯本着最适合的才是最好的原则，有时候这就已经够了。</p><h4 id="5-设计模式需要背"><a href="#5-设计模式需要背" class="headerlink" title="5. 设计模式需要背"></a>5. 设计模式需要背</h4><p>在早期，去背背设计模式吧！背上了你脑子里有了拓扑结构图，遇到场景的时候就能很快想出来。当然对于大神来说，天天玩设计模式，他们可能不需要背了，但是他们其实无形中已经肌肉记忆了，唯熟而。但是你不一定能做到，这需要天时地利人和。</p><p>（这一点可能很多人会觉得我在扯淡！而且个人感觉当程序员的都天命不凡，不喜欢死记硬背，我觉得这是一个误区！这个误区在你记忆力是惊世之才的时候，可能不存在，但可惜的绝大多数人都是普通人。这也是你为什么高中有些古诗词张口就来，让你面试说个知识点磕磕绊绊的，支支吾吾讲不清楚。诚然，理解很重要，但理解了再去背诵记忆会让你更上一层楼。不然知识点在你脑子里永远都是浆糊一样的概念，时间一长就忘了。）</p><h4 id="6-平时开发中多想想抽象"><a href="#6-平时开发中多想想抽象" class="headerlink" title="6. 平时开发中多想想抽象"></a>6. 平时开发中多想想抽象</h4><p>虽然我们可能大部分都是CURDers，但是产品的需求总是不断的在变化的，这种变不就是需要高的扩展性吗？在开发一个功能之前，我们应该多思考，哪些地方产品会脑子抽抽的来回改动，这种地方就需要多去抽象来实现后期的高扩展性。</p><h4 id="7-看源码注重其设计模式"><a href="#7-看源码注重其设计模式" class="headerlink" title="7. 看源码注重其设计模式"></a>7. 看源码注重其设计模式</h4><p>不懂设计模式，不看源码。这倒是真话！不带着设计模式的思想去看源码，那就是去看调用逻辑的，那看源码看不到精髓。如果看源码是为了学习的话，我觉得应该注重2点：1. 算法及实现思想（比如实现某个问题的方案） 2. 程序结构设计（就是设计模式了）。而不是纯粹的去追溯逻辑，这个类调那个类，那个类又调了那个类，这么看源码应该叫做人肉Debug吧。 </p><blockquote><p>TODO：我的文章还缺最重点的一个环节，就是以实际案例或者源码来解释模式的用法已经使用都带来的具体好处。</p></blockquote>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;【设计模式】序&quot;&gt;&lt;a href=&quot;#【设计模式】序&quot; class=&quot;headerlink&quot; title=&quot;【设计模式】序&quot;&gt;&lt;/a&gt;【设计模式】序&lt;/h1&gt;&lt;p&gt;在豆瓣上搜索了一下评价最好的一本书了&lt;/p&gt;
&lt;p&gt;&lt;img</summary>
        
      
    
    
    
    
    <category term="设计模式" scheme="http://lollipops.work/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>【设计模式】桥接模式</title>
    <link href="http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E6%A1%A5%E6%8E%A5%E6%A8%A1%E5%BC%8F/"/>
    <id>http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E6%A1%A5%E6%8E%A5%E6%A8%A1%E5%BC%8F/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2021-01-16T09:08:27.529Z</updated>
    
    <content type="html"><![CDATA[<h1 id="【设计模式】桥接模式"><a href="#【设计模式】桥接模式" class="headerlink" title="【设计模式】桥接模式"></a>【设计模式】桥接模式</h1><p>本文主要介绍：桥接模式概念和用法。</p><h2 id="模式背景"><a href="#模式背景" class="headerlink" title="模式背景"></a>模式背景</h2><p>生活中，我们如果一个笔有三种型号：大，中，小。颜色一共有M种。如果需要所有型号都有相对应的颜色，那么就需要3*M只笔。但是如果我们转换一下：M种颜色代表M种墨水，笔的三种型号就对应三支笔，那么只需要3+M个，然后组合就可以得到相同的结果，这种思维模式就是桥接。</p><p>应用到项目中，当系统中一些类存在多个独立变化的维度（注意2点：<strong>独立</strong>，<strong>变化维度</strong>），我们可以使用桥接模式来大大减少系统的中的类。如果不使用桥接模式，一般来说我们会形成一个庞大的继承系统（见下面的图2），这些变化的维度将耦合在一起，对于扩展什么的也会极为不便（无论是我们现在需要增加一个颜色，还是增加一个笔型号，势必都要添加大量的类）。</p><h2 id="定义-amp-概念"><a href="#定义-amp-概念" class="headerlink" title="定义&amp;概念"></a>定义&amp;概念</h2><p>桥接模式(Bridge Pattern)：将抽象部分与它的实现部分分离，使它们都可以独立地变化。它是一种对象结构型模式，又称为柄体(Handle and Body)模式或接口(Interface)模式。</p><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>如上面提到的，桥接模式需要先能分出系统中那些<strong>独立变化的维度</strong>，然后我们再进行分离。桥接模式的思想就是<strong>如何进行分离的过程</strong>。</p><p>所有设计模式的思想其实都希望我们更多的<strong>去利用组合，而不是继承</strong>。所以桥接模式的主要思想就是：<strong>将变化的维度抽象为不同的继承体系</strong>，每一个维度是自己的一个继承体系，然后通过组合将所需要的这些变化维度拼接为最后的对象。这维度和维度之间的联系组合我们把它称为桥。如下图Color和Pen之间就是一座桥，<strong>这个桥所连接是两个继承体系</strong>。两个继承体系独立变化。<strong>桥接模式主要是用来处理多维度变化的。</strong></p><p><strong>组成要素</strong></p><ul><li>抽象类（Abstraction）<ul><li>将原来的那个设计多维度的变化的对象。这个抽象类主要担当接口的作用！用来多态其实现类的。</li></ul></li><li>抽象类的扩充（RefinedAbstraction）<ul><li>也可以理解为抽象类的具体实现类。他相当于就完成各种变化的最终组合的实现类，组合方式就是调用桥的另一个组合对象来动态完成组合的。我们可以将其中一种变化放入到该抽象类实现中去。比如上面Pen的大小。这样Pen内部通过多态的Color来组合不同的情况。</li></ul></li><li>实现类接口（Implementor）<ul><li>定义实现类的接口，是桥的另一端的继承体系的祖宗。只封装一个变化。</li></ul></li><li>实现类的具体实现类（ConcreteImplementor）<ul><li>就是该维度变化的各种情况的实现。</li></ul></li></ul><h2 id="UML"><a href="#UML" class="headerlink" title="UML"></a>UML</h2><p><strong>生活模型</strong></p><ul><li>如果不使用桥接模式，就可能形成这样的继承体系。</li></ul><p><img src="/Users/zhaohaoren/workspace/mycode/blog-docs/docs/设计模式/bridge_bad.png" alt="bridge_bad"></p><ul><li>使用了桥接模式</li></ul><p><img src="/Users/zhaohaoren/workspace/mycode/blog-docs/docs/设计模式/bridge_pen.png" alt="bridge_pen"></p><p><strong>系统模型</strong></p><p><img src="/Users/zhaohaoren/workspace/mycode/blog-docs/docs/设计模式/Bridge.png" alt="image-20200818113435860"></p><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><pre><code class="lang-java">/** * 抽象类 */abstract class Pen &#123;    Color color;    public void setColor(Color color) &#123;        this.color = color;    &#125;    abstract void size();&#125;/** * 某个变化维度的接口 */interface Color &#123;    void color();&#125;/** * 变化维度接口的实现 */class Red implements Color &#123;    @Override    public void color() &#123;        System.out.println(&quot;red&quot;);    &#125;&#125;class Black implements Color &#123;    @Override    public void color() &#123;        System.out.println(&quot;black&quot;);    &#125;&#125;class Blue implements Color &#123;    @Override    public void color() &#123;        System.out.println(&quot;blue&quot;);    &#125;&#125;/** * 抽象类的具体实现 */class Pen1 extends Pen &#123;    @Override    void size() &#123;        color.color();        System.out.println(&quot;size:1&quot;);    &#125;&#125;class Pen2 extends Pen &#123;    @Override    void size() &#123;        color.color();        System.out.println(&quot;size:2&quot;);    &#125;&#125;</code></pre><p>使用</p><pre><code class="lang-java">Pen p = new Pen1();Color c = new Red();p.setColor(c);p.size();</code></pre><h2 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h2><p>整体来看，桥接模式的优缺点如下：</p><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul><li>解耦了继承体系。</li><li>分离抽象接口及其实现部分。提高了比继承更好的解决方案。</li><li>提高系统的可扩充性，在两个变化维度中任意扩展一个维度，都不需要修改原代码。</li></ul><h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul><li>增加系统的理解与设计难度，由于聚合关联关系建立在抽象层，要求开发者针对抽象进行设计与编程。</li><li>要求正确识别出系统中变化的维度，这让其使用范围具有一定的局限性。</li></ul><h2 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h2><ol><li>存在着独立的变化维度，且这些维度都需要进行扩展。</li><li>当我们系统中使用继承的时候，如果发现继承体系是可以继续拆分的，那么就使用桥接进行拆分。</li><li>是实现java虚拟机和jdbc这些程序的核心设计模式之一。</li><li>最好的就是和笔+油墨这类性质问题，那么就使用桥接模式。</li></ol><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ol><li>系统存在多个相互独立的变化维度，使用桥接第一步就是先分清这些维度。</li><li>每个维度一个继承体系，比如笔是一个继承体系，颜色是一个继承体系。最后使用组合拼成最终对象。</li><li>当系统中有一个继承体系庞大，你可以考虑看看这个继承是否可以按照维度进行拆分，然后使用桥接来组合。</li><li>桥接模式不是一个复杂的模式，<strong>他的工作就2个：按维度建立继承体系，将不同体系连一起（称为桥接）。</strong></li></ol><h2 id="扩展"><a href="#扩展" class="headerlink" title="扩展"></a>扩展</h2><h3 id="和适配器模式连用"><a href="#和适配器模式连用" class="headerlink" title="和适配器模式连用"></a>和适配器模式连用</h3><p><strong>桥接模式是设计初期的，适配器模式是可能开发中期的</strong>。他们位于系统设计的不同阶段。当我们发现现有的类和我们正在开发的系统无法协同工作就可以使用适配器模式。而所适配的可能就是桥接的某个维度接口的某个功能实现。比如：一个系统的数据采集可以有文本，csv，数据库，我们有现成的数据库的连接库，这时候就要一个适配器来适配这个库，并实现数据采集的接口来实现桥接。</p><h1 id="附"><a href="#附" class="headerlink" title="附"></a>附</h1><p>相关代码：<a href="https://github.com/zhaohaoren/design_pattern_tutorial">https://github.com/zhaohaoren/design_pattern_tutorial</a></p><p>如有代码和文章问题，还请指正！感谢！</p>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;【设计模式】桥接模式&quot;&gt;&lt;a href=&quot;#【设计模式】桥接模式&quot; class=&quot;headerlink&quot; title=&quot;【设计模式】桥接模式&quot;&gt;&lt;/a&gt;【设计模式】桥接模式&lt;/h1&gt;&lt;p&gt;本文主要介绍：桥接模式概念和用法。&lt;/p&gt;
&lt;h2</summary>
        
      
    
    
    
    
    <category term="设计模式" scheme="http://lollipops.work/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>【设计模式】模板方法模式</title>
    <link href="http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E6%A8%A1%E6%9D%BF%E6%96%B9%E6%B3%95%E6%A8%A1%E5%BC%8F/"/>
    <id>http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E6%A8%A1%E6%9D%BF%E6%96%B9%E6%B3%95%E6%A8%A1%E5%BC%8F/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2021-01-16T09:08:40.833Z</updated>
    
    <content type="html"><![CDATA[<h1 id="【设计模式】模板方法模式"><a href="#【设计模式】模板方法模式" class="headerlink" title="【设计模式】模板方法模式"></a>【设计模式】模板方法模式</h1><p>本文主要介绍模板方法模式的概念和用法。</p><h2 id="模式背景"><a href="#模式背景" class="headerlink" title="模式背景"></a>模式背景</h2><p>生活中，我们自己装主机，我们确定好机架后，里面的具体的CPU，内存条，电源，主板等等配件，就可以使用不同的方案去配置这个电脑主机了。这里面其实就是和模板方法模式相同的思想：机架也是可以有好多种的，那么每一种机架就相当于是一个模板。每一个机架可以有几套配件方案来进行配置，那么就要求，我们设计的这个模板的订制性很高，我们可以任意替换里面的配件，就是一个新构成的主机。</p><p>在平时的系统设计中也有大量这种类似的场景：我们实现某个功能的时候，需要多个步骤，这些步骤可能是固定的，也可能是不固定的（固定如：我必须装CPU和主板，不固定比如：我可以安装也可以不安装DVD机，我们可以装i7的CPU也可以装i5的等）。如果我们对于每种方案都单独写一个类，类中提供各个步骤的具体实现，存在2个问题：</p><ol><li>其中一个步骤的具体实现需要扩展，我们可能需要修改源代码去添加新的逻辑，这违背开闭原则（但是我们一般都不会这么做，直接继承就好了）。</li><li>系统中有很多地方对这些步骤的执行顺序是有要求的，那么就需要客户端来自己控制这个顺序的准确性，客户端需要按照我们想要的顺序来，才会是正确的结果。这没有强约束性，我们无法保证客户端能永远使用正确，并且可能存在大量的执行这些步骤的重复性代码。</li></ol><p>面对这些场景如何提高代码的复用性和系统的灵活性，就是模板方法模式需要解决的。</p><h2 id="定义-amp-概念"><a href="#定义-amp-概念" class="headerlink" title="定义&amp;概念"></a>定义&amp;概念</h2><p>它定义一个操作中的算法的框架，而将一些步骤延迟到了子类中。使得子类可以不改变一个算法的结构即可重定义该算法的某些步骤。是一种类行为模式。</p><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>首先，模板方法模式是一个<strong>结构很简单的设计模式</strong>。他<strong>只有一个继承结构</strong>，主要利用的就是多态。</p><p>然后，在使用模板模式前<strong>要梳理清楚系统中的2种方法</strong>：</p><ul><li><strong>基本方法（Primitive Operation）</strong>：那些会变的，执行步骤对应的具体实现。<ul><li>基本方法又可以分为三种：<ul><li>抽象方法(Abstract Method)：一个抽象方法由抽象类声明，由具体子类实现。在Java语言里抽象方法以abstract关键字标示。【抽象方法】</li><li>具体方法(Concrete Method)：一个具体方法由抽象类声明并实现，而子类并不实现或置换。【实现了的方法】</li><li>钩子方法(Hook Method)：一个钩子方法由抽象类声明并实现，而子类会加以扩展。通常抽象类给出的<strong>实现是一个空实现</strong>，作为方法的默认实现。【实现了的方法，但实现是空的，还可以是一个返回boolean的函数，见下文】</li></ul></li></ul></li><li><strong>模板方法（Template method）</strong>：相当于那个标准的流程，定义基本方法的执行次序的一个方法。</li></ul><p>模板方法的思想就是对于某一个固定的执行次序，<strong>定义一个抽象模板类</strong>；这些次序的<strong>具体执行让这个模板类的子类去实现</strong>。抽象模板类中<strong>定义一个模板方法</strong>来指定执行次序（一般不希望子类去使用它，所以<code>Java</code>中一般定义为<code>final</code>方法）。</p><p><strong>组成要素</strong></p><ul><li>抽象类：其实就是一个抽象模板，定义并<strong>实现了一个模板方法</strong>。这个模板方法一般是一个具体的方法，它给出了一个顶级逻辑的骨架。而逻辑的组成步骤在相应的抽象操作中，推迟到子类实现。<strong>顶级逻辑也有可能调用一些具体方法</strong>。</li><li>具体子类：实现父类所定义的一个或多个抽象方法。每一个抽象类都可以有任意多个具体子类与之对应，而每一个具体子类都可以给出这些抽象方法（也就是顶级逻辑的组成步骤）的不同实现，从而使得顶级逻辑的实现各不相同。</li></ul><h3 id="钩子方法"><a href="#钩子方法" class="headerlink" title="钩子方法"></a>钩子方法</h3><p>引入钩子方法主要就是让我们可以通过子类可以控制父类的行为，我们通过覆盖父类的一些方法，因为多态让父类引用子类对象的时候，使用子类覆盖的方法。</p><p>钩子方法我们写钩子方法可以有2类：</p><ul><li>返回boolean类型，方法名一般是<code>isXXX()</code>。这样我们可以在模板方法中通过这个钩子方法判断来决定执行的逻辑，</li><li>一个空实现的方法。这样我们父类模板方法也可以使用父类的默认基本方法，空的相当于执行时直接跳过，一旦我们想对该点插入逻辑，子类提供实现即可。</li></ul><h2 id="UML"><a href="#UML" class="headerlink" title="UML"></a>UML</h2><p><img src="D:\workspace\blog-docs\docs\设计模式\TemplateMethod.png" alt="image-20200912003959587"></p><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><p>抽象类</p><pre><code class="lang-java">public abstract class AbstractClass &#123;    /**     * 模板方法     */    public final void templateMethod() &#123;        op1();        op2();        op3();    &#125;    /*     * 基本方法     */    /**     * 基本具体方法     */    public void op1() &#123;        System.out.println(&quot;op1&quot;);    &#125;    /**     * 基本抽象方法     */    public abstract void op2();    /**     * 钩子方法     */    public void op3() &#123;    &#125;&#125;</code></pre><p>具体子类</p><pre><code class="lang-java">public class ConcreteClass extends AbstractClass &#123;    @Override    public void op2() &#123;        System.out.println(&quot;sub op2&quot;);    &#125;    @Override    public void op3() &#123;        System.out.println(&quot;sub op3&quot;);    &#125;&#125;</code></pre><p>使用</p><pre><code class="lang-java">AbstractClass abstractClass = new ConcreteClass();abstractClass.templateMethod();</code></pre><h2 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h2><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul><li>通过一个父类调用子类实现的操作，通过子类扩展增加新的行为，不同的子类可以提供不同的实现，符合开放-封闭和单一职责原则。</li><li>模板方法模式通过把不变的行为搬移到父类，去除了子类中的重复代码，也无需客户端去实现具体的模板逻辑，减少了代码量，提高了代码复用。</li></ul><h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul><li>父类中可变的基本方法太多，会导致系统类剧增，此时可以考虑结合使用桥接模式。</li></ul><h2 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h2><ul><li>当一个父类的多个子类有共有的行为的场景时。</li><li>当父类需要具体子类实现来决定某个步骤是否需要执行的时候。</li><li>重要、复杂的算法，可以把核心算法设计为模板方法，周边的相关细节功能则由各个子类实现。</li></ul><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>这个模式其实不用学，在开发自己去设计设计也能想到。唯一有点复杂的点就是子类和抽象父类之间的关系上，其实也没那么复杂，实际使用中，依据情况选择哪些方法上提至抽象层，哪些方法子类来自己实现。</p><h1 id="附"><a href="#附" class="headerlink" title="附"></a>附</h1><p>相关代码：<a href="https://github.com/zhaohaoren/design_pattern_tutorial">https://github.com/zhaohaoren/design_pattern_tutorial</a></p><p>如有代码和文章问题，还请指正！感谢！</p>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;【设计模式】模板方法模式&quot;&gt;&lt;a href=&quot;#【设计模式】模板方法模式&quot; class=&quot;headerlink&quot; title=&quot;【设计模式】模板方法模式&quot;&gt;&lt;/a&gt;【设计模式】模板方法模式&lt;/h1&gt;&lt;p&gt;本文主要介绍模板方法模式的概念和用法。&lt;/p&gt;
&lt;h2</summary>
        
      
    
    
    
    
    <category term="设计模式" scheme="http://lollipops.work/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>【设计模式】策略模式</title>
    <link href="http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E7%AD%96%E7%95%A5%E6%A8%A1%E5%BC%8F/"/>
    <id>http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E7%AD%96%E7%95%A5%E6%A8%A1%E5%BC%8F/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2021-01-16T09:08:49.601Z</updated>
    
    <content type="html"><![CDATA[<h1 id="【设计模式】策略模式"><a href="#【设计模式】策略模式" class="headerlink" title="【设计模式】策略模式"></a>【设计模式】策略模式</h1><p>本文主要介绍策略模式的概念和用法。</p><h2 id="模式背景"><a href="#模式背景" class="headerlink" title="模式背景"></a>模式背景</h2><p>当实现某一个功能有多条途径，每种途径都对应一种逻辑或者算法，如何灵活的来实现途径的选择，就可以使用策略模式。</p><p>比如我们使用地图导航软件去某地的时候，有公交路线，自行车路线，地铁，步行等路线。每一个路线都对应着一个执行策略，这些策略都有用，在不同的场景下我们会选择不同的策略。所以如何去管理和使用这些策略是策略模式要解决的问题。我们总不能使用<code>if-else</code>啥的全塞在一个地方去判断。那是面向过程的写法。</p><h2 id="定义-amp-概念"><a href="#定义-amp-概念" class="headerlink" title="定义&amp;概念"></a>定义&amp;概念</h2><p><strong>策略模式（Strategy）：</strong>定义一系列算法，将每一个算法都封装起来，并让他们可以互相替换。策略模式是一种对象行为型模式。</p><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>策略模式是一种<strong>很简单且容易使用</strong>的设计模式。策略模式主要是对算法的封装，让不同的算法委托给不同的对象去管理。首先我们肯定希望<strong>算法可以扩展</strong>，然后希望<strong>客户端对算法的使用可以松耦合</strong>。所以策略模式响应的提供了2个元素：<strong>Context类</strong>，<strong>抽象策略类</strong>。</p><p><strong>组成要素</strong></p><ul><li>环境类（Context）<ul><li>用来操作策略的上下文环境。它是客户端和具体策略之间的解耦层，它内部引用了一个抽象策略，客户端通过他来执行具体的策略。</li><li>之所以需要这个Context，还因为有<strong>可能我们执行一个具体的逻辑，是需要多个策略来配合的</strong>，这样这多个策略的执行逻辑，就可以放在Context类中，而不是耦合在客户端代码中。</li></ul></li><li>抽象策略类（Strategy）<ul><li>策略的抽象，就是为了方便扩展策略类，以及外部可以面向抽象编程。</li></ul></li><li>具体策略类（ConcreteStrategy）<ul><li>具体的策略实现。</li></ul></li></ul><p>对于<strong>如何指定策略</strong>，之所以需要将不同的策略拆分到不同的类中，就是可以让我们通过<strong>使用外部配置文件</strong>的方式，配置指定的策略。然后<strong>利用Java的反射和多态特性动态为其创建具体的策略类对象</strong>。</p><h2 id="UML"><a href="#UML" class="headerlink" title="UML"></a>UML</h2><p><img src="/Users/zhaohaoren/workspace/mycode/blog-docs/docs/设计模式/Strategy.png" alt="image-20200910112739914"></p><p>乍一看，我靠！这UML不和状态模式一德行吗？确实UML上是那么回事，他们本身解决问题思想也很相近，只是他们的服务目标不同，策略模式更重策略，状态模式更重状态，我个人感觉状态模式应该是策略模式的一种特殊情况（真的就只是个人感觉哈）。</p><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><p>抽象策略类</p><pre><code class="lang-java">public interface Strategy &#123;    void algorithm();&#125;</code></pre><p>具体策略类</p><pre><code class="lang-java">public class ConcreteStrategyA implements Strategy &#123;    @Override    public void algorithm() &#123;        System.out.println(&quot;use A&quot;);    &#125;&#125;</code></pre><p>环境类</p><pre><code class="lang-java">public class Context &#123;    /**     * 具体策略的引用     */    private Strategy strategy;    public void setStrategy(Strategy strategy) &#123;        this.strategy = strategy;    &#125;    public void execute() &#123;        this.strategy.algorithm();    &#125;&#125;</code></pre><p>使用（可以使用配置文件的方式指定策略）</p><pre><code class="lang-java">public static void main(String[] args) &#123;    Context context = new Context();    context.setStrategy(new ConcreteStrategyA());    context.execute();    context.setStrategy(new ConcreteStrategyB());    context.execute();&#125;</code></pre><h2 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h2><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul><li>完美支持开闭原则，可以无修改的添加和替换算法（使用配置指定策略的情况下）。</li><li>可以减少系统的大量的臃肿的<code>if-else</code>。</li></ul><h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul><li>策略类数量会增多，每个策略都是一个类，复用的可能性很小，任何一个细小的变化可能都需要添加一个新的策略类。</li><li>没法支持使用某个策略类的一部分，然后再使用另一个策略类的场景。</li></ul><h2 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h2><ul><li>系统对某个目标实现，有很多算法的场景。</li><li>算法需要自由切换的场景。</li><li>想要屏蔽算法具体规则的场景。</li></ul><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>策略模式就比较简单了，你知道知道多态，可能自己在开发中自己就已经使用了策略模式了。策略模式核心点就是抽象策略类和环境类。我们使用的时候，其实主要就设计好抽象策略类就行了。一个好的抽象策略类是一个优秀策略模式的关键。</p><h1 id="附"><a href="#附" class="headerlink" title="附"></a>附</h1><p>相关代码：<a href="https://github.com/zhaohaoren/design_pattern_tutorial">https://github.com/zhaohaoren/design_pattern_tutorial</a></p><p>如有代码和文章问题，还请指正！感谢！</p>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;【设计模式】策略模式&quot;&gt;&lt;a href=&quot;#【设计模式】策略模式&quot; class=&quot;headerlink&quot; title=&quot;【设计模式】策略模式&quot;&gt;&lt;/a&gt;【设计模式】策略模式&lt;/h1&gt;&lt;p&gt;本文主要介绍策略模式的概念和用法。&lt;/p&gt;
&lt;h2</summary>
        
      
    
    
    
    
    <category term="设计模式" scheme="http://lollipops.work/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>【设计模式】状态模式</title>
    <link href="http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E7%8A%B6%E6%80%81%E6%A8%A1%E5%BC%8F/"/>
    <id>http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E7%8A%B6%E6%80%81%E6%A8%A1%E5%BC%8F/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2021-01-16T09:08:34.424Z</updated>
    
    <content type="html"><![CDATA[<h1 id="【设计模式】状态模式"><a href="#【设计模式】状态模式" class="headerlink" title="【设计模式】状态模式"></a>【设计模式】状态模式</h1><p>本文主要介绍状态模式的概念以及用法。</p><h2 id="模式背景"><a href="#模式背景" class="headerlink" title="模式背景"></a>模式背景</h2><p>生活中很多东西都会有不同的状态，并且在不同的状态下会有着不同的行为。比如，水可以分为固态，液态，汽态，在每一种态下面他的表现形式也不同。一个个人银行的账户，可能依据存钱或者欠钱的额度在银行中标记为不同状态，VIP可以每日转额度10w，欠款用户账户被冻结等。</p><p>这些都会对应着一些系统设计的问题：如何才能在系统中更好的处理这些状态的转换？如果我们使用<code>if-else</code>的话，状态如果太多，以及添加和删除状态都会存在问题，要么<strong>类的职责太重<code>if-else</code>逻辑太多</strong>，要么<strong>添加移除状态会很麻烦</strong>。这就是状态模式要解决的问题。</p><h2 id="定义-amp-概念"><a href="#定义-amp-概念" class="headerlink" title="定义&amp;概念"></a>定义&amp;概念</h2><p>状态模式：允许一个对象在其内部状态改变时改变它的行为，对象看起来就好像修改了它的类一样。是一种对象行为型模式。</p><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>上面背景中，我们讨论的问题其实是一个问题。而状态模式其实要解决2个问题：</p><ol><li>如何让客户端进行一致的处理，让客户端不需要去处理状态的转换，状态的转换对客户端应该是透明的。</li><li>状态如何更好的进行转换？</li></ol><p>针对问题1，我们提供了一个环境类，</p><p>针对问题2，是我们状态模式核心。这个问题也分2步骤：1. 想要状态好扩展该怎么办？这个我们之前也看的多了——抽象呗！所以状态模式引入了抽象状态类和具体状态类。扩展的问题解决了，该<strong>如何进行转换呢？状态模式有2种方式</strong>：</p><ol><li><strong>每个具体状态自己做check以及状态的扭转</strong>，这样就需要获取到环境类的引用，我们可以状态类内部持有（推荐），或者参数传递的方式。（这种其实有点责任链的思想在里面了）</li><li>由<strong>环境类来控制转换</strong>，环境类里面进行<code>if-else</code>判断，但是这种有一定缺陷，就是扩充状态类的时候，需要修改环境类，违背了开闭原则。</li></ol><p><strong>组成要素</strong></p><ul><li>环境类<ul><li>又叫上下文类，内部引用了一个状态类的实例。主要就是这个类主要负责环境，而状态切割到各个状态类中去，实现解耦。它负责状态之间的转换。</li><li>环境类对象其实就是那些存在状态转换的对象，只是他的状态属性被隔离出去了，只引用一个抽象状态。状态+其他属性=这个对象。</li></ul></li><li>抽象状态类<ul><li>一个接口，封装了一个特定状态相关的行为。如果很多行为是共享的，那么也可以定位抽象类，将公共方法提取到抽象类方法中。</li></ul></li><li>具体状态类<ul><li>具体的状态类，一个具体状态类代表环境类里面某个环境的的一种状态。所以不同的状态类应该有不同的行为。这里面就是具体行为的逻辑。</li></ul></li></ul><h3 id="共享状态"><a href="#共享状态" class="headerlink" title="共享状态"></a>共享状态</h3><p>当多个环境类需要共享一个或者多个状态的时候（每个环境类其实对应一个具体的状态对象，多个环境类需要共享状态的意思就是说，这一组环境类的对象，当其中一个环境对象状态变化的时候，该环境类所有的对象的状态都要变化。书里面的例子：灯有开状态和关状态，开关就是对应的环境类，我们有2个开关，控制同一个灯，就属于共享状态。），可以将该状态定义为静态的引用。这样这个状态就属于这个类，而不是某个具体的环境对象。</p><h2 id="UML"><a href="#UML" class="headerlink" title="UML"></a>UML</h2><p><img src="/Users/zhaohaoren/workspace/mycode/blog-docs/docs/设计模式/state.png" alt="image-20200909114522143"></p><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><p>这里实现案例是基于环境类的状态转换。</p><p>环境类</p><pre><code class="lang-java">public class WaterContext &#123;    /**     * 引用该Context所有可能的State     */    private WaterState state;    public WaterState getState() &#123;        return state;    &#125;    /**     * 使用环境类来完成状态的转换，还可以使用状态实现类的方式。具体后面补充     *     * @param value 环境切换的判断依据     */    public void setWaterState(int value) &#123;        if (value == 0) &#123;            state = new IceWaterState();        &#125; else if (value == 1) &#123;            state = new WarmWaterState();        &#125; else if (value == 2) &#123;            state = new HotWaterState();        &#125;    &#125;&#125;</code></pre><p>状态抽象</p><pre><code class="lang-java">public interface WaterState &#123;    void showState();&#125;</code></pre><p>具体状态实现</p><pre><code class="lang-java">public class IceWaterState implements WaterState &#123;    @Override    public void showState() &#123;        System.out.println(&quot;i am ice&quot;);    &#125;&#125;//....WarmWaterState...HotWaterState</code></pre><p>客户端</p><pre><code class="lang-java">public static void main(String[] args) &#123;    WaterContext context = new WaterContext();    context.setWaterState(0);    context.getState().showState();    context.setWaterState(1);    context.getState().showState();    context.setWaterState(2);    context.getState().showState();&#125;</code></pre><h2 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h2><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul><li>可以减少系统中<code>if-else</code>分支太多的情况。</li><li>状态类方便管理和维护，状态很清晰，方便进行管理。</li></ul><h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul><li>会增加系统类和对象个数，增大系统开销。</li><li>会增加系统设计难度，可能存在某些情况下，使用状态模式会让编码更加复杂。</li><li>状态类的转换，对开闭原则支持不太好。</li></ul><h2 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h2><ul><li>系统中对象行为随状态改变而改变的场景。</li><li>系统中如果有大量的条件、分支语句，可以考虑使用状态模式代替（阿里巴巴java开发手册也提到过，超过三层的if-else分支时候，就可以考虑了）。</li></ul><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>要使用状态模式，抓住2个主要矛盾就行了：系统某个对象存在多个状态，每个状态具有不同的行为。确定好了使用状态模式后，我们就要抓住了，该对象会触发状态变化的那些方法。然后就是环境类，抽象状态，具体状态的实现了。</p><h1 id="附"><a href="#附" class="headerlink" title="附"></a>附</h1><p>相关代码：<a href="https://github.com/zhaohaoren/design_pattern_tutorial">https://github.com/zhaohaoren/design_pattern_tutorial</a></p><p>如有代码和文章问题，还请指正！感谢！</p>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;【设计模式】状态模式&quot;&gt;&lt;a href=&quot;#【设计模式】状态模式&quot; class=&quot;headerlink&quot; title=&quot;【设计模式】状态模式&quot;&gt;&lt;/a&gt;【设计模式】状态模式&lt;/h1&gt;&lt;p&gt;本文主要介绍状态模式的概念以及用法。&lt;/p&gt;
&lt;h2</summary>
        
      
    
    
    
    
    <category term="设计模式" scheme="http://lollipops.work/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>【设计模式】组合模式</title>
    <link href="http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E7%BB%84%E5%90%88%E6%A8%A1%E5%BC%8F/"/>
    <id>http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E7%BB%84%E5%90%88%E6%A8%A1%E5%BC%8F/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2021-01-16T09:08:56.136Z</updated>
    
    <content type="html"><![CDATA[<h1 id="【设计模式】组合模式"><a href="#【设计模式】组合模式" class="headerlink" title="【设计模式】组合模式"></a>【设计模式】组合模式</h1><p>本文主要介绍：组合模式原理和用法，以及组合模式的透明和安全实现。</p><h2 id="模式背景"><a href="#模式背景" class="headerlink" title="模式背景"></a>模式背景</h2><p>在书中给了一个案例：我们对给定的文件夹进行杀毒。文件夹和和子文件夹都需要递归进行查杀，并且需要依据文件是图片还是文本需要采用不同的查杀方式。</p><p>这是一个比较常见的业务场景。相当于我们要递归遍历一个树形结构，但是这个树里面的节点类型有好几种。我们自然会为每种类型的节点创建对应的类，但是问题出现在<strong>包含子文件夹和文件的文件夹这个类上（也就是非叶子节点）</strong>。对于这种节点，我们就需要对自己下面的文件类型进行区分，然后采用不同的杀毒策略。</p><p>这样就<strong>出现了一个问题</strong>：1. 非叶子节点的处理逻辑就很复杂，我们每种文件类型需要做区分在去调用查杀。2. 一旦添加了一个新的文件种类，需要修改文件夹类代码。</p><p>所以我们希望能让系统进行杀毒操作的时候，无论对什么文件和文件夹，他们都用同一套对外接口来操作，文件夹的代码就可以简化操作这个同一套标准的接口。</p><h2 id="定义-amp-概念"><a href="#定义-amp-概念" class="headerlink" title="定义&amp;概念"></a>定义&amp;概念</h2><p>组合模式是对单个对象和组合对象（叶子节点和非叶子节点）抽象，使其使用具有一致性，组合模式又叫“整体-部分”模式。是一种对象结构型模式。对于树形的对象结构群体来说很有用。<strong>组合模式是使用面向对象的思想来实现树形结构的构建和处理。</strong></p><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>前面我们说过，问题主要出在了文件夹类进行处理的时候，各个文件不同需要做区分并调用查杀方法。现在希望能够有统一的调用接口。这就很简单：<strong>抽象！</strong>我们将文本文件，图片文件，还是文件夹都抽象公共部分出来。然后<strong>操作都是通过这个抽象来进行操作</strong>。以后添加什么新的文件类型，也只需要添加一个抽象的新的实现就行了。</p><p>所以可以理解为：<strong>组合模式主要的核心思想，是将树型结构中的节点抽象出一个抽象构建类。</strong></p><p><strong>必要条件</strong></p><ul><li>Component（抽象的节点类）<ul><li><strong>这是组合模式的关键</strong>，就是将树形结构中的叶子节点和非叶子节点都抽象出一个父级（可以是接口也可以是抽象类）。这个抽象里面包含叶子和非叶子所有的属性和方法。可以广义的代表该树形体系中所有的节点。（是否组合模式就看是否将结构抽象出这样的一个公共抽象）</li></ul></li><li>Leaf（叶子节点）<ul><li>就是树形结构中不包含子节点的那些Component的实现。</li></ul></li><li>Composite（容器节点）<ul><li>非叶子节点部分，里面有个List，存放自己的子节点们。</li></ul></li></ul><p>因为系统中节点的种类不同，一般都可以会有各自的方法，比如文件夹节点有add，remove方法，但是叶子节点就没有。这时候我们对Component抽象的层次可以做一些控制，我们按照抽象层次程度分为透明模式和安全模式：</p><ul><li>透明模式<ul><li>抽象构建类定义了所有的方法。就是我们一直在说的组合模式。</li><li>说他不够安全因为，如果在运行中调用了叶子节点的add，remove方法（这些方法本身对于叶子没有意义）可能会出错。</li></ul></li><li>安全模式<ul><li>抽象构建类中只定义通用的方法。叶子还是中间节点他们各自需要的方法，他们自己实现。</li></ul></li></ul><p>当然我们还可以在抽象构建类中对方法提供默认实现，需要使用该方法的进行覆写即可，这也是不透明的。都只是为了减少编写无用的代码，但是提供不了强约束。</p><h2 id="UML"><a href="#UML" class="headerlink" title="UML"></a>UML</h2><p>透明组合模式</p><p><img src="/Users/zhaohaoren/workspace/mycode/blog-docs/docs/设计模式/Composite_1.png" alt="image-20200819140454462"></p><p>安全组合模式</p><p><img src="/Users/zhaohaoren/workspace/mycode/blog-docs/docs/设计模式/Composite_2.png" alt="image-20200819140637984"></p><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><h3 id="透明组合模式"><a href="#透明组合模式" class="headerlink" title="透明组合模式"></a>透明组合模式</h3><p>抽象层拥有所有的方法，这些方法可能叶子节点是不应该有的。 比如add和remove子节点。</p><p>抽象</p><pre><code class="lang-java">public interface Component &#123;    void add(Component c);    void remove(Component c);    List&lt;Component&gt; getChild();    void operation();&#125;</code></pre><p>叶子</p><pre><code class="lang-java">class Leaf implements Component &#123;    private String name;    public Leaf(String name) &#123;        this.name = name;    &#125;    @Override    public void add(Component c) &#123;        System.out.println(&quot;can&#39;t add&quot;);    &#125;    @Override    public void remove(Component c) &#123;        System.out.println(&quot;can&#39;t remove&quot;);    &#125;    @Override    public List&lt;Component&gt; getChild() &#123;        return null;    &#125;    @Override    public void operation() &#123;        System.out.println(&quot;叶子节点：&quot; + name);    &#125;&#125;</code></pre><p>非叶子</p><pre><code class="lang-java">class Composite implements Component &#123;    private List&lt;Component&gt; componentList = new ArrayList&lt;&gt;();    private String name;    public Composite(String name) &#123;        this.name = name;    &#125;    @Override    public void add(Component c) &#123;        componentList.add(c);    &#125;    @Override    public void remove(Component c) &#123;        componentList.remove(c);    &#125;    @Override    public List&lt;Component&gt; getChild() &#123;        return componentList;    &#125;    @Override    public void operation() &#123;        System.out.println(&quot;非叶子节点：&quot; + name);        componentList.forEach(Component::operation);    &#125;&#125;</code></pre><p>使用</p><pre><code class="lang-java">Component c1, c2, c3, f1, f2, f3;c1 = new Leaf(&quot;1&quot;);c2 = new Leaf(&quot;2&quot;);c3 = new Leaf(&quot;3&quot;);f1 = new Composite(&quot;1&quot;);f2 = new Composite(&quot;2&quot;);f3 = new Composite(&quot;3&quot;);f1.add(c1);f1.add(c2);f2.add(f1);f3.add(c3);f1.add(f3);f1.operation();</code></pre><h4 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h4><ul><li>简单，保证了叶子节点和非叶子节点具有对外一致性，在对该树形节点递归处理的时候可以同等对待。</li></ul><h4 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h4><ul><li>不安全，叶子的特性（不能添加子节点）有可能被破坏，一旦错误调用可能会出问题，没有代码约束保证。</li><li>其次，每次叶子节点实现抽象的时候，我们需要实现全部方法，add和remove这些就本是没必要去实现的，这就增加了编码复杂度。</li></ul><h3 id="安全组合模式"><a href="#安全组合模式" class="headerlink" title="安全组合模式"></a>安全组合模式</h3><p>安全组合模式就是让抽象层剥离叶子节点不需要的方法，具体的实现让Composite自己实现。</p><h4 id="实现1"><a href="#实现1" class="headerlink" title="实现1"></a>实现1</h4><p>我们可以使用适配器模式的缺省适配器来适配一个抽象，实现叶子的不需要的默认方法。然后让叶子节点来实现这个类。</p><pre><code class="lang-java">public abstract class AbstractComponent implements Component &#123;    @Override    public void add(Component c) &#123;        System.out.println(&quot;no support&quot;);    &#125;    @Override    public void remove(Component c) &#123;        System.out.println(&quot;no support&quot;);    &#125;    @Override    public List&lt;Component&gt; getChild() &#123;        System.out.println(&quot;no support&quot;);        return null;    &#125;    @Override    public abstract void operation();&#125;</code></pre><h4 id="实现2"><a href="#实现2" class="headerlink" title="实现2"></a>实现2</h4><p>抽象构建中只定义公共方法，其他的他们自己用自己定义。</p><pre><code class="lang-java">public interface ComponentSafe &#123;    void operation();&#125;</code></pre><p>叶子</p><pre><code class="lang-java">class LeafSafe implements ComponentSafe &#123;    private String name;    public LeafSafe(String name) &#123;        this.name = name;    &#125;    @Override    public void operation() &#123;        System.out.println(&quot;叶子：&quot; + name);    &#125;&#125;</code></pre><p>非叶子</p><pre><code class="lang-java">class CompositeSafe implements ComponentSafe &#123;    private String name;    private List&lt;ComponentSafe&gt; componentSafeList = new ArrayList&lt;&gt;();    public CompositeSafe(String name) &#123;        this.name = name;    &#125;    @Override    public void operation() &#123;        System.out.println(&quot;非叶子节点&quot; + name);        componentSafeList.forEach(ComponentSafe::operation);    &#125;    public void add(ComponentSafe c) &#123;        componentSafeList.add(c);    &#125;    public void remove(ComponentSafe c) &#123;        componentSafeList.remove(c);    &#125;    public List&lt;ComponentSafe&gt; getChild() &#123;        return componentSafeList;    &#125;&#125;</code></pre><p>使用</p><pre><code class="lang-java">ComponentSafe c1, c2, c3;c1 = new LeafSafe(&quot;1&quot;);c2 = new LeafSafe(&quot;2&quot;);c3 = new LeafSafe(&quot;3&quot;);CompositeSafe f1, f2, f3;f1 = new CompositeSafe(&quot;1&quot;);f2 = new CompositeSafe(&quot;2&quot;);f3 = new CompositeSafe(&quot;3&quot;);//buildf1.add(c1);f1.add(c2);f1.add(c3);f2.add(f3);f1.add(f2);f1.operation();</code></pre><h4 id="优点-1"><a href="#优点-1" class="headerlink" title="优点"></a>优点</h4><ul><li>安全了，叶子节点不用管不属于他的方法了，有强约束里保证，叶子节点决定不会调用到add remove方法。</li></ul><h4 id="缺点-1"><a href="#缺点-1" class="headerlink" title="缺点"></a>缺点</h4><ul><li>但是不透明了，构建叶子和构建容器需要使用不同的方法来构建。我们<strong>无法只面对抽象进行编程</strong>。</li></ul><h2 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h2><p>适用于有整体和部分的层次结构，这里的整体和部分指的是系统中类，他们的大致功能是相同的，小部分存在差异。我们如果希望以一种方式忽略整体和部分的差异，让客户单可以直接面向抽象编程，使用统一的标准对待他们。</p><p>有树形结构的使用场景，如树形菜单，文件、文件夹的管理。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>总的来说，<strong>组合模式的核心就是对系统中大同小异的类做抽象</strong>！以方便于客户端直接面向抽象编程。透明还是安全的结合场景选择最适合的。</p><h1 id="附"><a href="#附" class="headerlink" title="附"></a>附</h1><p>相关代码：<a href="https://github.com/zhaohaoren/design_pattern_tutorial">https://github.com/zhaohaoren/design_pattern_tutorial</a></p><p>如有代码和文章问题，还请指正！感谢！</p>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;【设计模式】组合模式&quot;&gt;&lt;a href=&quot;#【设计模式】组合模式&quot; class=&quot;headerlink&quot;</summary>
        
      
    
    
    
    
    <category term="设计模式" scheme="http://lollipops.work/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>【设计模式】责任链模式</title>
    <link href="http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E8%81%8C%E8%B4%A3%E9%93%BE%E6%A8%A1%E5%BC%8F/"/>
    <id>http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E8%81%8C%E8%B4%A3%E9%93%BE%E6%A8%A1%E5%BC%8F/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2021-01-16T09:09:00.400Z</updated>
    
    <content type="html"><![CDATA[<h1 id="【设计模式】责任链模式"><a href="#【设计模式】责任链模式" class="headerlink" title="【设计模式】责任链模式"></a>【设计模式】责任链模式</h1><p>本文主要介绍责任链模式的概念及用法。</p><h2 id="模式背景"><a href="#模式背景" class="headerlink" title="模式背景"></a>模式背景</h2><p>这个模式的应用场景也很多，最常见的就是一个审批流程：一个事件触发了需要一级一级的往下传递到对应的人进行处理的。对于这种审批流程<strong>最糟糕的写法就是在一个类中使用很长的<code>if-else</code>判断链来进行判断</strong>。这种写法缺点很多：</p><ol><li>类职责过重，所有的审批流程都走该类，违反单一职责。</li><li>如果需要修改审批流程，就一定需要修改这个类，违反开闭原则。</li><li>模板僵化，不可定制，一旦这个流程确定了，客户单只有调用的份，无法自己定义审批流程。</li></ol><p>责任链模式就是为了处理这类链式流程问题的。</p><h2 id="定义-amp-概念"><a href="#定义-amp-概念" class="headerlink" title="定义&amp;概念"></a>定义&amp;概念</h2><p><strong>责任链模式（Chain of Responsibility）</strong>：为了避免请求发送者与多个请求处理者耦合在一起，将所有请求的处理者<strong>通过前一对象记住其下一个对象的引用而连成一条链</strong>；当有请求发生时，可将请求沿着这条链传递，直到有对象处理它为止。</p><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>责任链模式是一个相对简单的模式。目的是将上述的那一长串<code>if-else</code>调用链进行解耦。其做法：将所有的审批处理逻辑抽象出一个抽象单元，所有的审批员都继承这个单元。每个审批员内部持有一个下一个审批的对象。职责链模式可以是一个直线，也可以是一个环或者树，最常见的是直线结构。当事件触发了，依据客户端定制的职责链进行调用。</p><p><strong>组成要素</strong></p><ul><li>抽象处理类<ul><li>定义处理请求的接口的统一抽象。一般设计为抽象类，因为每一个处理者还需要有下一个处理者，所以在抽象处理类中定义一个下一个处理类的引用。</li></ul></li><li>具体处理类<ul><li>抽象处理类的实现，真正的处理者。其主要作用就2个：<strong>处理请求</strong>，<strong>转发请求</strong>。</li><li>这些处理者以及其内部应用的下一个处理者连接起来形成一条链。</li></ul></li></ul><p>依据对请求处理的方式，职责链模式可以分为<strong>纯职责链</strong>和<strong>不纯职责链</strong>：</p><ul><li>纯职责链<ul><li>要么处理请求，要么转发请求。不能处理一部分然后再转发。就是<strong>2个处理者处理的责任不能有交集。</strong></li><li>请求必须被某一个处理者对象处理，不能没人接受。</li></ul></li><li>不纯职责链<ul><li>不纯的就很自由，请求可以被一个处理者处理了部分再传给下一个处理。</li><li>请求可以不被任何人处理。</li></ul></li></ul><h2 id="UML"><a href="#UML" class="headerlink" title="UML"></a>UML</h2><p><img src="/Users/zhaohaoren/workspace/mycode/blog-docs/docs/设计模式/RespChain.png" alt="image-20200824130315727"></p><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><p>抽象处理类</p><pre><code class="lang-java">public abstract class AbstractHandler &#123;    //下一个处理引用    protected AbstractHandler next;    public void setNext(AbstractHandler next) &#123;        this.next = next;    &#125;    public abstract void handleRequest(String request);&#125;</code></pre><p>具体处理类</p><pre><code class="lang-java">public class ConcreteHandler extends AbstractHandler &#123;    @Override    public void handleRequest(String request) &#123;        if (&quot;X&quot;.equals(request)) &#123;            System.out.println(&quot;i am X&quot;);        &#125; else &#123;            if (next == null) &#123;                System.out.println(&quot;no handler&quot;);                return;            &#125;            this.next.handleRequest(request);        &#125;    &#125;&#125;</code></pre><p>客户端</p><pre><code class="lang-java">AbstractHandler a, b;a = new ConcreteHandlerA();b = new ConcreteHandlerB();//构建链条结构: a-&gt;ba.setNext(b);//请求a.handleRequest(&quot;A&quot;);a.handleRequest(&quot;B&quot;);a.handleRequest(&quot;C&quot;);</code></pre><h2 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h2><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul><li>解耦，请求的处理逻辑被拆分在各个处理类中。</li><li>增加新的请求处理类很方便。</li><li>增强给对象指派职责的灵活性。通过改变链内的成员或者调动它们的次序，允许动态地新增或者删除责任。</li></ul><h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul><li>不能保证请求一定被接收。 </li><li>如果构建职责链不恰当，可能会造成循环调用造成死循环，系统性能将受到一定影响。 </li><li>可能不容易观察运行时的特征，在进行代码调试排错时不太方便。</li></ul><h2 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h2><p>审批流程，工单流程等等。这些业务场景都很鲜明。这种一个事件触发需要多个对象来处理的，并且存在处理层次顺序的场景尝试使用职责链。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>职责链的核心点就2个：</p><ul><li>抽象类，为所有的处理者提供统一访问入口。</li><li>每个抽象类的具体实现类都有一个内置引用，来引用下一个处理者。</li></ul><p>链条的结构并不是在具体处理类中的，具体处理类只是一个未被初始化的该处理者的下一个处理者的引用。真正初始化这个职责链条是在外部进行的。</p>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;【设计模式】责任链模式&quot;&gt;&lt;a href=&quot;#【设计模式】责任链模式&quot; class=&quot;headerlink&quot; title=&quot;【设计模式】责任链模式&quot;&gt;&lt;/a&gt;【设计模式】责任链模式&lt;/h1&gt;&lt;p&gt;本文主要介绍责任链模式的概念及用法。&lt;/p&gt;
&lt;h2</summary>
        
      
    
    
    
    
    <category term="设计模式" scheme="http://lollipops.work/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>【设计模式】装饰模式</title>
    <link href="http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E8%A3%85%E9%A5%B0%E6%A8%A1%E5%BC%8F/"/>
    <id>http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E8%A3%85%E9%A5%B0%E6%A8%A1%E5%BC%8F/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2021-01-16T09:09:04.206Z</updated>
    
    <content type="html"><![CDATA[<h1 id="【设计模式】装饰模式"><a href="#【设计模式】装饰模式" class="headerlink" title="【设计模式】装饰模式"></a>【设计模式】装饰模式</h1><p>本文主要介绍：装饰模式概念和使用，以及对应的透明和半透明形式概念。</p><h2 id="模式背景"><a href="#模式背景" class="headerlink" title="模式背景"></a>模式背景</h2><p>假设我们有一堆的类，这些类我们需要为他扩充一些功能。比如在执行该类的方法前或者后增加一些逻辑，该怎么做？相信你一定就已经能想到了装饰模式的用法！但是我们还是要说说最笨的方法：</p><p>最糟糕的设计就是，我们创建一个新的类去继承原始类，然后overwrite原来的方法。这很糟糕的点在于：</p><ol><li>会产生大量的类。如果我们要对所有的类扩展一下，那么每个类都继承一个新类？NO！</li><li>继承这个东西，不同语言实现机制不同，可能实现功能会受限制。有些语言支持多重继承，有些就不支持。</li></ol><p>我们想复用的时候，应当<strong>多用关联，少用继承</strong>。装饰模式就是用关联的方式为类扩展新的功能的。</p><h2 id="定义-amp-概念"><a href="#定义-amp-概念" class="headerlink" title="定义&amp;概念"></a>定义&amp;概念</h2><p>动态地给一个对象添加一些额外的职责。就增加功能来说，装饰器模式相比生成子类更为灵活。装饰模式是一种<strong>对象结构型模式</strong>。</p><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>装饰模式的思想很简单：<strong>我们可以将需要装饰的东西提取出来，创建一个专门用于装饰的类。然后这个类里面关联一个需要被装饰的对象。在使用的时候，我们先创建好需要被装饰的对象，然后用这个对象再通过装饰类去构造装饰好的对象。</strong></p><p>可以理解为：一个人需要穿衣服（这个人就是要被装饰的类），现在他可能有好几套衣服（装饰类）。我们需要这个人穿什么衣服，就把人塞进这个衣服里面就行了。</p><p><strong>组成要素</strong></p><ul><li>Component（抽象构建类）<ul><li>为了方便扩展的一个顶层抽象，创建这个类或者接口的目的是为了让客户端可以面向抽象编程，还有一个作用就是为了让原始类可以方便的扩展。</li><li>这个抽象里面提供着被装饰者的一些操作，这样客户端就可以完全面向Component编程了。</li></ul></li><li>ConcreteComponent（具体构建）<ul><li>抽象类的实现，就是那些需要被装饰的对象的类。原始类。</li></ul></li><li>Decorator（抽象装饰类）<ul><li>创建这个要素的原因：我们存在着装饰类也会扩展的可能，这个属于所有装饰器的抽象。</li><li>该抽象内部维持一个抽象构建类的一个引用。申明需要装饰增加的职责，但是具体的实现交给装饰实现类。<strong>该抽象装饰类的设计是装饰模式的核心。</strong></li></ul></li><li>ConcreteDecorate（具体装饰类）<ul><li>负责装饰职责的实现。即为原始类的方法添加具体的扩展逻辑的。</li></ul></li></ul><h3 id="透明装饰模式"><a href="#透明装饰模式" class="headerlink" title="透明装饰模式"></a>透明装饰模式</h3><p>我们上面所说的装饰模式就是透明装饰模式，也是一般所指的装饰模式。</p><h3 id="半透明装饰模式"><a href="#半透明装饰模式" class="headerlink" title="半透明装饰模式"></a>半透明装饰模式</h3><p><strong>因为透明模式存在一个问题：客户端想要使用装饰者的方法。</strong>我们前面是面向Component抽象编程的了，没法调用到装饰类的方法。这时候我们就不能完全面向抽象编程了，我们使用的时候就需要使用装饰类来引用具体的对象。这就是半透明装饰模式。<strong>但是我们一般都是使用的透明的装饰模式。</strong></p><h2 id="UML"><a href="#UML" class="headerlink" title="UML"></a>UML</h2><p><img src="/Users/zhaohaoren/workspace/mycode/blog-docs/docs/设计模式/Decorator.png" alt="image-20200820161135056"></p><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><p>抽象构建类</p><pre><code class="lang-java">public interface Component &#123;    void display();&#125;</code></pre><p>抽象装饰类</p><pre><code class="lang-java">public abstract class ComponentDecorator implements Component &#123;    /**重点 内部组合一个需要被装饰对象引用*/    private Component component;    public ComponentDecorator(Component component) &#123;        this.component = component;    &#125;        /**抽象类中没有对齐进行装饰，具体装饰交给装饰器实现类来做*/    @Override    public void display() &#123;        component.display();    &#125;&#125;</code></pre><p>实体构建类</p><pre><code class="lang-java">public class ComponentA implements Component&#123;    @Override    public void display() &#123;        System.out.println(&quot;A&quot;);    &#125;&#125;</code></pre><p>装饰器</p><pre><code class="lang-java">public class ComponentDecorator extends ComponentDecorator &#123;    public ComponentDecorator(Component component) &#123;        super(component);    &#125;    @Override    public void display() &#123;        System.out.println(&quot;装饰--开始&quot;);        super.display();        System.out.println(&quot;装饰--结束&quot;);    &#125;&#125;</code></pre><p>使用</p><pre><code class="lang-java">/**定义多个装饰器，一个对象可以被返回装饰多次*/Component c1 = new ComponentA();Component c2 = new ComponentDecoratorA(c1);Component c3 = new ComponentDecoratorB(c2);c3.display();</code></pre><h3 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h3><ul><li>构建实现类尽量保证轻量级，复杂的逻辑可以通过装饰去做（es的query builder 不就是装饰一个个聚合函数吗）</li><li>尽量使用透明的装饰模式，即装饰接口和构建接口一致。</li><li>如果系统确定对象只有一个具体的构建类，我们就不需要抽象构建类了，装饰抽象可以作为该具体构建的子类。</li></ul><h2 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h2><p>这个使用场景就太多了，只要你发现一个类不能满足我的需求，希望在这个类上扩展一些功能的就会使用到。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>装饰模式就是:</p><ol><li>现将装饰的部分提取出来</li><li>做2个抽象：一个是被装饰的抽象，一个是装饰类的抽象</li><li>需要装饰的时候，将被装饰对象，使用装饰类包裹下。</li></ol><p>他的实现手法和理念其实和前面很多模式有相似的地方，也很容易搞混！下面说说他们的区别：</p><h3 id="和代理模式区别"><a href="#和代理模式区别" class="headerlink" title="和代理模式区别"></a>和代理模式区别</h3><p>这个之前写<a href="https://juejin.im/post/6860867600298016781">代理模式</a>也提到过，这里简单描述下：和代理模式乍一看是一样的嘛！仔细一看还是一样的嘛！他们的不同之处更多的体现在思想上：</p><ul><li>装饰模式强调增强自身，自己还是自己，只是穿上了衣服的自己。外面打交道人家也只是认为你是穿了衣服的你罢了。</li><li><p>代理模式则强调控制，你已经不完全是你了，你找了个代理人帮你做事情，外面人家不认你，只认你的代理人，代理人怎么搞你都可以。还有就是代理模式一般都是做你本身不该做，或者做不了的事情的。</p><p>还有一点区别就是装饰类的扩展性！可以再UML上看到装饰类是有一层抽象的，而代理没有，因为代理</p></li></ul><h3 id="和适配器模式的区别"><a href="#和适配器模式的区别" class="headerlink" title="和适配器模式的区别"></a>和适配器模式的区别</h3><p>这个前面也提到过。适配器的UML和装饰模式还是有很大区别的。而且适配嘛，和装饰字面意思上也有差别。 客户端那边使用装饰和使用原对象是一样用的，但是适配不一样，适配是因为客户端用不了原对象的，所以用的适配器类，这个类本身和原来的类没有任何关系。他是为了满足客户端能使用来设计的类。服务对象是客户端！而装饰器服务对象是要被装饰的类！主体都不同。</p><blockquote><p>我自己个人感觉：这些模式的思想其实都是利用代理的思想！他们真的很像！我们实际用的时候可能就没必要这么抠字眼了，他们的思想其实都是基于代理的思想，所以代理模式代表性的东西，然后因为性质的不同分了这些装饰还是适配的。他们是有共同思想的，这种思想把握了就能优化系统的设计不是吗？（再次强调下：这只是我个人的感觉）</p></blockquote><h1 id="附"><a href="#附" class="headerlink" title="附"></a>附</h1><p>相关代码：<a href="https://github.com/zhaohaoren/design_pattern_tutorial">https://github.com/zhaohaoren/design_pattern_tutorial</a></p><p>如有代码和文章问题，还请指正！感谢！</p>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;【设计模式】装饰模式&quot;&gt;&lt;a href=&quot;#【设计模式】装饰模式&quot; class=&quot;headerlink&quot;</summary>
        
      
    
    
    
    
    <category term="设计模式" scheme="http://lollipops.work/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>【设计模式】观察者模式</title>
    <link href="http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E8%A7%82%E5%AF%9F%E8%80%85%E6%A8%A1%E5%BC%8F/"/>
    <id>http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E8%A7%82%E5%AF%9F%E8%80%85%E6%A8%A1%E5%BC%8F/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2021-01-16T09:09:07.956Z</updated>
    
    <content type="html"><![CDATA[<h1 id="【设计模式】观察者模式"><a href="#【设计模式】观察者模式" class="headerlink" title="【设计模式】观察者模式"></a>【设计模式】观察者模式</h1><p>本文主要介绍观察者模式的概念和用法，以及jdk提供的观察者模式支持。</p><h2 id="模式背景"><a href="#模式背景" class="headerlink" title="模式背景"></a>模式背景</h2><p>我们在项目开发中会存在当一个对象发生变化的时候，其他的一些对象也需要跟着变更自己的状态或者去做一些操作。比如一个微信公众号，当号主发布了一篇文章之后，所有关注了这个公众号的用户都会受到该文章的推送。这种一个对象的变化，需要引起了多个对象变化的方案可以使用观察者模式。我们可能听得更多的是发布订阅模式（特别是学习消息队列时候），其实也就是观察者模式。</p><h2 id="定义-amp-概念"><a href="#定义-amp-概念" class="headerlink" title="定义&amp;概念"></a>定义&amp;概念</h2><p>观察者模式：定义对象之间的一种一对多的依赖关系，是的当一个对象状态发生变化的时候，其相关依赖对象皆得到通知并被自动更新。<strong>观察者模式的别名有：发布-订阅（publish/subscribe）模式</strong>，<strong>模型-视图（model/view）模式</strong>，源-监听（source/listener）模式，从属者(dependents)模式。观察者模式是一种<strong>对象行为型模式</strong>。</p><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>观察者模式也是一种简单的设计模式，既然叫观察者模式，那么整个模式中核心的角色自然就2个：<strong>观察者</strong>和<strong>被观察者</strong>。<br>现在我们希望被观察者的变化，能够通知到观察者。这里要区分一下主动方和被动方，观察者模式不是说观察者主动去观察对象，如果这样做，势必需要通过不断的循环去检测对象状态是否发生了变化。观察者其实是被动的观察，他唯一的主动就是进行订阅（即告诉被观察者，我要观察你）。所以两者之间的耦合关系就是<strong>被观察者内部维系着一系列的观察者对象</strong>，当事件发生时，观察者再去通知内部所有的观察者，让观察者触发自己的监听逻辑。</p><p><strong>组成要素</strong></p><ul><li>目标（Subject）<ul><li>那个需要被观察的目标对象的抽象，内部有一个集合存储若干个观察者。</li><li>提供了一般被观察者对象类的通用模板：注册和注销方法。</li></ul></li><li>具体目标（ConcreteSubject）<ul><li>目标的实现类或者子类。具体注册和注销的实现逻辑。</li></ul></li><li>观察者（Observer）<ul><li>观察者类的抽象，一般定义为接口，接口声明了<code>update</code>方法。</li><li>观察者通过该抽象进行编程，事件发生后挨个调用观察者的<code>update</code>方法。</li></ul></li><li>具体观察者（ConcreteObserver）<ul><li>具体的观察者，实现<code>update</code>方法。</li><li>我们还可以在里面内持一个目标对象，不仅可以自己来进行自己注册和注销的逻辑，而不需要在客户端进行注册的编写，而且还可以获取到目标对象目前的状态。</li></ul></li></ul><h3 id="jdk观察者模式"><a href="#jdk观察者模式" class="headerlink" title="jdk观察者模式"></a>jdk观察者模式</h3><p>观察者模式使用很频繁，地位也很重要。所以JDK提供了对观察者模式的支持。JDK提供了<code>Observer</code>接口和<code>Observable</code> 类，</p><p><code>Observer</code> 代表了抽象观察者：</p><pre><code class="lang-java">public interface Observer &#123;    void update(Observable o, Object arg);&#125;</code></pre><p><code>Observable</code> 代表了目标类：内部维护的是一个Vector数组来维持着观察者们。</p><pre><code class="lang-java">public class Observable &#123;    private boolean changed = false;    private Vector&lt;Observer&gt; obs;    public Observable() &#123;        obs = new Vector&lt;&gt;();    &#125;    public synchronized void addObserver(Observer o) &#123;      //....    &#125;    public synchronized void deleteObserver(Observer o) &#123;      //....    &#125;    public void notifyObservers() &#123;      //....    &#125;    //......    protected synchronized void clearChanged() &#123;        changed = false;    &#125;    public synchronized boolean hasChanged() &#123;        return changed;    &#125;    public synchronized int countObservers() &#123;        return obs.size();    &#125;&#125;</code></pre><p>通过这2个类我们可以更方便的在java中使用观察者模式。</p><h3 id="MVC"><a href="#MVC" class="headerlink" title="MVC"></a>MVC</h3><p>MVC其实也使用了观察者模式。模型层的数据，就是视图层的观察对象，控制器是这2者之间的中介者。</p><h2 id="UML"><a href="#UML" class="headerlink" title="UML"></a>UML</h2><p><img src="/Users/zhaohaoren/workspace/mycode/blog-docs/docs/设计模式/Observer.png" alt="image-20200908112714044"></p><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><p>在实现中我们需要分清楚哪些是观察者行为，哪些是目标类的行为。观察者主要是那些需要对别的对象发生变化的时候做出反馈的那些对象，而目标类不一定某个具体实体。比方说游戏里面的队友攻击通知所有队友支援。那么观察者就可以是人，是队友，而目标类就是一个组织，是这个整个队的集合。当一个队友遇到攻击时候，传入整个队，然后让该队通知所有的观察者（队友）。</p><p><strong>抽象目标</strong></p><pre><code class="lang-java">public abstract class AllyControlCenter &#123;    /**     * 战队名     */    protected String allyName;    /**     * 存储观察者的集合     */    protected ArrayList&lt;Observer&gt; players = new ArrayList&lt;&gt;();    public String getAllyName() &#123;        return allyName;    &#125;    public void setAllyName(String allyName) &#123;        this.allyName = allyName;    &#125;    /**     * 注册方法，对目标类添加新的观察者     */    public void join(Observer obs) &#123;        System.out.println(obs.getName() + &quot; join &quot; + allyName);        players.add(obs);    &#125;    /**     * 注销方法：移除观察者     */    public void quit(Observer obs) &#123;        System.out.println(obs.getName() + &quot; leave &quot; + allyName);        players.remove(obs);    &#125;    /**     * 抽象通知方法，通知所有的观察者     */    public abstract void notifyObserver(String name);&#125;</code></pre><p><strong>具体目标</strong></p><pre><code class="lang-java">public class ConcreteAllyControlCenter extends AllyControlCenter &#123;    public ConcreteAllyControlCenter(String name) &#123;        this.allyName = name;        System.out.println(&quot;create team:&quot; + name);    &#125;    @Override    public void notifyObserver(String name) &#123;        System.out.println(&quot;notify all friends! &quot; + name + &quot; is attacked!&quot;);        for (Observer player : players) &#123;            // 调用除了自己以外的盟友            if (!player.getName().equalsIgnoreCase(name)) &#123;                player.help();            &#125;        &#125;    &#125;&#125;</code></pre><p><strong>抽象观察</strong></p><pre><code class="lang-java">public interface Observer &#123;    String getName();    void setName(String name);    /**     * 帮助队友的方法     */    void help();    /**     * 申明遭受攻击的方法，即观察者观察到了后，通知哪些对象     */    void beAttacked(AllyControlCenter acc);&#125;</code></pre><p><strong>具体观察</strong></p><pre><code class="lang-java">public class Player implements Observer &#123;    private String name;    public Player(String name) &#123;        this.name = name;    &#125;    @Override    public String getName() &#123;        return name;    &#125;    @Override    public void setName(String name) &#123;        this.name = name;    &#125;    @Override    public void help() &#123;        System.out.println(name + &quot; help you!&quot;);    &#125;    @Override    public void beAttacked(AllyControlCenter acc) &#123;        System.out.println(name + &quot; is attacked!&quot;);        acc.notifyObserver(name);    &#125;&#125;</code></pre><p><strong>客户端</strong></p><pre><code class="lang-java">//创建需要被观察的目标对象ConcreteAllyControlCenter team = new ConcreteAllyControlCenter(&quot;team1&quot;);//定义四个观察者Observer player1, player2, player3, player4;player1 = new Player(&quot;A&quot;);team.join(player1);player2 = new Player(&quot;B&quot;);team.join(player2);player3 = new Player(&quot;C&quot;);team.join(player3);player4 = new Player(&quot;D&quot;);team.join(player4);//让某一个遭受攻击player2.beAttacked(team);</code></pre><h2 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h2><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul><li>观察者和被观察者之间建立了一层抽象耦合，这样观察者容易扩展。被观察者只持有观察者抽象的集合，并不需要知道具体观察者内部的实现。</li><li>可以实现表示层和数据逻辑层的分离，支持多播通信，简化了一对多的系统设计难度。</li><li>满足开闭原则，可以无需修改代码的方式添加新的观察者。</li></ul><h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul><li>如果观察者太多，被观察者通知观察者消耗的时间很多，影响系统的性能。</li><li>如果观察者和观察目标之间存在循环依赖，观察目标会触发他们之间进行循环调用而导致系统崩溃。</li><li>观察者只能知道被观察者发生了变化，不能知道观察者发生了什么变化。</li></ul><h2 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h2><p>类似于这种发布-订阅，一个对象的变更需要通知给系统中多个对象的情况，都可以考虑使用观察者模式。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>观察者模式是一种解决一对多依赖关系的方案，并且这一对多的关系是当一个变化的时候，多个其他对象需要能够对其作出响应。需要清除系统中的观察者和被观察者。被观察者通过内部引用观察者的抽象list来对观察者做统一通知。另外，<strong>发布-订阅，MVC，事件驱动都是基于观察者模式。</strong></p><h1 id="附"><a href="#附" class="headerlink" title="附"></a>附</h1><p>相关代码：<a href="https://github.com/zhaohaoren/design_pattern_tutorial">https://github.com/zhaohaoren/design_pattern_tutorial</a></p><p>如有代码和文章问题，还请指正！感谢！</p>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;【设计模式】观察者模式&quot;&gt;&lt;a href=&quot;#【设计模式】观察者模式&quot; class=&quot;headerlink&quot;</summary>
        
      
    
    
    
    
    <category term="设计模式" scheme="http://lollipops.work/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>【设计模式】解释器模式</title>
    <link href="http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E8%A7%A3%E9%87%8A%E5%99%A8%E6%A8%A1%E5%BC%8F/"/>
    <id>http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E8%A7%A3%E9%87%8A%E5%99%A8%E6%A8%A1%E5%BC%8F/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2021-01-16T09:09:12.703Z</updated>
    
    <content type="html"><![CDATA[<h1 id="【设计模式】解释器模式"><a href="#【设计模式】解释器模式" class="headerlink" title="【设计模式】解释器模式"></a>【设计模式】解释器模式</h1><p>本文主要介绍解释器模式的概念和用法。</p><h2 id="模式背景"><a href="#模式背景" class="headerlink" title="模式背景"></a>模式背景</h2><p>类似于于Java，Cpp这类的语言，他们无法处理类似<code>1+2-3</code>这种语句的直接计算，但是如果我们需要这样的功能，该怎么实现呢？这就是需要解释器模式。这是一个比较复杂同时也相对比较冷门的设计模式，实际应用中很少，因为一般涉及到解释器模式的项目应该都是比较大的工程。</p><p>如果我们用过Scala或者Python应该知道他们都带了一个Shell工具，我们可以直接在Shell中输入一些简单的语句，他们能帮我们自动计算好，比如下面所示：</p><pre><code class="lang-shell">$ pythonPython 3.7.1 (default, Dec 14 2018, 13:28:58)[Clang 4.0.1 (tags/RELEASE_401/final)] :: Anaconda, Inc. on darwinType &quot;help&quot;, &quot;copyright&quot;, &quot;credits&quot; or &quot;license&quot; for more information.&gt;&gt;&gt; 12 * 3 + 238</code></pre><p>像这种功能，如果使用Python语言编写的话，需要自己定义变量，然后再通过式子运算等一系列操作。但是Python Shell就可以很方便的求出结果。我们可以使用解释器模式来实现这种功能，可以粗略的说，解释器模式就是为了在当前的语言上层构建一个新的语言（只是这个语言都很简单），这套语言拥有自己的规则文法。</p><h2 id="定义-amp-概念"><a href="#定义-amp-概念" class="headerlink" title="定义&amp;概念"></a>定义&amp;概念</h2><p>解释器模式：就是给定一个语言的文法表示，并且建立一个解释器，用来解释语言中的句子。解释器模式描述了怎样在有了一个简单的文法后，使用模式设计解释这些语句。解释器模式是一种类行为型模式。</p><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>解释器模式比较复杂，我们奔着目的去：外部给了我们一套文法规则，以及一条语句。我们使用手头上的语言（比如Java）能够去解释这个语句（可以是按照语句执行具体操作指令，也可以是用自己的话翻译一下提供的语句）。</p><p><strong>首先从语句表达式说起</strong></p><p><code>1+2+3-4</code>  我们假设就给了这样的一条语句。一个表达式是由一系列<strong>终结符表达式</strong>和<strong>非终结符表达式</strong>组成。终结符就是最小的不可分割的单元，比如<code>3</code>,<code>+</code>这些符号都是终结符。非终结符则是相当于表达式的一个子句，以及这个表达式都可以算一个非终结符（或者叫非终结表达式），比如<code>1+2</code>算一个非终结符。</p><p><strong>抽象语法树（AST）</strong></p><p>我们可以通过给定的句子来构建一个抽象语法树，可以更直观的区分终结符表达式和非终结符表达式。比如上式子可以表示为：</p><p> <img src="/Users/zhaohaoren/workspace/mycode/blog-docs/docs/设计模式/interpreter01.png" alt="image-20200826143546483" style="zoom: 67%;" /></p><p>可以看到加减号连接着左右，所以他们所连接在一起为非终结的表达式。而数字都是终结表达式。</p><p><strong>解释器模式</strong></p><p>所谓解释器模式，就是外界给定的文法，我们写出的程序能够解释出这段文法的意思。所以我们首先要明白文法的具体规则，然后就是区分好里面的终结符表达式和非终结符表达式。终结符表达式和非终结符表达式的区分是解释器模式的重点，不能粗浅的认为加减号用来连接数字的所以它就是非终结表达式，数字不起到连接的作用就是终结表达式，实际使用中还需要灵活多变。</p><p><strong>组成要素</strong></p><ul><li><p>抽象表达式（Abstract Expression）</p><ul><li>解释器统一抽象，约定解释器的解释操作，是终结符表达式和非终结符表达式的父类，主要包含解释方法 interpret()。</li></ul></li><li><p>终结符表达式（Terminal  Expression）</p><ul><li>是抽象表达式的子类，用来实现文法中与终结符相关的操作，文法中的每一个终结符都有一个具体终结表达式与之相对应。</li></ul></li><li><p>非终结符表达式（Nonterminal Expression）</p><ul><li>也是抽象表达式的子类，用来实现文法中与非终结符相关的操作，里面可以包含终结符表达式，也可以继续包含非终结符表达式，让下游去处理终结符表达式。</li></ul></li><li><p>环境类（Context）</p><ul><li>通常包含各个解释器需要的数据或是公共的功能，一般用来传递被所有解释器共享的数据，后面的解释器可以从这里获取这些值。</li></ul></li></ul><h2 id="UML"><a href="#UML" class="headerlink" title="UML"></a>UML</h2><p><img src="/Users/zhaohaoren/workspace/mycode/blog-docs/docs/设计模式/interpreter02.png" alt="image-20200826173129765"></p><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><p>这里举一个例子<code>&quot;LOOP 2 PRINT i SPACE PRINT am SPACE PRINT justin BREAK END PRINT hello SPACE PRINT world&quot;</code>。意思很简单：<code>LOOP</code>循环打印2次<code>i am justin</code>，BREAK表示换行，然后接着输出<code>hello world</code>。</p><ul><li>第一步，分析文法整理出文法规则：</li></ul><div class="table-container"><table><thead><tr><th>文法</th><th>说明</th></tr></thead><tbody><tr><td>expression : command *</td><td>一个表达式是多个命令组成的</td></tr><tr><td>command : loop \</td><td>primitive</td><td>大写部分都是命令，命令分2块：LOOP以及一些基本命令</td></tr><tr><td>loop : ‘LOOP number’ expression ‘END’</td><td>一个LOOP命令的表达式，LOOP和END为一组成对出现</td></tr><tr><td>primitive : ‘print string’ \</td><td>‘space’ \</td><td>‘break’</td><td>基本命令：这三个基本命令可以作为不可分割的单元了</td></tr></tbody></table></div><p>依据这规则我们可以区分出<strong>终结和非终结表达式</strong>：前三个都是非终结的，最后一个是终结的。</p><ul><li>第二步，设计Context类：</li></ul><pre><code class="lang-java">public class Context &#123;    //StringTokenizer将字符串分割为单个word，每个word叫做token    private StringTokenizer tokenizer;    //当前字符串标记    private String currentToken;    public Context(String text) &#123;        this.tokenizer = new StringTokenizer(text);        nextToken();    &#125;    //返回下一个标记    public String nextToken() &#123;        if (tokenizer.hasMoreTokens()) &#123;            currentToken = tokenizer.nextToken();        &#125; else &#123;            currentToken = null;        &#125;        return currentToken;    &#125;    //返回当前的标记    public String currentToken() &#123;        return currentToken;    &#125;    //跳过标记，说明这里语法有问题    public void skipToken(String token) &#123;        if (!token.equals(currentToken)) &#123;            System.out.println(&quot;错误提示：&quot; + currentToken + &quot;解释错误！&quot;);        &#125;        nextToken();    &#125;    //如果当前的标记是数字，返回其数组    public int currentNumber() &#123;        int number = 0;        try &#123;            number = Integer.parseInt(currentToken);        &#125; catch (NumberFormatException e) &#123;            System.err.println(&quot;错误提示：&quot; + e);        &#125;        return number;    &#125;&#125;</code></pre><p>Context的类的作用主要就是处理字符串，提供一系列的方法方便我们获取当前处理到哪个字符命令上，以及跳过改名了，取的下一个命令等操作。</p><ul><li>第三步，定义<strong>抽象表达式</strong>：</li></ul><pre><code class="lang-java">public abstract class Node &#123;    //解释语句的方法    public abstract void interpret(Context context);    //执行命令    public abstract void execute();&#125;</code></pre><ul><li>第四步，定义终结符表达式：</li></ul><pre><code class="lang-java">// 基本命令，最简单的命令，终结符表达式public class PrimitiveCommandNode extends Node &#123;    private String name;    private String text;    @Override    public void interpret(Context context) &#123;        name = context.currentToken();        context.skipToken(name);        if (!name.equals(&quot;PRINT&quot;) &amp;&amp; !name.equals(&quot;BREAK&quot;) &amp;&amp; !name.equals(&quot;SPACE&quot;)) &#123;            System.err.println(&quot;非法命令！&quot;);        &#125;        if (name.equals(&quot;PRINT&quot;)) &#123;            text = context.currentToken();            context.nextToken();        &#125;    &#125;    @Override    public void execute() &#123;        if (name.equals(&quot;PRINT&quot;)) &#123;            System.out.print(text);        &#125; else if (name.equals(&quot;SPACE&quot;)) &#123;            System.out.print(&quot; &quot;);        &#125; else if (name.equals(&quot;BREAK&quot;)) &#123;            System.out.println();        &#125;    &#125;&#125;</code></pre><p>我们对终结符表达式做了一个通用模板，终结符表达式涉及到了程序真正的输出，所有输出行为在execute中执行。一个PrimitiveCommandNode即命令和该命令对应的输出组成的一个tuple。</p><ul><li>第五步，定义非终结符表达式</li></ul><p>非终结符这里有三层：</p><ol><li>第一层表示一个子句，一个END结果我们可以看做是一个子句</li><li>第二层是命令，从Context获取到当前的命令类型是普通命令，还是LOOP命令来创建下一个子级。</li><li>第三层是循环，获取循环需要的信息，执行循环的逻辑。</li></ol><p>这三层将一个非终结符不断的拆分更细，一直到解释到非终结符上去。</p><p><strong>ExpressionNode：</strong></p><pre><code class="lang-java">public class ExpressionNode extends Node &#123;    //存储一个子表达式的命令    private ArrayList&lt;Node&gt; list = new ArrayList&lt;&gt;();    //第一级别解释，循环的取出所有的token    @Override    public void interpret(Context context) &#123;        while (true) &#123;            if (context.currentToken() == null) &#123;                //空的串，那就退出。                break;            &#125; else if (&quot;END&quot;.equals(context.currentToken())) &#123;                context.skipToken(&quot;END&quot;);                break;            &#125; else &#123;                Node commandNode = new CommandNode();                commandNode.interpret(context);                list.add(commandNode);            &#125;        &#125;    &#125;    @Override    public void execute() &#123;        Iterator iterator = list.iterator();        while (iterator.hasNext()) &#123;            ((Node) iterator.next()).execute();        &#125;    &#125;&#125;</code></pre><p>这个Node主要就是不断遍历输入表达式的词项（token），然后交给<code>CommandNode</code>，让<code>CommandNode</code>来对命令做分类处理。最终处理完，会将所有的命令存在list中，我们只需要迭代执行这个list，将结果输出。</p><p><strong>CommandNode：</strong></p><pre><code class="lang-java">public class CommandNode extends Node &#123;    private Node node;    @Override    public void interpret(Context context) &#123;        if (context.currentToken().equals(&quot;LOOP&quot;)) &#123;            node = new LoopCommandNode();            node.interpret(context);        &#125; else &#123;            node = new PrimitiveCommandNode();            node.interpret(context);        &#125;    &#125;    @Override    public void execute() &#123;        node.execute();    &#125;&#125;</code></pre><p>这个类的主要作用就是<code>ExpressionNode</code>遍历到了一个命令，给到该类来区分这个命令是普通命令还是LOOP命令。</p><p><strong>LoopCommandNode：</strong></p><pre><code class="lang-java">public class LoopCommandNode extends Node &#123;    private int number;    private Node commandNode;    @Override    public void interpret(Context context) &#123;        context.skipToken(&quot;LOOP&quot;);        number = context.currentNumber();        context.nextToken();        commandNode = new ExpressionNode();        commandNode.interpret(context);    &#125;    @Override    public void execute() &#123;        for (int i = 0; i &lt; number; i++) &#123;            commandNode.execute();        &#125;    &#125;&#125;</code></pre><p>当命令是LOOP的时候，我们需要获取后面的一个数字表示要循环几次，然后还需要获取后面遍历的表达式，所以递归去使用<code>ExpressionNode</code>获取后面表达式，一旦遇到<code>END</code>这个递归栈就结束了，然后回到<code>ExpressionNode</code>处理下面的命令。</p><ul><li>最后测试</li></ul><pre><code class="lang-java">public class InterpreterDemo &#123;    public static void main(String[] args) &#123;        String text = &quot;LOOP 2 PRINT i SPACE PRINT am SPACE PRINT justin BREAK END PRINT hello SPACE PRINT world&quot;;        Context context = new Context(text);        Node node = new ExpressionNode();        node.interpret(context);        node.execute();    &#125;&#125;/** output:i am justini am justinhello world*/</code></pre><ul><li>UML结构</li></ul><p><img src="/Users/zhaohaoren/workspace/mycode/blog-docs/docs/设计模式/interpreter03.png" alt="image-20200826174016951"></p><blockquote><p>这是书上的一个例子，个人觉得这个例子有点深了，看的有点晕晕乎乎的，对解释器模式的描述不是很直观。但是这个应该更贴近实际使用场景，所以还是用了这个例子。</p></blockquote><h2 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h2><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul><li>易于改变和扩展文法，实现文法比较容易。</li><li>解释器扩展性好，如果添加新的表达式，只需要添加对应的终结和非终结表达式即可。</li></ul><h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul><li>对于复杂文法难以维护。</li><li>执行效率太低，里面存在大量循环和递归，执行速度可能会很慢。</li></ul><h2 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h2><ul><li>可以将一个需要解释执行的语言中的句子表示为一个抽象语法树。</li><li>一些重复出现的问题可以用一种简单的语言来表达。</li><li>一个简单语法需要解释的场景。</li></ul><p>总之，这些场景其实也很特殊，一般项目中我们并不会用到，而且这个模式写起来也很复杂。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>解释器模式为自定义语言的设计和实现提供了一种解决方案，它用于定义一组文法规则并通过这组文法规则来解释语言中的句子。虽然解释器模式的使用频率不是特别高，但是它在<strong>正则表达式</strong>、<strong>XML文档解释</strong>等领域还是得到了广泛使用。</p><h1 id="附"><a href="#附" class="headerlink" title="附"></a>附</h1><p>相关代码：<a href="https://github.com/zhaohaoren/design_pattern_tutorial">https://github.com/zhaohaoren/design_pattern_tutorial</a></p><p>如有代码和文章问题，还请指正！感谢！</p>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;【设计模式】解释器模式&quot;&gt;&lt;a href=&quot;#【设计模式】解释器模式&quot; class=&quot;headerlink&quot; title=&quot;【设计模式】解释器模式&quot;&gt;&lt;/a&gt;【设计模式】解释器模式&lt;/h1&gt;&lt;p&gt;本文主要介绍解释器模式的概念和用法。&lt;/p&gt;
&lt;h2</summary>
        
      
    
    
    
    
    <category term="设计模式" scheme="http://lollipops.work/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>【设计模式】迭代器模式</title>
    <link href="http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E8%BF%AD%E4%BB%A3%E5%99%A8%E6%A8%A1%E5%BC%8F/"/>
    <id>http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E8%BF%AD%E4%BB%A3%E5%99%A8%E6%A8%A1%E5%BC%8F/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2021-01-16T09:09:20.050Z</updated>
    
    <content type="html"><![CDATA[<h1 id="【设计模式】迭代器模式"><a href="#【设计模式】迭代器模式" class="headerlink" title="【设计模式】迭代器模式"></a>【设计模式】迭代器模式</h1><p>本文主要介绍迭代器模式原理和实现。</p><h2 id="模式背景"><a href="#模式背景" class="headerlink" title="模式背景"></a>模式背景</h2><p>对于一个<code>list</code>型数据，一般都会承担2个职责：<strong>存储数据</strong>，<strong>遍历数据</strong>。我们系统一般都会存在一些类，这些类的主要作用就是内部有个聚合对象（就是一堆对象，可能是list，set，map各种形式），对外我们可以存储和访问这些数据。正常思路上，这很简单，就把数据的存储和遍历方法都写在一个类里面就行了嘛！但是这种写法存在很多的缺陷：</p><ol><li>这个聚合类职责过重，既负责存储，又负责遍历，可能会导致类很庞大。</li><li>我们存储数据和访问数据提供的方法五花八门，没法让客户端按照一个统一的方式来进行使用。（这个可以使用抽象来解决，但是如果所有的方法都抽象了，抽象类中的方法也挺多的，不利于子类实现）</li><li>我们设想一下，很多聚合类，他们存储是一样的，但是遍历不一定是一样的，所以遍历这个抽象一定会让子类自己实现，那如果系统很多聚合类都是list，偶尔几个是其他的特殊类型，我们那些list的遍历代码是一样的，就有大量的代码重复。</li></ol><p>所以迭代器模式的思路就是将数据的存储和遍历进行隔离解耦（本质奔着的目的还是因为这个聚合类的职责太重了）。我们认为数据的遍历是可以变化，可以分离的。</p><h2 id="定义-amp-概念"><a href="#定义-amp-概念" class="headerlink" title="定义&amp;概念"></a>定义&amp;概念</h2><p><strong>迭代器模式</strong>（Iterator）：提供一种方法顺序访问一个聚合对象中的各种元素，而又不暴露该对象的内部表示。其别名为游标（Cursor）。迭代器模式是一种对象行为型模式。</p><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>我们要将聚合类的数据存储和数据的遍历给隔离，将遍历数据的行为从聚合类中抽取出来。整个迭代器模式就分为了2个体系，这2个体系都有各自的一个抽象：<strong>聚合类的抽象</strong>，<strong>迭代器的抽象</strong>。让抽象聚合类及其子类专门负责数据的存储等操作，抽象迭代器以及子类专门负责数据的遍历等操作。</p><p><strong>组成要素</strong></p><ul><li>抽象迭代器：定义遍历集合元素的接口，以及一些其他的访问集合元素的操作。</li><li>具体迭代器：迭代器遍历的访问的具体逻辑实现。</li><li>抽象集合类：申明一个创建迭代器方法的抽象类。用于存储和管理对象。</li><li>具体聚合类：抽象集合类具体实现，主要返回一个针对该具体聚合类的具体迭代器对象。</li></ul><p>因为迭代器和聚合类本身又具有整体性，我们有时候也会将迭代器以内部类的形式放到聚合类里面。JDK的迭代器实现其实就是使用的内部类的方式。</p><p>不管迭代器是外部，还是内部，客户端访问使用的方式都是一致的。客户端只需要通过抽象聚合操作数据存储，通过迭代器进行遍历数据。</p><h2 id="UML"><a href="#UML" class="headerlink" title="UML"></a>UML</h2><p><img src="/Users/zhaohaoren/workspace/mycode/blog-docs/docs/设计模式/iterator.png" alt="image-20200827145040127"></p><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><p>抽象聚合类</p><pre><code class="lang-java">public abstract class AbstractObjectList &#123;    protected List&lt;Object&gt; objects = new ArrayList&lt;&gt;();    public AbstractObjectList(List objects) &#123;        this.objects = objects;    &#125;    public void addObject(Object object) &#123;        this.objects.add(object);    &#125;    public void removeObject(Object object) &#123;        this.objects.remove(object);    &#125;    public List&lt;Object&gt; getObjects() &#123;        return objects;    &#125;    /**     * 创建一个迭代器     */    public abstract AbstractIterator createIterator();&#125;</code></pre><p>具体集合类</p><pre><code class="lang-java">public class ConcreteList extends AbstractObjectList &#123;    public ConcreteList(List objects) &#123;        super(objects);    &#125;    @Override    public AbstractIterator createIterator() &#123;        return new ConcreteIterator(this);    &#125;&#125;</code></pre><p>迭代器抽象</p><pre><code class="lang-java">interface AbstractIterator &#123;    void next();    boolean isLast();    void previous();    boolean isFirst();    Object getNextItem();    Object getPreviousItem();&#125;</code></pre><p>具体迭代器</p><pre><code class="lang-java">public class ConcreteIterator implements AbstractIterator &#123;    /**     * 引用被迭代的数据     */    private ConcreteList concreteList;    private List data;    /**     * 开始位置 往后移动的标志     */    private int cursor1;    /**     * 结束位置 往前移动的标志     */    private int cursor2;    public ConcreteIterator(ConcreteList concreteList) &#123;        this.concreteList = concreteList;        this.data = concreteList.getObjects();        cursor1 = 0;        cursor2 = data.size() - 1;    &#125;    @Override    public void next() &#123;        if (cursor1 &lt; data.size()) &#123;            cursor1++;        &#125;    &#125;    @Override    public boolean isLast() &#123;        return cursor1 == data.size();    &#125;    @Override    public void previous() &#123;        if (cursor2 &gt; -1) &#123;            cursor2--;        &#125;    &#125;    @Override    public boolean isFirst() &#123;        return cursor2 == -1;    &#125;    @Override    public Object getNextItem() &#123;        return data.get(cursor1);    &#125;    @Override    public Object getPreviousItem() &#123;        return data.get(cursor2);    &#125;&#125;</code></pre><p>使用</p><pre><code class="lang-java">List data = new ArrayList();data.add(&quot;A&quot;);data.add(&quot;B&quot;);data.add(&quot;C&quot;);data.add(&quot;D&quot;);data.add(&quot;E&quot;);data.add(&quot;F&quot;);ConcreteList concreteList = new ConcreteList(data);AbstractIterator iterator = concreteList.createIterator();while (!iterator.isLast()) &#123;    System.out.println(iterator.getNextItem());    iterator.next();&#125;</code></pre><p><strong>内部类迭代器</strong></p><p>可以将迭代器放入到具体实现类中，作为一个内部类。JDK就是这么玩的。</p><pre><code class="lang-java">public class InnerClassIteratorList extends AbstractObjectList &#123;    public InnerClassIteratorList(List objects) &#123;        super(objects);    &#125;    @Override    public AbstractIterator createIterator() &#123;        return new InnerIterator();    &#125;    private class InnerIterator implements AbstractIterator &#123;        private int cursor1;        private int cursor2;        public InnerIterator() &#123;            cursor1 = 0;            cursor2 = objects.size() - 1;        &#125;        @Override        public void next() &#123;            if (cursor1 &lt; objects.size()) &#123;                cursor1++;            &#125;        &#125;        @Override        public boolean isLast() &#123;            return cursor1 == objects.size();        &#125;        @Override        public void previous() &#123;            if (cursor2 &gt; -1) &#123;                cursor2--;            &#125;        &#125;        @Override        public boolean isFirst() &#123;            return cursor2 == -1;        &#125;        @Override        public Object getNextItem() &#123;            return objects.get(cursor1);        &#125;        @Override        public Object getPreviousItem() &#123;            return objects.get(cursor2);        &#125;    &#125;&#125;</code></pre><h2 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h2><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul><li>可以使用不同的方式遍历一个聚合对象，只需要扩展迭代器抽象就行了</li><li>简化了聚合类</li><li>迭代器和聚合类都有抽象层，很方便扩展。</li></ul><h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul><li>一定程度增加了系统的复杂度</li></ul><h2 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h2><ul><li>需要访问一个聚合对象，但又不想暴露其内部实现细节的情况。</li><li>一个聚合对象需要多种遍历方式，后面可能需要动态变化其遍历方式。</li></ul><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>主要目的就是将对集合的遍历操作从类中抽取独立出来。形成2个继承系统：聚合类抽象体系负责数据的存储删除等操作，迭代器体系负责数据遍历读取的操作。</p><h1 id="附"><a href="#附" class="headerlink" title="附"></a>附</h1><p>相关代码：<a href="https://github.com/zhaohaoren/design_pattern_tutorial">https://github.com/zhaohaoren/design_pattern_tutorial</a></p><p>如有代码和文章问题，还请指正！感谢！</p>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;【设计模式】迭代器模式&quot;&gt;&lt;a href=&quot;#【设计模式】迭代器模式&quot; class=&quot;headerlink&quot; title=&quot;【设计模式】迭代器模式&quot;&gt;&lt;/a&gt;【设计模式】迭代器模式&lt;/h1&gt;&lt;p&gt;本文主要介绍迭代器模式原理和实现。&lt;/p&gt;
&lt;h2</summary>
        
      
    
    
    
    
    <category term="设计模式" scheme="http://lollipops.work/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>【设计模式】适配器模式</title>
    <link href="http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E9%80%82%E9%85%8D%E5%99%A8%E6%A8%A1%E5%BC%8F/"/>
    <id>http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E9%80%82%E9%85%8D%E5%99%A8%E6%A8%A1%E5%BC%8F/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2021-01-16T09:09:26.899Z</updated>
    
    <content type="html"><![CDATA[<h1 id="【设计模式】适配器模式"><a href="#【设计模式】适配器模式" class="headerlink" title="【设计模式】适配器模式"></a>【设计模式】适配器模式</h1><p>本文主要介绍：类适配器模式，双向适配器模式，缺省适配器模式。</p><h2 id="模式背景"><a href="#模式背景" class="headerlink" title="模式背景"></a>模式背景</h2><p>在我们生活中就存在着很多适配器模式，比如电源适配器，最新的苹果耳机接口（共用充电口的那个），插座适配器。这些都是因为我们现有的一些物件和外界提供的物件无法正确匹配，当需要能使用对方的时候，就需要一个中间的适配器来适配他们。</p><p>在开发中性质其实也是一样，我们现有的工程项目一直以来都依赖这一个老的模块（就像是我们2个孔的插座），现在因为某种原因我们要替换该模块使用一个别的模块（比如市面上有新的模块了，然后这个模块比老模块实现更好更强大），但是这个新模块的接口和我们原来使用的不一样（就像是我们一直用的2孔插座，现在眼前只有3孔插座）。这时候我们就需要一个适配器来让我当前的项目，适配到这个新模块上（我们要一个适配器，2孔的插上去就能再插3孔的了）。</p><h2 id="定义-amp-概念"><a href="#定义-amp-概念" class="headerlink" title="定义&amp;概念"></a>定义&amp;概念</h2><p>将一个接口转换为客户端希望的另一个接口，使得那些接口不兼容的类可以一起工作，其别名为包装器（Wrapper）。适配器模式是一种<strong>类结构性模式</strong>，也可以是<strong>对象结构型模式</strong>。</p><p>适配器模式可以分为<strong>对象适配器模式</strong>和<strong>类适配器模式</strong>。对象适配器中，适配器和适配者之间是关联关系。类适配器中，适配器和适配者之间是继承（或实现）关系。实际开发中<u>对象适配器的使用频率更高</u>。</p><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>适配器模式通过增加一个新的适配器类来解决接口不兼容的问题，使得原来没有任何关系的类可以协同工作。</p><p><strong>基本要素</strong></p><ul><li>适配者<ul><li>就是我们实际想要使用模块，他可以是一个类或接口，也可以是一个对象，只是这个适配者我们不能直接使用。</li></ul></li><li>适配器<ul><li>具体的转换器，通过继承适配者，或者组合适配者，来调用适配者的方法。</li></ul></li><li>目标抽象类<ul><li>用来定义客户需要使用的接口，他可以是一个抽象类或者接口。</li><li>他约束了适配器类的行为，同时也是为了方便系统对适配器类进行扩展（添加新的Adapter实现）。</li></ul></li></ul><h2 id="UML"><a href="#UML" class="headerlink" title="UML"></a>UML</h2><p><img src="D:\workspace\blog-docs\docs\设计模式\Adapter1.png" alt="Adapter1"></p><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><h3 id="对象适配器"><a href="#对象适配器" class="headerlink" title="对象适配器"></a>对象适配器</h3><p>适配器内部持有一个被适配的对象，然后通过这个对象类转发调用的。</p><p><strong>具体需要被适配的类</strong> </p><pre><code class="lang-java">public interface IAdaptee &#123;    void adapteeFunction();&#125;public class Adaptee implements IAdaptee &#123;    @Override    public void adapteeFunction() &#123;        // 客户端使用方 想要调用的方法        System.out.println(&quot;adaptee function&quot;);    &#125;&#125;</code></pre><p><strong>客户端使用的接口</strong></p><pre><code class="lang-java">public interface ITarget &#123;    void userFunction();&#125;</code></pre><p><strong>适配器类（注意核心：内部组合的adaptee对象）</strong></p><pre><code class="lang-java">class ObjectAdapter implements ITarget &#123;    /**内组合一个被适配的对象*/    Adaptee adaptee;    @Override    public void userFunction() &#123;        adaptee.adapteeFunction();    &#125;&#125;</code></pre><h3 id="类适配器"><a href="#类适配器" class="headerlink" title="类适配器"></a>类适配器</h3><p>只是将对象适配器中组合关联的 被适配对象使用<strong>继承的方式</strong>来实现适配器类。</p><p><strong>（注意核心：extends Adaptee类 ）</strong></p><pre><code class="lang-java">class ClassAdapter extends Adaptee implements ITarget &#123;    @Override    public void userFunction() &#123;        this.adapteeFunction();    &#125;&#125;</code></pre><h2 id="装饰模式区别"><a href="#装饰模式区别" class="headerlink" title="装饰模式区别"></a>装饰模式区别</h2><p>适配器模式的思想其实和装饰者模式很像，并且他们都属于结构型模式，很容易会混淆他们用法。他们实现的手法思想虽然相似，但是各自的目的却不一样。</p><p><strong>适配器模式的目的：</strong>现有的这个接口挺好的，只是我们这边客户端无法兼容它，他<strong>强调在于适配</strong>，只是适配一下然后原样调用适配者方法，我们不会去修改原先的接口（当然你非要修改也拦不住你，但是修改了就不属于适配的思想了，<strong>适配器主要的工作应该是将原来无法兼容的接口，通过你的操作变成兼容的，本质上不修改原来接口的任何逻辑</strong>）。</p><p><strong>装饰者模式的目的：</strong>现有的这个接口功能不够，我们希望对这个接口新加一些功能，对接口做扩展，他<strong>强调在于扩展</strong>。他会修改原先的接口实现。</p><h2 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h2><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul><li>将目标类和适配者进行解耦，当需要新的适配的时候，只需要添加新的适配器，不需要修改代码。</li><li>让同一个适配者类，可以在多个不同的系统中复用。提高了适配者的复用性。（因为市面上有好多转换器，所以我买手机买Type-C接口的都能找到对应的适配器来充电）</li></ul><h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul><li>Java，C#这些单继承的语言，一次只能继承一个适配者类，不能同时适配多个适配者。</li><li>适配者类不能是final的。</li><li>即对使用的编程语言存在局限性。</li></ul><h2 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h2><ul><li>系统需要使用现有的类，但现有的类却不兼容。</li><li>需要建立一个可以重复使用的类，用于一些彼此关系不大的类，并易于扩展，以便于面对将来会出现的类。</li><li>需要一个统一的输出接口，但是输入类型却不可预知。</li></ul><p><strong>实例</strong></p><ul><li>在系统重构的时候，发现以前人写的接口规范已经返回的数据有有问题，所以我重构了这些接口，但是老接口系统已经在用了，不能让前端再针对你的去全改下，这时候我就对我新写的接口加了一个适配层。这样老接口保留着，前端后面直接使用新的接口。</li></ul><h2 id="扩展"><a href="#扩展" class="headerlink" title="扩展"></a>扩展</h2><h3 id="双向适配器"><a href="#双向适配器" class="headerlink" title="双向适配器"></a>双向适配器</h3><p><strong>定义：</strong>在一个适配器中既包含用户端的方法，又包含被适配端的方法。使得通过该适配器可以当做A到B的适配，也能当做B到A的适配。</p><p>即做了一个适配器：这个适配器是客户端对适配者的适配，也是适配者对客户端的适配，所以这个适配器中需要有客户端的引用，和适配者的引用。</p><p><strong>实现</strong></p><pre><code class="lang-java">class TwoWayAdapter implements IAdaptee, ITarget &#123;    private IAdaptee adaptee;    private ITarget target;        /**我们想给客户端用 就可以使用被适配的对象初始化它*/    public TwoWayAdapter(IAdaptee adaptee) &#123;        this.adaptee = adaptee;    &#125;        /**我们想让被适配的类使用客户端的，则反之*/    public TwoWayAdapter(ITarget target) &#123;        this.target = target;    &#125;    /**     * 给客户端用     * 可以作为为客户端 适配的适配器     */    @Override    public void adapteeFunction() &#123;        target.userFunction();    &#125;    /**     * 给被适配类用，被适配的类成了客户端     * 也可以作为为被适配器类 适配了客户端     */    @Override    public void userFunction() &#123;        adaptee.adapteeFunction();    &#125;&#125;</code></pre><h3 id="缺省适配器"><a href="#缺省适配器" class="headerlink" title="缺省适配器"></a>缺省适配器</h3><p>这个也是使用很广泛的一个适配器模式：对于一个接口，我们不想实现这个接口所有的方法，可以使用一个抽象类来实现该接口并提供一个默认实现。然后再让子类去覆盖自己想要实现的方法。</p><p><strong>这么做的好处：</strong></p><p>当一个接口有很多方法的时候，我们项目中需要如果很多类需要实现这个接口的时候，那么就要每个类都要实现这个接口的所有方法，这么很费劲。可以在中间加入一个抽象类，这样具体的类只要覆盖这个抽象类就行了。</p><p><strong>必要条件</strong></p><ul><li>一个接口，最好是有很多方法</li><li>一个抽象类，实现该接口提供默认实现</li><li>具体的业务类，继承抽象类覆盖所需要的逻辑</li></ul><p><strong>实现</strong></p><pre><code class="lang-java">/** * 一个很多方法的接口 */interface ServiceInterface &#123;    void f1();    void f2();    void f3();    void f4();&#125;/** * 缺省适配器 * 使用抽象类适配一版本，提供接口默认实现 */abstract class AbastractServiceClass implements ServiceInterface &#123;    @Override    public void f1() &#123;        System.out.println(&quot;abs f1&quot;);    &#125;    @Override    public void f2() &#123;        System.out.println(&quot;abs f2&quot;);    &#125;    @Override    public void f3() &#123;        System.out.println(&quot;abs f3&quot;);    &#125;    @Override    public void f4() &#123;        System.out.println(&quot;abs f4&quot;);    &#125;&#125;/** * 对应具体业务去实现对应的接口方法即可。 */class Service1 extends AbastractServiceClass &#123;    @Override    public void f1() &#123;        System.out.println(&quot;service 1&quot;);    &#125;&#125;class ServiceN extends AbastractServiceClass &#123;    @Override    public void f4() &#123;        System.out.println(&quot;service 4&quot;);    &#125;&#125;</code></pre><h1 id="附"><a href="#附" class="headerlink" title="附"></a>附</h1><p>相关代码：<a href="https://github.com/zhaohaoren/design_pattern_tutorial">https://github.com/zhaohaoren/design_pattern_tutorial</a></p><p>如有代码和文章问题，还请指正！感谢！</p>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;【设计模式】适配器模式&quot;&gt;&lt;a href=&quot;#【设计模式】适配器模式&quot; class=&quot;headerlink&quot;</summary>
        
      
    
    
    
    
    <category term="设计模式" scheme="http://lollipops.work/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>【设计模式】访问者模式</title>
    <link href="http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E8%AE%BF%E9%97%AE%E8%80%85%E6%A8%A1%E5%BC%8F/"/>
    <id>http://lollipops.work/2020/05/10/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E8%AE%BF%E9%97%AE%E8%80%85%E6%A8%A1%E5%BC%8F/</id>
    <published>2020-05-09T16:49:54.000Z</published>
    <updated>2021-01-16T09:09:16.744Z</updated>
    
    <content type="html"><![CDATA[<h1 id="【设计模式】访问者模式"><a href="#【设计模式】访问者模式" class="headerlink" title="【设计模式】访问者模式"></a>【设计模式】访问者模式</h1><p>本文主要介绍访问者模式的概念及用法。</p><h2 id="模式背景"><a href="#模式背景" class="headerlink" title="模式背景"></a>模式背景</h2><p>在系统，有时候一系列对象需要被以不同的方式访问。比如医院开的药方，在医院财务那这个药房只是用来计算价格，在抓药那里这个药方是用来抓药的。类比到实际项目使用中：一个集合里面可能会有几类元素，不同类型的元素可以有不同的访问者访问他，并且有不同的访问执行逻辑。如果没有一个好的模式支持，那么我们可能需要在某个类中使用大量的<code>if-else</code>来对不同访问者进行判别来进行分开处理。但是这样问题也很明显：类的职责重，大量的<code>if-else</code>代码一定不优雅，一旦访问者或者被访问对象需要修改/添加/移除等操作，这种方式基本就毫无扩展性而言。所以这时候如何很好的添加新的访问者而不需要修改代码就是访问者模式提供的。</p><h2 id="定义-amp-概念"><a href="#定义-amp-概念" class="headerlink" title="定义&amp;概念"></a>定义&amp;概念</h2><p><strong>访问者模式</strong>：提供一个作用于某对象结构中的各元素的操作表示，它使得可以在不改变各元素的类的前提下定义作用于这些元素的新操作。访问者模式就是一种对象行为型模式。</p><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>访问者模式是一个比较复杂的设计模式。整体结构我们可以分为2层：访问层，元素层（也可以说是被访问层）。其核心的思想还是<strong>去除<code>if-else</code>将各个不同的操作封装到不同的访问者对象类中去</strong>。为了方便扩展，对元素和访问者都提供了抽象层，而元素层，我们还需要通过一个<strong>对象结构</strong>（可以就理解为是一个集合，来定义这些元素的结构的）来对元素的结构进行组织。</p><p>主要看看他们的组成元素，注意里面最精髓的2个地方就是：<code>accept</code>和<code>visit</code>方法。</p><p><strong>组成要素</strong></p><ul><li>抽象访问者（Visitor）<ul><li>主要是为了访问者的扩展。</li><li>里面定义了一系列的<code>visit</code>方法（这个方法可以通过方法名来区分，也可以使用参数来重载），用来指定不同元素对象该访问者所需要对应的不同操作逻辑。<strong>一般就一个元素提供一个visit方法。</strong></li></ul></li><li>具体访问者（ConcreteVisitor）<ul><li>实现抽象的访问逻辑。</li></ul></li><li>抽象元素（Element）<ul><li>定义一个<code>accept</code>方法，参数通常就是抽象访问者。</li></ul></li><li>具体元素（ConcreteElement）<ul><li>实现<code>accept</code>完成对一个元素访问想要做的操作。本质就是调用传递来的抽象访问者的方法（即调用visitor的visit方法）。</li><li>这种机制也称为<strong>双重分派</strong>，这样利用参数重载的特性，我们可以不用修改任何代码，添加的访问者都可以这种方式进行传递调用（通过参数重载来调用对应的方法）。</li></ul></li><li>对象结构（ObjectStructure）<ul><li>用来存放元素对象的。并提供遍历内部元素的方法。</li><li>可以使用组合模式来实现。也可以是一个简单的集合对象。</li></ul></li></ul><p>概括一下他的思想：一个叫对象结构的东西来封装集合的结构，然后具体的元素抽出去，形成一个体系。访问者又是另外一个体系。 <strong>访问者体系中，每一个访问者都要对需要的操作实现一个visit方法，这个方法将来通过将自己传递给具体的元素对象，然后通过元素对象来进行调用！</strong></p><p>其复杂性也正是在于visit和accept方法的设计上，确实很精巧，但也确实让结构变得没那么直观。</p><h2 id="UML"><a href="#UML" class="headerlink" title="UML"></a>UML</h2><p><img src="/Users/zhaohaoren/workspace/mycode/blog-docs/docs/设计模式/visitor.png" alt="image-20200914204847619"></p><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><p>抽象访问者</p><pre><code class="lang-java">public interface Visitor &#123;    void visit(ConcreteElementA concreteElementA);    void visit(ConcreteElementB concreteElementB);&#125;</code></pre><p>具体访问者</p><pre><code class="lang-java">public class ConcreteVisitorA implements Visitor &#123;    @Override    public void visit(ConcreteElementA concreteElementA) &#123;        System.out.println(&quot;A visit:&quot; + concreteElementA.getName());    &#125;    @Override    public void visit(ConcreteElementB concreteElementB) &#123;        System.out.println(&quot;A visit:&quot; +concreteElementB.getName());    &#125;&#125;</code></pre><p>抽象元素</p><pre><code class="lang-java">public interface Element &#123;    /**     * 这accept对该类的元素传入一个accept。则集合中该元素都会使用该访问者处理。     * 而这个访问者也实现了visit该元素的方法。所以传入this就可以重载到对应实现逻辑里面     * @param visitor     */    void accept(Visitor visitor);&#125;</code></pre><p>具体元素</p><pre><code class="lang-java">public class ConcreteElementA implements Element &#123;    private String name;    public ConcreteElementA(String name) &#123;        this.name = name;    &#125;    @Override    public void accept(Visitor visitor) &#123;        visitor.visit(this);    &#125;    public String getName() &#123;        return name;    &#125;&#125;</code></pre><p>对象结构</p><pre><code class="lang-java">public class ObjectStructure &#123;    /**     * 使用集合集合来存放所有的元素。     */    private List&lt;Element&gt; list = new ArrayList&lt;&gt;();    public void addElement(Element e) &#123;        list.add(e);    &#125;    public void removeElement(Element e) &#123;        list.remove(e);    &#125;    /**     * 使用某个访问者来遍历集合元素     */    public void accept(Visitor visitor) &#123;        list.forEach(item -&gt; item.accept(visitor));    &#125;&#125;</code></pre><p>使用</p><pre><code class="lang-java">Element e1, e2, e3, ee1, ee2, ee3;ObjectStructure objectStructure = new ObjectStructure();e1 = new ConcreteElementA(&quot;A1&quot;);e2 = new ConcreteElementA(&quot;A2&quot;);e3 = new ConcreteElementA(&quot;A3&quot;);ee1 = new ConcreteElementA(&quot;B1&quot;);ee2 = new ConcreteElementA(&quot;B2&quot;);ee3 = new ConcreteElementA(&quot;B3&quot;);objectStructure.addElement(e1);objectStructure.addElement(e2);objectStructure.addElement(e3);objectStructure.addElement(ee1);objectStructure.addElement(ee2);objectStructure.addElement(ee3);//可以通过配置来获取使用哪个访问类Visitor v = new ConcreteVisitorA();objectStructure.accept(v);</code></pre><h2 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h2><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul><li>各角色的职责相互隔离，符合单一职责原则。<ul><li>Visitor、Element 、ObjectStructure各司其责，职责清晰。</li></ul></li><li>扩展性好，添加新的访问者不需要修改原代码，对于访问者来说，符合开闭原则。</li></ul><h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul><li>不能解决添加一个元素类的问题，一旦添加一个元素类，就要修改所有相关的访问者，这一点违背了开闭原则。</li><li>可能破坏元素类的封装性，因为该模式需要访问者对象去调用元素对象的具体操作，所以可能需要元素对象暴露自己一些内部状态，来配合访问者对象一起完成操作。</li></ul><h2 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h2><p>访问者使用的条件较为苛刻，结构也很复杂，所以实际应用使用的频率不高。当你系统中存在一个比较复杂的对象结构，并且存在着不同的访问者并对其访问的操作也不同的时候，可以使用访问者模式。</p><p>现有的一些实际应用：XML文档解析，编译器设计等。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>我们要根据具体情况来评估是否适合使用访问者模式，例如，我们的对象结构是否足够稳定，是否需要经常定义新的操作，使用访问者模式是否能优化我们的代码，而不是使我们的代码变得更复杂。</p><h1 id="附"><a href="#附" class="headerlink" title="附"></a>附</h1><p>相关代码：<a href="https://github.com/zhaohaoren/design_pattern_tutorial">https://github.com/zhaohaoren/design_pattern_tutorial</a></p><p>如有代码和文章问题，还请指正！感谢！</p>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;【设计模式】访问者模式&quot;&gt;&lt;a href=&quot;#【设计模式】访问者模式&quot; class=&quot;headerlink&quot; title=&quot;【设计模式】访问者模式&quot;&gt;&lt;/a&gt;【设计模式】访问者模式&lt;/h1&gt;&lt;p&gt;本文主要介绍访问者模式的概念及用法。&lt;/p&gt;
&lt;h2</summary>
        
      
    
    
    
    
    <category term="设计模式" scheme="http://lollipops.work/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>【Java】HashMap的负载因子为什么是0.75</title>
    <link href="http://lollipops.work/2020/05/10/Java-JDK-HashMap%E7%9A%84%E8%B4%9F%E8%BD%BD%E5%9B%A0%E5%AD%90%E4%B8%BA%E4%BB%80%E4%B9%88%E6%98%AF0.75/"/>
    <id>http://lollipops.work/2020/05/10/Java-JDK-HashMap%E7%9A%84%E8%B4%9F%E8%BD%BD%E5%9B%A0%E5%AD%90%E4%B8%BA%E4%BB%80%E4%B9%88%E6%98%AF0.75/</id>
    <published>2020-05-09T16:45:03.000Z</published>
    <updated>2021-01-16T09:05:31.923Z</updated>
    
    <content type="html"><![CDATA[<p>起初看hashmap源码的时候，最让我困惑的地方就是这里。相信很多人也有过这样的问题，相信很多人和我一样也是网上百度了一通，相信很多人和我一样百度，google一通后，发现答案就那么几个，然后就默认了他们是对的，但是实际上还是很懵逼。这篇文章就来详细聊聊这0.75。</p><a id="more"></a><h1 id="最原始的回答"><a href="#最原始的回答" class="headerlink" title="最原始的回答"></a>最原始的回答</h1><p>这是jdk1.7源码上的注释</p><pre><code>As a general rule, the default load factor (.75) offers a good tradeoff between time and space costs. Higher values decrease the space overhead but increase the lookupcost (reflected in most of the operations of the HashMap class, including getand put).The expected number of entries in the map and its load factor should betaken into account when setting its initial capacity, so as to minimize the number of rehash operations. If the initial capacity is greater than the maximum number of entriesdivided by the load factor, no rehash operations will ever occur.</code></pre><p>大概意思就是：<strong>一般而言</strong>，默认负载因子为0.75的时候在时间和空间成本上提供了很好的折衷。太高了可以减少空间开销，但是会增加查找复杂度。我们设置负载因子尽量减少rehash的操作，但是查找元素的也要有性能保证。</p><p>网上通天彻尾的基本都是这个言论，毕竟官方的注释。只是这注释回答的未免也太过敷衍，就像我加粗的那4个字：“一般而言”。？(╯￣Д￣)╯╘═╛这个0.75的原因就是因为一个一般而言？这显然不够让人信服。所以很多小伙伴也就继续深究下去了。（注意加粗的字体，很鸡肋，但是却有很强的提示性）</p><p>这种回答就是：“嗯，你说的很有道理样子，也没什么错，但是为什么是0.75呢？这数怎么就出来了呢？”</p><h1 id="最扯淡的回答"><a href="#最扯淡的回答" class="headerlink" title="最扯淡的回答"></a>最扯淡的回答</h1><p>这是网上流行的第二个流行版本了，也来自官方，不过是jdk1.8的。</p><pre><code>Because TreeNodes are about twice the size of regular nodes, weuse them only when bins contain enough nodes to warrant use(see TREEIFY_THRESHOLD). And when they become too small (due toremoval or resizing) they are converted back to plain bins.  Inusages with well-distributed user hashCodes, tree bins arerarely used.  Ideally, under random hashCodes, the frequency ofnodes in bins follows a Poisson distribution(http://en.wikipedia.org/wiki/Poisson_distribution) with aparameter of about 0.5 on average for the default resizingthreshold of 0.75, although with a large variance because ofresizing granularity. Ignoring variance, the expectedoccurrences of list size k are (exp(-0.5) * pow(0.5, k) /factorial(k)). The first values are:0:    0.606530661:    0.303265332:    0.075816333:    0.012636064:    0.001579525:    0.000157956:    0.000013167:    0.000000948:    0.00000006more: less than 1 in ten million</code></pre><p>简单翻一下：</p><pre><code>因为TreeNode的大小约为链表节点的两倍，所以我们只有在一个拉链已经拉了足够节点的时候才会转为tree（参考TREEIFY_THRESHOLD）。并且，当这个hash桶的节点因为移除或者扩容后resize数量变小的时候，我们会将树再转为拉链。如果一个用户的数据他的hashcode值分布十分好的情况下，就会很少使用到tree结构。在理想情况下，我们使用随机的hashcode值，loadfactor为0.75情况，尽管由于粒度调整会产生较大的方差，桶中的Node的分布频率服从参数为0.5的泊松分布。下面就是计算的结果：桶里出现1个的概率到为8的概率。桶里面大于8的概率已经小于一千万分之一了。</code></pre><p>这个东西因为来自jdk1.8，而且提到了0.75，没有好好理解这段话的意思的话，很容易就认为这是在阐释0.75是怎么来的，然后就简单的把泊松分布给强关联到了0.75上去。然而，<strong>这段话的本意其实更多的是表示jdk1.8中为什么拉链长度超过8的时候进行红黑树转换。</strong>这个泊松分布的模型其实是基于已经默认因子就是0.75的模型去模拟演算的。</p><p>我其实很长一段时间也认为这就是标准答案了，虽然不懂，但是以后有人问直接甩他一个泊松分布就行了，直到—我忘了泊松分布的知识点了😂。其实也好理解，红黑树是1.8之后加进来的，所以jdk源码者并没有特地为我们解释下为啥当时设计了0.75，而是更多是想解释一些关于加入红黑树之后一些设计的原因。</p><h1 id="最深挖的回答"><a href="#最深挖的回答" class="headerlink" title="最深挖的回答"></a>最深挖的回答</h1><p>其实很高兴有人能挖掘到这里，我也以为这可以解答我这个问题了。</p><p><a href="https://stackoverflow.com/questions/10901752/what-is-the-significance-of-load-factor-in-hashmap/31401836#31401836">https://stackoverflow.com/questions/10901752/what-is-the-significance-of-load-factor-in-hashmap/31401836#31401836</a></p><p>这是stackoverflow上的一个帖子。Answers中top1其实就是最原始的回答，top2用了数学的一套理论推演出了 log(2)，然后来近似于0.75。我以为找到了真理，可惜写的太潦草了，没看懂这些公式罗列出来分别代表的意义。后来网上找到一篇有人详细<a href="https://wafer.li/Interview/hashmap-%E7%9A%84-loadfactor-%E4%B8%BA%E4%BB%80%E4%B9%88%E6%98%AF-0-75/">推演</a>了计算过程，可以参考参考，说的也不是很清楚，但是相对来说还是有很大的借鉴意义的。不否认，该思路的亮点将问题转变为了碰撞的概率问题。</p><p>唯一我存在疑问的点，就是这种概率问题真的服从二项分布吗？我自己给自己解释感觉也说的通，感觉也说不通。</p><h1 id="总结的回答1"><a href="#总结的回答1" class="headerlink" title="总结的回答1"></a>总结的回答1</h1><p>首先我们了解下概率论的相关东西（本人大学概率论没学好，选修课，还是早上的课，所以你懂得。）</p><p><u><strong>二项分布</strong></u></p><ul><li>什么是二项分布<ul><li>在做一件事情的时候，其结果的概率只有2中情况，和抛硬币一样，不是正面就是反面。这些实验做了n次，其所有成功的case离散概率的分布。</li></ul></li><li>特性<ul><li>在每次试验中只有两种可能的结果</li><li>每次实验是独立的，不同实验之间互不影响</li><li>每次实验成功的概率都是一样的</li></ul></li><li>公式<ul><li>$ binom(n,k) = C_n^k \times (p)^k \times (1 - p)^{n - k} $</li><li>n为实验的次数，k为成功的次数。上面公式就是描述该成功概率为p的情况下，n次实验，k次成功的概率为多少。</li></ul></li></ul><p>更具体的二项分布的内容，不懂的可以去学习下相关内容。</p><p><strong><u>hashmap的二项分布</u></strong></p><p>那么，这个和我们的这个负载因子有什么关系呢？我们先针对一下特性，来做一下思路的转换类比：</p><ul><li>实验只有2种结果<ul><li>我们往hash表put数据可以转换为key是否会碰撞？碰撞就失败，不碰撞就成功。</li></ul></li><li><p>实验相互独立</p><ul><li><strong>我们可以设想，实验的hash值是随机的，并且他们经过hash运算都会映射到hash表的长度的地址空间上，那么这个结果也是随机的。所以，每次put的时候就相当于我们在扔一个16面（我们先假设默认长度为16）的骰子，扔骰子实验那肯定是相互独立的。碰撞发生即扔了n次有出现重复数字。</strong></li></ul></li><li><p>成功的概率都是一样的</p><ul><li>这就是我难以理解的地方，这个地方可以说的过去，也可以说不过去。</li><li>说的过去<ul><li>每次一put的前面的位置我们不知道会在哪！可能前面一直都在一个位置上，那么我们理论上的概率一直都是 $\frac{1}{16}$。我们可以姑且抽象的认为概率p为$\frac{1}{s}$（设长度为s）。</li><li>需要说明的是：这里我并不确定是否合理，这也是过程中我认为不太严谨的地方。</li></ul></li><li>说不过去<ul><li>但是每次扔的大可能不会在同一个位置上，所以概率每次都会不一样，但是这个不一样又是我们无法估量和猜测的。</li></ul></li></ul></li></ul><p>然后，<strong>我们的目的是啥呢？</strong></p><p>就是掷了k次骰子，没有一次是相同的概率，需要尽可能的大些，一般意义上我们肯定要大于0.5（这个数是个理想数，但是我是能接受的）。</p><p>于是，n次事件里面，碰撞为0的概率，由上面公式得：</p><script type="math/tex; mode=display">\begin{aligned} binom(n,0) & = C_n^0 \times (\frac{1}{s})^0 \times (1 - \frac{1}{s})^{n - 0}  = (1 - \frac{1}{s})^n &\end{aligned}</script><p>这个概率值需要大于0.5，我们认为这样的hashmap可以提供很低的碰撞率。所以：</p><script type="math/tex; mode=display">(1 - \frac{1}{s})^n  \ge \frac{1}{2}</script><p>这时候，我们对于该公式其实最想求的时候长度s的时候，n为多少次就应该进行扩容了？<strong>而负载因子则是$n/s$的值</strong>。所以推导如下：</p><script type="math/tex; mode=display">\begin{aligned} n\ln(1 - \frac{1}{s}) & \ge -\ln2 ····两边取对数\\n & \le \frac{-\ln2}{\ln(1 - \frac{1}{s})} \rightarrow n \le \frac{\ln2}{\ln\frac{s}{s-1}}  ····提取n \\\frac{n}{s} &  \le \frac{\ln2}{s\ln\frac{s}{s-1}}  ····两边除以s \end{aligned}</script><p>所以可以得到</p><script type="math/tex; mode=display">\begin{aligned} loadFactor & = \lim_{s \to \infty}\frac{\ln2}{s\ln\frac{s}{s-1}} \end{aligned}</script><p>其中 </p><script type="math/tex; mode=display">\begin{aligned} \lim_{s \to \infty}s\ln\frac{s}{s-1} \end{aligned}</script><p>这就是一个求  <script type="math/tex">\infty \cdot 0</script> 函数极限问题，这里我们先令$s = m+1（m \to \infty）$则转化为</p><script type="math/tex; mode=display">\begin{aligned} \lim_{m \to \infty}(m+1)\ln(1+\frac{1}{m}) \end{aligned}</script><p>我们再令 $x = \frac{1}{m} （x \to 0）$ 则有，</p><script type="math/tex; mode=display">\begin{aligned} \lim_{s \to \infty}s\ln\frac{s}{s-1} & = \lim_{x \to 0}（\frac{1}{x}+1）\ln(1+x) \\&= \lim_{x \to 0} （\frac{1}{x}+1） x ····无穷小等价替换有\ln(1 + x) \sim x （证明去百度）\\&= \lim_{x \to 0}(1+x) \\ & \sim 1\end{aligned}</script><p>所以，</p><script type="math/tex; mode=display">\begin{aligned} loadFactor & = \lim_{s \to \infty}\frac{\ln2}{s\ln\frac{s}{s-1}} \\ &\sim \ln2 \\ & \sim 0.693 \end{aligned}</script><p>这也就是为什么stackoverflow上说接近于ln2的原因了。然后再去考虑hashmap一些内置的要求：</p><ul><li><strong>乘16可以最好一个整数。</strong></li></ul><p>那么在0.5~1之间找一个小数，满足这要求的只有0.625（5/8），0.75（3/4），0.875（7/8）。这三个数让我选，从审美角度，还是从<strong>中位数</strong>角度，我都会挑0.75。毕竟碰撞是个概率问题，这个0.75我觉得不错，我没办法预知使用者的数据到底什么样子的，0.75是最为折中的一个选择。</p><h1 id="总结的回答2"><a href="#总结的回答2" class="headerlink" title="总结的回答2"></a>总结的回答2</h1><p>事先申明，我否认了上面的一些答案，不代表我就很支持我总结的答案1，很坦白的说，我目前的看法还是保留着这只是作者的一个心理衡量出来的一个值的猜想。只是上面的那个答案1目前还算能让我心里把自己说得过去的。</p><p>我猜想也许从源头上，就是最原始的回答上我们被误导了，总觉得这个0.75不是简简单单来了，就觉得这个数一定是经过某种数理逻辑推演出来的。可以像上面那个回答一样可以用公式完美的一步步可以推算出来。但是，事实呢？我们从设计者的角度复演一下当时设计者的考虑：</p><p>如果要设值，这个值，在心理合理的范围应该是0.5~1之间的某个数。原因很简单：</p><ul><li>小于0.5，空着一半就扩容了，这在心理上很多人都会觉得不合理吧，空间肯定会很浪费。</li><li>但是如果是1的话，只能说有超级大的概率，会发生碰撞，这不符合我们的初衷。</li></ul><p>然后就为什么是0.75呢？我的猜想是这样：当时因为已经设置了hash table的长度为16。其实负载因子并不重要，重要的其实是那个阈值。负载因子也是为了计算那个阈值的。<strong>上面也提到了0.5~1之间找一个小数，乘16可以是一个整数时候0.75很合适。（这也正如1.7注释里面说的。所以也许作者也没法和我们准确交代0.75到底怎么来的，不妨换位思考下，如果这个0.75是你经过精密推演得出来的数字，注释肯定会详细解释说明，怎么可能”一般而言”就简单带过了呢？）</strong></p><p>所以我还是保留这个答案2，这个数字就是作者感觉差不多满足他想要的一些条件，感觉上也差不多的一个值，不会太浪费空间，也不会高碰撞概率。并且这是一个调优参数，用户可以根据自己的数据去动态调整这些参数来实现最优。所以就设了个这么个值。</p><p>但还是要提一下：</p><blockquote><p><strong>C#中的类似于Java的HashMap的类叫HashTable，而它的负载因子是0.72。这也是让我为什么一直要钻这个牛角尖的主要原因。取的数不同只是相似，这个数肯定没那么简单。</strong></p></blockquote><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这个问题，困扰了我很久，其实每次回头看hashmap的时候，我都会想这个问题，也和周围的人讨论过，得到的回答基本是：”不要钻牛角尖”，”面试问这个就太过分了”，””知道有啥用，记住数字就行”。可是不明所以，就如鲠在喉，希望这文章可以帮助和我有同样的感觉的人，也希望有什么异议可以告诉我。</p><p>其实几个回答分析下来，发现整个过程是：简单→复杂→简单的过程。如果面试我直接回答最后一个的结论，我觉得面试官也听的很没劲。需要一步步趟过来，这种返璞归真才有意思。</p><p>当然，对这个问题，也确实可能没必要钻这牛角尖。就和讨论P和NP问题一样，可以讨论，没实际多大意义。没准就如我所猜想，就是作者心理选了这么个数字呢？特别是网上看到的一些乱七八糟，逻辑都站不住脚的回答，我觉得就是有点强行了。仿佛世间一切的因果必是很强的逻辑性，没有因果就乱设因果，容易造成就是硬造也要造一个让自己满意的答案（这在学术圈屡见不鲜：很多从根上错的东西含糊其辞的说过去了，就开始继续往下推演）。</p><p>最后还是要提一句，如果你真的有严密的推演逻辑，还请告诉我。</p><h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><p>还是感谢下，没有2篇文章，我可能很长时间都无从下手。</p><p><a href="https://stackoverflow.com/questions/10901752/what-is-the-significance-of-load-factor-in-hashmap/31401836#31401836">https://stackoverflow.com/questions/10901752/what-is-the-significance-of-load-factor-in-hashmap/31401836#31401836</a></p><p><a href="[https://wafer.li/Interview/hashmap-%E7%9A%84-loadfactor-%E4%B8%BA%E4%BB%80%E4%B9%88%E6%98%AF-0-75/](https://wafer.li/Interview/hashmap-的-loadfactor-为什么是-0-75/">HashMap 的 loadFactor 为什么是 0.75</a>)</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;起初看hashmap源码的时候，最让我困惑的地方就是这里。相信很多人也有过这样的问题，相信很多人和我一样也是网上百度了一通，相信很多人和我一样百度，google一通后，发现答案就那么几个，然后就默认了他们是对的，但是实际上还是很懵逼。这篇文章就来详细聊聊这0.75。&lt;/p&gt;</summary>
    
    
    
    
    <category term="java" scheme="http://lollipops.work/tags/java/"/>
    
    <category term="hashmap" scheme="http://lollipops.work/tags/hashmap/"/>
    
  </entry>
  
  <entry>
    <title>【Java】深入理解SPI机制</title>
    <link href="http://lollipops.work/2020/05/10/Java-SPI/"/>
    <id>http://lollipops.work/2020/05/10/Java-SPI/</id>
    <published>2020-05-09T16:45:03.000Z</published>
    <updated>2021-01-16T09:05:07.308Z</updated>
    
    <content type="html"><![CDATA[<h1 id="深入理解SPI机制"><a href="#深入理解SPI机制" class="headerlink" title="深入理解SPI机制"></a>深入理解SPI机制</h1><blockquote><p>突然看到的新词汇，了解一下！词感觉很高端，实际并不是什么高深的技术。</p></blockquote><p>SPI，全称Service Provider Interface，是Java内置的服务发现机制（这个服务发现不是微服务里面注册中心那个服务发现）。</p><p>简单的来讲，Java的SPI机制就是指：<strong>针对一个接口</strong>，我们<strong>需要加载外部对该接口的实现</strong>，只要约定好<strong>将该实现配置在classPath路径下的META-INF/services文件夹的文件</strong>，使用放就可以自动加载文件里所定义的类。</p><p>SPI中三个重要的角色：</p><ul><li>接口</li><li>配置文件</li><li>ServiceLoader反射获取</li></ul><p>我们可以直观的理解：SPI就是JDK提供的一个功能，JDK中提供的<code>ServiceLoader</code>可以读取第三方在META-INF/services文件夹中配置的文件，然后自动加载文件里所定义的类，这样我们引入第三方包的时候，无需任何硬编码就可以使用到第三方包中提供的实现类。</p><p>具体为什么有该机制见下文：jdbc案例。</p><p>SPI给人的感觉有点像Spring的IOC，指定一个接口，通过JDK的ServiceLoader就可以自动装配该接口的实现类。而装配的控制权移到了程序之外（在第三方包中），并且实现在模块装配的时候不用在程序中动态指明。所以SPI的核心思想就是解耦，这在模块化设计中尤其重要。</p><p>SPI为<strong>很多框架</strong>扩展提供了可能，比如在Dubbo、Spring、SpringBoot相关starter组件、JDBC中都使用到了SPI机制。注意重点词是：<strong>框架</strong>！我们一般使用SPI的也是框架中使用，因为框架有些东西只需要定义标准，然后具体的实现需要依据不同的场景来选取最时候的实现，这时候框架中可以使用SPI接口来扩展自己的功能。</p><p>BTW，JDK的SPI机制有一些缺点，类似于Dubbo这些框架有自己的SPI实现。</p><h2 id="JDK-SPI实战"><a href="#JDK-SPI实战" class="headerlink" title="JDK SPI实战"></a>JDK SPI实战</h2><ol><li><p>需要一个接口</p><pre><code class="lang-java">public interface SpiService &#123;    void exe();&#125;</code></pre></li><li><p>定义多个实现</p><pre><code class="lang-java">public class SpiServiceImplA implements SpiService &#123;    public void exe() &#123;        System.out.println(&quot;I am A...&quot;);    &#125;&#125;</code></pre></li><li><p>配置/META-INF/services/work.lollipops.tutorial.java.SpiService</p><pre><code class="lang-java">work.lollipops.tutorial.java.SpiServiceImplAwork.lollipops.tutorial.java.SpiServiceImplB</code></pre></li><li><p>测试</p><pre><code class="lang-java">ServiceLoader&lt;SpiService&gt; spiServices = ServiceLoader.load(SpiService.class);spiServices.forEach(SpiService::exe);</code></pre></li></ol><p>输出：</p><pre><code>I am A...I am B...I am A...I am B...</code></pre><h2 id="ServiceLoader源码分析"><a href="#ServiceLoader源码分析" class="headerlink" title="ServiceLoader源码分析"></a>ServiceLoader源码分析</h2><p><code>sun.misc.Service</code> 源码属于sun的，我们无法看到，所以分析下<code>ServiceLoader</code>。</p><p>这个类的实现很简单，可以分为三个大块来看：</p><pre><code class="lang-java">public final class ServiceLoader&lt;S&gt; implements Iterable&lt;S&gt;&#123;    // 属性值    private static final String PREFIX = &quot;META-INF/services/&quot;;    // The class or interface representing the service being loaded    private final Class&lt;S&gt; service;    // The class loader used to locate, load, and instantiate providers    private final ClassLoader loader;    // The access control context taken when the ServiceLoader is created    private final AccessControlContext acc;    // Cached providers, in instantiation order    private LinkedHashMap&lt;String,S&gt; providers = new LinkedHashMap&lt;&gt;();    // The current lazy-lookup iterator    private LazyIterator lookupIterator;    // 初始化这个Loader      public static &lt;S&gt; ServiceLoader&lt;S&gt; load(Class&lt;S&gt; service) &#123;...&#125;       private ServiceLoader(Class&lt;S&gt; svc, ClassLoader cl) &#123;...&#125;    // LazyIterator迭代器类    private class LazyIterator implements Iterator&lt;S&gt; &#123;...&#125;      // iterator迭代方法      public Iterator&lt;S&gt; iterator() &#123;...&#125;&#125;</code></pre><h3 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h3><p><code>ServiceLoader.load(SpiService.class)</code> 本质是调用：</p><pre><code class="lang-java">private ServiceLoader(Class&lt;S&gt; svc, ClassLoader cl) &#123;    // 需要加载的接口    service = Objects.requireNonNull(svc, &quot;Service interface cannot be null&quot;);    // 配置类加载器    loader = (cl == null) ? ClassLoader.getSystemClassLoader() : cl;    // 访问权限控制    acc = (System.getSecurityManager() != null) ? AccessController.getContext() : null;    // 清空lookupIterator 并初始化 LazyIterator：new LazyIterator(service, loader);    reload();&#125;</code></pre><p><code>SpiService.class</code>接口需要加载的类对象，是采用懒加载的方式。初始化完成后，<code>ServiceLoader</code>内部<code>lookupIterator</code>持有这个懒加载迭代器。</p><h3 id="调用迭代"><a href="#调用迭代" class="headerlink" title="调用迭代"></a>调用迭代</h3><pre><code class="lang-java">public Iterator&lt;S&gt; iterator() &#123;    return new Iterator&lt;S&gt;() &#123;        //...          public boolean hasNext() &#123;        if (acc == null) &#123;            return hasNextService();        &#125; else &#123;            PrivilegedAction&lt;Boolean&gt; action = new PrivilegedAction&lt;Boolean&gt;() &#123;                public Boolean run() &#123; return hasNextService(); &#125;            &#125;;            return AccessController.doPrivileged(action, acc);        &#125;    &#125;    public S next() &#123;        if (acc == null) &#123;            return nextService();        &#125; else &#123;            PrivilegedAction&lt;S&gt; action = new PrivilegedAction&lt;S&gt;() &#123;                public S run() &#123; return nextService(); &#125;            &#125;;            return AccessController.doPrivileged(action, acc);        &#125;    &#125;        public boolean hasNext() &#123;            if (knownProviders.hasNext())                return true;            // 转去调用LazyIterator迭代器的方法            return lookupIterator.hasNext();        &#125;        public S next() &#123;            if (knownProviders.hasNext())                return knownProviders.next().getValue();            return lookupIterator.next();        &#125;        //...    &#125;;&#125;</code></pre><p>这里就是一个转发，实际调用的是<code>LazyIterator</code>的迭代方法。</p><h3 id="开始迭代获取（核心）"><a href="#开始迭代获取（核心）" class="headerlink" title="开始迭代获取（核心）"></a>开始迭代获取（核心）</h3><p><code>LazyIterator</code>的<code>hasNext</code> 和 <code>next</code> 实际分别对应调用的是 <code>hasNextService</code> 以及 <code>nextService</code>。</p><pre><code class="lang-java">  private boolean hasNextService() &#123;      // nextName 用来存储接口实现类的全限定类名      if (nextName != null) &#123;          return true;      &#125;      if (configs == null) &#123;          try &#123;              String fullName = PREFIX + service.getName();              // 如果没有获取到类加载器              if (loader == null)                  // 从指定的路径&lt;//META-INF/services/work.lollipops.tutorial.java.SpiService&gt;下面加载配置                  configs = ClassLoader.getSystemResources(fullName);              else                  configs = loader.getResources(fullName);          &#125; catch (IOException x) &#123;              fail(service, &quot;Error locating configuration files&quot;, x);          &#125;      &#125;      while ((pending == null) || !pending.hasNext()) &#123;          if (!configs.hasMoreElements()) &#123;              return false;          &#125;          // pending也是一个迭代器，parse就是解析文件，返回文件中读取的内容          pending = parse(service, configs.nextElement());      &#125;      // 接口的实现类全限定类名      nextName = pending.next();      return true;  &#125;</code></pre><p><code>hasNextService</code>就是读取我们配置的//META-INF/services/work.lollipops.tutorial.java.SpiService文件，迭代的读取里面配置的全限定类名。</p><pre><code class="lang-java">   private S nextService() &#123;       if (!hasNextService())           throw new NoSuchElementException();       String cn = nextName;       nextName = null;       Class &lt; ? &gt; c = null;       try &#123;           // 反射加载类           c = Class.forName(cn, false, loader);       &#125; catch (ClassNotFoundException x) &#123;           fail(service,               &quot;Provider &quot; + cn + &quot; not found&quot;);       &#125;       if (!service.isAssignableFrom(c)) &#123;           fail(service,               &quot;Provider &quot; + cn + &quot; not a subtype&quot;);       &#125;       try &#123;           S p = service.cast(c.newInstance());           providers.put(cn, p);           return p;       &#125; catch (Throwable x) &#123;           fail(service,               &quot;Provider &quot; + cn + &quot; could not be instantiated&quot;,               x);       &#125;       throw new Error(); // This cannot happen   &#125;</code></pre><p><code>nextService</code> 通过反射创建具体的实现类并返回。</p><blockquote><p>本质上就是通过反射加载指定了固定位置下配置的类</p></blockquote><h2 id="SPI-应用场景"><a href="#SPI-应用场景" class="headerlink" title="SPI 应用场景"></a>SPI 应用场景</h2><p>这里以JDBC为例子。</p><h3 id="JDBC"><a href="#JDBC" class="headerlink" title="JDBC"></a>JDBC</h3><p>JDBC4.0以前， 在使用mysql的时候还需要写<code>Class.forName(&quot;xxx&quot;)</code>的方式来装载mysql方提供驱动实现，JDBC4.0之后基于spi的机制来发现驱动提供商了，JDK通过<code>METAINF/services/java.sql.Driver</code>文件里指定实现类的方式来加载驱动的实现类。</p><ul><li>JDBC4.0之前</li></ul><pre><code class="lang-java">Class.forName(&quot;com.mysql.cj.jdbc.Driver&quot;);conn = DriverManager.getConnection(DB_URL, USER, PASS);//....</code></pre><p>这里面的forName就是硬编码了，不是很优雅。我们追求的是面向接口编程，像下面这样：</p><ul><li>JDBC4.0之后</li></ul><pre><code class="lang-java">//Class.forName(&quot;com.mysql.cj.jdbc.Driver&quot;);conn = DriverManager.getConnection(DB_URL, USER, PASS);//....</code></pre><p>主要的加载逻辑在<code>DriverManager</code>里面，我们可以看到DriverManager有一个静态代码块：</p><pre><code class="lang-java">static &#123;    loadInitialDrivers();    println(&quot;JDBC DriverManager initialized&quot;);&#125;</code></pre><p>在<code>loadInitialDrivers</code>方法通过SPI机制加载驱动实现类：</p><pre><code class="lang-java">private static void loadInitialDrivers() &#123;//...                      //spi调用加载Driver.class在METAINF/services/java.sql.Driver指定的类            ServiceLoader&lt;Driver&gt; loadedDrivers = ServiceLoader.load(Driver.class);            Iterator&lt;Driver&gt; driversIterator = loadedDrivers.iterator();//...    println(&quot;DriverManager.initialize: jdbc.drivers = &quot; + drivers);    if (drivers == null || drivers.equals(&quot;&quot;)) &#123;        return;    &#125;    String[] driversList = drivers.split(&quot;:&quot;);    println(&quot;number of Drivers:&quot; + driversList.length);    for (String aDriver : driversList) &#123;        try &#123;            println(&quot;DriverManager.Initialize: loading &quot; + aDriver);            Class.forName(aDriver, true,                    ClassLoader.getSystemClassLoader());        &#125; catch (Exception ex) &#123;            println(&quot;DriverManager.Initialize: load failed: &quot; + ex);        &#125;    &#125;&#125;</code></pre><p>而我们的<code>mysql-connector-java.jar</code> 指定了</p><p><img src="/Users/zhaohaoren/workspace/mycode/blog-docs/docs/Java-SPI/image-20201104194557695.png" alt="image-20201104195828105"></p><p>本质上也就相当于<code>DriverManager</code> 帮我们从<code>mysql-connector-java.jar</code> 中<code>forName</code>了配置的驱动类。</p><h4 id="思考"><a href="#思考" class="headerlink" title="思考"></a>思考</h4><p>JDK是标准的制定方，他指定了使用Java程序读取数据库这类的东西，都需要符合JDBC的标准。标准出来了，各大数据厂商就需要针对这个标准提供自己数据库的实现， <code>mysql-connector-java.jar</code>就是mysql数据库厂商为了实现JDK的JDBC标准的mysql实现。（JDK不可能提供Driver的实现，他只能提供一个规范，不然不可能把各个数据库厂商的实现都放在JDK里面吧！）</p><p>在没有<strong>SPI机制之前</strong>，我们写一个Java程序没有办法去自动发现mysql方给我们提供的实现的，或者说我们想加载mysql驱动的时候，就必须要知道mysql给我们提供的驱动的全限定类名（即<code>com.mysql.cj.jdbc.Driver</code>）是啥才能去加载。</p><p>有了<strong>SPI机制之后</strong>，在JDK程序方，我们不需要手动指定了，这个指定交给mysql提供方jar包来完成，我们只要双方统一一个约定：<strong>指定的配置必须要放在METAINF/services/下面。</strong>这样，JDK自己就能加载该目录的实现类了。</p><p>所以SPI给我们带来的好处：引入第三方包如 <code>mysql-connector-java.jar</code>，我们可以不用任何硬编码如<code>Class.forName(&quot;com.mysql.cj.jdbc.Driver&quot;);</code>就可以使用<code>com.mysql.cj.jdbc.Driver</code>了。JDK自己可以找到那个实现类。</p><h2 id="破坏双亲委派？"><a href="#破坏双亲委派？" class="headerlink" title="破坏双亲委派？"></a>破坏双亲委派？</h2><p>先回忆下什么是双亲委派：当某个类加载器需要加载某个<code>.class</code>文件时，它首先把这个任务委托给他的上级类加载器，递归这个操作，如果上级的类加载器没有加载，自己才会去加载这个类。</p><p>再以JDBC为例：<code>Driver</code>和<code>DriverManager</code>都是在JDK中的，他们是BootstrapClassLoader启动类加载器进行加载的，而<code>com.mysql.cj.jdbc.Driver</code>是第三方的实现，他是AppClassLoader系统类加载器进行加载的。</p><p>我们可以执行：</p><pre><code class="lang-java">System.out.println(DriverManager.class.getClassLoader());System.out.println(Driver.class.getClassLoader());System.out.println(Connection.class.getClassLoader());System.out.println(conn.getClass().getClassLoader());//返回结果nullnullnullsun.misc.Launcher$AppClassLoader@18b4aac2</code></pre><p>可以看到Driver和DriverManager和Connection这些都是通过BootstrapClassLoader加载的（java无法获取该加载器所以返回null）。但是<code>conn.getClass()</code>的类加载器是AppClassLoader。</p><p>理一下流程：JVM首先会接受到了DriverManager的类加载请求，于是向上委派到了BootstrapClassLoader进行了加载，以及Connection和Driver这些都是该加载器进行加载的。但是Driver的具体的实现类都是由各个厂商提供的，如果这些实现类放在JDK里面自然没有问题，都是BootstrapClassLoader来加载。但是这些不在jdk的lib下面，BootstrapClassLoader是无法加载的。</p><p>这时候BootstrapClassLoader在加载DriverManager的时候，DriverManager其实内部使用的都是jdk目录里面的的类，所以DriverManager相关的类都应该是BootstrapClassLoader来加载的（即整个加载DriverManager的过程应该都是在BootstrapClassLoader下完成的，因为这些类都在jdk的lib下面）。但是我们获取到的Connection却是AppClassLoader来加载的。这意味着：<strong>BootstrapClassLoader在加载DriverManager的过程中，又委派了其子级AppClassLoader来加载第三方的驱动类。</strong>所以说SPI破坏了双亲委派机制（只能下级委派给上级，上级不行再由下级加载，而这里是上级加载的过程中委托下级App加载器去加载第三方包的类，即<strong>上级委托了下级</strong>！）。</p><p>但是，我们输出：</p><pre><code class="lang-java">System.out.println(conn.getClass());// 输出class com.mysql.cj.jdbc.ConnectionImpl</code></pre><p>发现这个Connection类其实本质上是com.mysql的类！“AppClassLoader加载一个第三方类看起来并没有违反模型 <a href="https://www.zhihu.com/question/49667892">知乎</a> ”，这是SPI破坏委派的争议点。这么说也是有道理的。</p><p>但是从类加载的角度将，按照双亲委派的说法，我觉得还是破坏了！原因如下：</p><ul><li>DriverManager是JDK的东西，是BootstrapClassLoader加载的。BootstrapClassLoader加载的 DriverManager 是不可能拿到AppClassLoader加载的实现类的，对于BootstrapClassLoader加载器，他是不可见的。</li><li>我们可以想象一下这些类加载器就是一个套娃，最里面是BootstrapClassLoader，然后最里面娃娃里面存放的是DriverManager这些类，他在加载DriverManager的时候，只会加载自己能看到的jdk/lib下的东西。但是SPI不同，在加载DriverManager的时候，还加载了第三方包的东西，而这部分东西在套娃的最外层。这明显不符合套娃的规则。</li><li>双亲委派的目的是为了重复加载类，同时防止核心类被覆盖了。显然使用了SPI在外观上讲，JDK核心的Driver和Connection似乎就是被外层的第三方实现给覆盖了。</li><li>而且主要因为双亲委派模型并非强制模型，Java通过一个线程上下文类加载器，通过<code>setContextClassLoader()</code>默认情况就是应用程序类加载器然后<code>Thread.current.currentThread().getContextClassLoader()</code>获得类加载器来加载。</li></ul><pre><code class="lang-java">public static &lt;S&gt; ServiceLoader&lt;S&gt; load(Class&lt;S&gt; service) &#123;    // 设置上下文类加载器    ClassLoader cl = Thread.currentThread().getContextClassLoader();    return ServiceLoader.load(service, cl);&#125;</code></pre><p>我们就可以通过从线程上下文（ThreadContext）来获取 classloader去加载class。这就是相当于套娃上掏了一个洞，这个洞是个管子，管子里面只有这个加载器（一般就是AppClassLoader）。</p><p>【参考】</p><ul><li><a href="https://www.zhihu.com/question/49667892">https://www.zhihu.com/question/49667892</a></li><li><a href="https://segmentfault.com/a/1190000020422160?utm_source=tag-newest">https://segmentfault.com/a/1190000020422160?utm_source=tag-newest</a></li></ul>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h1 id=&quot;深入理解SPI机制&quot;&gt;&lt;a href=&quot;#深入理解SPI机制&quot; class=&quot;headerlink&quot;</summary>
        
      
    
    
    
    
    <category term="java" scheme="http://lollipops.work/tags/java/"/>
    
  </entry>
  
  <entry>
    <title>【Java】HashMap源码分析</title>
    <link href="http://lollipops.work/2020/05/10/Java-JDK-HashMap/"/>
    <id>http://lollipops.work/2020/05/10/Java-JDK-HashMap/</id>
    <published>2020-05-09T16:39:07.000Z</published>
    <updated>2021-01-16T09:05:28.066Z</updated>
    
    <content type="html"><![CDATA[<p>HashMap虽然常用，但是内部隐藏了很多实现细节，有太多值得推敲的东西，这里尽量去收录HashMap所有涉及的相关知识点。</p><a id="more"></a><h1 id="数据结构-散列表"><a href="#数据结构-散列表" class="headerlink" title="数据结构-散列表"></a>数据结构-散列表</h1><p>先从数据结构说起，HashMap对照的是数据结构中的<strong>散列表</strong>结构。在数据结构中有一种结构叫<strong>字典</strong>。有的书上定义：“以集合为基础的一些元素的集合，每个元素都有一个对应的key。支持元素的存在判断，插入和删除三种运算的的抽象数据类型就叫做字典“。散列表是字典的一个形式。散列表将元素的key使用一个<strong>hash函数</strong>映射到一个表上去（可以理解为一个地址连续的数组）来存储元素。获取数据时，只需要将key使用同样的hash函数映射对应的位置获取数据就行了。</p><pre><code>Address = Hash(key)</code></pre><p>但是key的数量要比计算机地址空间大很多，我们使用的hash函数必须是收敛的（即将一堆数据映射到限定大小范围内的地址空间中），因此肯定会存在冲突（2个不同的key使用hash method计算出同一个结果，有的地方也叫hash碰撞）。所以实现这样的一个散列表引出2个问题：</p><ol><li>对于给定的k-v对，找出一个合适的hash函数，能够使得k计算结果分布均匀，并且这个函数还不能太复杂。</li><li>hash冲突不可以避免，那在发生冲突时如何解决冲突。</li></ol><h2 id="常用的散列函数"><a href="#常用的散列函数" class="headerlink" title="常用的散列函数"></a>常用的散列函数</h2><ol><li>除留余数法</li><li>数字分析法</li><li>平方取中法</li><li>折叠法</li></ol><p>这里面只列举了一些简单的常见的hash函数，具体使用什么样的hash函数取决于你的数据性质。我们应当针对我们的数据选用合适的hash函数。</p><h2 id="常见的冲突解决策略"><a href="#常见的冲突解决策略" class="headerlink" title="常见的冲突解决策略"></a>常见的冲突解决策略</h2><h3 id="闭散列"><a href="#闭散列" class="headerlink" title="闭散列"></a>闭散列</h3><p>发生冲突的关键码存储在表中另一个槽内。具体方法有：</p><ol><li>线性探查法</li><li>二次探查法</li><li>双散列法</li></ol><h3 id="开散列"><a href="#开散列" class="headerlink" title="开散列"></a>开散列</h3><p>发生冲突的关键码存储在散列表主表之外。具体方法有：</p><ol><li>拉链法</li><li>桶式散列</li></ol><h1 id="HashMap源码"><a href="#HashMap源码" class="headerlink" title="HashMap源码"></a>HashMap源码</h1><p>HashMap在jdk集合中算一个很重要的知识点，面试也老爱问，在阅读HashMap的源码中，发现了很多亮点（算法的巧妙运用以及一些思想），但是不是总能久记不忘，这里写一篇记录下所有的分析过程，以及那些亮点。本文主要就JDK1.8做的分析，1.7只是面试可能需要对比做的捎带，本着对技术偏喜新厌旧的原则，就不分析1.7的了，对比的那些就是网上搜罗的，反正也没多少，个人觉得看1.8的完全不要再管1.7了。</p><p><strong>HashMap的特性</strong></p><ul><li>key和value可以为null</li><li>线程不安全。如果需要满足线程安全，可以用 Collections的synchronizedMap方法使HashMap具有线程安全的能力，或者使用ConcurrentHashMap。</li></ul><p><strong>数据结构概览</strong></p><p>hashmap基本结构是基于散列表这一数据结构。采用的是数组+链表的方式存储元素。jdk8之后，引入了红黑树来优化链表过长的情况。</p><p><img src=":java-jdk-hashmap/ds.png" alt=""></p><p>如图所示，我们将1-16这个数组成为哈希表（hash table，不是jdk里面的HashTable！），数组的每一块地址空间称为一个槽（bin）。</p><h2 id="源码分析（基于1-8）"><a href="#源码分析（基于1-8）" class="headerlink" title="源码分析（基于1.8）"></a>源码分析（基于1.8）</h2><h3 id="类图"><a href="#类图" class="headerlink" title="类图"></a>类图</h3><p><img src=":java-jdk-hashmap/HashMap.png" alt="HashMap"></p><h3 id="构造"><a href="#构造" class="headerlink" title="构造"></a>构造</h3><p>这是使用hashmap的第一步，hashmap有4个构造函数。</p><pre><code class="lang-java">public HashMap(int initialCapacity, float loadFactor);public HashMap(int initialCapacity);public HashMap();public HashMap(Map&lt;? extends K, ? extends V&gt; m);</code></pre><p>通过构造函数我们可以看到hashmap的几个很重要的属性（也可以称为参数，更贴切）。这些参数对hashmap的性能有很大的影响。在分析构造函数之前我们有必要先了解这些参数：</p><pre><code class="lang-java">//初始容量 默认16static final int DEFAULT_INITIAL_CAPACITY = 1 &lt;&lt; 4; // aka 16//负载因子 默认0.75static final float DEFAULT_LOAD_FACTOR = 0.75f;//阈值int threshold;</code></pre><h4 id="性能因子"><a href="#性能因子" class="headerlink" title="性能因子"></a>性能因子</h4><h5 id="初始容量initial-capacity"><a href="#初始容量initial-capacity" class="headerlink" title="初始容量initial capacity"></a>初始容量initial capacity</h5><ul><li>默认值为<strong><code>16</code></strong>，并且源码注释特别指出：<strong><u>该数必须是2的次幂！</u></strong>（原因后面会分析到）</li></ul><p>初始容量就是hashmap中上图那个哈希表的长度。如果该长度过长，则会浪费空间，如果该长度过短，则会造成大量的hash冲突，降低put和get的效率。</p><h5 id="负载因子loadFactor"><a href="#负载因子loadFactor" class="headerlink" title="负载因子loadFactor"></a>负载因子loadFactor</h5><ul><li><strong>默认值为<code>0.75</code>。</strong></li></ul><p>描述hashmap被填满的程度的系数。该负载因子越大，在每次扩容前所能容纳的键值对个数越多。而越多则可能会导致碰撞越多。但是太小，会导致频繁的扩容内存操作，性能更差。所以需要一个折中的值。</p><p>负载因子的存在其实是为了让hashmap的hash表可以更加均匀的存放存入的元素（最理想的结果是一个拉链也没有，每个槽刚好就一个元素）。这样查询的时候可以提供最高的性能O(1)级别，时间复杂度是最低的。</p><p><strong><u>至于该值为什么是0.75?</u></strong> 因为篇幅比较长，我在另一篇<a href="">《hashmap负载因子为什么是0.75》</a>做了解答。<u><strong><em>【#1. 为什么是0.75】</em></strong></u></p><h5 id="阈值threshold"><a href="#阈值threshold" class="headerlink" title="阈值threshold"></a>阈值threshold</h5><ul><li>该值为 <strong><code>capacity * load factor</code></strong></li></ul><p>表示当hashmap存入多少元素的时候，就需要该对hash表进行扩容。</p><h4 id="最简单的构造函数"><a href="#最简单的构造函数" class="headerlink" title="最简单的构造函数"></a>最简单的构造函数</h4><pre><code class="lang-java">public HashMap() &#123;    this.loadFactor = DEFAULT_LOAD_FACTOR; // DEFAULT_LOAD_FACTOR = 0.75f;&#125;</code></pre><p>只设置了一下负载因子，其他的都是使用的默认值。</p><h4 id="带调优参数的构造函数"><a href="#带调优参数的构造函数" class="headerlink" title="带调优参数的构造函数"></a>带调优参数的构造函数</h4><pre><code class="lang-java">public HashMap(int initialCapacity, float loadFactor) &#123;    if (initialCapacity &lt; 0)        throw new IllegalArgumentException(&quot;Illegal initial capacity: &quot; +                                           initialCapacity);    if (initialCapacity &gt; MAXIMUM_CAPACITY)        initialCapacity = MAXIMUM_CAPACITY;    if (loadFactor &lt;= 0 || Float.isNaN(loadFactor))        throw new IllegalArgumentException(&quot;Illegal load factor: &quot; +                                           loadFactor);    //主要是这里                                           this.loadFactor = loadFactor;    this.threshold = tableSizeFor(initialCapacity);&#125;</code></pre><p>主要校验了一下传来参数：初始容量，负载因子，并设置到hashmap的属性上。并且在设置阈值的时候，并不是直接赋值的，而是使用通过tableSizeFor方法计算后的值。</p><p>注意一点：我们这里并没有设置初始容量直接到内部的属性上，而是<strong>预先设置到了阈值上</strong>。这是为啥呢？因为我们构造函数的时候是不会申请内存的，申请内存的操作是在put的时候resize里面进行的，但是我们hashmap中又没有定义一个属性来存储我们设置的值，所以就使用暂存在threshold上。这么写估计是作者希望减少一个内置的属性，毕竟如果去单独定义这个属性使用的频率会比较低，而且在初始化完成后，我们也不需要再通过属性来获取，而是直接通过数组的length来获取。<u><strong><em>【#2. 为什么构造函数预先设置到阈值】</em></strong></u></p><h5 id="tableSizeFor解析"><a href="#tableSizeFor解析" class="headerlink" title="tableSizeFor解析"></a><strong>tableSizeFor解析</strong></h5><pre><code class="lang-java">static final int tableSizeFor(int cap) &#123;    int n = cap - 1;    n |= n &gt;&gt;&gt; 1;    n |= n &gt;&gt;&gt; 2;    n |= n &gt;&gt;&gt; 4;    n |= n &gt;&gt;&gt; 8;    n |= n &gt;&gt;&gt; 16;    return (n &lt; 0) ? 1 : (n &gt;= MAXIMUM_CAPACITY) ? MAXIMUM_CAPACITY : n + 1;&#125;</code></pre><p>该函数的作用其实就是依据给定值，返回大于等于该给定值，并距离最近的2的次幂数。（比如给1返2，给3返4，给5返8等）。</p><p>上面具体的计算逻辑使用一个例子可以很清楚的看到这些位运算做了什么：</p><div class="table-container"><table><thead><tr><th>编码</th><th>操作</th></tr></thead><tbody><tr><td>0100 0000 0000 0000 0000 0000 0000 1000</td><td>假设用户设置了该cap</td></tr><tr><td>0<strong><u><font color='red'>10</font></u></strong>0 0000 0000 0000 0000 0000 0000 0111</td><td>-1操作，n = cap -1</td></tr><tr><td>0<strong><u><font color='red'>01</font></u></strong>0 0000 0000 0000 0000 0000 0000 0011</td><td>无符号右移一位，n &gt;&gt;&gt; 1</td></tr><tr><td>0<strong><u><font color='red'>11</font></u></strong>0 0000 0000 0000 0000 0000 0000 0111</td><td>n &#124;= n&gt;&gt;&gt;1</td></tr><tr><td>0<strong><u><font color='red'>001 1</font></u></strong>000 0000 0000 0000 0000 0000 0001</td><td>n&gt;&gt;&gt;2</td></tr><tr><td>0<strong><u><font color='red'>111 1</font></u></strong>000 0000 0000 0000 0000 0000 0111</td><td>n &#124;= n &gt;&gt;&gt; 2</td></tr><tr><td>0<u><strong><font color='red'>000 0111 1</font></strong></u>000 0000 0000 0000 0000 0000</td><td>n &gt;&gt;&gt; 4</td></tr><tr><td>0<strong><u><font color='red'>111 1111 1</font></u></strong>000 0000 0000 0000 0000 0111</td><td>n &#124;= n &gt;&gt;&gt; 4</td></tr><tr><td>0<strong><u><font color='red'>000 0000 0111 1111 1</font></u></strong>000 0000 0000 0000</td><td>n &gt;&gt;&gt; 8</td></tr><tr><td>0<strong><u><font color='red'>111 1111 1111 1111 1</font></u></strong>000 0000 0000 0111</td><td>n &#124;= n &gt;&gt;&gt; 8</td></tr><tr><td>0<strong><u><font color='red'>000 0000 0000 0000 0111 1111 1111 1111</font></u></strong></td><td>n &gt;&gt;&gt; 16</td></tr><tr><td>0<strong><u><font color='red'>111 1111 1111 1111 1111 1111 1111 1111</font></u></strong></td><td>n &#124;= n &gt;&gt;&gt; 16</td></tr><tr><td>1000 0000 0000 0000 0000 0000 0000 0000</td><td>n+1</td></tr></tbody></table></div><p>可以看出来，每一次右移都是为了让第一个出现1的高位后面都变成1，从1开始变位2个，然后再以这2个的基础变位4个，4个变8个，直到16则可以<strong>将一个4字节的int数字从它的第一个高位开始后面的位置全部的变成1</strong>。这时候我们再加1就得到了给定值离它最近的那个2的次幂数，其幂就是原来数的最高位的前一位或者自身（如果本书就是2的次幂的话）。</p><p>其中，第一步我们先做了减一的操作，这是为了让本身就是2的次幂的数，返回的就是自己，比如给值8，如果不减一那么结果就是16，这显然不是我们想要的。<u><strong><em>【#3. 为什么tableSizeFor的cap要先减一】</em></strong></u></p><p>最后return的时候，我们会判断是否大于了hashmap内置的最大容量，如果大于了则就设置为最大容量。</p><p><u><strong><em>【#4. tableSizeFor做了什么？】</em></strong></u></p><h4 id="设置初始容量的构造函数"><a href="#设置初始容量的构造函数" class="headerlink" title="设置初始容量的构造函数"></a>设置初始容量的构造函数</h4><pre><code class="lang-java">public HashMap(int initialCapacity) &#123;    this(initialCapacity, DEFAULT_LOAD_FACTOR);&#125;</code></pre><p>其本质也是调用的是上面那个，只是负载因子使用的是默认的0.75。</p><p>这本应该是hashmap应该最常用的构造函数，阿里巴巴代码规范里面有相应的说明：</p><p><img src=":java-jdk-hashmap/al.png" alt=""></p><p>至于为什么是那个公式，后面我们也会介绍。这里可以去看下一个<a href="https://zhuanlan.zhihu.com/p/39924972">有趣的小实验</a>。</p><h4 id="map构造新map的构造"><a href="#map构造新map的构造" class="headerlink" title="map构造新map的构造"></a>map构造新map的构造</h4><pre><code class="lang-java">public HashMap(Map&lt;? extends K, ? extends V&gt; m) &#123;    this.loadFactor = DEFAULT_LOAD_FACTOR;    putMapEntries(m, false);&#125;</code></pre><p>这个构造使用频率相对较低。</p><h4 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h4><p>通过上面的构造可以发现，HashMap对于内部的hash表示<strong>采用懒加载的</strong>，在没有开始存入元素前，只是定义了一下内部的一些设置参数。</p><h3 id="put流程"><a href="#put流程" class="headerlink" title="put流程"></a>put流程</h3><p>put方法如下</p><pre><code class="lang-java">public V put(K key, V value) &#123;    return putVal(hash(key), key, value, false, true);&#125;</code></pre><h4 id="HashMap的hash-函数"><a href="#HashMap的hash-函数" class="headerlink" title="HashMap的hash()函数"></a>HashMap的hash()函数</h4><pre><code class="lang-java">static final int hash(Object key) &#123;    int h;    return (key == null) ? 0 : (h = key.hashCode()) ^ (h &gt;&gt;&gt; 16);&#125;</code></pre><p>通过该hash函数主要的目的是将hashcode进一步进行<code>位干扰</code>。hashcode对于Object来说是物理地址转换来的一个整数，但是考虑到用户重写了hashcode方法，但不是很好的情况的话，那么会严重增大key的碰撞率。</p><p><strong>具体做法：</strong>将key的hashcode值的高16位和低16位进行亦或。</p><p><u><strong><em>【#5. 为什么要进行位干扰？】</em></strong></u></p><p>这首先取决于我们hashmap的取模算法：(len - 1) &amp; hash，即将hashcode值和hash表长度-1做与运算。那么对于最后取模的结果影响因子永远都是hashcode值的低位。我们希望我们使用的hashcode值能充分利用好高位和低位的特性，所以将高位16位和低位做了一个亦或。这样新的hash值低16位也能保有了高位的信息了。</p><p>举个例子：</p><div class="table-container"><table><thead><tr><th>原HashCode</th><th>111 0100 1000 0110 1000 1001 1000 0000</th></tr></thead><tbody><tr><td>无符号右移16位</td><td>000 0000 0000 0000 0111 0100 1000 0110</td></tr><tr><td>异或运算</td><td>111 0100 1000 0110 1111 1101 0000 0110</td></tr><tr><td>len-1</td><td>000 0000 0000 0000 0000 0000 0000 1111</td></tr><tr><td>没干绕的运算</td><td>000 0000 0000 0000 0000 0000 0000 0000</td></tr><tr><td>干扰后的与运算</td><td>000 0000 0000 0000 0000 0000 0000 0110</td></tr></tbody></table></div><p>干扰后高位的信息就影响到了最后的取模结果了。</p><p><strong>总的来说，干扰主要就是能充分利用好hashcode的整体信息，将高位的信息传播到低位上去（因为hashmap取模算法的限制）。这样做可以进一步降低碰撞的几率。</strong></p><h4 id="再看看putVal"><a href="#再看看putVal" class="headerlink" title="再看看putVal"></a>再看看putVal</h4><pre><code class="lang-java">final V putVal(int hash, K key, V value, boolean onlyIfAbsent,               boolean evict) &#123;    Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; p; int n, i;    // 如果hash表为空，初始化下表    if ((tab = table) == null || (n = tab.length) == 0)        n = (tab = resize()).length;    // hash值和hash表长度-1 做与运算 计算出来的位置，判断上面有没有元素    if ((p = tab[i = (n - 1) &amp; hash]) == null)        //没有元素，直接插入节点        tab[i] = newNode(hash, key, value, null);    else &#123;        //已经存在元素，则走判断逻辑。// p 为该table位置上已经存在的元素        Node&lt;K,V&gt; e; K k;        if (p.hash == hash &amp;&amp;            ((k = p.key) == key || (key != null &amp;&amp; key.equals(k))))            // 判断得插入的元素key和当前位置上的元素相同。则让e引用 原位置上的node            e = p;        else if (p instanceof TreeNode)            // 如果发现table处位置已经是TreeNode了，就按照树的形式put            e = ((TreeNode&lt;K,V&gt;)p).putTreeVal(this, tab, hash, key, value);        else &#123;            // 如果和hash表元素不一样，则开始拉链以及后面可能的树化            for (int binCount = 0; ; ++binCount) &#123; // 一个死循环，遍历当前位置上的拉链。当前判断节点为e                if ((e = p.next) == null) &#123;                    // 如果遍历到链条的末尾了，就创建新node加进去                    p.next = newNode(hash, key, value, null);                    // 此时我们判断下，遍历了多少次了，如果大于等于8-1，那么说明就要转为红黑树                    if (binCount &gt;= TREEIFY_THRESHOLD - 1) // -1 for 1st                        treeifyBin(tab, hash);                    //完成                    break;                &#125;                if (e.hash == hash &amp;&amp;                    ((k = e.key) == key || (key != null &amp;&amp; key.equals(k))))                    // 如果在这中间发现了key重复的，就退出。                    break;                //处理下一个                p = e;            &#125;        &#125;        // 上面处理完了，此时的e指向的是该元素所应该在的位置。        if (e != null) &#123; // existing mapping for key            V oldValue = e.value;            // 如果设置了替换老的值 或者 老的值为null            if (!onlyIfAbsent || oldValue == null)                //将新值替换                e.value = value;            afterNodeAccess(e);            // 返回旧的值            return oldValue;        &#125;    &#125;    //hashmap变化计数器+1    ++modCount;    //这时候检验hashmap是否超过阈值了，超过就扩容    if (++size &gt; threshold)        resize();    afterNodeInsertion(evict);    return null;&#125;</code></pre><p>大致流程如下</p><p><img src=":java-jdk-hashmap/flow.png" alt=""></p><p>这里面有下面几个重点：</p><h5 id="hashmap取模算法"><a href="#hashmap取模算法" class="headerlink" title="hashmap取模算法"></a><strong>hashmap取模算法</strong></h5><pre><code class="lang-java">p = tab[i = (n - 1) &amp; hash] //n为当前hash表的长度</code></pre><p>n-1和hash做与运算可以保证，最后的结果等会散列到0-n的地址空间上。并且位运算的计算效率高。</p><h5 id="resize-扩容-amp-初始化方法"><a href="#resize-扩容-amp-初始化方法" class="headerlink" title="resize()扩容&amp;初始化方法"></a>resize()扩容&amp;初始化方法</h5><pre><code class="lang-java">final Node&lt;K,V&gt;[] resize() &#123;    Node&lt;K,V&gt;[] oldTab = table;    //因为要进行初始化/扩容，所以这两个是原始的初始容量和阈值    int oldCap = (oldTab == null) ? 0 : oldTab.length;    int oldThr = threshold;    int newCap, newThr = 0;    if (oldCap &gt; 0) &#123;        // 如果原始容量大于0 说明这次是扩容操作        if (oldCap &gt;= MAXIMUM_CAPACITY) &#123;            //检验下是否达到了最大的容量，达到就不扩容了。            threshold = Integer.MAX_VALUE;            return oldTab;        &#125;        // 检验下扩容2倍后是否达到最大容量，如果没有达到就扩容2倍        else if ((newCap = oldCap &lt;&lt; 1) &lt; MAXIMUM_CAPACITY &amp;&amp;                 oldCap &gt;= DEFAULT_INITIAL_CAPACITY)            //扩容2倍            newThr = oldThr &lt;&lt; 1; // double threshold    &#125;    // 如果原来的阈值大于0 就将其设置为新的容量大小（这里主要来源是构造函数里面tableSizeFor设置的是该值）    else if (oldThr &gt; 0) // initial capacity was placed in threshold        newCap = oldThr;    else &#123;               // zero initial threshold signifies using defaults        //如果该值为0，那么全部使用默认去构造他，因为他一定使用了无参的默认构造        newCap = DEFAULT_INITIAL_CAPACITY;        newThr = (int)(DEFAULT_LOAD_FACTOR * DEFAULT_INITIAL_CAPACITY);    &#125;    if (newThr == 0) &#123;        // 如果上面走了一轮了 发现没有设置新的阈值，那么就是上面oldThr &gt; 0的情况了，这时候这里设置下新的阈值。        float ft = (float)newCap * loadFactor;        newThr = (newCap &lt; MAXIMUM_CAPACITY &amp;&amp; ft &lt; (float)MAXIMUM_CAPACITY ?                  (int)ft : Integer.MAX_VALUE);    &#125;    threshold = newThr;    //申请新的内存空间    @SuppressWarnings(&#123;&quot;rawtypes&quot;,&quot;unchecked&quot;&#125;)    Node&lt;K,V&gt;[] newTab = (Node&lt;K,V&gt;[])new Node[newCap];    table = newTab;    if (oldTab != null) &#123;        // 如果原来有表，那么久开始迁移表里面的数据        for (int j = 0; j &lt; oldCap; ++j) &#123;            Node&lt;K,V&gt; e;            if ((e = oldTab[j]) != null) &#123;                oldTab[j] = null;                if (e.next == null)                    //该位置原来只有一个node，直接重新散列到新位置                    newTab[e.hash &amp; (newCap - 1)] = e;                else if (e instanceof TreeNode)                    //如果是树，就使用树的散列操作。分成2个树，而且如果树里面node小于6的时候会再转为链表。                    ((TreeNode&lt;K,V&gt;)e).split(this, newTab, j, oldCap);                else &#123; // preserve order                    //原来位置上是一个链表，就要将该链表拆成2份，一份还是在原来位置，一份会在其2倍便宜的高位。                    // l 代表低位                    Node&lt;K,V&gt; loHead = null, loTail = null;                    // h 代表高位                    Node&lt;K,V&gt; hiHead = null, hiTail = null;                    Node&lt;K,V&gt; next;                    //遍历链表                    do &#123;                        next = e.next;                        //这个其实就是判断该节点是在原位置还是在下一个便宜位置的。                        if ((e.hash &amp; oldCap) == 0) &#123;                            //在原位置                            if (loTail == null)                                loHead = e;                            else                                loTail.next = e;                            loTail = e;                        &#125;                        else &#123;                            //不在原位置，在其偏移位置                            if (hiTail == null)                                hiHead = e;                            else                                hiTail.next = e;                            hiTail = e;                        &#125;                    &#125; while ((e = next) != null);                    //拆分后的链表放到新的位置上去                    if (loTail != null) &#123;                        loTail.next = null;                        newTab[j] = loHead;                    &#125;                    if (hiTail != null) &#123;                        hiTail.next = null;                        newTab[j + oldCap] = hiHead;                    &#125;                &#125;            &#125;        &#125;    &#125;    return newTab;&#125;</code></pre><p>这也是一个很关键的函数。其主要目的是初始化内部的hash表，或者做2倍扩容。并且设置好初始化或者扩容后的初始容量和阈值的值。</p><p>这里面有下面几个关键的地方</p><h6 id="设置-threshold和newCap"><a href="#设置-threshold和newCap" class="headerlink" title="设置 threshold和newCap"></a>设置 threshold和newCap</h6><p>在这里面我们先计算出我们扩容后这些属性的值之后才申请数组内存的。在计算过程中，要考虑到边界问题（是否超过了最大的容量）。</p><h6 id="链表的再散列"><a href="#链表的再散列" class="headerlink" title="链表的再散列"></a>链表的再散列</h6><p>当我们扩容后需要对原来的链表再散列。这里主要说下原来是链表的情况（红黑树后期会单独讲，其他的很简单看代码就明白了）。</p><p>首先明白一点：扩容后，因为我们设计的原因，原来的拉链里面的node会重新散列的位置也只有2处：原来的位置，原来位置2倍的偏移位置。</p><p>为什么呢？举个例子：</p><p>扩容前：</p><pre><code>1010 1001 </code></pre><p>&amp; 0000 1111 （16-1）</p><p>=  0000 1001 </p><p>扩容后：</p><pre><code>101**&lt;font color=&#39;red&#39;&gt;0&lt;/font&gt;** 1001 </code></pre><p>&amp; 000<strong><font color='red'>1</font></strong> 1111 （32-1）</p><p>=  0000 1001 </p><p>我们可以看到扩容后，其实和数组长度掩码做运算其实就是多了一个1位（红色加粗）。这个1位和原来的hash值的&amp;结果，决定了答案只有2种：0000 1001 / 0001 10001。这2个值差的就是一个2倍偏移位置（即原来的位置+扩容扩大的长度）</p><p>所以影响到位置变不变的是原来的hash值中的绿色加粗的那一位（如果这个位置是0，那么位置就不变，如果那个位置是1，那么就偏移）。</p><h6 id="什么时候进行树化"><a href="#什么时候进行树化" class="headerlink" title="什么时候进行树化"></a>什么时候进行树化</h6><p>红黑树相关的内容我们这里不多讲，但是hashmap什么时候转为红黑树有需要注意的点。</p><p>先看下树化的方法</p><pre><code class="lang-java">final void treeifyBin(Node&lt;K,V&gt;[] tab, int hash) &#123;    int n, index; Node&lt;K,V&gt; e;    //1    if (tab == null || (n = tab.length) &lt; MIN_TREEIFY_CAPACITY) //MIN_TREEIFY_CAPACITY =64        resize();    else if ((e = tab[index = (n - 1) &amp; hash]) != null) &#123;        ...    &#125;&#125;</code></pre><p>从上面可以看出，链表转为红黑树需要满足2个条件</p><ul><li>链表长度超过8个（这是HashMap内部定义的一个边界值，至于为什么是8注释里面有介绍，即在0.75的负载因子下，根据泊松分布公式计算出来一个bin中node超过8个的概率已经很低了（0.00000006），所以设置为8）</li><li>map中存储的元素总数大于等于64个的时候，如果小于64会优先考虑先去扩容</li></ul><h6 id="什么时候树转为链表"><a href="#什么时候树转为链表" class="headerlink" title="什么时候树转为链表"></a>什么时候树转为链表</h6><p>若桶中链表元素个数小于等于6时，树结构还原成链表。</p><pre><code class="lang-java">final void split(HashMap&lt;K,V&gt; map, Node&lt;K,V&gt;[] tab, int index, int bit) &#123;    ....    // 树的拆分    if (loHead != null) &#123;        if (lc &lt;= UNTREEIFY_THRESHOLD)            tab[index] = loHead.untreeify(map);        else &#123;            tab[index] = loHead;            if (hiHead != null) // (else is already treeified)                loHead.treeify(tab);        &#125;    &#125;    if (hiHead != null) &#123;        if (hc &lt;= UNTREEIFY_THRESHOLD)            tab[index + bit] = hiHead.untreeify(map);        else &#123;            tab[index + bit] = hiHead;            if (loHead != null)                hiHead.treeify(tab);        &#125;    &#125;&#125;</code></pre><p>在树的拆分中，会判断拆分后的数是不是小到可以转为链表了。如果可以转为就转成链表。</p><p>因为红黑树的平均查找长度是log(n)，长度为8的时候，平均查找长度为3，如果继续使用链表，平均查找长度为8/2=4，这才有转换为树的必要。链表长度如果是小于等于6，6/2=3，虽然速度也很快的，但是转化为树结构和生成树的时间并不会太短。</p><p>还有选择6和8，中间有个差值7可以有效防止链表和树频繁转换。假设一下，如果设计成链表个数超过8则链表转换成树结构，链表个数小于8则树结构转换成链表，如果一个HashMap不停的插入、删除元素，链表个数在8左右徘徊，就会频繁的发生树转链表、链表转树，效率会很低。<a href="[https://www.cnblogs.com/xc-chejj/p/10825676.html]">参考</a></p><h3 id="get流程"><a href="#get流程" class="headerlink" title="get流程"></a>get流程</h3><p>hashmap有2种常用的get方法：</p><pre><code class="lang-java">public V get(Object key)  //依据key 获取值public V getOrDefault(Object key, V defaultValue) //依据key获取值，如果没有key，返回用户给定的默认值</code></pre><p>get的流程就很简单了，主要的逻辑都在这里面：</p><pre><code class="lang-java">final Node&lt;K,V&gt; getNode(int hash, Object key) &#123;    Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; first, e; int n; K k;    if ((tab = table) != null &amp;&amp; (n = tab.length) &gt; 0 &amp;&amp;        (first = tab[(n - 1) &amp; hash]) != null) &#123;        if (first.hash == hash &amp;&amp; // always check first node            ((k = first.key) == key || (key != null &amp;&amp; key.equals(k))))            return first;        if ((e = first.next) != null) &#123;            if (first instanceof TreeNode)                return ((TreeNode&lt;K,V&gt;)first).getTreeNode(hash, key);            do &#123;                if (e.hash == hash &amp;&amp;                    ((k = e.key) == key || (key != null &amp;&amp; key.equals(k))))                    return e;            &#125; while ((e = e.next) != null);        &#125;    &#125;    return null;&#125;</code></pre><p>主要就是计算key所在的位置，然后看所在位置上是链表还是树。然后使用不同的策略去查询出对应key的node。</p><h3 id="remove流程"><a href="#remove流程" class="headerlink" title="remove流程"></a>remove流程</h3><p>remove的主要逻辑在</p><pre><code class="lang-java">final Node&lt;K,V&gt; removeNode(int hash, Object key, Object value,                           boolean matchValue, boolean movable) &#123;    Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; p; int n, index;    // 判断数组不为空，并且该key的hash值不为散列的位置上bin不为空    if ((tab = table) != null &amp;&amp; (n = tab.length) &gt; 0 &amp;&amp;        (p = tab[index = (n - 1) &amp; hash]) != null) &#123;        Node&lt;K,V&gt; node = null, e; K k; V v;        if (p.hash == hash &amp;&amp;            ((k = p.key) == key || (key != null &amp;&amp; key.equals(k))))            // 如果第一个位置就相同 就指向第一个            node = p;        else if ((e = p.next) != null) &#123;            // 第一个位置不相同，但是后面有拉链            if (p instanceof TreeNode)                //如果是树，去遍历树查看是否有该key                node = ((TreeNode&lt;K,V&gt;)p).getTreeNode(hash, key);            else &#123;                //如果是拉链，遍历他                do &#123;                    if (e.hash == hash &amp;&amp;                        ((k = e.key) == key ||                         (key != null &amp;&amp; key.equals(k)))) &#123;                        node = e;                        break;                    &#125;                    p = e;                &#125; while ((e = e.next) != null);            &#125;        &#125;        //上面主要是查找，并node标记位置。下面是开始删除        if (node != null &amp;&amp; (!matchValue || (v = node.value) == value ||                             (value != null &amp;&amp; value.equals(v)))) &#123;            if (node instanceof TreeNode)                ((TreeNode&lt;K,V&gt;)node).removeTreeNode(this, tab, movable);            else if (node == p)                // 如果是第一个就相同就让该位置指向p的下一个节点                tab[index] = node.next;            else                //p是node的前节点，p的next执行node的下一个，就删除了node。                p.next = node.next;            ++modCount;            --size;            afterNodeRemoval(node);            return node;        &#125;    &#125;    return null;&#125;</code></pre><h2 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h2><p>先列举一些经常使用的方法：</p><pre><code class="lang-java">Map&lt;K,V&gt; map = new HashMap&lt;&gt;(16 /*initialCapacity*/); //申明，建议构造指定初始散列表大小map.put(k,v); //存放，k和v都可以为nullmap.get(k); //获取map.getOrDefault(k,v2); //获取，没有值则返回v2map.containsKey(k); //判断key是否存在map.keySet(); //获取所有的key，同理还有values()获取所有的值</code></pre><p>这里强调下，在初始化一个HashMap的时候，最好指定下初始化初始容量。具体的规则（Alibaba代码规范）：</p><ul><li>如果不确定元素的个数，指定16，也就是内置默认值；</li><li>如果确定：initialCapacity=(需要存储的元素个数 / 负载因子) + 1； 负载因子默认0.75</li></ul><h2 id="并发存在问题"><a href="#并发存在问题" class="headerlink" title="并发存在问题"></a>并发存在问题</h2><p>hashmap是线程不安全的，所以put和get的时候本身就存在并发的常见问题，写未读，重复写等，不过有一个特殊的线程不安全的行为是发生在hash表扩容的时候的。</p><p>在jdk1.7的时候，因为扩容后节点的rehash过程会导致，hash表的拉链出现一个死环（尾结点指向头节点）。不过在jdk1.8中已经不存在了，这块逻辑后面后期整理。(导致的原因主要是jdk1.7的rehash过程)</p><p>此时线程1和线程2此时都走到了rehash这一步。jdk的rehash代码如下：</p><pre><code class="lang-java">void transfer(Entry[] newTable) &#123;    Entry[] src = table;    int newCapacity = newTable.length;    for (int j = 0; j &lt; src.length; j++) &#123;        Entry&lt;K,V&gt; e = src[j];        if (e != null) &#123;            src[j] = null;              //就是遍历链表，判断是否需要移位，如果需要移位就摘出该元素放到新的位置上去。            do &#123;                Entry&lt;K,V&gt; next = e.next; // * 假设此时线程1被挂起                int i = indexFor(e.hash, newCapacity);                e.next = newTable[i];                newTable[i] = e;                e = next;            &#125; while (e != null);        &#125;    &#125;&#125;</code></pre><p>假设当前HashMap结构如图（图片来自网络）：</p><p><img src=":java-jdk-hashmap/hashmap_cycle.png" alt="image-20200215220430888"></p><p>此时老的表长度为2，在1位置上有3，7，5三个节点，现在扩容长度为4，需要将3，5，7节点进行偏移。</p><p>此时【线程1：e-&gt;3，next-&gt;7】【线程2将链表已经完全处理结束：将位置1处的3和7全部移动到了位置3处。】</p><p>这时候我们可以发现此时3和7的顺序是颠倒的（线程2上e和next是线程1当前的e和next指向）。这时候线程1开始执行。</p><pre><code class="lang-java">e.next = newTable[i];newTable[i] = e;e = next;</code></pre><p>这时候执行到最后一行next=e。此时【线程1：e-&gt;7】。继续下一次循环。此时更新了next【线程1：e-&gt;7，next-&gt;e.next-&gt;3】如下图：</p><p><img src=":java-jdk-hashmap/hashmap_cycle2.png" alt="hashmap_cycle2"></p><p>循环继续：当前e指向了7。table[3]执行了3。这时候我们按照线程1的要求会应该将7摘下放入table[3]位置，然后e和next后移后如图：</p><p><img src=":java-jdk-hashmap/hashmap_cycle3.png" alt="hashmap_cycle3"></p><p>之后继续，线程1将3摘下来放入table[3]位置。</p><p><img src=":java-jdk-hashmap/hashmap_cycle4.png" alt="hashmap_cycle4"></p><p>e的next=newTable[i]=7，newTable[i]=e=3，e=next=null遍历到此结束。此时3头插入7，7的next是3，3的next再指向了7构成了死环。</p><p>其实主要原因就是1.7中，rehash的时候遍历列表需要移位的元素是采用<strong>头部插入</strong>的方式进行插入的，当另外的一个线程将当前线程已经指定了但是还没有进行偏移的节点给插入新位置了，那么当前线程原来的e和next的顺序是颠倒的，这在当前线程后面的操作中会导致死循环。</p><h1 id="附全部源码和注释"><a href="#附全部源码和注释" class="headerlink" title="附全部源码和注释"></a>附全部源码和注释</h1><p>todo</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;HashMap虽然常用，但是内部隐藏了很多实现细节，有太多值得推敲的东西，这里尽量去收录HashMap所有涉及的相关知识点。&lt;/p&gt;</summary>
    
    
    
    
  </entry>
  
</feed>
